import type { IVolumeViewport } from '../../types';
export type ICompatibleVolumeViewport = Pick<IVolumeViewport, 'setVolumes' | 'addVolumes'>;
declare function isVolumeCompatible(viewport: unknown): viewport is ICompatibleVolumeViewport;
export default isVolumeCompatible;
