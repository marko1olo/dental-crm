import type { Panel } from './types';
export declare class RenderModesPanel implements Panel {
    dom: HTMLDivElement;
    private readonly list;
    constructor();
    update(): void;
    setContent(entries: Array<{
        renderingEngineId: string;
        viewportId: string;
        viewportType: string;
        bindings: RenderModePanelBinding[];
    }>): void;
}
export type RenderModePanelBinding = {
    actorUID?: string;
    dataId: string;
    referencedId?: string;
    renderMode: string;
    role: 'source' | 'overlay' | 'data';
    scalarType?: string;
    scalarRange?: [number, number];
    numberOfComponents?: number;
};
