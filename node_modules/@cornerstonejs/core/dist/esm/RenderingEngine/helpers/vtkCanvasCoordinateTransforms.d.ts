import type vtkRenderer from '@kitware/vtk.js/Rendering/Core/Renderer';
import type { Point2, Point3 } from '../../types';
export declare function canvasToWorldContextPool(args: {
    canvas: HTMLCanvasElement;
    renderer: vtkRenderer;
    canvasPos: Point2;
}): Point3;
export declare function worldToCanvasContextPool(args: {
    canvas: HTMLCanvasElement;
    renderer: vtkRenderer;
    worldPos: Point3;
}): Point2;
