import vtkPlane from '@kitware/vtk.js/Common/DataModel/Plane';
import type { ImageActor } from '../../types/IActor';
interface CreateVolumeSliceActorOptions {
    volumeId: string;
    callback?: ({ volumeActor, volumeId, }: {
        volumeActor: ImageActor;
        volumeId: string;
    }) => void;
}
declare function createVolumeSliceActor(props: CreateVolumeSliceActorOptions, element: HTMLDivElement, viewportId: string, suppressEvents?: boolean): Promise<{
    actor: ImageActor;
    slicePlane: vtkPlane;
}>;
export default createVolumeSliceActor;
