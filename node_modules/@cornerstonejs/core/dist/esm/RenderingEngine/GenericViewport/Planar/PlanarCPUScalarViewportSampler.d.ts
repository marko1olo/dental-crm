import { InterpolationType } from '../../../enums';
import type { IImageVolume, PixelDataTypedArray, Point3 } from '../../../types';
type SliceArray = PixelDataTypedArray;
export type ScalarViewportSampleResult = {
    min: number;
    max: number;
};
type AxisInterpolationPlan = {
    lower: Int32Array;
    upper: Int32Array;
    fraction: Float64Array;
    valid: Uint8Array;
};
export default class PlanarCPUScalarViewportSampler {
    private completeScalarDataCache;
    private axisAlignedGeometryCache;
    clearCachedVoxelManager(voxelManager: NonNullable<IImageVolume['voxelManager']>): void;
    getCompleteScalarDataArray(voxelManager: NonNullable<IImageVolume['voxelManager']>): ArrayLike<number> | undefined;
    sampleAxisAligned(args: {
        volume: IImageVolume;
        voxelManager: NonNullable<IImageVolume['voxelManager']>;
        pixelData: SliceArray;
        width: number;
        height: number;
        rowStartIndex: Point3;
        xStepIndexDelta: Point3;
        yStepIndexDelta: Point3;
        right: Point3;
        up: Point3;
        normal: Point3;
        interpolationType: InterpolationType;
        numberOfComponents: number;
        fallbackMin: number;
        fallbackMax: number;
    }): ScalarViewportSampleResult | undefined;
    private sampleGeneralAxisAligned;
    private trySampleFixedAxisAligned;
    private getOrCreateAxisAlignedGeometry;
    createAxisInterpolationPlan(dimension: number, start: number, step: number, length: number, interpolationType: InterpolationType): AxisInterpolationPlan;
    private createSingleAxisInterpolation;
    private sampleNearestFixedAxis;
    private sampleLinearFixedAxis;
    private sampleScalarDataAtContinuousCoordinates;
    private sampleNearestScalarDataAtContinuousCoordinates;
    private sampleLinearScalarDataAtContinuousCoordinates;
}
export {};
