import { ViewportType } from '../../../enums';
import type { DataAddOptions, LoadedData, RenderPathAttachment, RenderPathDefinition, RenderPath } from '../ViewportArchitectureTypes';
import type { PlanarDataPresentation, PlanarViewportRenderContext, PlanarVtkVolumeAdapterContext } from './PlanarViewportTypes';
export declare class VtkVolumeSliceRenderPath implements RenderPath<PlanarVtkVolumeAdapterContext> {
    addData(ctx: PlanarVtkVolumeAdapterContext, data: LoadedData, options: DataAddOptions): Promise<RenderPathAttachment<PlanarDataPresentation>>;
    private updateDataPresentation;
    private applyViewState;
    private syncRenderCamera;
    private canvasToWorld;
    private worldToCanvas;
    private getFrameOfReferenceUID;
    private getImageData;
    private render;
    private resize;
    private removeData;
}
export declare class VtkVolumeSlicePath implements RenderPathDefinition<PlanarViewportRenderContext, PlanarVtkVolumeAdapterContext> {
    readonly id = "planar:vtk-volume-slice";
    readonly type = ViewportType.PLANAR_NEXT;
    matches(data: LoadedData, options: DataAddOptions): boolean;
    createRenderPath(): VtkVolumeSliceRenderPath;
    selectContext(rootContext: PlanarViewportRenderContext): PlanarVtkVolumeAdapterContext;
}
