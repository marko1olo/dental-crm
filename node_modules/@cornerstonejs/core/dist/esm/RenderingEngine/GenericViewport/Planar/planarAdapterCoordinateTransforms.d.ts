export { canvasToWorldContextPool, worldToCanvasContextPool, } from '../../helpers/vtkCanvasCoordinateTransforms';
import type { CPUFallbackEnabledElement, ICamera, IImage, Point2, Point3 } from '../../../types';
import { type PlanarScaleInput } from './planarCameraScale';
type PlanarResolvedView = Pick<Required<ICamera>, 'focalPoint' | 'parallelScale' | 'viewPlaneNormal' | 'viewUp'> & {
    presentationScale?: PlanarScaleInput;
};
export declare function canvasToWorldPlanarViewState(args: {
    camera: PlanarResolvedView;
    canvasWidth: number;
    canvasHeight: number;
    canvasPos: Point2;
}): Point3;
export declare function worldToCanvasPlanarViewState(args: {
    camera: PlanarResolvedView;
    canvasWidth: number;
    canvasHeight: number;
    worldPos: Point3;
}): Point2;
export declare function getCanvasCssDimensions(canvas: HTMLCanvasElement): {
    canvasWidth: number;
    canvasHeight: number;
};
export declare function canvasToWorldCPUImage(enabledElement: CPUFallbackEnabledElement, image: IImage, canvasPos: Point2): Point3;
export declare function worldToCanvasCPUImage(enabledElement: CPUFallbackEnabledElement, image: IImage, worldPos: Point3): Point2;
export declare function getCpuEquivalentParallelScale(args: {
    canvasHeight: number;
    canvasWidth: number;
    columnPixelSpacing: number;
    columns: number;
    rowPixelSpacing: number;
    rows: number;
}): number;
export declare function getOrthogonalVolumeSliceGeometry(args: {
    dimensions: Point3;
    direction: number[] | ArrayLike<number>;
    spacing: Point3;
    viewPlaneNormal: Point3;
    viewUp: Point3;
}): {
    columnPixelSpacing: number;
    columns: number;
    rowPixelSpacing: number;
    rows: number;
} | undefined;
