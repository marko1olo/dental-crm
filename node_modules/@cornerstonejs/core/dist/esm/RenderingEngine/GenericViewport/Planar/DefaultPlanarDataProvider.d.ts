import type { LoadedData } from '../ViewportArchitectureTypes';
import type { PlanarDataProvider, PlanarDataLoadOptions, PlanarPayload } from './PlanarViewportTypes';
export declare class DefaultPlanarDataProvider implements PlanarDataProvider {
    load(dataId: string, options?: PlanarDataLoadOptions): Promise<LoadedData<PlanarPayload>>;
    private getDataSet;
}
