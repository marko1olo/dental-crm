import { ViewportType } from '../../../enums';
import type { DataAddOptions, LoadedData, RenderPathAttachment, RenderPathDefinition, RenderPath } from '../ViewportArchitectureTypes';
import type { PlanarDataPresentation, PlanarCpuVolumeAdapterContext, PlanarViewportRenderContext } from './PlanarViewportTypes';
export declare class CpuVolumeSliceRenderPath implements RenderPath<PlanarCpuVolumeAdapterContext> {
    private readonly sampler;
    addData(ctx: PlanarCpuVolumeAdapterContext, data: LoadedData, _options: DataAddOptions): Promise<RenderPathAttachment<PlanarDataPresentation>>;
    private updateDataPresentation;
    private applyViewState;
    private canvasToWorld;
    private worldToCanvas;
    private getFrameOfReferenceUID;
    private getImageData;
    private render;
    private resize;
    private removeData;
    private syncRenderCamera;
    private releaseSampledSliceState;
}
export declare class CpuVolumeSlicePath implements RenderPathDefinition<PlanarViewportRenderContext, PlanarCpuVolumeAdapterContext> {
    readonly id = "planar:cpu-volume-slice";
    readonly type = ViewportType.PLANAR_NEXT;
    matches(data: LoadedData, options: DataAddOptions): boolean;
    createRenderPath(): CpuVolumeSliceRenderPath;
    selectContext(rootContext: PlanarViewportRenderContext): PlanarCpuVolumeAdapterContext;
}
