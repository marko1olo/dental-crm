import type { BaseViewportRenderContext, DataAddOptions, LoadedData, RenderPathDefinition, RenderPathResolver } from './ViewportArchitectureTypes';
import type ViewportType from '../../enums/ViewportType';
export declare class DefaultRenderPathResolver implements RenderPathResolver {
    private paths;
    constructor(paths?: ReadonlyArray<RenderPathDefinition>);
    register<TRootContext extends BaseViewportRenderContext, TAdapterContext extends BaseViewportRenderContext>(path: RenderPathDefinition<TRootContext, TAdapterContext>): void;
    private add;
    resolve<TContext extends BaseViewportRenderContext>(type: ViewportType, data: LoadedData, options: DataAddOptions): RenderPathDefinition<TContext, BaseViewportRenderContext>;
}
export declare const defaultRenderPathResolver: DefaultRenderPathResolver;
