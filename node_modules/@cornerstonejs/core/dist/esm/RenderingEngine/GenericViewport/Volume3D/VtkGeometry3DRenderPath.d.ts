import '@kitware/vtk.js/Rendering/Profiles/Geometry';
import { ViewportType } from '../../../enums';
import type { DataAddOptions, LoadedData, RenderPathAttachment, RenderPathDefinition, RenderPath } from '../ViewportArchitectureTypes';
import type { Volume3DDataPresentation, Volume3DViewportRenderContext, Volume3DVtkGeometryAdapterContext } from './viewport3DTypes';
export declare class VtkGeometry3DRenderPath implements RenderPath<Volume3DVtkGeometryAdapterContext> {
    addData(ctx: Volume3DVtkGeometryAdapterContext, data: LoadedData, options: DataAddOptions): Promise<RenderPathAttachment<Volume3DDataPresentation>>;
    private updateDataPresentation;
    private applyViewState;
    private canvasToWorld;
    private worldToCanvas;
    private getFrameOfReferenceUID;
    private render;
    private resize;
    private removeData;
}
export declare class VtkGeometry3DPath implements RenderPathDefinition<Volume3DViewportRenderContext, Volume3DVtkGeometryAdapterContext> {
    readonly id = "volume3d:vtk-geometry";
    readonly type = ViewportType.VOLUME_3D_NEXT;
    matches(data: LoadedData, options: DataAddOptions): boolean;
    createRenderPath(): VtkGeometry3DRenderPath;
    selectContext(rootContext: Volume3DViewportRenderContext): Volume3DVtkGeometryAdapterContext;
}
