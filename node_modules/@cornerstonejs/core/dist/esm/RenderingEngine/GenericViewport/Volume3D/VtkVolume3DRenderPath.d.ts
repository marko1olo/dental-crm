import '@kitware/vtk.js/Rendering/Profiles/Volume';
import { ViewportType } from '../../../enums';
import type { DataAddOptions, LoadedData, RenderPathAttachment, RenderPathDefinition, RenderPath } from '../ViewportArchitectureTypes';
import type { Volume3DDataPresentation, Volume3DViewportRenderContext, Volume3DVtkVolumeAdapterContext } from './viewport3DTypes';
export declare class VtkVolume3DRenderPath implements RenderPath<Volume3DVtkVolumeAdapterContext> {
    addData(ctx: Volume3DVtkVolumeAdapterContext, data: LoadedData, options: DataAddOptions): Promise<RenderPathAttachment<Volume3DDataPresentation>>;
    private updateDataPresentation;
    private applyViewState;
    private canvasToWorld;
    private worldToCanvas;
    private getFrameOfReferenceUID;
    private getImageData;
    private render;
    private resize;
    private removeData;
}
export declare class VtkVolume3DPath implements RenderPathDefinition<Volume3DViewportRenderContext, Volume3DVtkVolumeAdapterContext> {
    readonly id = "volume3d:vtk-volume";
    readonly type = ViewportType.VOLUME_3D_NEXT;
    matches(data: LoadedData, options: DataAddOptions): boolean;
    createRenderPath(): VtkVolume3DRenderPath;
    selectContext(rootContext: Volume3DViewportRenderContext): Volume3DVtkVolumeAdapterContext;
}
