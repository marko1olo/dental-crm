import type { ActorEntry, ICamera, IVolumeInput, VolumeViewportProperties } from '../../../types';
import VolumeViewport3D from './viewport3D';
import type { Volume3DCamera } from './viewport3DTypes';
declare class VolumeViewport3DLegacyAdapter extends VolumeViewport3D {
    private readonly managedDataIds;
    private readonly volumeDataIds;
    private readonly legacyProperties;
    private readonly defaultLegacyProperties;
    getCamera(): Volume3DCamera & ICamera;
    setCamera(cameraPatch: Partial<Volume3DCamera>): void;
    getViewPresentation(): {
        camera: Volume3DCamera & ICamera;
    } | undefined;
    setViewPresentation(viewPresentation?: {
        camera?: Partial<Volume3DCamera>;
    } | Partial<Volume3DCamera>): void;
    resetCamera(): boolean;
    resetCameraForResize(): boolean;
    setVolumes(volumeInputArray: IVolumeInput[], immediate?: boolean, suppressEvents?: boolean): Promise<void>;
    addVolumes(volumeInputArray: IVolumeInput[], immediate?: boolean, suppressEvents?: boolean): Promise<void>;
    setProperties(properties?: VolumeViewportProperties, volumeId?: string, suppressEvents?: boolean): void;
    getProperties(volumeId?: string): VolumeViewportProperties;
    resetProperties(volumeId?: string): void;
    setSampleDistanceMultiplier(multiplier: number): void;
    removeData(dataId: string): void;
    getActors(): ActorEntry[];
    getDefaultActor(): ActorEntry | undefined;
    private mountVolumes;
    private buildActorEntries;
    private applyPresetToBinding;
    private getBindingActor;
    private resolveTargetDataId;
    private resolveVolumeIdForDataId;
    private removeBindingsExcept;
    private getLegacyVolumeDataId;
    private forgetData;
    protected onDestroy(): void;
}
export default VolumeViewport3DLegacyAdapter;
