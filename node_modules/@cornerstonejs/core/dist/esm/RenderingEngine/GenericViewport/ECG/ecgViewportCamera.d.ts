import type { Point2 } from '../../../types';
import type { ViewAnchor } from '../ViewportCameraTypes';
import type { ECGViewState, RenderWindowMetrics } from './ECGViewportTypes';
export interface ECGCanvasMapping {
    anchorWorld: [number, number];
    anchorCanvas: ViewAnchor;
    canvasHeight: number;
    canvasWidth: number;
    centeredXOffset: number;
    centeredYOffset: number;
    effectiveRatio: number;
    xOffset: number;
    yOffset: number;
}
export declare function createDefaultECGViewState(args: {
    timeRange: [number, number];
    valueRange: [number, number];
}): ECGViewState;
export declare function normalizeECGViewState(camera: ECGViewState): ECGViewState;
export declare function resolveECGCanvasMapping(args: {
    metrics: RenderWindowMetrics;
    camera?: ECGViewState;
    canvas: HTMLCanvasElement;
}): ECGCanvasMapping;
export declare function getPanForECGCanvasMapping(mapping: ECGCanvasMapping): Point2;
export declare function getAnchorWorldForPan(pan: Point2, mapping: ECGCanvasMapping): [number, number];
export declare function getAnchorWorldForCanvasPoint(canvasPoint: Point2, mapping: ECGCanvasMapping): [number, number];
