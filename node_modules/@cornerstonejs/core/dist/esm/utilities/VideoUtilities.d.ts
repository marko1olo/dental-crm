import type { Point3 } from '../types';
export interface VideoImageMetadata {
    bitsAllocated: number;
    numberOfComponents: number;
    origin: Point3;
    rows: number;
    columns: number;
    direction: number[];
    dimensions: [number, number, number];
    spacing: [number, number, number];
    hasPixelSpacing: boolean;
    numVoxels: number;
    imagePlaneModule: Record<string, unknown>;
}
export interface LoadedVideoStreamMetadata {
    renderedUrl: string;
    modality?: string;
    metadata: VideoImageMetadata;
    cineRate?: number;
    numberOfFrames?: number;
}
export declare function getVideoImageDataMetadata(imageId: string): VideoImageMetadata;
export declare function loadVideoStreamMetadata(imageId: string): LoadedVideoStreamMetadata;
export declare function normalizeVideoPlaybackInfo(args: {
    durationSeconds: number;
    cineRate?: number;
    numberOfFrames?: number;
}): {
    fps: number;
    numberOfFrames: number;
    frameRange: [number, number];
};
export declare function frameNumberToTimeSeconds(frameNumber: number, fps: number): number;
export declare function timeSecondsToFrameNumber(timeSeconds: number, fps: number): number;
