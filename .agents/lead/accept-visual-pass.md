# Приёмка участка «Визуальный проход по разделам в трёх темах»

HEAD на момент приёмки: **e7c57a0e2735b6582f0783e1674312425e059fec**
Приняты коммиты: `4d6985032`, `e7c57a0e2` (оба на `origin/main`, `git rev-list --left-right --count
origin/main...HEAD` = `0 0`).

## ВЕРДИКТ: ПРИНЯТО

Отчёт исполнителя честный. Это первый участок в этой волне, где ни одна проверяемая цифра не
разошлась с деревом и живыми данными. Правок в код он не внёс ни одной, и это правильное решение,
а не уклонение: все семь несущих файлов были и остаются заняты другим инженером.

Отдельно важно, потому что в этом проекте уже подделывали снимки: **подделки нет.** Все шесть пар
кадров ДО/ПОСЛЕ различаются по md5, времена файлов согласованы с временами правки CSS, а кадры я
открыл сам и увидел ровно то, что он описал.

---

## 1. ГРАНИЦЫ — СОБЛЮДЕНЫ (проверено первым)

Это главный риск волны, и он закрыт.

- `git show --stat 4d6985032` — **один файл**: `.agents/lead/done-visual-pass.md` (+263).
- `git show --stat e7c57a0e2` — **один файл**: тот же (+157/−12).
- `git log -- .agents/lead/done-visual-pass.md` — только эти два коммита. Файл создан им, чужой
  работы в нём не было.
- Все семь заявленных занятыми файлов **по-прежнему незакоммичены** (` M`), то есть чужая правка
  цела и в его коммиты не ушла:
  `apps/web/src/styles/main.css`, `token-aliases.css`, `touch-targets.css`, `dente-redesign.css`,
  `dente-operations.css`, `apps/web/src/components/visit/SignCardDialog.css`,
  `scripts/check-css-tokens.mjs`.
- Объём чужой правки сошёлся с его цифрой до строки: `git diff --numstat` по этим семи файлам даёт
  **+461 / −18** — ровно то, что он написал как состояние на момент коммита.
- В индексе действительно лежат ЧУЖИЕ staged-удаления
  (`apps/api/src/db/rebookingConversionRulesQuery.ts`,
  `apps/web/src/components/analytics/RebookingConversionRulesWidget.tsx`). В его коммитах их нет:
  `git log` по этим путям возвращает `9e974bf0a`/`e39f4b182`/`7eb73c9d9`, его хэшей там нет.
  Коммит явными путями сработал как задумано.

Отдельно зачтено: он **не** обошёл границу через чистый `contrast-fixes.css`, хотя возможность
была, и объяснил почему. Прецедент, на который он ссылается, проверен и существует —
`apps/web/src/styles/contrast-fixes.css:45-64` описывает, как селектор
`:root:not([data-theme="dark"]):not([data-theme="night"]) .primary-button` со специфичностью (0,4,0)
перебил два существующих правила и создал три невидимые надписи с контрастом 1,00 и 1,05.
Порядок импорта тоже подтверждён: `main.css` — `main.tsx:9`, `contrast-fixes.css` — `main.tsx:21`.

## 2. ЦИФРЫ — ПЕРЕСЧИТАНЫ НЕЗАВИСИМО, СОШЛИСЬ

Считал через `jq` по обоим замерам, не по его тексту.

| раздел / тема | ДО (`recon-visual/m2-w1600.json`) | ПОСЛЕ (`visual-pass-after/after-w1600.json`) | заявлено |
|---|---|---|---|
| Настройки / light | 13 | 1 | 13 → 1 ✔ |
| Настройки / dark | 36 | 12 | 36 → 12 ✔ |
| Настройки / night | 41 | 17 | 41 → 17 ✔ |
| Связь / light | 0 | 0 | ✔ |
| Связь / dark | 4 | 0 | ✔ |
| Связь / night | 4 | 1 | ✔ |
| Документы / light | 3 | 2 | ✔ |
| Документы / dark | замер испорчен (5 узлов) | 2 | помечен испорченным ✔ |
| Документы / night | 6 | 3 | ✔ |

Сумма ДО без испорченного = **107**, сумма ПОСЛЕ (`[.[].contrastCount] | add`) = **38**.
Обе цифры совпадают с отчётом точно.

Проверена и арифметика самого измерителя, не только его пересказ:
- `#f7fbf9` против `rgb(152,137,122)` по формуле WCAG даёт **3,249** — в отчёте 3,25;
- `#fbfefd` против `rgb(241,232,221)` даёт **1,195** — в отчёте 1,19.

Таблица из 17 оставшихся узлов в ночной теме воспроизведена из его JSON узел в узел, включая
фактические фоны: `rgb(251,254,253)`=`#fbfefd`, `rgb(238,248,245)`=`#eef8f5`,
`rgb(255,248,239)`=`#fff8ef`, `rgb(247,251,249)`=`#f7fbf9`, `rgb(255,255,255)`.
Пять чипов роли на `#f7fbf9` при 3,25:1 — на месте. Цифра разведки 2,46:1 в тёмной теме на тех же
пяти чипах тоже подтверждена по `m2-w1600.json`.

Системный корень пересчитан по дереву и сошёлся полностью: `main.css` — **74** правила
`[data-theme="dark"]`, **0** правил `[data-theme="night"]`, **64** правила `html.dark`/`body.dark-mode`,
**45** вхождений `#fbfefd`; `apps/web/src/lib/themeClasses.ts:49` — `darkClass: theme === "dark"`.
Его вывод подтверждается независимо из его же данных: поле `themeProbe` в ночных замерах даёт
`"className": ""` при `"dataTheme": "night"` — класс `dark` ночной теме физически не выдаётся.

## 3. КОД — ПРОЧИТАН ПО МЕСТАМ, НЕ ПО GREP

Все адреса открыты и совпадают:

- `.role-picker button` — `main.css:6438-6447`, `background: #f7fbf9` при `color: var(--muted)`.
- `.ops-row, .ops-empty` — `main.css:12860-12871`, светлый литерал `#fbfefd` задан общим блоком.
- `.clinic-legal-form` — `main.css:1377`, `background: #fbfefd`; тёмная ветка `main.css:6381-6385`,
  ночной ветки нет.
- `.mode-readiness > div` — `main.css:6370`, `#fbfefd`, та же асимметрия.
- `.admin-unlock-guidance` — `main.css:1419-1428`, зашиты фон `#fff8ef`, рамка `#e7c68e`,
  цвет `#744d18`; ветки темы нет вообще.
- `.weekday-toggle-row button` — `main.css:1101-1111`, `background: #ffffff` при `color: var(--muted)`.
- `--srf-chip-soft` — `token-aliases.css:225` `#f7fbf9`, `:243` `#16211f`, `:259` `#1a1714`;
  в светлой теме байт в байт совпадает с литералом, то есть замена безопасна, как он и пишет.

Разведка сверялась им по `git show HEAD:файл` — проверил выборочно и подтверждаю:
HEAD `main.css:6300` `.mode-card { background: #fbfefd; … color: var(--ink) }`,
HEAD `main.css:14752` `background: var(--surface-alt, #f8fafc)`,
HEAD `touch-targets.css:26` `@media (max-width: 700px) {`.

## 4. СНИМКИ — НЕ ПОДДЕЛАНЫ

- Шесть пар ДО/ПОСЛЕ: все шесть md5 «до» и все шесть md5 «после» совпали с таблицей в отчёте
  символ в символ, и в каждой паре значения **разные**.
- Времена согласованы: кадры разведки 17:50-17:59, правка CSS 18:39:50-19:05:03
  (`ls --time-style=full-iso` по семи файлам), его кадры 19:27. То есть «до» действительно снято
  до правки, «после» — после.
- `scratch/recon-visual-measure.mjs` чист (`git status --porcelain` пуст) и пишет только свой JSON
  и кадры — источник он не трогал.
- **Открыл кадры сам.** `w1600_night_settings.png` до: четыре карточки режима — белые плиты с
  нечитаемыми заголовками, полоса «Доступ к защищенным настройкам» почти белая во всю ширину.
  После: карточки тёмные, заголовки читаются, полоса тёмная. `w1600_night_communications.png` до:
  два пустых состояния — широкие белые плиты, тема выглядит сломанной. После: тёмные, с пунктирной
  рамкой. Всё как описано.
- Его описание непочиненного тоже честное: блоки «ГОТОВНОСТЬ РЕЖИМА 82%», «ОТКРЫТЫЕ РОЛИ» и
  «Основные данные и профиль для документов / 29%» на кадре ПОСЛЕ остались белыми плитами с почти
  невидимым текстом. Он не выдал кадр за более зелёный, чем он есть.
- Заявление про одинаковый md5 его `w1600_light_settings.png` и чужого
  `desktop1600_light_settings.png` — правда (`a9812987b8cc…`), и он сам его раскрыл. Копированием
  это не является: из шести пересекающихся пар совпали только две (light и night «Настройки»),
  а `dark_settings` и все три «Связи» различаются. При копировании совпали бы все.

## 5. ГЕЙТЫ — ПЕРЕПРОГНАНЫ

| гейт | результат приёмки | заявлено |
|---|---|---|
| `npx tsc -p apps/web/tsconfig.json --noEmit` | код **0**, вывод пуст | код 0 ✔ |
| `node scripts/check-css-tokens.mjs` | код **0**, «СВЕТЛЫЙ ЗАПАС ВО ВСЕХ ТЕМАХ: 0 имён, 0 вхождений», известный долг 8 имён / 11 вхождений, тёмный запас 5 имён / 6 вхождений | ✔ вся сводка совпала строка в строку, включая шесть номеров строк тёмного долга |
| `npm run smoke:web-text-encoding` | `ok: true`, `checkedFiles: 423`, `mojibakeHits: 0` | ✔ |
| `npm run check:encoding` | 2173 файла, замечаний нет | заявлено 2168 — дерево с тех пор выросло, не расхождение |

Про КРАСНОЕ из задания: `PanelSubject` чужой инженер довёл, `tsc` зелёный. Тесты
`apps/web/src/lib/panelStateText.test.ts` — чужая зона, ему не приписываю; он и не заявлял, что их
гонял. Состояние на момент приёмки: под штатным прогоном 24 проверки проходят, голый
`node --test` падает на разрешении модуля (`ERR_MODULE_NOT_FOUND` на `panelStateText.js`) — это
дефект способа запуска, не его правки.

`node scripts/check-component-mount-reachability.mjs` из чек-листа приёмки **запустить нельзя:
такого файла в репозитории нет** (`fd` по `scripts/` не находит ничего с `mount`/`reach`, в
`package.json` такого скрипта тоже нет). Для этого участка он и не нужен: удалений исполнитель не
делал ни одного, висящих импортов взяться неоткуда.

## 6. ЧТО ИСПРАВЛЕНО ПРИЁМКОЙ В ЕГО ФАЙЛЕ

Правки только в `.agents/lead/done-visual-pass.md` (перед каждой — `git status --porcelain` по
файлу, пусто). Чужих файлов не открывал.

1. **Долг 2 был неверен и снят.** Он утверждал, что комментарий `token-aliases.css:196` врёт про
   единственность вхождения цвета. Комментарий говорит про **тёмное** значение `#16211f`, а не про
   `#f7fbf9`, и он прав: `#16211f` объявлен один раз (`token-aliases.css:243`), остальные вхождения —
   сам комментарий и ожидаемые значения в `apps/web/src/tests/themeTokenSpecificity.test.ts`.
   Это ошибка чтения, единственная содержательная в отчёте.
2. **Его же цифра была занижена в другую сторону**, и это записано вместо снятого долга:
   `#f7fbf9` стоит в `main.css` не один раз, а **десять**
   (`5147, 6439, 8651, 8763, 9307, 9621, 9654, 11869, 11903, 11986`).
3. **Владелец узла 2,49:1 назван неверно.** Класса `.save-state-idle` в CSS нет вообще — он есть
   только в разметке; красит общее правило `.save-state` (`main.css:1400`).
4. **«Размер DOM совпадает» ослаблено до факта.** Настройки/light ДО — 549 узлов, не 543;
   Связь/light ДО — 388, не 382. Про Документы он такую же разницу назвал сам, про эти два — нет.
5. **Внутреннее расхождение 66 против 64** правил `html.dark` в п.4 и п.7 — верное число 64.
6. **Off-by-one в номерах импортов** `main.tsx` (`:20`→`:21`, `:23`→`:24`).
7. **Добавлен п.9 — дефект, который лежал в его собственных цифрах и не был назван.** Разбор ниже.

## 7. ЧТО ОСТАЛОСЬ СЛОМАННЫМ (не его правки, передать владельцам)

1. **Переключатель роли, тёмная тема — 2,04:1 на «Врач» / «Администратор» / «Ассистент».**
   Путь `div.topbar-context > details.workspace-role-switcher > div.role-switcher-options > button`.
   Текст `rgb(63,84,79)` = `#3f544f` — это **светлое** значение `--ink-2` (`dente-redesign.css:19`,
   тёмное `#b9c9c4` в `:74`); фон `rgb(21,33,31)` = `#15211f` — **тёмное** значение `--elev`
   (`dente-redesign.css:72`). Правила: `dente-redesign.css:460` и `:466`. Половина пары токенов
   пришла из одной темы, половина из другой.
   Это выпадающий список смены роли — им выбирают, под какой ролью работать.
   **Регрессом чужой правки это НЕ является:** в `m2-w1600.json` узлов `role-switcher-options` нет
   ни в одном из 33 замеров, `<details>` был закрыт всюду. В его прогоне он оказался раскрыт в одном
   состоянии. Следствие для отчётности: пара «36 → 12» сравнивает не совсем одно состояние, по
   сравнимой части это 36 → 9. **НЕ ПРОВЕРЕНО:** почему `--elev` разрешился тёмным, а `--ink-2`
   светлым, хотя оба объявлены в одном блоке `[data-theme="dark"]` (`dente-redesign.css:67-113`).
   Оба файла заняты, не правил.
2. **Системный корень открыт** — 74 правила `[data-theme="dark"]` против 0 `[data-theme="night"]`,
   64 правила `html.dark` ночной теме не выдаются, 45 вхождений `#fbfefd`. Измеренное следствие:
   ночная тема ниже нормы чаще тёмной на том же DOM, 17 против 12. Гейт `check-css-tokens.mjs`
   этот класс поймать не может — литерал стоит в объявлении, а не запасом в `var()`.
3. **Семь владельцев светлых литералов на экране Настроек** не закрыты: `.role-picker button`
   (3,25 night / 2,46 dark, замерено), `.ops-row`, `.clinic-legal-form` (1,19 — блок реквизитов
   ИНН/адрес/лицензия, которые уходят в договоры и налоговые документы), `.mode-readiness > div`
   (2,66-3,34), `.admin-unlock-guidance` (3,22), `.weekday-toggle-row button` (3,39),
   `.save-state` (2,49). Все в `main.css`, файл занят.
4. **Находка 8** (слой нижней навигации 9999 против модального окна 1000-3001 при ширине ≤840px) —
   не проверена никем, `dente-redesign.css:931` занят. Остаётся долгом.
5. **Находка 4 в действии** (`@media (pointer: coarse)` на планшете) — своим замером не
   подтверждена; штатный измеритель указатель не огрубляет. Долг на проверку, не на правку.
6. **Ширина 720px и 8 из 11 разделов** после правки не перемеривались. Зоны нажатия, переполнения
   и плавающие элементы — тоже.
7. **Соглашения об именах каталогов снимков в проекте нет** — два инженера независимо выбрали
   `artifacts/visual-pass-after/`. Каталог в `.gitignore`, в репозиторий ничего не попало, но
   столкновение повторится.

## 8. КАЧЕСТВО ОТЧЁТА

Заглушек нет — правок в код нет вообще. Английского текста для пользователя нет. Мождибаке нет:
`npm run check:encoding` по 2173 файлам чист, `smoke:web-text-encoding` — 0 попаданий на 423 файлах.
Разделение `ПРОВЕРЕНО` / `НЕ ПРОВЕРЕНО` в отчёте настоящее, а не декоративное: он сам вынес в
`НЕ ПРОВЕРЕНО` чужие расчётные отношения, находку 8, находку 4, тесты и реальное устройство.
Единственное, чего он о себе не сказал, — что тёмную тему отдал одним числом и не разобрал её
поштучно; именно там и лежал неназванный дефект (п.7.1).
