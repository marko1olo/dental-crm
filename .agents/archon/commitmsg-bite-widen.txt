[ARCHON] chore(гигиена): мутационная копия с именем *.test.ts ИСПОЛНЯЛАСЬ в общем наборе

Расширение защиты, поставленной в a30d31dd1. Тогда закрыл `**/__bite*`, и шаблон оказался узок: ревьюеры
называют свои копии иначе. Найдено ведущим в дереве: apps/web/src/tests/__revBiteAfter.test.ts и
__revBiteBefore.test.ts — префикс `__rev`, под `__bite*` не попадают.

ГЛАВНОЕ НЕ В ИМЕНИ, А В РАСШИРЕНИИ. Копия, названная `*.test.ts`, попадает под тестовую маску проекта
(apps/web/package.json: --test "src/**/*.test.ts" "src/**/*.test.tsx") и ИСПОЛНЯЕТСЯ в общем наборе. Это не
сор в дереве, а изменение результата: число тестов web скакало 1259 → 1250 между двумя прогонами ведущего
именно из-за появления и исчезновения таких файлов, и разобрать причину падений стало нельзя — я не мог
отличить настоящий красный от артефакта чужой мутации.

ЧТО УЖЕ СЛОМАЛОСЬ ДО ЭТОГО, и это измерено, а не предположено. Копии пакета QQ1
(sources/__biteOldAsAny.tsx, sources/__biteNewContract.tsx) сломали настоящий страж проекта
«ни один компонент apps/web/src не остаётся несмонтированным незаявленно»: он верно опознал их как
несмонтированные компоненты и покраснел. После удаления страж дал EXIT=0, 10 тестов, 10 прошло.
Плюс они портили ЗАМЕР, которым судят сам пакет: из 52 TS6133 в sources/ тридцать девять вносили сами
копии, и честные 11 я получил только исключив их вручную.

То есть остаток мутации бьёт по трём вещам сразу: ломает страж, портит замер вердикта и делает общий набор
неразличимым. Прежняя формулировка закона «удали за собой» слаба — нужно «удали ДО отчёта», потому что и
ревьюер, и ведущий читают ДЕРЕВО, а не только диff.

УБРАНО ВЕДУЩИМ (всё неотслеживаемое, в git не попало ничего):
  apps/web/src/components/settings/sources/__biteOldAsAny.tsx
  apps/web/src/components/settings/sources/__biteNewContract.tsx
  apps/web/__biteIsoNew.json, __biteIsoOld.json, __biteIsoPass.json
  apps/api/src/_chaintmp/ — временный каталог ВНУТРИ исходников api
Файлы `__revBite*` работающего сейчас ревьюера НЕ ТРОНУТЫ намеренно: он ими пользуется, а шаблон игнора
теперь не даст им уехать в коммит.

ПОЛНЫЙ НАБОР WEB ПОСЛЕ УБОРКИ: истинный код выхода 0, 1250 тестов, 1250 прошло, 0 пропущено.
Число тестов называю намеренно: код выхода необходим и не достаточен — правило этого же цикла.
