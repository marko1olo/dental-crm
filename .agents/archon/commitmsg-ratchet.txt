[ARCHON] test(схема): запись долга утверждала, что can_manage_imports не объявлен — он объявлен

Страж переписи схемы стал красным, и покраснел он ПРАВИЛЬНО: сработал его собственный храповик.

Текст падения дословно:
  undeclaredColumns["users"]: колонки уже объявлены в apps/api/src/db/schema.ts — can_manage_imports;
  удалите их из записи

Проверено: `canManageImports: boolean("can_manage_imports").notNull().default(false)` стоит в schema.ts:352.
Из трёх полномочий, созданных миграцией 0000, две записи снял пакет MM6 (can_manage_money и
can_sign_medical_records), а третья колонка была объявлена позже, и запись про неё осталась лежать.

ЭТО ТОТ ЖЕ КЛАСС, ЧТО ДАЛ КРАСНЫЙ HEAD В ЦИКЛЕ 24: маршрут DELETE /api/clinical/rules был СДЕЛАН, а строка
в реестре долга продолжала утверждать, что его нет. Реестр долга без храповика в обратную сторону сам
становится ложью, и именно поэтому храповик в этих стражах и заведён. Здесь он отработал без человека.

Комментарий в schema.ts:318-348 проверен отдельно и НЕ ИСПРАВЛЯЛСЯ: фраза «can_manage_imports не объявлен»
в нём стоит в прошедшем времени и заканчивается словами «Третья колонка добавлена, набор снова совпадает с
таблицей». То есть комментарий описывает историю верно, и правка ему не нужна — я сначала прочёл его как
ложный и это было моей ошибкой чтения.

ПОСЛЕ ПРАВКИ: node --import tsx --test scripts/smoke-schema-missing-declarations.test.mjs → истинный код 0,
5 тестов, 5 прошло. Сам скрипт → истинный код 0, ok:true.

ПОЛНАЯ БАТАРЕЯ ГЕЙТОВ ВЕДУЩЕГО НА ЭТОМ HEAD, все коды выхода истинные, снятые без конвейера:
  npm test -w @dental/api            0   — 1509 тестов, 1509 прошло, 0 упало, 0 пропущено
  npm run typecheck -w @dental/api   0
  npm run typecheck -w @dental/web   0
  npm run build -w @dental/api       0
  npm run build -w @dental/shared    0
  npm run smoke:pricelist-analyzer   0
  npm run smoke:dist-freshness       0
  smoke-schema-missing-declarations   0

Полный набор api ЗЕЛЁНЫЙ ЦЕЛИКОМ — на момент передачи в этот диалог он давал 1317 тестов при двух упавших,
и в таблице гейтов зелёным не был ни разу. Тестов стало 1509.
