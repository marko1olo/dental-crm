[ARCHON] chore(кампания): наряд цикла 23 — два пункта из шести оказались уже закрытыми на перемере

ПЕРЕМЕРИЛ КАЖДЫЙ ПУНКТ НА HEAD ПЕРЕД ОТПРАВКОЙ, и это окупилось сразу: два из шести намеченных были уже
сделаны, и по старому порядку я отправил бы двух исполнителей в пустоту тринадцатый раз.

Закрыты не мной и обнаружены только замером:
  `PUT /api/xray/scans/:id` — маршрута по-прежнему нет, но веб его больше не зовёт. В
  `components/imaging/VisiographAnalyzer.tsx:678-681` записано, чем именно он заменён, с пометкой
  «проверено живьём на работающем API». Дефекта нет: звавшая сторона исправлена.
  Сирота `pages/PublicBookingWidget.tsx` — смонтирована в `main.tsx:78` и закреплена стражем
  `tests/publicPortalRoute.test.ts:149`. Осталась одна сирота из двух.

Оба заменены живыми пунктами: сведение двух идиом авторизации в `routes/visits.ts` (пять рукописных проверок
против одной общей — худший смешанный файл в репозитории) и контракт статуса ЕГИСЗ.

Шесть пакетов, наборы файлов не пересекаются:
  LL1 `DELETE /api/clinical/rules/:id` — кнопка удаления клинического правила не работала никогда
  LL2 номер кабинета продаётся как услуга: `.at(-1)` в analyzer.ts:742
  LL3 единственный красный тест из 1278 зависит от чужих фикстур в общей базе
  LL4 статус ЕГИСЗ проверяет только Postgres — контракта в @dental/shared нет
  LL5 в `routes/visits.ts` два разных ответа на «можно ли этому вызывающему»
  LL6 последняя сирота: `ComparativePlannerDashboard` с производителем и без потребителя

ЧТО ИСПРАВЛЕНО В САМОМ НАРЯДЕ, а не в коде. Ревьюеры цикла 22 не записали ни одной строки `## VERDICT`, и я
не смог снять с диска ни один вердикт — весь проход ревью ушёл впустую. Причина была в наряде: текст
ревьюера содержал пять проверок, зашитых под пакет ПРЕДЫДУЩЕГО цикла (пересчёт денежных подстановок в
guards.ts), то есть ревьюер получал устаревший бриф так же, как исполнитель. Теперь проверки ревьюера
строятся из его собственного пакета, ему передаётся исходный бриф целиком, и строка `## VERDICT` затребована
буквально.

Добавлено в закон цикла: запрет `grep -r` и `find /` на этой машине с причиной (процессы переживают
родительский шелл, Windows их не убирает, один `find /` сжёг 5,25 часа процессорного времени за ночь) и
правило единственного источника денежной арифметики.
