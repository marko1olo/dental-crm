# AU4 — аудит коммита ec4050199 «отказ записи заключения в карту больше не молчит»

Аудитор: adversarial auditor под [ARCHON]. Только чтение. Файл пишется по мере подтверждения находок.

Коммит: `ec4050199040be5ec3c4f5288aec09467271353b`, автор marko1olo, Tue Jul 28 21:05:21 2026 +0400
Родитель: `12bc39e9f1882bf5b45806d3c3f420be32d80a12`
HEAD на момент аудита: `9603c7d5c08f51bc3132125e6c33fe04e7ef3d57`
Файл: `apps/web/src/components/imaging/VisiographAnalyzer.tsx` (+101/-15)
Файл после коммита ещё раз менялся: `39a729523 fix(приём, снимки, записи): спасённые правки трёх оборванных инженеров`.

---

## НАХОДКА 1 — КРИТИЧЕСКАЯ. §10 ВЫДУМАННЫЙ КОНТРАКТ. Маршрута `PUT /api/xray/scans/:id` НЕ СУЩЕСТВУЕТ.

Коммит целиком построен вокруг «второго запроса, дописывающего заключение»:
`await fetch('/api/xray/scans/' + saved.id, { method: 'PUT', ... })`
(`apps/web/src/components/imaging/VisiographAnalyzer.tsx:500`).

В `apps/api/src/routes/xray.ts` зарегистрированы РОВНО пять маршрутов:
- `POST /api/xray/scans` (xray.ts:99)
- `POST /api/xray/scans/:id/analyze` (xray.ts:144)
- `GET /api/xray/scans` (xray.ts:206)
- `GET /api/xray/scans/:id` (xray.ts:227)
- `DELETE /api/xray/scans/:id` (xray.ts:249)

**PUT нет.** Команды и истинные коды выхода:

```
$ rg -n "app\.(put|PUT)|\.put\(" --glob 'apps/api/src/**/*.ts' | rg -i "xray|scan"
(пусто)  rc=1
$ rg -n "setNotFoundHandler|app\.all\(|app\.route\(" --glob 'apps/api/src/**/*.ts'
(пусто)  rc=1
```

Ни одного `app.put` на `xray`, ни catch-all маршрута, ни своего notFoundHandler. Значит PUT попадает
в штатный 404 Fastify **всегда**.

### Что это значит для продукта
1. Заключение ИИ **никогда** не попадало и не попадает в карту пациента. Дефект, названный в теле
   коммита («снимок мог лечь в карту БЕЗ текста разбора»), — не «мог», а «всегда».
2. После коммита плашка «Снимок в карту лёг, а текст заключения сохранить не удалось» показывается
   на **100 % успешных разборов**, а не в редком отказе. Это не «отказ больше не молчит» — это
   постоянная тревога на счастливом пути, которого не существует.
3. Коммит закрыл симптом (молчание), не тронув причину (нет маршрута). §1 DEPTH NOT FACADE.
4. Ряд в БД остаётся `status:"pending"` (xray.ts:132) без `aiReport`. `loadHistory` фильтрует
   `s.status === 'done'` (VisiographAnalyzer.tsx:310) — то есть такой снимок в архиве **не виден
   никогда**. Изображение лежит в БД сиротой.

Автор коммита сам это признал в теле, не сделав вывода: «отдельного контракта ошибок у
/api/xray/scans я не подтверждал».

(проверка вживую — ниже)
