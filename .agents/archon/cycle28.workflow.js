export const meta = {
  name: 'dente-cycle28-QQ',
  description: 'Цикл 28, волна QQ: ноль вместо тридцати в тексте коммита, живая нулевая дверь, охрана слепая к трём формам, выдуманный контекст, классификация 64 красных гейтов, недоделанная миграция telegram',
  phases: [
    { title: 'Build' },
    { title: 'Review' }
  ]
};

const LAW = `Ты исполнитель в волне QQ. Проект: C:\\Clinic_MVP\\dental-crm (Windows, bash). Работай молча и коммить.

=== ДОКАЗАТЕЛЬСТВО: ТРИ ЧИСЛА, НЕ ОДНО ===

1. EXIT_PASS — код выхода на твоей правке.
2. EXIT_BITE — код выхода на искусственно СЛОМАННОЙ КОПИИ. Отчёт без него — НЕВЫПОЛНЕННЫЙ пакет.
3. COUNT — СКОЛЬКО ТЕСТОВ прогналось. Обязательно, и вот почему:
   \`node --test\` с несуществующим путём В СПИСКЕ выходит с кодом 0 И МОЛЧИТ — код 1 бывает только когда не
   найдены ВСЕ пути. Проверено ведущим: реальный путь + опечатка → «код 0, tests 6 pass 6», то есть
   правдоподобный зелёный вместо всей батареи. Пустая маска тоже даёт 0.
   **КОД ВЫХОДА НЕОБХОДИМ И НЕ ДОСТАТОЧЕН.** Называй число тестов и сверяй с ожидаемым. \`skipped\` тоже называй:
   пропуск прячется за зелёным кодом так же, как отсутствующий файл.
   Предпочитай МАСКУ списку: \`--test "src/pricelist/*.test.ts"\` — переехавший файл тогда не выпадет молча.

КАК ЛОМАТЬ: копию файла мутировать и прогнать по ней копию набора. МУТАЦИЯ БЬЁТ В ПОВЕДЕНИЕ, а не в
компиляцию: сломать сборку — не доказательство. Верни ровно тот дефект, который чинишь.
КОПИЮ УДАЛИ ЗА СОБОЙ. Если копия обязана лежать рядом с оригиналом (у компонента иначе не разрешаются
импорты) — назови её \`__bite<что>.tsx\`: такие имена закрыты от коммита в .gitignore. \`git status\` по продукту
обязан остаться чист.

=== ОБЫЧНЫЙ ЗАКОН ===
- Коммить, КАК ТОЛЬКО КОМПИЛИРУЕТСЯ. Улучшение — вторым коммитом. Нечего коммитить = провал пакета.
- Сообщение в файл .agents/archon/packets/<ID>/commitmsg.txt, потом
  for i in $(seq 1 20); do git add -- <твои пути> && break || sleep 6; done
  for i in $(seq 1 20); do git commit -F .agents/archon/packets/<ID>/commitmsg.txt -- <твои пути> && break || sleep 6; done
  РЕТРАЙ 20×6 с: индекс общий. \`git add\` проваливается МОЛЧА, а \`git commit\` потом врёт «pathspec did not match».
- НЕ ПУШИТЬ. origin двигает только ведущий. git stash, rebase, reset --hard, amend ЗАПРЕЩЕНЫ. Чужие stash не трогать.
- **ЧИСЛО В СООБЩЕНИИ КОММИТА — ЭТО ЗАЯВКА, КОТОРУЮ ПРОЧТУТ КАК ФАКТ.** В прошлой волне пакет написал
  «17 имён / 0 мёртвых», а компилятор находил 34 мёртвых — и следующий агент прочитал бы файлы как чистые.
  Не пиши числа, которых не измерил командой. Ноль — самое дорогое из них.
- Тема с [ARCHON], по-русски. НИКАКОЙ атрибуции инструмента. git log -1 --format='%(trailers)' обязан быть ПУСТ.
- Шаг 0: git status --porcelain -- <твой путь>. ГРЯЗНЫЙ файл правь ТОЧЕЧНО через Edit, целиком не перезаписывай.
- Никаких скриптов и node -e, ПИШУЩИХ исходники. Никакой regex-хирургии: один такой скрипт уничтожил 10 554
  кириллических символа в routes/telegram.ts. node -e только на чтение, и лучше скриптом-файлом: bash съедает
  обратные слэши в \`node -e '…'\`, ведущий на этом получил ложный замер и синтаксическую ошибку.
- НИКОГДА git remote -v: в URL живые токены. Не читать .env, .env.local, local-secrets/.
- Поиск ТОЛЬКО rg и fd с областью: rg -g '!node_modules' -g '!dist' -g '!.git'. grep -r и find / ЗАПРЕЩЕНЫ.
- Общие гейты ведущего НЕ ЗАПУСКАТЬ: npm run typecheck (полный), npm run build, весь npm test, миграции, сиды.
  Свои \`npm run smoke:*\` — ОБЯЗАТЕЛЬНО. Срез по одной области (\`npx tsc -p apps/web/tsconfig.json --noEmit\`)
  допустим, но скажи в отчёте, что запускал.
  ВЕБ-ТЕСТЫ ТРЕБУЮТ ЗАГЛУШКУ CSS: из apps/web — \`node --import tsx --import ./testCssStub.mjs --test <путь>\`.
  Без неё падает ЦЕЛЫЙ файл, и это выглядит как сломанный тест.
- ЕСЛИ ЗАМЕР ПРОТИВОРЕЧИТ БРИФУ — ВЕРЕН ЗАМЕР, скажи первой строкой. За четыре волны агенты опровергли
  ведущего шесть раз и каждый раз были правы.

ВОЗВРАТ: COMMITS | EXIT_PASS / EXIT_BITE / COUNT / MUTATION | MEASURED | CLAIM | CONTRADICTED | FOUND`;

const PACKETS = [
  {
    id: 'QQ1',
    label: 'ноль подставлен вместо тридцати, и число живёт в тексте коммита',
    files: 'apps/web/src/components/settings/sources/** (SourcesConnectorGrid.tsx, SourcesIntegrationPresets.tsx, SourcesDicomCapability.tsx)',
    brief: `P0, ПЕРЕДЕЛКА. Пакет прошлой волны заявил в ПОСТОЯННОМ тексте коммита «17 имён / 0 мёртвых и 19 / 0».
Ревьюер измерил: компилятор находит **34 мёртвых локали** в этих двух файлах, а до экрана доходят **2 пропса
из 17** и **4 из 19**.

ПОЧЕМУ ЭТО P0, А НЕ УБОРКА: ложное число в сообщении коммита — durable дезинформация. Следующий агент
прочитает эти файлы как чистые и не станет их смотреть. Это тот же класс, что реестр долга, врущий про
закрытый долг, — и он уже держал HEAD красным в этой кампании.

ЧТО ИЗМЕРЕНО РЕВЬЮЕРОМ, повтори это первым делом сам:
  npx tsc -p apps/web/tsconfig.json --noEmit --noUnusedLocals
даёт 3752 TS6133 по всему apps/web, из них **62 в двух файлах третьего коммита**, и 17 мёртвых локалей
\`typed*\` в КАЖДОМ из них. Плюс критерий пакета (\`rg -c <имя>\` = 1, единственная строка деструктуризации)
показывает 30 мёртвых пропсов.
\`SourcesConnectorGrid.tsx\` — 133 строки, и весь его \`return\` использует ДВА имени.
\`SourcesIntegrationPresets.tsx\` — ЧЕТЫРЕ.

МЕХАНИЗМ: пропсы «отмыты» через один промежуточный \`const\`. Исполнитель этот же приём РАСПОЗНАЛ и убрал в
своём первом коммите (\`SettingsAiTab\`), то есть механизм видел — и в третьем коммите его пропустил.

ЧТО СДЕЛАТЬ:
1. Пересчитай сам, командой, и назови ЧЕСТНЫЕ числа: сколько имён в контракте, сколько живых, сколько мёртвых.
2. Мёртвые — либо начать использовать, либо СНЯТЬ ИЗ ЦЕПОЧКИ целиком (контракт → передача → деструктуризация).
   Оставить протянутым — единственный незаконный исход.
3. Ложное число исправь ЗАПИСЬЮ: сообщение прошлого коммита переписать нельзя (историю не трогаем), поэтому
   в своём сообщении явно скажи, какое число было заявлено, какое измерено и чем измерено. Тогда поиск по
   истории даст правду, а не только ошибку.
4. Коммиты 1 и 2 прошлой волны ревьюер воспроизвёл и признал честными — ИХ НЕ ТРОГАЙ.

ДОКАЗАТЬ: EXIT_PASS среза \`npx tsc -p apps/web/tsconfig.json --noEmit\` = 0; COUNT = число TS6133 в твоих
файлах до и после с флагом \`--noUnusedLocals\`; EXIT_BITE — в копии добавь чтение несуществующего пропса и
покажи, что контракт ЛОВИТ это (а с \`as any\` молчал).`
  },
  {
    id: 'QQ2',
    label: 'живая нулевая дверь: неизвестный итог печатается как «0 ₽»',
    files: 'apps/web/src/FinanceView.tsx + компонент FinancePlanningOverview (найди его сам) + apps/web/src/useAppLogic.tsx ТОЧЕЧНО (файл ГРЯЗНЫЙ, чужая работа в полёте)',
    brief: `P1. Починка прошлой волны сделала \`money(null)\` → «не определено», но через этот путь она ИНЕРТНА.

ЗАМЕР ВЕДУЩЕГО ПО HEAD, дверей две и живая ОДНА:
  • ЖИВАЯ — \`useAppLogic.tsx:5030\`: \`patientBillingSummary = useMemo<Dashboard["billingSummary"]>(() => {
    if (!dashboard || !documentPatient) return { totalPlannedRub: 0, totalDiscountRub: 0, … } … })\`.
    То есть «дашборда нет» и «пациент не выбран» дают объект ИЗ НУЛЕЙ, и \`money(0)\` честно печатает «0 ₽».
  • НЕДОСТИЖИМАЯ СЕГОДНЯ — умолчание \`billingSummary = EMPTY_BILLING_SUMMARY\` (\`FinanceView.tsx:150\`):
    единственный вызывающий \`App.tsx:4276\` пропс ВСЕГДА передаёт. Проверено поштучно. Не выдумывай ей вред.

ЧТО ЭТО ЗНАЧИТ ДЛЯ КЛИНИКИ: «пациент не должен ничего» и «итог не посчитан» — разные утверждения, а на экране
одно. Финансовый итог по пациенту при незагруженном дашборде выглядит как ноль долга.

ГЛАВНАЯ ТРУДНОСТЬ, ИЗМЕРЕНА ВЕДУЩИМ, НЕ ОБХОДИ ЕЁ МОЛЧА: поля \`billingSummarySchema\`
(\`packages/shared/src/index.ts:2056\`) объявлены \`nonNegativeMoneyRubSchema\`, то есть \`number\` БЕЗ null.
Значит есть два пути, и выбор надо обосновать письменно:
  а) \`| null\` на самом ПРОПСЕ (\`billingSummary: BillingSummary | null\`), и потребитель рисует блок целиком как
     неопределённый. Дешевле, не трогает общий контракт денег, правка в трёх файлах;
  б) сделать поля контракта nullable. Рябь в api, в базу и во всех потребителей — это смена рамок, и в этом
     пакете её делать НЕ НАДО. Если считаешь, что только так — напиши это в FOUND и остановись на (а).
ОБЩИЙ КОНТРАКТ ДЕНЕГ (\`packages/shared\`) В ЭТОМ ПАКЕТЕ НЕ ПРАВИТЬ.

\`useAppLogic.tsx\` ГРЯЗНЫЙ — другой автор в полёте. Правь Edit по конкретным строкам, коммить только свои пути.

ДОКАЗАТЬ: тест на apps/web с заглушкой CSS. Обе стороны обязательны: при отсутствии дашборда экран НЕ
показывает «0 ₽» по итогам, а при загруженном дашборде с настоящими нулями показывает «0 ₽» и не прячет их.
EXIT_BITE: верни объект из нулей в копии и покажи, что тест краснеет.`
  },
  {
    id: 'QQ3',
    label: 'охрана денег слепа к трём формам, и одна из них хуже дефекта',
    files: 'apps/web/src/tests/moneyUnknownNotZero.test.ts (+ при необходимости места вызова, которые она сторожит, КРОМЕ FinanceView.tsx и useAppLogic.tsx — их держит QQ2)',
    brief: `P1. Охрана, написанная в прошлой волне, ловит одну форму из четырёх. Измерено ревьюером четырьмя мутациями
с кодами выхода:
  money(x ?? 0)          → EXIT 1   ЛОВИТ
  money(x || 0)          → EXIT 0   не ловит
  money((x) ?? 0)        → EXIT 0   не ловит
  money(Number(x ?? 0))  → EXIT 0   не ловит

\`|| 0\` ХУЖЕ ИСХОДНОГО ДЕФЕКТА, и это главное в пакете: \`?? 0\` гасит только \`null\` и \`undefined\`, а \`|| 0\`
гасит ЕЩЁ и настоящий ноль, и пустую строку, и \`NaN\`, и \`0\` сам по себе. То есть охрана пропускает форму,
которая наносит больше вреда, чем та, которую она сторожит.

ЧТО СДЕЛАТЬ: охрана обязана краснеть на ВСЕХ ЧЕТЫРЁХ формах и на любых пробелах внутри них. Подумай, чем
именно ловить: текстовый поиск по одной подстроке очевидно недостаточен (он и дал эту дыру). В репозитории уже
есть образец, где рукописный текстовый разбор заменён настоящим компилятором TypeScript — пакет MM2 в
\`apps/api/src/tests/webCallsExistingRoutes.test.ts\` (\`import ts from "typescript"\`, \`ts.isStringLiteralLike\`),
и причина была та же: текстовое совпадение не отличало живой код от закомментированного и теряло шаблонные
строки. \`typescript\` уже в зависимостях. Решение твоё, но обоснуй.

НЕ РАСШИРЯЙ ОБЛАСТЬ БЕЗ НУЖДЫ: предмет пакета — сама охрана. Если найдёшь новые места с \`|| 0\` на деньгах —
в FOUND, а чинить их можно только если они НЕ в файлах QQ2.

ДОКАЗАТЬ: COUNT и EXIT_PASS набора; EXIT_BITE — четыре мутации, по одной на форму, и все четыре обязаны дать
код 1. Приведи таблицу форма | код выхода до | после. Это и есть весь пакет.`
  },
  {
    id: 'QQ4',
    label: 'контекст ВЫДУМЫВАЕТ значения вместо отказа',
    files: 'apps/web/src/contexts/AppLogicContext.tsx + его потребители, КРОМЕ FinanceView.tsx / useAppLogic.tsx (QQ2) и components/settings/sources/** (QQ1)',
    brief: `P1. Четвёртое место стирания типов в этой кампании и худшее из четырёх: остальные делают чтения
непроверяемыми, а это ВЫДУМЫВАЕТ значение.

\`apps/web/src/contexts/AppLogicContext.tsx:21\` — \`return {} as AppLogicContextType;\`
Найдено ревьюером волны PP, ведущим НЕ перемерено — проверь сам и скажи, если замер не подтвердится.

ЧТО ЭТО ДЕЛАЕТ: без провайдера хук отдаёт ПУСТОЙ ОБЪЕКТ, приведённый к типу контекста. Каждый пропс
становится \`undefined\`, при этом НЕТ ни исключения, ни предупреждения в консоли. Компонент, отрендеренный вне
провайдера, тихо показывает пустоту вместо того, чтобы упасть на разработчике.

ЭТО ТА ЖЕ ТЕМА, ЧТО ВСЯ КАМПАНИЯ, только в другом слое: «неизвестно» подменяется значением. В деньгах это был
ноль (\`money(null)\` → «0 ₽»), в предупреждениях — английский ключ, здесь — пустой объект.

ЧТО СДЕЛАТЬ: отсутствие провайдера обязано быть ЯВНЫМ. Стандартный ход в React — бросать понятную ошибку из
хука («useAppLogic вызван вне AppLogicProvider»), потому что это дефект сборки дерева, а не состояние данных,
и увидеть его должен разработчик, а не пациент. Но решение твоё:
  • сначала ИЗМЕРЬ, есть ли сегодня потребители ВНЕ провайдера. Если есть — падение сломает их, и тогда
    порядок обратный: сперва починить дерево, потом ужесточать хук. Скажи, что нашёл;
  • если потребителей вне провайдера нет, бросок безопасен и это самый честный вариант;
  • тесты, рендерящие компонент без провайдера, — отдельный случай: у них должен быть провайдер-обёртка, а не
    поблажка в продукте.

ДОКАЗАТЬ: COUNT и EXIT_PASS. EXIT_BITE особый и он же смысл пакета: тест, который рендерит потребителя БЕЗ
провайдера, обязан КРАСНЕТЬ (получать ошибку), а не проходить на пустом объекте. Покажи, что до правки он
проходил, а после — падает с внятным текстом.`
  },
  {
    id: 'QQ5',
    label: 'разведка: 64 красных smoke-гейта — среда или дефект',
    files: 'ТОЛЬКО ЧТЕНИЕ по scripts/**. Пишешь ЕДИНСТВЕННЫЙ файл: .agents/archon/recon/QQ5-smoke-triage/DOSSIER.md',
    brief: `P1, разведка, и она снимает главную слепоту кампании.

ЗАМЕР ВЕДУЩЕГО: все циклы я считал только семейство \`-source\` (41 скрипт, 35 зелёных / 6 красных) и называл
это «слоем стражей». Прогон ВСЕХ \`smoke:*\` из package.json дал **127 скриптов, 63 зелёных, 64 КРАСНЫХ**.

**ЧИСЛО 64 НЕЛЬЗЯ ПЕРЕДАВАТЬ КАК «64 ДЕФЕКТА», И ЭТО ТВОЯ ГЛАВНАЯ ЗАДАЧА — РАЗДЕЛИТЬ.** В красный набор
заведомо попали гейты, которым нужна СРЕДА, а не исправный код:
  • живой сервер api — \`smoke:workspace-live-*\`, \`smoke:visit-live-workflow\`;
  • безголовый браузер — \`smoke:browser-*\`, \`smoke:mobile*\` (кампанией уже измерено: при большом числе
    процессов браузер не поднимается ВОВСЕ, и гейт умирает молча);
  • живая PostgreSQL с фикстурами — \`smoke:chains\`, \`smoke:db-runtime-contract\`,
    \`smoke:telegram-outbox-persistence\`, \`smoke:schema-column-parity\`;
  • свежая сборка \`dist\` — \`smoke:dist-freshness\` был красным ЗАКОННО, потому что во время прогона агенты
    правили исходники api.
Мой прогон шёл при 32 процессах \`claude\` и \`commit\` под 54 ГБ, то есть часть красных — артефакт нагрузки.
Повторить на тихой машине я не смог: ОЗУ упало до 1 ГБ и прогон не уложился в десять минут.

ЧТО ОТ ТЕБЯ НУЖНО — ДОСЬЕ С КЛАССОМ ПО КАЖДОМУ ИЗ 64:
  NEEDS_ENV:<что нужно>  — сервер / браузер / база / сборка. Гейт не оценивался, нужна среда. Скажи, ЧТО именно.
  BROKEN                  — скрипт физически не работает: путь собран сложением строк («dental-crmpps\\web»),
                            вызов без обязательного аргумента, needles описывают мёртвую копию файла.
  STALE                   — текст переехал при разборе монолита, поведение есть.
  BRITTLE                 — краснеет за \`?.\`, \`?? []\`, переименование, пробелы, то есть за улучшение кода.
  RESURRECTION            — требует ВЕРНУТЬ удалённый дефект.
  COMMENT                 — требование «выполняет» строка \`// Compliance:\` в
                            \`apps/web/src/useSettingsDerivations.tsx\`, а в разметке текста нет. Замер прошлой
                            волны: добавь этот файл в набор стража — красных 13 вместо 49.
  REAL                    — поведения в продукте НЕТ. Самый ценный класс: это будущие пакеты.

СПОСОБ: прогоняй по одному, \`npm run <имя> > /tmp/t.log 2>&1; echo $?\`, и ЧИТАЙ текст падения — класс почти
всегда виден по нему. Не чини НИЧЕГО: ты пишешь досье. Правка стража в этом пакете запрещена, иначе следующая
волна не сможет опереться на твои числа.

ДОСЬЕ: таблица гейт | класс | текст падения (сокращённо) | что нужно для оценки. В конце — СЧЁТ ПО КЛАССАМ и
отдельно список REAL с точной строкой требования по каждому. Плюс ответь на вопрос, который я не смог
проверить: сколько из 64 краснеют ТОЛЬКО из-за среды. Это число и есть цена моей слепоты.

ДОКАЗАТЬ: досье с дословными кодами выхода. COUNT здесь — число разобранных гейтов из 64; если не успеешь все,
разбери столько, сколько успеешь ДО КОНЦА, и честно скажи сколько осталось. Двадцать разобранных точно лучше
шестидесяти угаданных.`
  },
  {
    id: 'QQ6',
    label: 'миграция telegram на базу недоделана: состояние теряется при перезапуске',
    files: 'apps/api/src/telegram/** + apps/api/src/routes/telegram.ts (ОСТОРОЖНО, см. бриф) + при необходимости новый тест в apps/api/src/tests/',
    brief: `P0 архитектурный, и это самый крупный «сырые файлы вместо системы» в проекте. ЗАМЕР ВЕДУЩЕГО.

\`routes/telegram.ts\` импортирует **29 имён из \`../sampleData.js\`** и обслуживает **17 маршрутов**. А в
\`sampleData.ts\` — **четыре массива В ПАМЯТИ и НОЛЬ обращений к базе**:
\`sampleData.ts:275\` — \`export const denteTelegramChatLinks: DenteTelegramChatLink[] = [];\`
плюс \`denteTelegramLinkCodes\`, \`denteTelegramWebhookEvents\`, \`denteTelegramOutboxDeliveryReceipts\`.

МИГРАЦИЮ НАЧАЛИ И НЕ ДОВЕЛИ, и это видно по числу импортёров:
  \`telegram/outbox.ts\`     — **3 импорта, ПОДКЛЮЧЁН** (отсюда и существует \`smoke:telegram-outbox-persistence\`)
  \`telegram/chatLinks.ts\`  — **0 импортов**, при том что написан НА БАЗЕ:
                             \`import { db } from "../db/client.js"\`, \`db.select().from(denteTelegramChatLinks)\`
  \`telegram/linkCodes.ts\`  — **0 импортов**, тоже на базе
Таблицы в схеме ЕСТЬ. Одну сущность из четырёх перевели, для двух модули готовы и лежат мимо.

СЛЕДСТВИЕ, выведенное из инициализации пустым массивом: активные chat-links и выданные link-codes **теряются
при каждом перезапуске сервера**. Link-code — это то, чем пациент привязывает свой Telegram к клинике;
chat-link — сама привязка. Перезапуск обнуляет привязки, и никто об этом не узнаёт.

ВАЖНОЕ УТОЧНЕНИЕ ВЕДУЩЕГО, не потеряй: это НЕ сироты. Функции \`listDenteTelegramChatLinks\`,
\`revokeDenteTelegramChatLink\` и родня имеют по 5-6 совпадений в репозитории — их ЗОВУТ, только из
\`sampleData.ts\`, где лежит ВТОРАЯ реализация тех же имён (третья в \`sampleData_opt.ts\`, исключён из сборки).
То есть **два владельца одного правила, и подключён худший**.

ЧТО СДЕЛАТЬ. Начни с ОДНОЙ сущности и доведи до конца — так у тебя будет рабочий образец и коммит, который
есть чем защитить. Бери \`chatLinks\`, она проще: две функции против трёх.
1. Прочитай \`telegram/outbox.ts\` и то, КАК его подключили: это готовый образец миграции в этом же проекте,
   выполненной кем-то до тебя. Повтори форму, не изобретай свою.
2. Синхронная версия из \`sampleData.ts\` зовётся БЕЗ \`await\` (\`routes/telegram.ts\` около :2838 —
   \`revokeDenteTelegramChatLink(request.params.linkId, {…})\`). Асинхронная потребует протянуть \`await\` по
   вызывающим местам. Это и есть основная работа пакета.
3. Проверь границу арендатора: у маршрутов telegram общий секрет периметра
   (\`requireTelegramControlPlaneAccess\`), а арендатор берётся ПО ДАННЫМ КЛИЕНТА — это отдельный незакрытый
   пункт очереди, и в этом пакете его чинить НЕ НАДО. Но новый путь к базе обязан фильтровать по
   \`organizationId\`, а не читать всё. Если у функции на базе фильтра нет — это находка, скажи.
4. Мёртвую синхронную реализацию из \`sampleData.ts\` НЕ УДАЛЯЙ в этом же коммите: сперва подключение и
   зелёный тест, удаление — вторым коммитом и с проверкой \`rg\` по всем вызывающим.

\`routes/telegram.ts\` ОПАСЕН: в нём скрипт с regex-хирургией однажды уничтожил **10 554 кириллических
символа**. Только точечные Edit, никаких скриптов на запись, никакого переписывания файла целиком.

ДОКАЗАТЬ: COUNT и EXIT_PASS нового теста. Тест обязан показать ГЛАВНОЕ — что состояние ПЕРЕЖИВАЕТ перезапуск,
то есть запись видна во ВТОРОМ независимом чтении из базы, а не в той же переменной процесса.
EXIT_BITE: верни маршрут на синхронную версию из \`sampleData.ts\` в копии и покажи, что тест краснеет.
Плюс \`npm run smoke:telegram-outbox-persistence\` и \`npm run smoke:telegram-bot\` — оба до и после, с кодами.`
  }
];

phase('Build');

const done = await pipeline(
  PACKETS,
  (packet) =>
    agent(
      `${LAW}\n\n=== ТВОЙ ПАКЕТ ${packet.id}: ${packet.label} ===\nФАЙЛЫ, КОТОРЫЕ ТЫ ДЕРЖИШЬ: ${packet.files}\nЧужих файлов не трогай — их держат другие агенты этой же волны.\n\n${packet.brief}`,
      { label: `build:${packet.id}`, phase: 'Build', effort: 'high' }
    ),
  (buildResult, packet) => {
    if (!buildResult) return null;
    return agent(
      `Ты ревьюер пакета ${packet.id} в волне QQ. Проект C:\\Clinic_MVP\\dental-crm.
Ты НЕ ПРАВИШЬ КОД. Пишешь ОДИН файл: .agents/archon/packets/${packet.id}/review.md, коммитишь только его
(ретрай 20×6 с, сообщение в файл, [ARCHON] в теме, без атрибуции, трейлеры пусты). НЕ ПУШИТЬ.

ПЕРВОЙ СТРОКОЙ — вердикт в форме РОВНО \`## VERDICT: <ОДНО СЛОВО>\`:
  IN_PROGRESS      — пока пишешь. Ставь это СРАЗУ, и это разрешённое начальное значение: в прошлых волнах
                     заглушка «NEEDS_REWORK» дважды была прочитана ведущим как настоящий вердикт.
  SOUND / SOUND_WITH_NITS / NEEDS_REWORK / REVERT — окончательные.
НЕ КОПИРУЙ СПИСОК ВАРИАНТОВ в строку вердикта: строка с несколькими словами вердиктом не считается.
И ОБЯЗАТЕЛЬНО ДОПИШИ ТЕЛО: вердикт с телом в 3 строки ведущий читает как незаконченный.

=== БРИФ ИСПОЛНИТЕЛЯ (суди по нему) ===
${packet.brief}

=== ЧТО ОН ВЕРНУЛ ===
${buildResult}

ПРОВЕРЯЙ ПО ДИСКУ, НЕ ПО СЛОВАМ:
1. Вернул ли он EXIT_PASS, EXIT_BITE, COUNT и MUTATION. Нет EXIT_BITE или нет COUNT — NEEDS_REWORK.
   ПОВТОРИ МУТАЦИЮ САМ. Мутация, ломающая компиляцию вместо поведения, доказательством НЕ является.
2. **ПРОВЕРЬ ЧИСЛА В СООБЩЕНИИ КОММИТА.** В прошлой волне пакет написал «0 мёртвых», а компилятор находил 34,
   и это осталось в постоянном тексте. Всякое число из сообщения — пересчитай командой.
3. Нет ли дыры \`node --test\`: если он давал явный СПИСОК путей, проверь, что все они существуют. Несуществующий
   путь в списке даёт код 0 и молчание, то есть правдоподобный зелёный на меньшем числе тестов.
4. Коммиты есть? git show --stat. Только заявленные пути? Трейлеры пусты? Чужая незакоммиченная работа не
   подметена? В дереве постоянно лежат правки другого автора.
5. Решена ЗАЯВЛЕННАЯ задача или соседняя подешевле?
6. Прогони его проверку сам, истинный код без конвейера: cmd > /tmp/rev-${packet.id}.log 2>&1; echo $?
   Веб-тесты — только с \`--import ./testCssStub.mjs\` из apps/web, иначе падает целый файл.
7. Зелёный тест, не проверяющий ничего. В этой кампании класс встречался СЕМЬ раз: тест читал поля без
   проверки статуса; «замер» сравнивал две синхронные ветки; страж падал до первого из 11 утверждений;
   помощник теста повторял последовательность боевой ветки вместо вызова; требование «выполнял» комментарий;
   страж проверял НАЛИЧИЕ ИМЕНИ вместо ВЫЗОВА; охрана ловила одну форму \`?? 0\` из четырёх.
8. Не подставлено ли число вместо «неизвестно» — ноль, пустой объект, английский ключ.
9. Оставил ли он за собой мутационные копии (\`__bite*\`, \`_*tmp*\`): \`git status --porcelain\` по продукту.
10. REVERT только при реальном вреде: потеря чужих правок, утечка секрета, разрушение кириллицы, запись в
    исходники скриптом. Несделанная работа — NEEDS_REWORK.

Дальше: что проверено командой дословно с кодами выхода, что осталось, и раздел «## FOUND».
FOUND — самая ценная часть: за четыре волны ревьюеры нашли то, чего не видел никто, — год как цену услуги,
зелёный тест на сломанном коде, английский токен на экране, регресс, вернувший закрытый P0, подделку
требований комментарием, ноль вместо тридцати в тексте коммита и дыру в самом методе замера.`,
      { label: `review:${packet.id}`, phase: 'Review', effort: 'high' }
    );
  }
);

log(`волна QQ завершена: ${done.filter(Boolean).length}/${PACKETS.length} пакетов вернули ревью`);

return {
  packets: PACKETS.map((packet, index) => ({ id: packet.id, label: packet.label, review: done[index] ?? null }))
};
