[ARCHON] docs(вердикт): светлый снимок панели дублей оказался ночным, убраны протухшие метки ПУСТО

Свежий прогон ops-panels-shots против живой пары: выход 0, 35 файлов, 33 уникальных MD5.
Плиты открыты глазами.

Главное. light_duplicateAlert.png побайтно равен night_duplicateAlert.png, а
dark_duplicateAlert.png отличается. Я открыл ночной кадр: тёплая тёмно-оливковая панель с
янтарной левой границей. Значит светлый прогон отрисовал ночную панель.

Палитра ни при чём: три значения существуют и различаются — token-aliases.css:130 светлое
#f7fbf9, :140 тёмное #16211f, :149 ночное #1a1714, потребитель main.css:9583. Снятая
поверхность совпадает с ночным значением. Похоже на гонку: тема лежит в localStorage
(dente_theme_mode), и панель снимается до применения светлой. Причём сам конвейер в логе
печатает применённую тему и значение токена — то есть средства проверить у него есть, а
проверки нет.

Следствие для кампании: каждая светлая плита этого конвейера под подозрением, пока захват
не начнёт утверждать data-theme прямо перед снимком.

Убраны четыре файла *_duplicateAlert_ПУСТО.png от 07-27 22:19, тогда как прогон 09:03 снял
эту панель успешно. В .dente-ops-shots нет ни одного отслеживаемого файла, это чистый
мусор, который сообщил бы следующему проверяющему, что панель снять нельзя. Тот же класс,
что клоны и устаревшая сборка: артефакт, переживший условие, которое он фиксировал,
превращается в ложь.

Подтверждено глазами: угол починен — три кнопки стоят рядом над нижней навигацией и не
налезают на неё. Мёртвая ширина 45% на 720x1100 никуда не делась, пакет V6 бьёт верно.
