[ARCHON] chore(снимки): удалены 14 подделанных кадров — 2 картинки, разложенные под 14 именами

Из .dente-redesign-shots убраны 14 отслеживаемых PNG. Это не уборка по вкусу: файлы были
поддельным доказательством, лежавшим в репозитории.

ЧТО ЭТО БЫЛО, ПРОВЕРЕНО ХЕШАМИ ИЗ GIT ПЕРЕД УДАЛЕНИЕМ. На 14 файлов приходится ровно ДВА
уникальных md5:
  2e0e8e9e6846 — десять раз: desktop_{light,dark,night}_{schedule,shift,visit}
                 и desktop_light_shift_collapsed
  4272a7bfa564 — четыре раза: mobile_{light,dark}_{shift,visit}
Первую картинку я открывал лично: это НЕ экран приложения, а оверлей ошибки сборки CSS
(«[plugin:vite:css] [postcss] main.css:16846:24: Unknown word display» со строкой
«.nav-copy small {\n display: none;» — литеральный \n, записанный в стиль, то есть след правки
файла регулярным выражением) плюс полный стек postcss. Один кадр ошибки был подшит под десятью
именами разделов и тем.

ПОЧЕМУ ЭТО ОПАСНО, А НЕ ПРОСТО НЕОПРЯТНО. Именно на этом наборе прошлая проверка milestone
получила вердикт «одобрено» с формулировкой «56 уникальных MD5, ноль пустых страниц». Пока
файлы лежат в репозитории, следующий проверяющий пересчитает их и выдаст тот же вердикт снова.
Разделы schedule, shift и visit при этом не имеют ни одного годного десктопного снимка вообще —
их «снимки» и были этими клонами.

ЧЕСТНО ОБ АВТОРСТВЕ: удаление сделал не я. Я нашёл его несохранённым в рабочем дереве, проверил
все 14 файлов по хешам из git и подтвердил, что это ровно те две группы клонов, которые описаны
в .agents/archon/VISUAL_VERDICT.md §0 и в дополнении A. Фиксирую, потому что наполовину
удалённый набор поддельных артефактов — худшее из трёх состояний: он и не удалён, и не
восстановлен, и потеряется при любой следующей операции с деревом.

Уборка ПУСТЫХ МЕСТ, не уборка проблемы: годных кадров у schedule, shift и visit по-прежнему нет.
Заново снять их обязан конвейер после того, как в цикле 7 он научился проверять применённую тему
перед кадром (f8792f6c9, 59b685f32). Это долг, а не закрытый пункт.
