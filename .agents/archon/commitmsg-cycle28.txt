[ARCHON] chore(флот): цикл 28, волна QQ — доказательство теперь ТРИ числа, а не одно

Шесть пакетов. Пять из шести — прямые находки ревьюеров прошлой волны, то есть слой ревью снова дал больше,
чем слой исполнения.

ГЛАВНОЕ ИЗМЕНЕНИЕ ЗАКОНА, купленное дырой в моём же методе замера. Требовалось два числа (EXIT_PASS и
EXIT_BITE), теперь три: добавлен COUNT — сколько тестов прогналось.

ПРИЧИНА. Ревьюер PP1 обнаружил на СЕБЕ, а ведущий воспроизвёл контрольным прогоном:
  node --import tsx --test src/pricelist/analyzer.test.ts src/pricelist/ЭТОГО_ФАЙЛА_НЕТ.test.ts
  → код выхода 0, «tests 6 pass 6 fail 0»
node --test с несуществующим путём В СПИСКЕ выходит с кодом 0 И МОЛЧИТ; код 1 бывает только когда не найдены
ВСЕ пути. То есть переехавший файл молча выпадает, а отчёт выглядит зелёным. Ревьюер получил правдоподобные
«75/75, код 0» вместо настоящих 93/93.

САМОПРОВЕРКА: мои собственные заявки не пострадали. Все семь путей батареи прайса существуют (проверено
поштучно), в src/pricelist ровно семь тестовых файлов, прогон маской даёт те же 114/114. Проверил, а не
предположил. Но правило теперь шире прежнего «истинный код выхода без конвейера»: у маски СВОЯ дыра — пустая
маска тоже даёт 0. Значит КОД ВЫХОДА НЕОБХОДИМ И НЕ ДОСТАТОЧЕН, надо называть число тестов и сверять с
ожидаемым. Заодно объяснилось, почему ревьюеры настойчиво писали «skipped 0»: пропуск прячется за зелёным
кодом так же, как отсутствующий файл.

ВТОРОЕ: в закон вписан запрет писать в сообщение коммита числа, которых не измерил командой. Пакет прошлой
волны заявил «17 имён / 0 мёртвых», а компилятор находил 34 мёртвых локали — и следующий агент прочитал бы
эти файлы как чистые. Ложное число в постоянном тексте коммита — durable дезинформация, того же класса, что
реестр долга, врущий про закрытый долг. Ноль — самое дорогое из чисел.

ТРЕТЬЕ: IN_PROGRESS разрешён как НАЧАЛЬНОЕ значение строки вердикта. Придумал это ревьюер PP1 сам, и он
решил мою проблему: требование «пиши вердикт первой строкой» дважды порождало заглушку «NEEDS_REWORK»,
которую я принимал за настоящий вердикт. Теперь заглушка называется своим именем.

ЧЕТВЁРТОЕ: в закон вписана грабля с веб-тестами — они требуют загрузчик заглушки CSS
(node --import tsx --import ./testCssStub.mjs --test из apps/web), иначе падает ЦЕЛЫЙ файл и это выглядит
как сломанный тест агента. Я на этом чуть не обвинил исполнителя PP1.

ПАКЕТЫ: QQ1 переделка ложного «0 мёртвых» в sources/; QQ2 живая нулевая дверь useAppLogic:5030, из-за которой
починка money() инертна; QQ3 охрана денег ловит одну форму из четырёх, причём пропущенная `|| 0` хуже
исходного дефекта; QQ4 AppLogicContext:21 `return {} as` ВЫДУМЫВАЕТ пустой объект вместо отказа; QQ5 разведка
— разделить 64 красных smoke-гейта на «нужна среда» и настоящие дефекты, потому что все циклы я мерил только
41 из 127 и называл это слоем; QQ6 недоделанная миграция telegram на базу — 17 маршрутов живут на четырёх
массивах в памяти, при том что модули на базе написаны и подключены НУЛЬ раз.
