START: брифинг называет дефект на accessGuard.ts:18/22, но на HEAD там уже `namedDevelopmentModeActive()` (JJ2 сел коммитами f97acbe3d+f91d8b1f4) — измерение опровергает брифинг; беру реальную переработку из review.md пунктом 1 (экспорт единого предиката вместо четырёх копий) в пределах своего файла.
DONE: 9ed20ce03 (предикат экспортирован как namedDevelopmentModeActive + unguardedBypassAllowed, две клинические функции зовут его) и коммит теста; `node --import tsx --test src/tests/accessGuard.test.ts` → 30/30, истинный код возврата 0. НЕ закрыто и вне моей области: четыре копии предиката в imaging/schedule/settings/telegram и отслеживаемый в git корневой `.env` — он подставляет NODE_ENV, из-за чего правка не защищает `node dist/server.js`; передано ведущему.
