[ARCHON] test(настройки): храповик объявленного долга и второй конец цепочки пропсов

Первая редакция проверки объявляла долг по мёртвым именам двух тяжёлых вкладок
флагом `everyNameMustBeRead: false` — без числа. Такой флаг сторожит ноль: долг
объявлен, и дальше в него можно доливать сколько угодно новых мёртвых пропсов,
проверка останется зелёной. Ровно этот класс записи уже стоил кампании красного
HEAD: маршрут был СДЕЛАН, а строка в реестре долга продолжала утверждать, что
его нет.

Добавлено:

1. Храповик `measuredDeadNames` (338 в SettingsAuditTab из 478 имён, 251 в
   SettingsImportsTab из 481) — работает в обе стороны. Долг вырос — красный,
   появился новый пропс, который вынимают и не читают. Долг упал до нуля — тоже
   красный, потому что запись начала врать, и её место занимает
   `everyNameMustBeRead: true`.

2. Второй конец цепочки: App.tsx не должен передавать снятые шесть имён в
   <SettingsView …>. Снять имя из вкладки и оставить проброс в родителя — не
   починка: пропс продолжают передавать, и никто его не принимает. Разбор идёт по
   блоку самого элемента, а не по всему файлу: те же имена законно живут в App.tsx
   для других мест.

Замерено при написании и записано в комментарии, а не предположено: в
<SettingsView …> передаётся 493 пропса, а SettingsView.tsx разбирает из них РОВНО
один — activeStaffUser. Остальные 492 выбрасываются, и компилятор молчит из-за
индексной подписи [key: string]: any в SettingsViewProps. Числом-храповиком это
не записано умышленно: пока правило нарушено почти повсеместно, храповик стерёг бы
не дефект, а его размер. Предмет отдельный, размером с App.tsx.

Проверка: 29 утверждений, зелено. Каждое новое утверждение проверено мутацией
копии (рабочее дерево не менялось): долг 339 против 338 — красный; возвращённый
проброс pricelistWarningsText в <SettingsView …> — красный.
