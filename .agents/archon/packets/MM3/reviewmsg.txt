[ARCHON] ревью MM3: A/B на остатке воспроизведён своим зондом — SOUND_WITH_NITS

Проверено по диску, не по отчёту. Оба коммита существуют и трогают только заявленные
пять тестовых файлов, трейлеры пусты, чужая незакоммиченная работа не подметена.

Пять прогонов из apps/api, код выхода снят без конвейера: communicationCampaigns 15/15,
communicationsOutbox 16/16, deliveryReceipts 21/21, appointmentReminders 10/10,
appointmentConfirmation 19/19 — все exit=0 при skipped 0. Нуль пропусков здесь весом:
пропуск в этих файлах выражен через context.skip(), то есть попал бы в счётчик, если бы
база лежала.

Замер исполнителя воспроизведён своим зондом, а не принят на слово. В базу уложен тот же
остаток оборванного прогона (приёмы фикстуры уже в confirmed и cancelled, даты вчерашние),
на нём прогнаны обе версии файла:

    прежняя версия (60aadc4a0^)   exit=1  pass 14  fail 5
    эта версия, остаток заново    exit=0  pass 19  fail 0

Упали ровно те пять проверок, что названы в отчёте. Зелёного, не проверяющего ничего, нет:
ни одна проверка не ослаблена и не удалена — по обоим коммитам нет ни одной изменённой
строки с assert/test(/describe(, число тест-кейсов до и после совпадает (15/16/21/10/19).
Снятых .onConflictDoNothing() ровно 20, как заявлено; добавленных живых — нуль. Уборка
стоит перед каждым засевом. Ограничения, на которые опирается разбор, существуют в схеме
(schema.ts:2489 и 2510), блоки UUID пяти файлов не пересекаются.

Нитки: пятая цель брифа dayConfirmations на HEAD всё ещё сломана (7 живых мест, уборка
только в after) — её починка живёт лишь в незакоммиченном дереве другого агента; и перепись
в отчёте снята с грязного рабочего дерева, из-за чего аргумент «бриф врёт в счётчиках»
преувеличен — на HEAD счётчики брифа много ближе к правде.

FOUND в очередь ведущего: dispatcherConsentGate (3 места, засев 60/69/93, уборка только в
after 100-105) и inboundOptOut (2 места, засев 98/108, уборка 115-123) — тот же живой
дефект на той же таблице согласий; перепись из отчёта снята с грязного дерева; ручные
списки таблиц в purgeFixtures против готовых fixtureUuid()/purgeFixtureOrganizations().
