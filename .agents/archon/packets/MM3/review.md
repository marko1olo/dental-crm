## VERDICT: SOUND_WITH_NITS

Ревью пакета MM3. Всё ниже снято своими командами по диску, а не с его слов. Общие гейты
(полный typecheck, build, весь `npm test`) не запускались — они ведущего.

## 1. Коммиты и трейлеры

    git show --stat 692ad956c   -> 4 файла, только apps/api/src/tests/routes/:
                                   appointmentReminders.test.ts, communicationCampaigns.test.ts,
                                   communicationsOutbox.test.ts, deliveryReceipts.test.ts
    git show --stat 60aadc4a0   -> 1 файл: appointmentConfirmation.test.ts
    git log -1 --format='%(trailers)' 692ad956c  -> ПУСТО
    git log -1 --format='%(trailers)' 60aadc4a0  -> ПУСТО

Лишних путей нет ни в одном коммите: только пять тестовых файлов, ни исходников, ни чужих
каталогов. Чужая незакоммиченная работа не подметена — `dayConfirmations`, `patientRecall`,
`waitlistMatches` остались `M` в рабочем дереве и в его коммиты не попали. Временных зондов
(`_mm3*`) на диске нет: `fd -H '_mm3|mm3Contam|mm3OldAc'` пусто. Все пять тронутых файлов
закоммичены целиком — `git status --porcelain` по ним пуст.

## 2. Прогоны, истинный код выхода без конвейера

Из `apps/api`, `node --import tsx --test src/tests/routes/<файл> > /tmp/rev-MM3-<файл>.log 2>&1; echo $?`:

    communicationCampaigns    exit=0   tests 15  pass 15  fail 0  skipped 0
    communicationsOutbox      exit=0   tests 16  pass 16  fail 0  skipped 0
    deliveryReceipts          exit=0   tests 21  pass 21  fail 0  skipped 0
    appointmentReminders      exit=0   tests 10  pass 10  fail 0  skipped 0
    appointmentConfirmation   exit=0   tests 19  pass 19  fail 0  skipped 0

Идемпотентность, дважды подряд одним файлом: `communicationCampaigns run1=0 run2=0`, оба
`pass 15 fail 0 skipped 0`.

`skipped 0` здесь действительно доказательство, а не украшение: в этих файлах пропуск выражен
через `context.skip("база недоступна")` — настоящий пропуск node:test, который попадает в
счётчик. Если бы база лежала, счётчик был бы не нулём, а не тихим `pass`. Postgres слушает
127.0.0.1:5432 (проверено `netstat`).

## 3. Главный вопрос: заявленная задача или соседняя подешевле

Задача решена, и она НЕ соседняя. Правка не косметическая и не «зелёное ради зелёного» —
я воспроизвёл его A/B сам, своим зондом (удалён после замера, в коммиты не попадал).

Уложен ровно тот остаток, какой оставляет прогон, оборванный до `after`: фикстура
`dce70000-…-0601` на месте, но приёмы уже перевёрнуты — `604` в `confirmed`, `606` в
`cancelled`, даты вчерашние. Затем на ОДНОМ И ТОМ ЖЕ остатке прогнаны две версии файла:

    прежняя версия (git show 60aadc4a0^)  exit=1   tests 19  pass 14  fail 5  skipped 0
    эта версия, остаток уложен заново     exit=0   tests 19  pass 19  fail 0  skipped 0

Упали у прежней версии ровно те пять, что он назвал, без единого расхождения:
«подтверждение переводит приём в confirmed и показывает страницу», «повторное нажатие не
считается ошибкой», «отмена освобождает слот, снимает напоминание и ставит задачу
администратору», «повторная отмена не создаёт вторую задачу», «переход по ссылке отмечается
временем». Замер в отчёте — настоящий.

Зелёного, не проверяющего ничего, нет, и проверено это не рассуждением:

- Ни одна проверка не ослаблена и не удалена. `git show <hash> -U0 | rg '^[+-].*(assert|test\(|describe\()'`
  по ОБОИМ коммитам не дал ни одной строки. Число тест-кейсов до и после совпадает во всех
  пяти файлах: 15/16/21/10/19.
- Число снятых `.onConflictDoNothing()` совпадает с заявленным ровно: 14 в `692ad956c` плюс
  6 в `60aadc4a0` = **20**, добавленных живых — 0. Остатки `onConflictDoNothing` в этих пяти
  файлах, которые видит `rg`, — только текст комментариев, живого кода ноль.
- Уборка стоит перед КАЖДЫМ засевом, а не в одном месте: число блоков `before` против числа
  вызовов `purgeFixtures()` — 1/2, 1/2, 1/2, 1/2 и 2/4 (то есть вход плюс выход в каждом).
- Разбор сделан по схеме, а не на глаз: оба ограничения, на которые он ссылается,
  существуют — `apps/api/src/db/schema.ts:2489` `communication_outbox_org_dedupe_unique`
  (org, dedupe_key) и `schema.ts:2510` `patient_communication_consents_unique`
  (org, patient, channel, scope).
- Блоки UUID пяти файлов не пересекаются (`…0001`, `…0101`, `…0201`, `…0501`, `…0601`),
  поэтому уборка по организации не может снести фикстуру соседнего файла при параллельном
  прогоне по маске.

Подмена списка файлов оправдана, проверил все три его CONTRADICTED:

1. `5c82ade0b` — предок HEAD, и трогает он ровно четыре из пяти названных в брифе файлов
   (`patientCommunicationLog`, `patientDuplicates`, `patientRecall`, `waitlistMatches`).
   В `patientRecall` и `waitlistMatches` на HEAD `purgeFixtures()` стоит ДО засева (строки 80
   и 90 против засева с 82 и 98) — эти места закрыты.
2. Механизм действительно обратный описанному в брифе, и это подтверждено исполнением, а не
   чтением: остаток с ЗАДАННЫМ id даёт конфликт, и прежняя версия на нём краснеет пятью
   проверками. Рецепт «задать детерминированный id» здесь не лечит — id уже задан.
3. `managerReports.test.ts` последним тронут коммитом `0950647d0` (чужим, не его), и там
   `removeFixtureRows()` вызван до засева (строка 94) — то есть и «файл не мой», и «не P0»
   верно.

## 4. Что осталось

- **`dayConfirmations.test.ts` — пятая цель брифа — на HEAD всё ещё сломана**: 7 живых
  `onConflictDoNothing` в засеве, уборки перед засевом нет, только `after`. Починка
  существует ТОЛЬКО в незакоммиченном рабочем дереве другого агента (`git diff HEAD` по этому
  файлу показывает снятие `onConflictDoNothing` и уборку до засева). Он правильно её не
  тронул, но в коммитах кампании этой починки пока нет — если чужая работа потеряется,
  место останется сломанным.
- Пять сломанных мест того же класса не тронуты сознательно: `dispatcherConsentGate` (3),
  `inboundOptOut` (2). Оба подтверждены построчно, см. FOUND.
- Декоративные `onConflictDoNothing` остаются в `patientDuplicates` (11 по `rg`),
  `patientCommunicationLog` (6), `managerReports` (10) — от накопления они защищены уборкой
  до засева, то есть не P0, но об идемпотентности лгут.

## FOUND

Новое, в очередь ведущего.

1. **`apps/api/src/tests/dispatcherConsentGate.test.ts` — тот же дефект, живой** (его находка,
   подтверждена построчно). Засев с `onConflictDoNothing` в `before`: строки 60, 69, 93.
   Уборка только в `after`: 100-105. Таблица — `patientCommunicationConsents`, ровно та, на
   которой подмена `granted`→`revoked` доказана исполнением. 3 места.
2. **`apps/api/src/tests/inboundOptOut.test.ts` — то же.** Засев 98, 108; уборка только в
   `after` 115-123. 2 места.
3. **Перепись в отчёте снята с ГРЯЗНОГО рабочего дерева, и из-за этого аргумент «бриф
   врёт в счётчиках» преувеличен.** На HEAD `rg -c` даёт `dayConfirmations 7` (бриф: 7 —
   совпадает), `patientRecall 7` (бриф: 5), `waitlistMatches 8` (бриф: 7),
   `patientDuplicates 11` (бриф: 8), `patientCommunicationLog 6` (бриф: 4). Его числа
   (3/3/2) — это рабочее дерево с незакоммиченными правками других агентов. Счётчики брифа
   не «не сходятся с замером», а просто сняты до `5c82ade0b` и до чужой работы. Дефекта в
   коде нет, дефект в обосновании; вывод «список цели устарел» при этом верен.
4. **Списки таблиц в `purgeFixtures()` держатся руками, по файлу.** Таблица, которую
   оборванный прогон задел, а список не перечисляет, даст падение по внешнему ключу на
   `delete organizations` во входном `before` следующего прогона. Направление правильное —
   падает громко, не тихо, — но класс снимается не этим: в
   `apps/api/src/tests/support/fixtureOrganizations.ts` уже есть `fixtureUuid()` (строка 95)
   и каталожная `purgeFixtureOrganizations()` (строка 181), а все пять файлов держат ручные
   блоки UUID и на этот механизм не переведены. Он это сам отметил; подтверждаю, что
   механизм существует и что перевод — отдельный пакет, снимающий весь класс сразу.

Нитки, из-за которых вердикт не SOUND: пятая цель брифа (`dayConfirmations`) в коммитах
кампании не закрыта и висит на чужом незакоммиченном дереве (п. 4 раздела «Что осталось»),
и перепись в отчёте снята с грязного дерева (FOUND 3). Обе — про учёт и охват, ни одна не
подрывает доказанную часть.

За собой ничего запущенного не оставил: своих серверов, фоновых процессов и портов нет,
временный зонд удалён с диска и не коммитился. Postgres на 5432 был поднят до ревью — не мой.
