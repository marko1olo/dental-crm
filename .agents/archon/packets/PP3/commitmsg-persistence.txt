[ARCHON] PP3: страж одонтограммы красный ЗАКОННО (класс REAL), но отправлял читателя не на ту строку

Требование не снято и продукт не тронут. Диагноз предыдущей волны перепроверен
по живому коду и подтверждён: apps/api/src/routes/odontogram.ts рассылает
состояния зубов КОНКРЕТНОГО пациента через
`wsBroker.broadcastToOrganization(organizationId, { payload: { patientId,
states } })`, то есть всем сокетам клиники. Узкий канал существует и
протестирован — `broadcastToPatient` в services/websocketBroker.ts:44, тест
tests/websocketBroker.test.ts:87.

ЧТО БЫЛО НЕ ТАК. Сообщение требования называло «odontogram.ts:341». Номер уехал:
рассылка стоит на строке 426, а на 341 сейчас разбор тела запроса. Страж,
отправляющий читателя не на ту строку, обесценивает верный диагноз. Номер больше
не хранится в тексте — он вычисляется по файлу на месте прогона и уехать не
может. Путь к брокеру в комментарии тоже был неполон: указано
`websocketBroker.ts:44`, а файла apps/api/src/websocketBroker.ts не существует,
брокер лежит в services/. Исправлено.

ОБРАТНАЯ ПРОБА вместо кусаемости: требование УЖЕ красное, ломать нечего, поэтому
доказано противоположное — что оно выполнимо и закроется починкой продукта.
По копии odontogram.ts в C:/tmp/bite-dp, продукт не тронут:
  контроль (копия как есть)                     -> exit 1, назван именно дефект рассылки
  broadcastToOrganization -> broadcastToPatient  -> exit 0
Значит страж не «вечно красный по недосмотру»: он держит конкретную починку.

Код выхода стража: было 1, осталось 1 — законно, класс REAL. Изменился только
адрес, по которому он посылает читателя (341 -> 426, вычисляемый).
