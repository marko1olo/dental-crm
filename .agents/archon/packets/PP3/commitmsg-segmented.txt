[ARCHON] PP3: семь требований доступности были зелёными ПО КОММЕНТАРИЮ — страж читал сырой текст SettingsView.tsx

НАХОДКА, А НЕ ПОЧИНКА КРАСНОГО. Страж докладывал 11 красных из 36. Настоящее
число несоблюдённых требований было 12, а семь ЗЕЛЁНЫХ держались подделкой.

apps/web/src/SettingsView.tsx:1935-2007 — блочный комментарий ПОСЛЕ `return`, в
который сложены строки разметки, и среди них ровно семь `aria-pressed={...}`:
режим клиники, роль сотрудника, специальность сотрудника, рабочие дни
расписания и три переключателя оснащения кресла. Страж искал `.includes()` по
СЫРОМУ тексту, поэтому закомментированная строка «выполняла» требование к
разметке. Класс подделки тот же, что `// Compliance: …` в
useSettingsDerivations.tsx, только другой файл и другая форма.

ЗАМЕР, А НЕ ПРЕДПОЛОЖЕНИЕ. Проба сравнила все 36 требований по сырому тексту и
по тексту без комментариев на 9 файлах разметки плюс все домены логики:
переворачиваются РОВНО 7, все в SettingsView.tsx, ложных переворотов в других
файлах нет (длина текста при вырезке сохраняется, строки и JSX-текст не
затрагиваются). Теперь требования ищутся только в КОДЕ.

Ни одно требование не удалено. Разбор всех 12 по классам:

  STALE (11) — текст переехал при разборе монолита, поведение живо. Шесть из
  семи «комментарийных» нашлись живыми в SettingsClinicTab.tsx, шесть красных —
  в SettingsImportsTab.tsx. Вкладки ЖИВЫЕ: SettingsView.tsx рендерит их по
  условию активной вкладки (1586 / 1831 / 1835 / 1894). Набор файлов расширен.
  Замер устойчив к чужой работе в полёте: SettingsClinicTab.tsx сейчас грязный,
  но все шесть маркеров есть и в копии из HEAD.

  BRITTLE (5) — краснело за то, что разметка стала ПРАВИЛЬНЕЕ:
    ImagingView.tsx:618,627 — кнопки фильтра получили role="tab" и выражают
      выбор через aria-selected. Для role="tab" это ЕДИНСТВЕННЫЙ верный атрибут:
      aria-pressed определён для кнопок-переключателей, не для вкладок.
    SettingsPricesTab.tsx:475,549 — переключатели заменены на НАТИВНЫЕ
      input[type=radio] и input[type=checkbox] с `checked={...}`; нативное
      состояние доступнее любого aria-атрибута. Там же сменилось имя переменной
      цикла (`kind` -> `key`) — образец его не пиннит.
    SettingsClinicTab.tsx:424 — режим клиники читается через optional chaining.
  Образец принимает любой атрибут, НЕСУЩИЙ состояние выбора (aria-pressed /
  aria-selected / нативный checked), но требует привязки именно к тому выражению,
  которое означает «этот элемент выбран». Сегодняшнее написание подставлять было
  нельзя — снова прибило бы требование к имени переменной и к сорту атрибута.

  REAL (1) — ОСТАЁТСЯ КРАСНЫМ ЗАКОННО, продукт не правится этим пакетом.
  «Settings recognition preset picker must expose selected state»:
  SettingsAiTab.tsx:266 держит выбранный пресет ТОЛЬКО классом оформления
  (`className={...active...}`), на кнопке нет ни aria-pressed, ни aria-selected,
  ни role. Связи `recognitionKind === preset.kind` с любым атрибутом состояния
  нет ни в одном файле apps/web/src. Выбранный пресет распознавания виден только
  глазами. Требование в реестре объявленного долга с причиной и ХРАПОВИКОМ.

КОДЫ ВЫХОДА ТЕПЕРЬ РАЗЛИЧАЮТСЯ: 0 — долг пуст; 1 — только объявленный долг,
живые локи держат; 2 — СЛОМАН ЖИВОЙ ЛОК либо реестр начал врать.

КУСАЕМОСТЬ 9/9 по копиям в C:/tmp/bite-sc, продукт не тронут:
  снят aria-pressed у переключателя РВГ-датчика -> exit 2, назван именно он
    РЕШАЮЩЕЕ: ПРЕЖНИЙ страж на ТОЙ ЖЕ мутации это требование НЕ НАЗВАЛ —
    он считал его выполненным по закомментированной строке. Это и есть
    доказательство подделки, а не рассуждение о ней.
  снят aria-selected у фильтра «все снимки» -> exit 2
  снят aria-selected у кнопок фильтра по типу -> exit 2
  checked={pricelistSourceKind === key} -> checked={false} -> exit 2
  checked={usePricelistAi} -> checked={true} -> exit 2
  снят aria-pressed у выбора режима клиники -> exit 2
  ХРАПОВИК ДОЛГА: доступность пресетов НАПИСАНА -> exit 2 «ДОЛГ ЗАКРЫТ»
  ХРАПОВИК РЕЕСТРА: запись без требования -> exit 2 «РЕЕСТР РАЗОШЁЛСЯ»
  контроль на НЕмутированных копиях -> exit 1 (только объявленный долг)

Код выхода стража: было 1 (11 красных, 7 ложно зелёных), стало 1 — но теперь это
ОДИН объявленный долг класса REAL, а не смесь переезда, починенной разметки и
подделки. Продукта в коммите нет.
