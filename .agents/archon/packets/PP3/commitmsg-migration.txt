[ARCHON] PP3: страж прогресса переноса — BRITTLE на переносе строк плюс STALE на переезде вкладки, и он умирал на первом маркере

Три разных дефекта стража, поведение продукта не менялось ни в одном.

1. СТРАЖ УМИРАЛ НА ПЕРВОМ НЕСОВПАДЕНИИ. `assertIncludes` бросал исключение, из
   50 проверок наружу выходила ОДНА строка. Замер 29.07.2026: падение на маркере
   №31 из 34, а за ним МОЛЧА не исполнялись ни остаток требований к логике, ни
   ВСЕ 11 требований к интерфейсу, ни 5 запретов. Настоящее число несовпадений
   было 10 — видно было одно. Теперь собираются все, запреты исполняются всегда.

2. КЛАСС BRITTLE, отличие РОВНО в пробелах. Оба ограничения обхода живы,
   форматирование перенесло их на несколько строк (useAppLogic.tsx:9166-9169 и
   9044-9045). Доказательство, что дело в ширине печати, а не в поведении:
   близнец smoke:browser-imaging-scan-progress-source ЗЕЛЁНЫЙ, и его
   `const scanCount = Math.min(selectedFileCount, browserImagingScanFileLimit);`
   стоит в том же файле строкой 9455 одной строкой — имя лимита на четыре
   символа короче и строка влезла в ширину. Оба требования переведены на
   образцы, закрепляющие СВЯЗЬ (обрезание по лимиту; обход ограничен И папками,
   И файлами), а не написание.

3. КЛАСС STALE. Восемь требований к интерфейсу краснели за переезд:
   SettingsView.tsx разобран на вкладки, разметка прогресса переноса теперь в
   components/settings/SettingsImportsTab.tsx. Вкладка ЖИВАЯ, а не мёртвая
   копия — SettingsView.tsx:1894 рендерит её по условию активной вкладки.
   Набор файлов расширен, ни одно требование не ослаблено.

Замер устойчив к чужой работе в полёте: useAppLogic.tsx сейчас грязный, но оба
образца проходят и по копии из HEAD, а старого написания одной строкой в HEAD
нет — страж был красным и против HEAD.

КУСАЕМОСТЬ 4/4 по копиям в C:/tmp/bite-bm, продукт не тронут. Каждая мутация
даёт exit 1 И называет ИМЕННО своё требование:
  Math.min(selectedFileCount, browserMigrationScanFileLimit) -> selectedFileCount
    (снят предел: папка на 40 000 файлов сканируется целиком и вешает вкладку)
  из условия обхода убран `scannedFiles < browserMigrationScanFileLimit`
  browser-cancel-migration-source-scan -> browser-scan-cancel-legacy
    (кнопка отмены переименована, прервать разбор папки нечем)
  потоковый проход заменён на files.slice(0, browserMigrationScanFileLimit)
    — это ЗАПРЕТ, который до этой правки НИКОГДА не исполнялся, потому что
    страж умирал раньше него
Контроль: та же копия стража на НЕмутированных копиях даёт exit 0 — красное
приходит от мутации, а не от перенаправления путей.

Код выхода стража: было 1 (падал исключением на маркере №31), стало 0.
