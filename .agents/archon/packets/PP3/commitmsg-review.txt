[ARCHON] review(PP3): бита воспроизведена 22/22 плюс два храповика — но зелёный по комментарию нашёлся и ВНЕ пакета

SOUND_WITH_NITS. Проверял по диску: прогнал все 19 стражей истинными кодами без конвейера,
вытащил версии из commit^ для кодов ДО, повторил каждую мутацию своим мутатором по копиям в
C:/tmp-pp3 (полное дерево git archive HEAD плюс зеркало рабочей копии). Продукт не тронут,
в репозиторий не писал.

Бита настоящая: 22 мутации из 22 краснеют и называют ИМЕННО своё требование — finance 4/4,
migration 4/4, offline-queue 6/6, segmented 8/8 (каждая exit 2, ровно один сломанный лок).
Оба храповика сегментного стража кусаются: закрытый долг -> exit 2, разошедшийся ключ реестра
-> exit 2. На классе REAL обратная проба: починка в копии -> exit 0.

Два его сильнейших заявления подтверждены не рассуждением, а прогоном. Запрет потокового обхода
действительно не исполнялся никогда: старый страж на материализации всего FileList выдаёт ту же
жалобу на перенос строки и о запрете молчит. Подделку комментарием воспроизвёл живьём — снял
aria-pressed с ЖИВОГО переключателя РВГ, старый страж требование не назвал вообще.

Класс COMMENT пересчитал СВОЕЙ вырезкой комментариев по 8 файлам разметки: ровно 7 переворотов,
все в SettingsView.tsx, ложных 0, длина сохранена. Его цифра верна.

Коммиты: пять, все [ARCHON], трейлеры пусты, атрибуции нет, по два файла каждый, ни одного файла
из apps/ или packages/. Чужая работа в полёте цела — 26 изменённых продуктовых файлов и 4
неотслеживаемых скрипта другого автора не подметены, остатков __bite* в дереве нет.

FOUND, четыре:

1. Он повторил в сегментном страже ровно тот дефект, который в этом же пакете вылечил у
   одонтограммы: SettingsAiTab.tsx:266 в реестре долга — строка 304, и была 304 уже на момент его
   коммита. Из 19 координат 18 сходятся точно; врёт единственная, обращённая к оператору. На 266
   лежит href={provider.sourceUrl}. У одонтограммы он решил это вычислением номера на месте
   прогона — здесь захардкодил неверное число.

2. Восьмое требование payment-capture классифицировано неверно. Он доложил, что голого токена
   className="payment-capture" в HEAD нет. Он есть: className="payment-capture bg-white
   dark:bg-slate-900 …". Класс BRITTLE, лечится его же classNameToken(). По его докладу следующий
   исполнитель пойдёт писать разметку, которая уже написана.

3. Он ослабил требование и не ослабил парный запрет. finance: requirePattern принял токен в списке
   классов, а forbidIn на App.tsx остался дословным. Проверено мутацией: вклеенный в App.tsx
   className="finance-summary-grid" ловится (exit 1), а className="finance-summary-grid bg-white …"
   — нет (exit 0). Второе есть ровно то написание, которым сводка размечена сегодня: разбор
   монолита можно откатить копипастом живой строки при зелёном страже.

4. Крупнее его находки и вне его пакета. Прогнал его приём по всем 16 стражам, читающим
   SettingsView.tsx: заражён ещё ровно один — smoke:web-render-gating-source, который ведущий
   считает одним из 32 ЗЕЛЁНЫХ. Он зелен исключительно из-за девятистрочного блочного комментария
   SettingsView.tsx:2010-2018, замыкающего файл: ноль исполняемого кода и ровно три иглы, которых
   стражу не хватает. Сузил удалением ТОЛЬКО этого блока: exit 0 -> 1, те же три требования.
   За ними класс STALE — условный рендер жив и верен (1586 clinic, 1587 access, 1601 telegram),
   иглы не совпадают потому, что clinic и access переехали в компоненты, а у telegram появилась
   проверка роли. Заодно шапка того стража (строки 93-96) врёт: утверждает, что все три вкладки
   лежат разметкой в SettingsView. В день, когда мёртвый комментарий уберут как обычный мусор,
   страж покраснеет и отправит человека восстанавливать то, что никуда не девалось.

Слой не добит: 6 красных из 19, из них 5 законно (REAL) и payment-capture отложен — бриф это
разрешал, и он сделал больше требуемого, разобрав все 8 требований по HEAD.

Общие гейты не запускал, они ведущего. Не пушил.
