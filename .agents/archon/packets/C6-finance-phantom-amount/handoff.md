# C6-finance-phantom-amount — сдача

HEAD: a4907fe624759209dfda47c04fbff0c192e81f14
(мои коммиты: 8f9243bddfa9704952ff4ca5fe17fe7d5d0f8ac8 — починка, a4907fe62 — замок)
HEAD на старте пакета: 26f1f3c59f5f64b3a4caa83ec2f6e05a03e14b88

---

## ПОПРАВКА ПАКЕТА R3 (2026-07-28): УТВЕРЖДЕНИЕ О ДОСЯГАЕМОСТИ БЫЛО НЕВЕРНЫМ

Разбор вернул этот пакет как NEEDS_REWORK. Поправки внесены пакетом
`R3-finance-rework`, коммиты `e90e1b276` (починка) и `6d97e0e7d` (замок).

**Что было заявлено неверно.** В сдаче этого пакета (поле «reachability» доклада)
утверждалось: «the God Context is instantiated once at the app root and is mounted
for the entire session» и «Because it lives in an always-mounted hook rather than in
a view, it fires on every patient switch regardless of which screen the operator is
on». Первое предложение неверно, второе верно лишь наполовину.

**Как есть на самом деле.** `useAppLogic()` вызывается из ДВУХ мест, проверено
`rg -n "useAppLogic\(\)" apps/web/src`:
- `apps/web/src/App.tsx:956` — корневой экземпляр;
- `apps/web/src/components/useVisitDiaryLogic.ts:27` — второй полный экземпляр
  контекста, минуя `useAppLogicContext()`.

Значит, эффект сброса монтируется заново при каждом открытии вкладки «Зубная формула
и Дневник»: `App.tsx:3745 <VisitView>` → `VisitView.tsx:353-355 <VisitOdontogramTab>`
→ `VisitOdontogramTab.tsx:68-73 <VisitDiaryEditor>` → `VisitDiaryEditor.tsx:103
useVisitDiaryLogic` → `useAppLogic.tsx:2383 usePatientLogic`.

**Чем это обошлось.** `useEffect` на первом прогоне выполняется всегда, а сброс,
добавленный этим пакетом, первого прогона не отличал. Поэтому починка «сумма не
переезжает на следующего пациента» одновременно завела новый дефект: кассир, набравший
сумму и переписавший ФН/ФД/ФПД с чека, терял их молча, просто заглянув на «Приём» и
открыв вкладку дневника. Разбор это поймал; закрыто в R3 признаком первого прогона
(`PAYMENT_COMPOSER_PATIENT_UNTRACKED` + `resetPaymentComposerOnPatientChange`).

**Поправки к номерам строк этой сдачи** (были поданы как свежепрочитанные):
`useAppLogic.tsx:2378-2392` → вызов `usePatientLogic` на `2383`;
`usePatientLogic.ts:199-207` → после починки эффект был на `220-222`;
`documentPatient` — `usePatientLogic.ts:112`, а не `:111`;
сброс после платежа — `useAppLogic.tsx:12662-12675` (`paymentMutationIdRef` на `12661`,
`await loadDashboard()` на `12676`), а не `12661-12677`.

---

## Что было сломано (file:line)

### 1. Заявленный дефект — «3800» в поле суммы — УЖЕ ПОЧИНЕН ДО МЕНЯ. НЕ МОЯ РАБОТА.

Источник подстановки найден: начальное состояние хранилища,
`apps/web/src/store/documentStore.ts` — ключи `paymentAmount` и `refundAmountRub`,
оба литерал `"3800"`. Случай из списка пакета — **жёстко зашитый литерал, остаток
демонстрационных данных**. Не производная величина, не sampleData, не пресет-чип.

Починено чужим (не из флота) автором `marko1olo`, коммит `0baa1f723`
от 2026-07-28 01:13:43 +0400. На HEAD сейчас:
- `documentStore.ts:2103` → `refundAmountRub: ""`
- `documentStore.ts:2125` → `paymentAmount: ""`
Регрессионная проверка у него же: `apps/web/src/tests/moneyFieldsStartEmpty.test.ts`.

**Скриншот `.dente-redesign-shots/desktop_light_finance.png` датирован
2026-07-27 04:02:26 — он старше починки.** Лид читал устаревшую пластину.

### 2. Живой дефект, найден по ветке 3 пакета «stale state not cleared on deselect»

`apps/web/src/hooks/domains/usePatientLogic.ts:199-207` (до починки):

```ts
useEffect(() => {
    setPaymentFeedback("");
    setPaymentPayerFullName("");
    setPaymentPayerInn("");
    setPaymentPayerBirthDate("");
    setPaymentPayerIdentityDocument("");
    setPaymentPayerRelationship("пациент");
    setPaymentTaxDeductionCode("");
}, [documentPatient?.id]);
```

Это сброс формы приёма оплаты при смене пациента. Он гасил 6 полей из 14.
**Не гасил: `paymentAmount` и весь фискальный блок** — `paymentFiscalReceiptNumber`,
`paymentFiscalReceiptIssuedAt`, `paymentFiscalFn`, `paymentFiscalFd`,
`paymentFiscalFpd`, `paymentFiscalCashierName`, `paymentFiscalReceiptUrl`.

Эталон свежей формы — сброс после записанного платежа,
`apps/web/src/useAppLogic.tsx:12661-12677`, он гасит все 14. Два сброса разошлись,
и расхождение и есть дефект.

Причина расхождения: сеттеры прокидывались в `usePatientLogic` россыпью через
пропсы (`useAppLogic.tsx:2378-2392`), прокинули 6 — очищалось ровно 6.

Как срабатывает: кассир набирает сумму пациенту А и переписывает ФН/ФД/ФПД с его
чека, «Принять оплату» не нажимает, переключается на пациента Б. Форма у Б
выглядит заполненной им самим. Нажатие пишет деньги Б с суммой А и фискальными
признаками чужого чека — тело запроса собирается из тех же значений состояния
(`useAppLogic.tsx:12610-12653`). Фискальные признаки уходят в налоговые документы.
Пациент — `selectedPatient ?? activePatient` (`usePatientLogic.ts:112`), поэтому
его двигает любое переключение в программе, не только на экране «Оплаты».

Ни одно поле формы приёма оплаты в хранилище не сохраняется между запусками
(в `documentStore.ts` ноль совпадений на persist/createJSONStorage; `localStorage`
хранилище всё же читает — `documentStore.ts:44` вызывает `loadUiPreferences()` и
подставляет из неё `paymentMethod` на `:2127`, но ни суммы, ни фискальных полей там
нет). Поэтому перенос живёт в пределах одной сессии и перезагрузку страницы не
переживает — а вот способ оплаты переживает, и это осознанная настройка рабочего
места, а не данные платежа.

---

## Что изменено

1. **НОВЫЙ** `apps/web/src/components/finance/paymentComposerReset.ts` (124 строки) —
   один перечень полей свежей формы: `PaymentComposerFields`,
   `emptyPaymentComposerFields()`, `resetPaymentComposer(setters)`,
   `DEFAULT_PAYER_RELATIONSHIP`. Полностью типизирован, без `any`.
   `paymentMethod` намеренно не входит: это настройка рабочего места кассира, и
   сброс после платежа её тоже не трогает.
   Сумма гасится пустой строкой, а не нулём — поле хранит набранное человеком
   вместе с копейками (`rubAmountInput.ts:20-33`), подставленный ноль касса
   приняла бы за введённую сумму.
2. `apps/web/src/hooks/domains/usePatientLogic.ts` — эффект смены пациента теперь
   вызывает общий сброс, сеттеры берутся из `useDocumentStore.getState()`.
   Семь ставших лишними пропсов-сеттеров убраны из деструктуризации.
3. `apps/web/src/useAppLogic.tsx` — те же семь ключей убраны из объекта-аргумента
   вызова `usePatientLogic` (строки 2378-2392). Функция контекста и её блок
   возврата не тронуты. Локальные сеттеры остаются в использовании на 12465 и
   12664-12680.
4. **НОВЫЙ** `apps/web/src/tests/paymentComposerReset.test.ts` (241 строка, node:test).

---

## ПРОВЕРЕНО

**TYPECHECK VERIFIED** — `npm run typecheck -w @dental/web` → `tsc -b --noEmit`,
EXIT=0. Прогнан дважды: после починки и после добавления файла проверок.
Доказывает только то, что сборка не сломана.

**UNIT VERIFIED** — `node --import tsx --test apps/web/src/tests/paymentComposerReset.test.ts`,
EXIT=0:
```
ℹ tests 12
ℹ suites 3
ℹ pass 12
ℹ fail 0
```
Проверки:
- пациент не выбран → сумма `""`, и явно не `"0"`;
- свежая форма не содержит ни одного унаследованного реквизита (обход всех полей);
- форма пациента А (сумма 12000,50, чек 00042, ФН/ФД/ФПД, ссылка НФД,
  плательщик-мать с ИНН и паспортом, код услуги 1) после сброса пуста по каждому
  полю — значения читаются обратно из подставного хранилища;
- модель зависимости `[documentPatient?.id]`: снятие выбора (`undefined`) даёт
  сброс, четыре подряд загрузки при том же пациенте дают один сброс, а не четыре;
- статический замок: сброс после платежа в `useAppLogic.tsx` обязан гасить каждое
  поле из `PaymentComposerFields` (перечень берётся из самого перечня, руками не
  переписан), сброс при смене пациента обязан вызывать общий перечень по
  `documentPatient?.id`.

**Замок не пустой — проверено на коде до починки** (`git show 26f1f3c59:...`):
из 14 полей сброс при смене пациента не гасил 8 — `paymentAmount`,
`paymentFiscalCashierName`, `paymentFiscalFd`, `paymentFiscalFn`,
`paymentFiscalFpd`, `paymentFiscalReceiptIssuedAt`, `paymentFiscalReceiptNumber`,
`paymentFiscalReceiptUrl`. Регулярное выражение второй проверки на том исходнике
не срабатывает (`false`).

**Кодировка** — 0 строк мождибаке и 0 BOM во всех четырёх задетых файлах.

---

## НЕ ПРОВЕРЕНО

- **Поведение в живом браузере.** Ни один сценарий не прогонялся через React.
  Проверки моделируют семантику массива зависимостей, а не исполняют его.
  Закрывается: открыть 127.0.0.1:5173, экран «Оплаты», выбрать пациента А,
  набрать сумму и ФН/ФД/ФПД, переключиться на пациента Б, посмотреть поля.
  Инструмент у лида:
  `npm run shots:redesign` (или та же связка, что делала
  `.dente-redesign-shots/desktop_light_finance.png`) — токен на съёмку у лида.
- **Внешний вид.** UI VERIFIED мне не принадлежит. Закрывается тем же прогоном
  съёмки лидом и чтением новой пластины `desktop_light_finance.png`.
- **API/БД.** Ни одного запроса к 127.0.0.1:4100 и ни одного SQL к 127.0.0.1:5432
  не выполнялось: изменение целиком во фронтовом состоянии, контракт запроса не
  менялся. Закрывается (если нужно подтвердить, что тело платежа берёт именно эти
  значения): `curl -s -X POST http://127.0.0.1:4100/api/billing/payments -H
  "x-dente-clinic-token: <токен>" -H "content-type: application/json" -d '{...}'`.
- **Полный набор проверок веба.** Прогонял только свой файл. Закрывается:
  `npm test -w @dental/web`, если такой скрипт есть; иначе
  `node --import tsx --test apps/web/src/tests/*.test.ts`.

---

## Коммит

- `8f9243bddfa9704952ff4ca5fe17fe7d5d0f8ac8`
  `[ARCHON] fix(касса): сумма и фискальные признаки чужого чека переезжали на следующего пациента`
  3 файла, +158 −21.
- `a4907fe624759209dfda47c04fbff0c192e81f14`
  `[ARCHON] test(касса): замок на перенос суммы и чужого чека при смене пациента`
  1 файл, +241.

Оба коммита сделаны с явным списком путей. Индекс перед каждым проверялся:
чужого в нём не было. Автор в обоих — `marko1olo`: это настроенная в репозитории
личность git, я её не менял и менять не вправе.

---

## Соседние дефекты из того же кадра (пункт 4 пакета, только доклад)

1. **Обрезанная подсказка «Пример: Оплата 5000 ка» — УЖЕ ПОЧИНЕНА.**
   `apps/web/src/PaymentCapture.tsx:558-566`: класс `.smart-ai-booking` в
   `main.css` задаёт `flex-direction: column`, строка сворачивалась в столбик
   шириной около 200px и подсказка обрезалась. Теперь стоит явный
   `flexDirection: 'row'`. Тот же устаревший скриншот.
2. **Пять несгруппированных кластеров в полосе оплаты — ЖИВОЙ, не чинил.**
   `apps/web/src/PaymentCapture.tsx:566-672`. Без подписи именно два кластера:
   строка пресетов на `:597-601` (нет ни `aria-label`, ни видимого заголовка) и
   строка быстрых сумм на `:632-656` (то же). Для сравнения, подписаны:
   способ оплаты `:660` (`aria-label="Способ оплаты"`) и код вычета `:288`
   (`aria-label="Код медицинской услуги для налогового вычета"`).
   Отдельный пакет: один дефект — один пакет.

---

## Поправки к документам расследования

**`.agents/archon/VISUAL_VERDICT.md`, раздел A2:**
- пункт 1 («3800» в поле суммы) — **закрыт на HEAD** коммитом `0baa1f723`;
- пункт 3 (обрезанная подсказка) — **закрыт на HEAD**;
- пункт 2 (пять кластеров, два без подписи) — **подтверждён, живой**.
Причина: пластина `desktop_light_finance.png` снята 2026-07-27 04:02:26, починки
пришли в ночь на 28-е. Любой вердикт по этой пластине нужно переснимать.

**`.agents/archon/RECON_DOSSIER.md`:** утверждение «amountRub is an integer»
**неверно на HEAD**. `apps/web/src/rubAmountInput.ts:20-33`: поле принимает
копейки, запятая и точка равноправны, три знака после запятой — отказ, а не
округление; возвращается `Math.round(rub*100)/100`. Пакет об этом предупреждал,
подтверждаю чтением файла.

---

## Долг

1. **Сброс после платежа не переведён на общий перечень.**
   `apps/web/src/useAppLogic.tsx:12664-12677` по-прежнему перечисляет 14 полей
   руками. Не трогал сознательно: это God Context под тремя параллельными
   авторами, а поведение там уже правильное. От повторного расхождения держит
   статическая проверка. Правка на будущее: заменить 14 строк на
   `resetPaymentComposer(useDocumentStore.getState())` перед `await loadDashboard()`,
   строку состояния выставлять после.
2. **Форма приёма оплаты живёт в `documentStore` — глобально, а не при экране.**
   Поэтому она вообще способна пережить смену пациента. Правильное место —
   состояние, привязанное к пациенту. Большая перестройка, отдельный пакет.
3. **Пропсы `usePatientLogic` типизированы как `any`** (`usePatientLogic.ts:39`).
   Именно поэтому забытые сеттеры не дали ошибки компиляции: восемь отсутствующих
   полей typecheck не заметил. Пока стоит `any`, эта форма ошибки повторима.
4. **i18n.** Новых видимых пользователю строк я не добавил: `resetPaymentComposer`
   только гасит поля, а `DEFAULT_PAYER_RELATIONSHIP = "пациент"` — перенос уже
   существовавшего литерала из `usePatientLogic.ts:205`. Долг i18n не увеличен и
   не уменьшен.
5. **Подчистки не делал**: слушателей, подписок, интервалов и таймеров не
   добавлял, эффект остался тот же самый — снимать нечего.
