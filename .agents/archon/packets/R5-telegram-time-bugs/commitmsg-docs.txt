[ARCHON] docs(пакет R5): досье приписало Telegram UTC-ключ сводки, которого в живом пути нет

Сдача R5: закрыт дефект (c) — двойная отправка фото пациенту. Два оставшихся дефекта переданы лидеру
с точными строками на HEAD, а не пересказом досье.

Поправка к RECON_DOSSIER.md §5.7 строка 344. Живой ключ дедупликации ежедневной сводки —
staffDailyDigestOutboxId (sampleData.ts:9128-9130) — считается через appointmentClinicDateKey
(sampleData.ts:2185-2203), то есть по часовому поясу клиники через именованную зону IANA и Intl, а не
в UTC и не через вшитое смещение «+4». Сценарий «сводка в 02:00 по местному попадает на предыдущий
день» в живом пути не воспроизводится. UTC-ключ существует только в telegram/outbox.ts:75, и этот
файл не подключён ни к одному роуту: единственный импортёр — telegram/benchmark.ts:2. Плюс сводка
адресована сотруднику, а не пациенту.

Оговорка внесена там же, чтобы поправка не выглядела обелением: зона по умолчанию — литерал
"Europe/Samara" в sampleData.ts:179, не из .env. Это хардкод конфигурации по §1 и отдельный мелкий
долг, но на дедупликацию сводки он не влияет.

Найдено по пути и передано отдельно: routes/dayConfirmations.ts:93 считает «завтра» в UTC, хотя
остальной модуль работает в поясе клиники, и его собственный тест
tests/routes/dayConfirmations.test.ts:211 красный на HEAD на границе суток. Это ровно тот дефект
семьи (a), который досье приписало Telegram, — только здесь он живой. Оба файла чистые в git status,
провал существует без правок этого пакета.
