[ARCHON] fix(разделы): переименование сломало проверку исходников smoke

Предыдущий коммит 800bde54f убрал из workspaceRouteErrorBoundary.tsx имя
`workspaceRouteErrorDetail`, а scripts/smoke-workspace-shell-source.mjs:160-164
требует именно эту строку в исходнике («Route error boundary must explain lazy
route failures with operator-readable copy»). Смоук и до этого падал на двух
проверках, поэтому третья, добавленная мной, потерялась бы в общем красном.
Замерено: до правки три провала, после — ровно два прежних.

Функция возвращена под своим именем и стала точной парой соседней границы
ошибок оболочки — AppShell.tsx, `appShellErrorDetail`. Так честнее по смыслу:
она и есть операторская формулировка, а `workspaceRouteErrorPresentation`
остаётся обёрткой, которая решает, добавлять ли к ней диагностику.

Проверка на неё заведена отдельно: имя теперь держит не только смоук.
