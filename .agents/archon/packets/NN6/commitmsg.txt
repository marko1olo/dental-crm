[ARCHON] досье брошенных таблиц: перегенерировано новым скриптом, снят нулевой агрегат и три неверных числа

Досье — то, по чему решают судьбу таблиц с данными. Оно содержало вывод старой версии скрипта, то есть
опиралось на итоговую строку, где «не определяется» было сложено как ноль.

ЧТО ПЕРЕГЕНЕРИРОВАНО. Дословный блок вывода заменён прогоном 2026-07-29 10:15 (истинный код возврата 0).
В нём появился раздел замера ссылок с четырьмя корзинами, а итоговая строка теперь состоит из двух и
отличает ноль от незнания. Добавлен раздел «Инструмент врал агрегатом» с таблицей «до / после» той самой
строки, с воспроизведённым старым выводом на заряженном наборе и с командой `--selfcheck`, которой это
свойство проверяется без базы.

ЧТО СКАЗАНО ЧЕСТНО, А НЕ ЗАМЕТЕНО. Вердикты по таблицам не изменились ни одного: все восемнадцать пусты,
`null` сегодня не возникает ни разу, поэтому старый агрегат печатал верное число по случайной причине.
Дефект был заряжен, а не сработал — так и написано, вместе с тем, что напечатал бы старый скрипт при 500
строках в `payment_installments`.

ТРИ НЕВЕРНЫХ ЧИСЛА, ИСПРАВЛЕНЫ ПО ЗАМЕРУ:
  1. «колонка organization_id есть у 13 из 18» → 12 из 18. Нет её у шести: doctor_assistants,
     ingested_patients_mapping, patient_anamnesis, payment_installments, signed_outpatient_cards,
     ztl_lab_orders. Шесть строк «НЕТ» в собственной таблице досье — пересчитываются глазами.
  2. «у 15 из 18 двойников тоже ноль строк» → у 10 из 14. Знаменатель неверен с самого начала:
     двойника нет у четырёх таблиц вообще, поэтому их 14, а не 18. На момент MM4 было 11 из 14.
  3. «clinical_tasks — 18 упоминаний» → 5 обращений в рабочем коде, 5 упоминаний в прозе шапок,
     8 ссылок из теста. Вердикт «удалять нельзя» держится на пяти, а не на восемнадцати.

БАЗА ЕДЕТ ПОД ЗАМЕРОМ, И ЭТО НАПИСАНО В ДОСЬЕ. За полтора часа этой волны параллельные пакеты сдвинули
контрольные счёта (organizations 2→3, patients 17→18, visits 10→15), а bi_analytics_snapshots получила
6 строк, отчего «двойников с данными» стало 4 вместо 3. Счёта по восемнадцати брошенным таблицам не
двинулись ни на строку — они и есть предмет досье. Добавлена оговорка, что досье снимок, а не гарантия:
решение об удалении обязано опираться на прогон.

ЗАМЕРЕНО (истинные коды возврата, без конвейера):
  node scripts/count-orphan-table-rows.mjs             → 0
  node scripts/count-orphan-table-rows.mjs --selfcheck → 0

БРИФ НЕВЕРЕН В ЧИСЛЕ: колонки organization_id нет у ШЕСТИ таблиц из восемнадцати, а не у пяти. Шестая —
ztl_lab_orders.
