[ARCHON] docs(ревью): MM2 — снятая строка долга проверена мутацией, страж бьёт в обе стороны

Вердикт SOUND_WITH_NITS. Заявленная задача решена та самая: строки
"DELETE /api/clinical/rules/:param" в KNOWN_METHOD_MISMATCH нет, комментарий
превращён в правдивую историю, самопроверка на выдуманных данных (pathsOnly:980,
закрепление isServed:984-989, фикстуры methodMismatches:1010/1041) не задета.

Прогнал его замер сам, без конвейера: EXIT=0, pass 9 / fail 0. Маршрут не только
объявлен (clinical.ts:124), но и проведён до Fastify (server.ts:375).

Главное — правку сломал обратимо, а не рассуждал. Вставил строку долга назад:
EXIT=1, fail 2, краснеют ровно «починенные адреса удаляются из списка долга»
(«уберите строку из KNOWN_METHOD_MISMATCH: DELETE /api/clinical/rules/:param») и
«список известного долга не разрастается молча» («стало больше: 2») — значит и
сверхбрифовое опускание потолка <= 2 → <= 1 бьёт, а не украшает. Файл восстановлен
байт в байт (git diff HEAD пуст); первый checkout упал на чужом .git/index.lock,
восстановлено ретраем.

Снятие строки УСИЛИЛО охрану, и это тоже замер: пара DELETE была заглушена списком
долга, теперь она обязана обслуживаться, и удаление app.delete из clinical.ts
покраснеет на живом вызове useAppLogic.tsx:11066.

Нити: блок истории 216-234 висит без предмета прямо над комментарием к записи PUT;
чужая работа проглочена коммитом без сохранения отдельного .diff, хотя прецедент
такого сохранения в репозитории есть (AA2-guard-root-cause).

FOUND четыре, все чужие: 324 вставки нерецензированного перехода на компилятор
TypeScript легли в HEAD под чужим коммитом и владелец увидит путь чистым; докстринг
stripComments объявляет живой измеренный дефект — клиентский разбор теряет
/api/speech/recordings/:param/assemble и расходится с лексикой на трёх файлах
imaging; зелень стража не доказывает проводку модуля до Fastify; гонка за
.git/index.lock внутри волны молча съедает коммит у пакета без ретрая.
