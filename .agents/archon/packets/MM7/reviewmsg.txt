[ARCHON] ревью MM7: перепись двух идиом авторизации — вердикт SOUND_WITH_NITS

Заявленная задача сделана та самая: разведка без правки кода, 203 изменяющих маршрута,
все пять полей брифа. Проверено по диску, а не по словам исполнителя.

Воспроизведено самостоятельно, истинные коды выхода: rg из §3 досье -> 203 (exit 0);
mm7-census3 -> TOTAL=203 (exit 0); mm7-census4 -> 203 {SOUND 111, RISK 55, OK 37}
(exit 0); mm7-table (exit 0). Таблица §5 досье побайтово равна выводу инструмента
(diff exit 0, 0 строк) — руками не подправлена. Скрипты только читают, записи в
исходники нет. Коммит d89b7dc0f трогает два пути (досье и commitmsg), кода не трогает,
чужой незакоммиченной работы не подметено, трейлеры пусты.

Контрольные запросы повторены мной на 127.0.0.1:4100 и совпали до кода и до тела:
без ключей 404 CommunicationTaskNotFound, с чужим x-organization-id то же, отсутствующий
путь 404 RouteNotFound, общая идиома 401 AuthRequired. Проверка не вакуумная: при
организации из токена проба без ключей вернула бы 401.

Главный нит. Разбивка 55 RISK в §2 досье сходится к 55 двойным счётом: четыре маршрута
контура Telegram посчитаны и как «без данных», и отдельно, а четыре строки с данными
арендатора (lab.ts:302, telegram.ts:2641-2643) не попали ни в одну подгруппу. Headline
«границу можно пересечь у 7» собран по этой разбивке.

FOUND в очередь: POST /api/portal/lab-order/:token/status (lab.ts:302) — единственная
строка переписи с данными арендатора без организации И без секрета периметра, в прозе
досье отсутствует; контрольный запрос сделан в ревью (404 OrderNotFound анонимно, база
опрошена, запись статуса без предиката организации). POST /api/telegram/webhook
(telegram.ts:2641) не содержит организацию нигде. Четыре строки routes/audit.ts:74-77
раздувают RISK: обработчик — жёсткий отказ всем (403 AuditLogImmutable). Дрейф номеров
строк в рукописном §4: imaging.ts:149, schedule.ts:134, settings.ts:556,
telegram.ts:1491, insurance UPDATE на :261. Мёртвый requireScheduleMutationAccess уже
записан в moneyChainProof.ts:542. На стенде активен dev-обход клинического гейта.

Не REVERT и не NEEDS_REWORK: данные верны и полны, дефект в производной сводке, цена
исправления — абзац и один curl, уже приведённый в ревью.
