[ARCHON] docs(диктовка): сдача пакета S2 о слиянии текста двух приемов

Отчёт и состояние пакета: подтверждённые прогоном строки дефекта до правки
(коммит b46ddf7b4), что именно изменено и на каких строках сейчас, отдельно
ПРОВЕРЕНО и НЕ ПРОВЕРЕНО с точными закрывающими командами.

Заявлено прямо: живой HTTP-отказ 409 через общий сервер не прогонялся (холодный
кэш там воспроизводится только 80 посторонними записями, а перезапускать сервер
нельзя), поведение клиента на 409 прочитано в коде, но не проиграно в браузере, и
межпроцессная гонка не закрыта — для неё нужен уникальный индекс на
(organization_id, input_storage_path), который принадлежит пакету S3.

Записано также, что HEAD ушёл под пакетом: S3 закоммитил в тот же
apps/api/src/speech/storage.ts, дерево при этом было чистым, файл перечитан
целиком, строки дефекта проверены заново.

Co-Authored-By: Claude <noreply@anthropic.com>
