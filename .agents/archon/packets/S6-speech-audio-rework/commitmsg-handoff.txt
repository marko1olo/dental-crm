[ARCHON] docs(диктовка): сдача S6 — разбор R6 закрыт по пунктам, включая неверную ссылку на каталог окружения

Сдача переделки: каждый пункт разбора и каждая находка F1-F7 помечены как закрытые,
объявленные долгом или оспоренные с командой и её выводом.

Отдельно зафиксированы четыре неточности в сдаче R6, включая процитированные счётчики
кодировки, снятые до его последнего коммита, и утверждение «общий сервер подхватил
правку сам», которое тем запросом проверить нельзя: живой маршрут на этой машине идёт
по одноразовой ветке, а её текст в обеих версиях совпадает дословно.

Поправка к разбору: `docs/05-speech-transcription-plan.md`, названный каталогом
окружения проекта, удалён из индекса в 99bba4e0c и на HEAD не отслеживается — запись
в него git не унесёт. Настройки внесены в `.env.example`.

Скрипт проверки кодировки снимает счётчики на сдаваемом HEAD, а не переносит их из
прошлого прогона: именно на этом R6 и поймали.
