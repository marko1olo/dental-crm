[ARCHON] настройки(источники): 264 мёртвых чтения пропсов, скопированных из соседнего файла

Замер по деструктуризациям от mergedProps (имя встречается только в строке
деструктуризации и нигде ниже):

  SourcesConnectorGrid.tsx      150 имён, 133 мёртвых, живых 17
  SourcesIntegrationPresets.tsx 150 имён, 131 мёртвое,  живых 19
  SourcesDicomCapability.tsx    150 имён,   7 мёртвых,  живых 143

Один и тот же список на 150 имён лежит во всех трёх файлах побайтово одинаково.
Использует его по-настоящему только SourcesDicomCapability; в две меньшие панели
он скопирован целиком. 264 мёртвых чтения — это не 264 мёртвых пропса в цепочке:
производитель обязан их отдавать, потому что третий файл берёт 143 из 150. Убран
артефакт копипасты, функциональность не тронута.

Почему это жило: `as any` на строке Object.assign делает чтение любого имени
бесплатным, и деструктуризация лишних 133 имён ничего не стоит — ни ошибки, ни
предупреждения. Это тот же механизм, что скрыл девять имён прайса.

Здесь только вычитание: типизированный контракт этим двум файлам не ставится,
он потребовал бы 17 и 19 объявлений типов, и ошибка в любом из них была бы
хуже нынешней тишины. Сначала честная поверхность чтения, контракт — отдельно.

В SourcesDicomCapability оставшиеся 7 мёртвых имён не тронуты: файл на 1694
строки, у него отдельная цена проверки.

Проверка: npx tsc -p apps/web/tsconfig.json --noEmit — код выхода 0.
Пересчёт после правки: 17 имён / 0 мёртвых и 19 имён / 0 мёртвых.
