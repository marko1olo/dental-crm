[ARCHON] docs(снимки): в сдаче R2 стояло число, измеренное против кривой, которой не бывает

Пакет R2 отправлен на переработку не за код, а за запись. Исправляю запись.

Отозвано три утверждения сдачи R2, каждое — в сторону «выглядит безопасно»:

1. «Цена отказа от polyline измерена и мала: 91.9670 мм против 91.9684 мм, 0.002%,
   максимальное отклонение 0.3879 мм — меньше двух вокселей КЛКТ.» База — ОТКРЫТАЯ
   polyline, про которую тот же документ доказывает, что до `buildPanoramicArch` она
   не доходит никогда. И метрика была «столбец -> ближайшая ВЕРШИНА», у которой пол
   равен половине шага вершин базы (0.3606 мм). Против кривой, которую врач видит на
   экране: короче на 7.26% и 7.54%, отклонение 4.0081 и 4.0460 мм — порядка
   шестнадцати вокселей, а не «меньше двух».
2. «В JSX русских литералов не осталось.» Ложно: `:427`, `:444`, `:463`, `:495`,
   `:558` — буквальные текстовые узлы, `:236` и `:387` идут в разметку через state,
   `:233` — в `console.error`. `:495` это «Развернуть», кнопка из закрывающей команды
   самого пакета. Фраза стоит и в теле коммита b4292f74d; историю на отправленной
   ветке не перезаписывают, поэтому там она останется, а опровержение живёт в сдаче.
3. Цифры рантайм-проб («91.968 / 155.288 / 63.319 мм / 40.8%») не воспроизводимы:
   контрольные точки не записаны. Существо воспроизводится, величины заменены на
   измеренные на фикстурах репозитория. Сверх ревью: «40.8%» тоже считались на
   смешанной базе, верная доля обратного прохода — 36.5%.

Добавлено то, чего в сдаче не было и что меняет вывод: реконструкция по точкам врача
отклоняется от аналитически известной дуги на 0.5802 мм, а то, что cornerstone рисует
для замкнутого контура, — на 3.6448 мм. Геометрия правильная и менять её нельзя,
а панорама СОЗНАТЕЛЬНО строится не по экранной кривой. Заявлено долгами №8 (двойная
копия объёма, ~630 МБ на серию 512x512x400), №9 (`isSliceDecoded` принимает
`FAR_REPLICATE` и не проверяет `image.voxelManager`) и №10 (список выживших литералов).

Все цифры получены прогоном
apps/web/src/tests/panoramicArchVsCornerstone.test.ts, контрольные точки лежат в самом
тесте — прежним числам не хватало именно этого.
