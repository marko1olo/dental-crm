## VERDICT: SOUND_WITH_NITS

Ревью пакета OO1, коммит `5ccb48e96` «[ARCHON] OO1: нейро-ветка прайса проверяется вызовом
analyzePricelist, а не пересборкой в тесте». Всё ниже проверено по диску и прогонами ревьюера, не принято
со слов исполнителя.

Главное для этой волны: **EXIT_BITE вернут и воспроизведён мной самостоятельно**. Мутация ломает
поведение, а не компиляцию, и набор краснеет ровно теми утверждениями, которые составляют предмет пакета.
Ложная зелёная, на которой волна NN получила 15/15 на полностью сломанном коде, закрыта.

---

## ЧТО ПРОВЕРЕНО КОМАНДОЙ, ДОСЛОВНО, С КОДОМ ВЫХОДА

### 1. EXIT_PASS — подтверждён

```
cd apps/api && node --import tsx --test src/pricelist/pricelistYearPrice.test.ts
→ EXIT=0   tests 17 / suites 6 / pass 17 / fail 0 / duration_ms 439
```
Совпадает с заявкой. Было 15, стало 17: добавлены обратный тест на откат Groq и тест выключателя записи
состояния ключей.

### 2. EXIT_BITE — ВОСПРОИЗВЕДЁН РЕВЬЮЕРОМ, не принят на слово

Мутация собрана заново по его описанию, тремя заменами `split/join` в копии
`apps/api/src/pricelist/analyzerBite.tmp.ts`. Каждая замена шла под контролем счётчика попаданий —
скрипт падал бы с `hits !== 1`, чтобы «мутация» не оказалась немой:

```
["    const selected = selectPricelistServiceRows(parsedRows.items, parsedRows.droppedRows);" → ""]
["items: selected.items," → "items: parsedRows.items,"]
["        ...skippedRowsWarnings(selected.skippedRows)" → ""]
```

Все три строки в `analyzer.ts` уникальны — проверено отдельно: детерминированная ветка пишет
`const selected = selectPricelistServiceRows(parsedRows);` (без второго аргумента),
`const items = selected.items;` и `warnings.push(...skippedRowsWarnings(...));`. Значит мутация НЕ задевает
детерминированный путь и режет ровно нейро-ветку. Полученный текст — ровно дефект `b1cb29df3`:

```ts
    return responseFromItems({
      request,
      items: parsedRows.items,
      parserMode: "groq_json",
      warnings: [
        ...(request.imageBase64 ? ["photo_ocr_requires_visual_review"] : []),
      ],
```

```
cd apps/api && node --import tsx --test src/pricelist/yearPriceBite.tmp.ts
→ EXIT=1   tests 17 / pass 13 / fail 4
```

Красное — четыре утверждения по существу, все `AssertionError [ERR_ASSERTION]`:

| падает | actual → expected |
|---|---|
| «записи модели исчезли, не оставив следа: []» | `null` → `2` |
| «гейт не применён к записям модели: ["Прайс-лист действителен с 01.01.2025","Коронка"]» | `2` → `1` |
| «потери на двух шагах складываются в одно число» | `2` → `1` |
| «состав прайса зависит от режима разбора» | `2` → `1` |

**Мутация НЕ ломает компиляцию**: остальные 13 тестов того же файла на мутированной копии прошли, то есть
модуль вычислился и исполнился. Это поведенческая бита, а не отказ сборки. Цифры и текст сообщений
совпали с заявкой исполнителя дословно.

Копии удалены: `git status --porcelain -- apps/api/src/pricelist/` пуст, в каталоге ровно восемь исходных
файлов, `*.tmp.ts` нет.

### 3. Ветка исполняется НАСТОЯЩАЯ — проверено кодом и НЕЗАВИСИМЫМ зондом ревьюера

Подделан ровно один вызов — `globalThis.fetch`. Ниже границы работает боевой `analyzer.ts`:
`selectProviderKey` из `keyPool`, `response.json()`,
`contentToString(payload.choices?.[0]?.message?.content)`, `safeParseJsonObject`,
`pricelistItemsFromGroqRows`, затем `selectPricelistServiceRows` + `skippedRowsWarnings` — то есть именно
тот участок, за который пакет отвечает (`analyzer.ts:1616-1650`, `1680-1704`).

Проверено НЕ его тестом: я собрал свой зонд вне его файла (заглушка `fetch`, ключ в пуле,
`DENTAL_SPEECH_KEY_HEALTH_FILE=off`) и получил `parserMode: groq_json` на боевом `analyzePricelist`.
Механизм пакета работает независимо от его набора.

Помощник `analyzeGroqRows`, пересобиравший последовательность в тесте, удалён: в файле не осталось ни
одного его вызова и ни одного импорта `pricelistItemsFromGroqRows`/`selectPricelistServiceRows`
(упоминания сохранились только в поясняющем комментарии, строки 294-295 — это описание убранного
дефекта, не код). Единственный импорт продукта теперь `import { analyzePricelist } from "./analyzer.js"`.

`assertGroqBranchExecuted` (строки 457-483) — не украшение, а второй замок: без него откат на
детерминированный разбор ТОГО ЖЕ `rawText` дал бы совпадающий ответ, и набор был бы зелен, не исполнив
предмет. Он требует ровно один вызов, адрес `https://api.groq.com/openai/v1/chat/completions`, `Bearer` с
ключом пула, `json_object` в теле, `parserMode === "groq_json"`, `aiVision.used === true`, отсутствие
`groq_failed:`. Признак НЕ тавтологичен, проверено: `rg -n '"groq_json"' analyzer.ts` даёт РОВНО одно
попадание — строку 1696 в успешной нейро-ветке; а отдельный тест (566-591) показывает, что на ответе 500
тот же путь даёт `deterministic_groq_fallback` и `aiVision.used === false`.

### 4. Изоляция ключа отказа — ИЗМЕРЕНА, а не заявлена

Он пишет, что тесту с отказом Groq нужен свой ключ, иначе остывание после `recordProviderKeyFailure`
унесло бы остальные прогоны файла. Я это проверил, а не поверил: в копии заменил
`poolKey: SYNTHETIC_GROQ_FAILURE_KEY` на общий `SYNTHETIC_GROQ_KEY`.

```
node --import tsx --test src/pricelist/sharedKeyProbe.tmp.ts → EXIT=1  tests 17 / pass 15 / fail 2
падают ровно те два теста, что стоят ПОСЛЕ теста отказа
```
Причина видна в `keyPool.ts:517-530`: `selectProviderKey` отбрасывает остывающие ключи и возвращает
`null`, когда кандидатов не осталось, а `getProviderKeyCandidates` читает среду ЖИВОЙ (кэша нет), поэтому
подмена `GROQ_API_KEY` действительно даёт другой отпечаток. Предосторожность несущая, не декоративная.
Копия удалена.

### 5. Отговорка предыдущего исполнителя опровергнута ЗАМЕРОМ

Тест 622-666 ведёт `DENTAL_SPEECH_KEY_HEALTH_FILE` на сторожевой путь в `tmpdir()` и проверяет ОБЕ
половины: без выключателя файл появляется (эффект реален, иначе выключать было бы нечего), при `off` не
появляется. Механика подтверждается кодом: `keyPool.ts:198-201` возвращает `null` при значении `off`, а
`saveKeyHealthToDisk` на `null` выходит.

### 6. Регресс — прогнан заново, семь наборов по отдельности

```
analyzer.test.ts              EXIT=0  tests 6   fail 0
groqPricelistKopecks.test.ts  EXIT=0  tests 13  fail 0
pricelistBrandDigits.test.ts  EXIT=0  tests 9   fail 0
pricelistKopecks.test.ts      EXIT=0  tests 33  fail 0
pricelistLastNumber.test.ts   EXIT=0  tests 15  fail 0
pricelistRowGate.test.ts      EXIT=0  tests 11  fail 0
pricelistYearPrice.test.ts    EXIT=0  tests 17  fail 0
TOTAL=104, fail 0
```
102 до пакета + 2 новых = 104. Заявка подтверждена ровно.

### 7. Коммит — чист

```
git show --stat 5ccb48e96
 .agents/archon/packets/OO1/commitmsg.txt          |  42 +++
 apps/api/src/pricelist/pricelistYearPrice.test.ts | 379 ++++++++++++++++++----
 2 files changed, 360 insertions(+), 61 deletions(-)
git log -1 --format='%(trailers)' 5ccb48e96 → пусто
Author: marko1olo
```
Только заявленные пути, трейлеров нет, атрибуции инструмента нет. Чужая работа не подметена: правка
`analyzer.ts` (промпт v2 + перечисление `crownType`), которую он видел незакоммиченной, ушла отдельными
коммитами другого агента (`4607ca9fc`, `fb9eb4787`) и в его коммит не попала. Продукт этим пакетом не
менялся вовсе — только набор.

Решена ЗАЯВЛЕННАЯ задача, а не соседняя подешевле: подешевле здесь было бы оставить композицию из двух
функций и дописать утверждений. Он снёс помощника целиком и поднял ветку.

### 8. Ноль вместо «неизвестно» — не подставлен

```
git show 5ccb48e96 -- ...pricelistYearPrice.test.ts | rg '^\+.*(\?\? 0|\|\| 0)'  → нет попаданий
```
«Без потерь» сверяется с `null`, и это единственный правильный выбор: `skippedRowsWarnings`
(`analyzer.ts:1287-1289`) при нуле предупреждения не создаёт вовсе, поэтому `0` был бы напечатан клинике
как ИЗМЕРЕННЫЙ ноль потерь. Единственный `?? 0` в `analyzer.ts` — строка 1008, максимум в подборе
кандидата, на экран не идёт.

### 9. `smoke:pricelist-analyzer` — расхождение с обычным законом подтверждено, вина не его

```
npm run smoke:pricelist-analyzer → EXIT=1
Error: Сборка старше исходника: dist/pricelist/analyzer.js собран 2026-07-29T07:04:49.809Z,
а src/pricelist/analyzer.ts изменён 2026-07-29T07:32:52.109Z. Этот страж проверяет dist,
поэтому пересоберите: npm run build -w @dental/api
```
Страж сверяет mtime `dist` против `src` и падает ДО первого утверждения о прайсе. Красный он для всех
участников волны, пока `analyzer.ts` правят другие агенты и `dist` не пересобран. Файл OO1
(`pricelistYearPrice.test.ts`) в `dist` не попадает и на исход стража не влияет никак. CONTRADICTED
исполнителя честен, `npm run build` ему запрещён — это долг ведущего, не дефект пакета.

---

## ЧТО ОСТАЛОСЬ НЕПРОВЕРЕННЫМ

- Сеть, приём заголовков стороной Groq, поведение при смене формата ответа модели — предел назван в самом
  файле (строки 313-319), не спрятан.
- Сквозной путь `POST /api/pricelist/analyze` не поднимается: соответствие ответа `analyzePricelist` тому,
  что доедет до экрана клиники, набором не закрыто. Ниже, в F1 и F2, видно, что этот зазор не теоретический.
- Общие гейты (полный typecheck, `build`, весь `npm test`) не запускались — по правилу ревью.

---

## FOUND

### F1 (P1, ИЗМЕРЕНО, дефект продукта). Английский текст от модели печатается русской клинике как есть

`apps/api/src/pricelist/analyzer.ts:1560` + `apps/web/src/pricelistUiMeta.ts:294-296`

Тот самый класс, за который волна уже заплатила («unknown» на экране русской клиники), только источник
другой: не наш enum, а свободный текст модели. Зонд ревьюера через боевой `analyzePricelist` с
`useServerAi: true`, запись модели с полем `warnings`:

```
item.warnings     : ["material_uncertain","price ambiguous","two prices in one row","price not found"]
НА ЭКРАНЕ КЛИНИКИ : Материал требует проверки, price ambiguous, two prices in one row, price not found
```

Механика: `itemFromGroq` кладёт предупреждения модели в позицию без всякого списка допустимых значений —
`warnings: Array.from(new Set([...fallback.warnings, ...asWarnings(record.warnings)]))`. На экране
`pricelistWarningText` кончается строкой `pricelistWarningLabels[normalized] ?? normalized.replace(/[_-]+/g, " ")`,
то есть неизвестный ключ печатается СЫРЫМ. Сетка `technicalPricelistWarningPattern`
(`groq|openai|…|invalid`) такие фразы не ловит: в них нет ни одного технического слова.

Почему это реально, а не гипотеза: `groqSystemPrompt` (`analyzer.ts:1374-1387`) перечисляет допустимые
значения для `category`, `specialty`, `materialKind`, `restorationType`, `crownType` — и НИ СЛОВА о
допустимых значениях `warnings`, хотя требует это поле у каждой позиции. Промпт целиком английский, а
модель послушна ровно там, где ей дали список. Именно `crownType` уже поймали на этом в `3ce31c940` и
`fb9eb4787` — тот же промпт, то же место, необработанное поле рядом.

Дальше по адресу: либо закрытый список ключей предупреждений в промпте плюс отбрасывание неизвестных в
`asWarnings`, либо запасной русский текст вместо сырого ключа в `pricelistWarningText`. Первое лучше:
непереведённый ключ на экране — следствие, а не причина.

### F2 (P1, ИЗМЕРЕНО, дефект продукта; найдено и исполнителем — подтверждаю замером). `warnings` уровня прайса от модели выбрасываются молча

`apps/api/src/pricelist/analyzer.ts:1637-1640`

```ts
const parsed = safeParseJsonObject(contentText);
const rows = Array.isArray(parsed.items) ? parsed.items : [];
```

`parsed.warnings` не читается НИГДЕ (`rg -n 'parsed\.warnings' analyzer.ts` → пусто), хотя
`groqSystemPrompt` требует `Return only JSON with keys items and warnings`. Тем же зондом: модель вернула
`warnings: ["some rows are unreadable","column header lost"]`, ответ пришёл с
`response.warnings: []`. Предупреждение модели уровня ВСЕГО прайса («часть строк нечитаема», «колонка цен
обрезана») до клиники не доходит ни в каком виде; `asWarnings(record.warnings)` покрывает только позиции.

Это ровно класс, закрытый `b1cb29df3`, но по другому полю: правило есть в одной ветке, отсутствует в
другой, потеря невидима. Подтверждает это и сам новый набор: `groqSuccessReply` формирует
`{ items: rows, warnings: [] }` — поле в заглушке есть, потому что его требует промпт, и ни одно
утверждение набора его не касается, потому что продукт его не читает. Пакету OO1 не в упрёк (его предмет
— гейт и счётчик по `items`), но дефект живой.

### F3 (P2, дефект продуктовой документации). В `analyzer.ts` осталась ЗАПИСАННАЯ отговорка, которая и породила ложную зелёную

`apps/api/src/pricelist/analyzer.ts:1506-1509` и `1576-1579` — оба докблока до сих пор утверждают:

> Экспортируется ради проверки: ветка Groq в этом окружении не исполняется (ключа нет, а платный вызов
> запрещён), поэтому единственное честное доказательство разбора — вызвать эту функцию напрямую

Пакет OO1 это утверждение ОПРОВЕРГ прогоном: ветка исполняется, ключ синтетический, платного вызова нет.
Исполнитель убрал следствие (помощник в тесте), но оставил в продукте причину — инструкцию, которая
следующему агенту прямо разрешает написать ещё одну композиционную проверку вместо вызова. За этот
конкретный класс волна платит второй раз; убрать формулировку дешевле, чем ловить её последствие в
третий.

### F4 (нит, ложная опора в отчёте исполнителя). Отсутствие побочных эффектов доказано на файле, которого набор не касается

Он пишет: «`.data/speech-key-health.json` остался 5418 байт, mtime Jul 27 09:25». Файл по этому пути
действительно такой. Но набор запускается из `apps/api`, а `keyHealthFilePath` делает
`resolve(configured || ".data/speech-key-health.json")` — от CWD, то есть боевой путь прогона это
`apps/api/.data/speech-key-health.json`, а он совсем другой: 3092 байта, mtime Jul 29 10:42, и в рабочем
дереве изменён (`git diff --stat` → 17 insertions, чужой прогон). Корневой файл трогают smoke-скрипты из
корня, а не тесты.

Вывод исполнителя тем не менее ВЕРЕН — я проверил на правильном пути:

```
stat apps/api/.data/speech-key-health.json → 2026-07-29 10:42:45.137 / 3092 байта
cd apps/api && node --import tsx --test src/pricelist/pricelistYearPrice.test.ts → EXIT=0
stat .data/speech-key-health.json          → 2026-07-29 10:42:45.137 / 3092 байта  (не тронут)
```
Плюс сама механика гарантирует это сильнее: `loadKeyHealthFromDisk` защёлкивает
`keyHealthLoadedFromDisk` на первом же вызове, а к этому моменту путь уже `off`, поэтому боевой файл не
только не пишется, но и не читается. Записываю нит потому, что доказательство, указывающее на не тот
артефакт, — ровно тот способ, которым эта кампания уже получала ложную уверенность; в следующий раз
верным может оказаться не вывод, а только совпадение.

### F5 (нит теста). `assertGroqBranchExecuted` привязан к `SYNTHETIC_GROQ_KEY` литералом, поэтому тест отказа проверяет исполнение вручную и слабее

`pricelistYearPrice.test.ts:464-468` сверяет заголовок с `Bearer ${SYNTHETIC_GROQ_KEY}`, а не с
`options.poolKey ?? SYNTHETIC_GROQ_KEY`. Поэтому тест отказа Groq (566-591) со своим ключом через общий
страж пройти не может и проверяет только `run.calls.length === 1` — без адреса и без ключа. Дыры в
предмете это не создаёт (там предмет — `parserMode` отката), но у правила «ветка исполнилась» появляется
второй владелец, а за размножение владельцев одного правила этот файл платит уже третий раз (свёртка
убывающей пары, граница длительности, гейт строк).

### F6 (нит теста, хрупкость). Среда правится на верхнем уровне модуля и не восстанавливается

`pricelistYearPrice.test.ts:365-381` присваивает `DENTAL_SPEECH_KEY_HEALTH_FILE`,
`DENTAL_SPEECH_MAX_NUMBERED_KEYS`, `GROQ_API_KEY` и удаляет `GROQ_API_KEYS`, `GROQ_API_KEY_1`,
`PROXY_URL`, `HTTPS_PROXY`, `HTTP_PROXY` — обратно ничего не кладётся. Причина в комментарии названа и она
верная (тела `node:test` исполняются после вычисления модуля). Сегодня это безопасно: `apps/api`
запускает `node --import tsx --test "src/**/*.test.ts"`, а `node:test` даёт каждому файлу свой процесс,
так что утечки за пределы файла нет. Нит на будущее: снятие прокси и подмена пула станут видимы другим
наборам, если этот файл однажды импортируют или прогон переедет в один процесс. Лечится `after()` со
снимком `process.env`.

### F7 (наблюдение, не дефект). Литералы формата ключа в исходнике

`pricelistYearPrice.test.ts:355` и `362-363` держат `gsk_synthetic_pricelist_branch_key_do_not_leak_4444`
/ `..._5555`. Строки заведомо синтетические и для Groq непригодные, префикс `gsk_` нужен пулу для
распознавания кандидата, в сеть они не уходят (прокси снят, `fetch` подменён) — утечки НЕТ. Отмечаю
потому, что `gsk_*` в исходнике будет тратить время сканеров секретов и следующего ревьюера, а
`do_not_leak` внутри значения читается как предупреждение о настоящем ключе. Достаточно префикса
`gsk_test_`.

---

## ПОЧЕМУ НЕ SOUND И НЕ NEEDS_REWORK

Предмет пакета закрыт и это проверено, а не заявлено: EXIT_BITE воспроизведён ревьюером и краснеет
поведением, а не компиляцией; ветка исполняется настоящая, что подтверждено независимым зондом вне его
файла; признак исполнения не тавтологичен (`"groq_json"` в продукте ровно в одном месте, плюс обратный
тест на откат); изоляция ключа отказа измерена; регресс держит 104 при 102 до пакета; ноль вместо
«неизвестно» не подставлен; мёртвого кода пакет не включал, сырых значений на экран не льёт. Ни одного
зелёного теста, не проверяющего ничего, в новом наборе я не нашёл — искал именно так, как требует бриф:
мутацией предмета, а не чтением.

`SOUND_WITH_NITS`, а не `SOUND`, из-за F4, F5 и F6: доказательство отсутствия побочных эффектов указывало
на не тот файл на диске (вывод верен, опора — нет), у правила «ветка исполнилась» два владельца, среда
процесса не восстанавливается. Ни одно из трёх не делает набор зелёным на сломанном коде.

`NEEDS_REWORK` не применим: EXIT_BITE есть, воспроизведён и бьёт по существу. `REVERT` не применим: чужих
правок не потеряно, секрета не утекло, кириллица цела, в исходники ничего не писало, временные копии
удалены. F1-F3 — дефекты продукта, а не этого пакета; они за пределами его предмета и адресованы
следующим пакетам волны.
