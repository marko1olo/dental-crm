[ARCHON] docs(доступ): сдача U1 — правка досье о apply-dev-env.ps1 и разбор радиуса requireOrganizationId

Отчёт пакета: подтверждение дефекта на реальных строках, замер радиуса
(89 попаданий в 17 файлах, две двери в accessGuard.ts, которые мне были
запрещены), матрица проб и раздельные списки ПРОВЕРЕНО / НЕ ПРОВЕРЕНО.

Здесь же зафиксирована правка досье: утверждение «один запуск apply-dev-env.ps1
открывает дыру во всём API» неверно. Скрипт был UTF-8 без BOM и падал с
ParserError в Windows PowerShell 5.1 до первой записи — ни одного файла он не
менял. Замысел вооружить флаг в трёх .env был, исполнения не было.
