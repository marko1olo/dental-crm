# V1-corner-reserve-regression — handoff

HEAD: 0d728da9d83c65a9bec3d5682ed75014755fe4fb
(HEAD на старте пакета: 8ff0ba18e209d6c9c00812af3a4c2dd9fd85a229 — за время работы
уехал до a0ee75eba чужими коммитами, см. «Коллизия»)

Мои коммиты: `bda50170d9d4cd170b01a4bff2cc974902ff0a00`, `0d728da9d83c65a9bec3d5682ed75014755fe4fb`.

---

## Что было сломано (file:line)

Всё ниже — не чтение кода, а замер живой страницы: `node scratch/probe-corner-reserve.mjs before`
(экран `#patients`, тема light, окна 390x844 / 840x900 / 1600x1100). Сырые числа в
`scratch/v1-corner-before.json`.

### 1. Резерв низа не применялся на телефоне НИ РАЗУ (F2 — но не так, как написано в review)

| окно | опубликовано в `--corner-dock-reserve-block` | `padding-bottom` у `main.app-shell.dente-redesign` | `padding-bottom` у `.workspace` |
|---|---|---|---|
| 390x844 | 144px | **96px** | **80px** |
| 840x900 | 144px | **96px** | **80px** |
| 1600x1100 | 96px | 0px | 96px |

- `dente-redesign.css:687` (до правки) — `main, .app-content { padding-bottom:
  var(--corner-dock-reserve-block, 100px) !important }`. Селектор типа `main` даёт
  специфичность (0,0,1) и проигрывает `main.css:13013` — `.app-shell, .workspace,
  .panel, .work-grid, .imaging-layout, .patients-panel, .finance-panel,
  .visit-panel, .schedule-panel { padding-bottom: calc(96px +
  env(safe-area-inset-bottom)) !important }` (0,1,0). Проверено, что это НЕ спор
  слоёв: `@layer legacy` в main.css закрывается на строке 655, правило 13013
  вне слоёв. Правило U4 было мертво с первого дня.
- `.app-content` в разметке проекта не существует вовсе (`rg app-content apps/web/src`
  даёт только само это правило). `.dnt-content` — тоже мёртвый селектор.
- `dente-redesign.css:813-815` (до правки) — правило резерва для `.workspace` (0,3,0).
  `dente-redesign.css:1024-1026` — `padding: 10px 12px 80px !important` для того же
  селектора (0,3,0), НИЖЕ по файлу, значит побеждает. Резерв отменялся зашитым числом.

### 2. Замер геометрии шёл в каждом кадре прокрутки и заказывал принудительный layout (F4)

`CornerDock.tsx:188` (до правки) — `host.style.setProperty(VAR_LIFT, "0px")` прямо
перед `bar.getBoundingClientRect()`. Запись свойства, от которого зависит `bottom`,
помечает layout грязным, и следующее чтение геометрии становится принудительным
пересчётом. Замерено на 120 кадрах прокрутки: 59 полных проходов, 295 вызовов
`document.elementsFromPoint` (2.46 на кадр), 5.74-6.84 чтений прямоугольников на
кадр, 12.9-19.8 мс суммарно внутри `getBoundingClientRect`.
`document.elementsFromPoint` имеет РОВНО одного вызывающего в `apps/web/src`
(`CornerDock.tsx:155`), поэтому счётчик принадлежит углу и никому больше.

### 3. Мишени снимались только в исходном положении (F3)

`CornerDock.tsx:205-209` (до правки) собирал мишени при подъёме 0, а
`resolveCornerPlacement` перебирал по этому же списку все подъёмы. Всё, что висело
выше угла и не попало в точку замера, было невидимо.

### 4. Уступать было решено наполовину (найдено замером, следствие правки №3)

Замер `scratch/probe-corner-obstacles.mjs` назвал помеху по имени: это не кнопка, а
`<article class="clickable-card" role="button" tabindex="0">` — плитка быстрого
действия из `.patient-feature-grid`.
- 390x844: плитка 364x113, панель накрывает **16.3%** её площади;
- 840x900: плитка 399x168, панель накрывает **12%** её площади.
`isCornerObstacle` отвечал «можно ли это нажать» — верно. Второй половины вопроса
(«отнимет ли панель эту возможность») не было. `.patient-feature-grid` — стена таких
плиток высотой 368px, поэтому после включения досъёма панель пошла по ней вверх:
подъём **116px -> 299px** на 840x900 и **46px -> 158px** на 1600x1100, и на 299px, по
тому же замеру, всё равно стояла на плитке (перекрытие 1px на границе 512/513).

### 5. Ложные утверждения в комментариях того же региона (F6)

- `cornerDockLayout.ts:14` — «Никто больше не имеет права ставить `position: fixed` в
  правый нижний угол». Запрета нет, в углу живут ещё два чужих fixed-элемента.
- `CornerDock.tsx:260` — «страница прокручивается внутри `.workspace`». Замерено
  обратное: у `.workspace` `scrollHeight == clientHeight` на всех трёх окнах
  (9659/9659, 6753/6753, 6352/6352), скроллер — `html`.

---

## Что изменено

`bda50170d` (7 файлов, +735/-144):
- `dente-redesign.css` — резерв применяется РОВНО ОДИН РАЗ, к `.workspace`. Из обоих
  shorthand-ов `.workspace` убран низ (остались `padding-top` + `padding-inline`),
  поэтому спорить с правилом резерва больше нечему. Мёртвое правило `main,
  .app-content` заменено на `main.app-shell.dente-redesign { padding-bottom: 0
  !important }` (0,2,1 перебивает 0,1,0): снята чужая компенсация 96px под
  легаси-полосу `.sidebar`, которой на <=840px нет. Диапазон 841-860px не тронут —
  там полоса реальна.
- `cornerDockLayout.ts` — `resolveCornerPlacementSampled` (досъём выбранного
  положения, множество мишеней только растёт, предел `CORNER_MAX_SOLVE_PASSES`),
  `shouldRunCornerPass` + `CORNER_STREAM_INTERVAL_MS` (потоковые поводы не чаще
  10 Гц, последний догоняется отложенным проходом), роли `dialog`/`alertdialog`
  признаны мишенями.
- `CornerDock.tsx` — исходное положение панели восстанавливается вычитанием
  применённого подъёма (`measureRestingFootprint` через `liftCornerRect(rect,
  -appliedLift)`), а не записью в стили; значения, не изменившиеся с прошлого
  прохода, не пишутся вообще; два `ResizeObserver` разведены по срочности
  (панель и навигация — немедленно, рост body — потоком); `deferTimer` снимается
  в `detach()`.
- `cornerDock.css` — порог узкой плотности 52.5rem -> 840px, порог подсказок
  диктовки 60rem -> 768px.
- `cornerDockLabels.ts`, `CornerDock.tsx` — удалены мёртвые экспорты.
- `Omnibar.tsx` — указан источник `backdrop-filter` у `.workspace`.

`0d728da9d` (3 файла, +119/-1):
- `cornerBlocksTarget` + `CORNER_OBSTACLE_BLOCK_SHARE = 0.5`: мишень считается
  заблокированной, только если панель накрывает не меньше половины её площади.
  Применяется в `collectObstacles` для того положения, которое проверяется.

---

## ПРОВЕРЕНО

**TYPECHECK VERIFIED** — `npm run typecheck -w @dental/web`, TRUE_EXIT=0, вывода
кроме баннера npm нет. Оба гейта дерева зелёные.

**UNIT VERIFIED** — `node --import tsx --test apps/web/src/components/floatingCorner/cornerDockLayout.test.ts`
TRUE_EXIT=0: `tests 54 / suites 11 / pass 54 / fail 0`, duration 216.141 ms.

**UNIT VERIFIED** — `npm test -w @dental/web` TRUE_EXIT=0:
`tests 533 / suites 95 / pass 533 / fail 0`, duration 4723.36 ms. Ни один чужой
веб-тест не сломан.

**АРИФМЕТИКА РЕЗЕРВА, ЧИСЛОМ (то, что просил пакет)** — тест
«резерв на 390x844 равен 144px»: `computeCornerReserve({ barHeight: 48, gutter: 16,
barClearance: 64 }) === 144`. Тот же вход и результат на пороге 840px. На 1600x1100
`{ barHeight: 48, gutter: 24, barClearance: 0 } === 96`. Входы — не выдумка, это
замеренные значения живой страницы.

**РЕЗЕРВ ПРИМЕНЯЕТСЯ ОДИН РАЗ — ДВА НЕЗАВИСИМЫХ ДОКАЗАТЕЛЬСТВА**
1. UNIT: тест «переменную резерва читает ровно ОДНО правило во всех стилях проекта»
   читает НАСТОЯЩИЕ `.css` файлы (`styles/` + `floatingCorner/`), вырезает
   комментарии и требует ровно одно вхождение `var(--corner-dock-reserve-block`.
2. БРАУЗЕР: `node scratch/probe-corner-reserve.mjs after2` —
   вычисленный `padding-bottom` стал `main.app-shell` = **0px**, `.workspace` =
   **144px** = ровно опубликованное значение (390x844 и 840x900); на 1600x1100
   0px / 96px. Было 96px + 80px = 176px двумя зашитыми числами, стало 144px одним
   измеренным. Пустого низа на телефоне стало НА 32px МЕНЬШЕ, и он наконец
   соответствует реальной высоте угла.

**ЗАМЕР В БРАУЗЕРЕ: РОВНО ОДИН ХОСТ, НОЛЬ ПЕРЕСЕЧЕНИЙ С НАВИГАЦИЕЙ**
Это закрывающая проверка, которую просили и U4 (НЕ ПРОВЕРЕНО №1), и рецензент (§6).
`scratch/v1-corner-after2.json`: `dockHosts: 1` на всех трёх окнах.
- 390x844: угол 708..756, навигация 780..844 — зазор 24px;
- 840x900: угол 764..812, навигация 836..900 — зазор 24px;
- 1600x1100: угол 1028..1076 при высоте окна 1100, навигации нет — 24px от низа.
Подъём на всех трёх окнах равен нулю: угол вернулся В УГОЛ.

**F4 BEFORE/AFTER, ЧИСЛАМИ** (120 кадров прокрутки, экран `#patients`, тот же пробник)

| окно | метрика | BEFORE | AFTER | итог |
|---|---|---|---|---|
| 390x844 | вызовов `elementsFromPoint` | 295 | **90** | −69% |
| | на кадр | 2.46 | **0.75** | −70% |
| | чтений прямоугольника на кадр | 5.74 | **1.74** | −70% |
| | мс внутри `getBoundingClientRect` | 19.34 | **0.36** | **−98%** |
| | мс внутри `elementsFromPoint` | 21.05 | **6.58** | −69% |
| | средний кадр, мс | 16.71 | 16.69 | без изменений |
| 840x900 | вызовов `elementsFromPoint` | 295 | **130** | −56% |
| | мс внутри `getBoundingClientRect` | 12.90 | **0.47** | **−96%** |
| | средний кадр, мс | 25.66 | 25.38 | без изменений |
| 1600x1100 | вызовов `elementsFromPoint` | 295 | **380** | **+29% (хуже)** |
| | мс внутри `getBoundingClientRect` | 19.79 | **1.38** | **−93%** |
| | средний кадр, мс | 51.18 | 57.32 | +12%, НЕ приписываю углу |

То, что F4 назвал по имени («forcing 2 full layouts»), измеримо исчезло: время
внутри `getBoundingClientRect` упало на 93-98% на всех трёх окнах, потому что чтение
геометрии больше не идёт сразу после записи в стили.

**ЗАМЕР ПРОТИВ F6.2** — вычисленный `backdrop-filter` у `section.workspace` равен
`blur(12px) saturate(1.8)` на всех трёх окнах. Источник найден: `premium.css:147`
перечисляет `.workspace`, правило на `premium.css:172` объявляет
`backdrop-filter: var(--glass-blur) saturate(180%)`, `--glass-blur: blur(12px)`.

**КОДИРОВКА** — `node scratch/v1-mojibake-check.cjs` TRUE_EXIT=0, `TOTAL_BROKEN=0`
по всем семи моим файлам. Русский текст читается: 5 совпадений «резерв» в
`cornerDockLayout.ts`, 9 в `dente-redesign.css`.

**ТОКЕНЫ** — `rg -c '#[0-9a-fA-F]{3,8}' apps/web/src/components/floatingCorner/cornerDock.css`
выходит с кодом 1: статических hex в CSS угла нет.

---

## НЕ ПРОВЕРЕНО

1. **Отрендеренный результат. Ни одного пикселя я не видел.** Я мерил DOM и
   вычисленные стили, не картинку. Не проверено: как выглядит угол в трёх темах,
   не обрезаны ли русские подписи, читается ли компактный режим.
   Закрывающая команда (пайплайн скриншотов принадлежит лиду): перезаснять
   `.dente-ops-shots/light_duplicateAlert_ПУСТО.png` и `narrow_full.png` и открыть
   плиты глазами.
2. **Тост входящего звонка.** Роли `dialog`/`alertdialog` теперь мишени, тест на
   предикат проходит, но живого звонка в замере не было, поэтому уступание тосту
   в браузере не наблюдалось. Закрывающая команда:
   `node scratch/probe-corner-obstacles.mjs 1600 1100` при открытом
   `IncomingCallToast` (нужен вебхук телефонии или ручной рендер тоста) — ожидается
   подъём панели выше прямоугольника тоста.
3. **React StrictMode.** Двойной прогон эффектов снимает и заново создаёт хост.
   `dockHosts: 1` замерено в браузере, но StrictMode в этой сборке не включён.
   Закрывающая команда: включить StrictMode в `main.tsx` и повторить пробник.
4. **Компактный режим на живой странице.** `density` во всех шести замерах —
   `comfortable`, то есть повторный проход и уменьшение следа ни разу не
   выполнялись в браузере. Проверены только тестом. Закрывающая команда:
   `node scratch/probe-corner-obstacles.mjs 390 700` на экране со сплошной колонкой
   маленьких кнопок в правом нижнем углу (панель плана лечения в приёме).
5. **Кадр на 1600x1100 стал длиннее на 12%** (51.18 -> 57.32 мс). Углу это НЕ
   приписываю: его собственное измеренное время — около 0.23 мс на кадр из 57, а
   два прогона шли с разницей в минуты на общем dev-сервере. Закрывающая команда:
   профиль прокрутки в DevTools с фильтром по `applyLayout`.

---

## Коммит

1. `bda50170d9d4cd170b01a4bff2cc974902ff0a00` —
   `[ARCHON] fix(угол): резерв низа не применялся на телефоне ни разу, а замер шёл в каждом кадре прокрутки`
   7 файлов, +735/-144.
2. `0d728da9d83c65a9bec3d5682ed75014755fe4fb` —
   `[ARCHON] fix(угол): панель уезжала на середину экрана, уступая карточке, которую закрывала на 16%`
   3 файла, +119/-1.

Индекс перед каждым коммитом проверен `git diff --cached --name-only` (пусто),
коммит сделан формой с `--` и списком путей. `git show --name-only` подтверждает:
только мои файлы, ни одного чужого, ни `dist/`, ни `.data/`, ни `scratch/`.

---

## Коллизия (не блокирующая, чужая работа НЕ откачена)

Пока я работал, HEAD уехал `8ff0ba18e -> a0ee75eba`. Чужой (не-fleet, без префикса
`[ARCHON]`) коммит **`f50f7f67d`** «Экран отправки перестал спорить сам с собой;
плавающие кнопки не вылезают за край» правил МОЙ заявленный файл
`apps/web/src/components/floatingCorner/cornerDock.css` (+29/-7):
один диаметр для всех трёх кнопок (`--corner-dock-control-primary:
var(--corner-dock-control)`), `max-width` от `100%` вместо `100vw` (в `vw` входит
полоса прокрутки), новая страховка `--corner-dock-bar-floor` под `max()`.
Его работу я **не откатывал и не «исправлял»**, свои правки F7 положил поверх.
Остальные мои файлы он не трогал. Следствие для замеров: базовый замер BEFORE снят
уже НА дереве с его правкой (высота панели 48px = 3rem), поэтому BEFORE и AFTER
сравнимы между собой — размеры кнопок я не менял.

---

## Долг

1. **`--corner-dock-bar-floor: 4.5rem`** (`cornerDock.css`, чужая правка f50f7f67d)
   — зашитое число вернулось в угол как «пол» под `max()`. Опустить угол на
   навигацию оно не может, но если реальная навигация ниже 72px, угол висит выше
   нужного. Замерено: навигация 64px, то есть страховка сейчас на 8px выше замера.
2. **`main.css:13013`** раздаёт `padding-bottom: calc(96px + env(...))` не только
   оболочке, но и `.panel`, `.work-grid`, `.imaging-layout`, `.patients-panel`,
   `.finance-panel`, `.visit-panel`, `.schedule-panel` на <=860px. Это лишний
   пустой низ ВНУТРИ каждой панели. Не мой клейм (main.css), не трогал.
3. **Вызовов `elementsFromPoint` на 1600x1100 стало на 29% больше** (295 -> 380).
   Причина названа: при кадре 51-57 мс порог 100 мс не ограничивает ничего (проходы
   и так шли раз в ~104 мс за счёт склейки в rAF), а отложенная попытка добавляет
   ещё один повод. Что я предлагаю вместо третьей попытки чинить это здесь: не
   вооружать таймер, если проход уже запланирован в кадре, и привязать интервал к
   реально наблюдаемой длительности кадра, а не к константе.
4. **`IncomingCallToast.tsx:67` и `WaitlistDrawer.tsx:188`** по-прежнему ставят
   свой `position: fixed` в этот угол (z-index 999999 и 50). Контракт теперь
   обращается с ними как с содержимым страницы и уступает подъёмом, но переселить их
   в слоты угла нельзя без правки этих файлов — вне моего клейма. Нужен либо пакет
   на переселение, либо линт-запрет на `position: fixed` с `bottom`+`right` вне
   `floatingCorner/`.
5. **`.patients-search-bar` / `.patient-search-row`** (`dente-redesign.css:1076-1087`)
   — `position: sticky; bottom: 60px` на <=840px, то есть второй претендент на низ
   экрана рядом с углом. Сейчас конфликта нет (угол стоит выше), но два
   низ-привязанных элемента в одном месте — источник будущей коллизии.
6. **Предел досъёма (`CORNER_MAX_SOLVE_PASSES = 3`).** На входе, где каждый снимок
   открывает новую мишень (непрерывная лестница с шагом в высоту панели), цикл
   упирается в предел и возвращает непроверенное положение. Задокументировано в
   коде, проверено тестом на ограниченность. Реальных страниц с такой геометрией
   в замере не встретилось.
7. **`hintType` в `VoiceAssistantUI`** читает `window.location.hash` на рендере без
   подписки на `hashchange`. Существовало до U4, не трогал.
8. **Подъём панели не анимирован намеренно** — угол «прыгает». Наследие U4,
   обоснование там же: плавность потребовала бы отказаться от детерминированного
   замера.

---

## Разбор пунктов рецензии U4 (каждый — CLOSED / DECLARED DEBT / DISPUTED)

**F1 (HIGH) — четвёртый остров в углу, тост звонка при z-index 999999.**
**CLOSED частично + DECLARED DEBT.** Закрыто то, что в моём клейме: роли
`dialog`/`alertdialog` добавлены в `INTERACTIVE_ROLES`, тост стал мишенью, панель
ему уступает. Подтверждено фактом из кода: `IncomingCallToast.tsx:67` —
`<div className="fixed bottom-6 right-6 z-[999999] ..." role="dialog">`.
Переселение тоста и `WaitlistDrawer.tsx:188` в слоты угла — вне клейма, долг №4.
Уступание тосту в браузере НЕ наблюдалось (НЕ ПРОВЕРЕНО №2).
Ложное утверждение `cornerDockLayout.ts:14` о запрете `position: fixed` исправлено в
самом файле: оба нарушителя названы по файлу и строке.

**F2 (MEDIUM) — резерв применяется дважды, ~304px из 844px вьюпорта.**
**CLOSED по существу, DISPUTED по формулировке.** Дефект реальный и ХУЖЕ
заявленного, но описан неверно в трёх местах, и все три опровергаются замером
живой страницы (`scratch/v1-corner-before.json`):
- «применяется дважды» — на <=840px не применялся НИ РАЗУ. Вычисленный
  `padding-bottom` был 96px у оболочки (из `main.css:13013`, специфичность 0,1,0
  против 0,0,1 у правила U4) и 80px у `.workspace` (из shorthand на
  `dente-redesign.css:1026`, ниже по файлу при равной специфичности 0,3,0).
  Опубликованные 144px не доходили ни до одного элемента.
- «152+152 = ~304px» — резерв равен 144px, не 152px: реальная высота панели 48px
  (все три кнопки 3rem после чужого коммита f50f7f67d), а не 56px.
- «из 844px вьюпорта» — ни один из этих отступов не вычитается из вьюпорта.
  `.workspace` объявляет `overflow-y: auto`, но по высоте не ограничен:
  `scrollHeight == clientHeight` на всех трёх окнах, прокручивается `html`. Значит
  оба отступа — хвостовой отступ в конце документа, прокручиваемый, а не пустая
  полоса на экране.
Итог правки: 96px + 80px двумя зашитыми числами -> 144px одним измеренным.

**F3 (MEDIUM) — мишени снимаются только в неподнятом положении.**
**CLOSED.** `resolveCornerPlacementSampled` доснимает выбранное положение;
множество мишеней только растёт, поэтому подъём монотонен, а число досъёмов
ограничено. Тест «мишень, видимая только из поднятого положения, всё равно
учитывается» проверяет цепочку замеров целиком (`[0, 36, 90]`), нулевое пересечение
с ОБЕИМИ мишенями и то, что итоговое положение входит в число измеренных.
Замер в браузере после этой правки вскрыл вторую половину дефекта — панель уступала
плитке, которую закрывала на 16%; исправлено `cornerBlocksTarget` (коммит 2).

**F4 (MEDIUM) — проход в каждом кадре прокрутки, 2 layout + 5 hit-тестов.**
**CLOSED по названной причине, ОСТАТОК ОБЪЯВЛЕН.** Принудительный layout убран:
время внутри `getBoundingClientRect` упало на 93-98% на всех трёх окнах. Вызовов
`elementsFromPoint` на 390x844 и 840x900 стало на 69% и 56% меньше. На 1600x1100 их
стало на 29% БОЛЬШЕ — заявляю это открыто как остаток с названной причиной и
предложением, долг №3. Числа BEFORE/AFTER в таблице выше, сырые данные в
`scratch/v1-corner-{before,after,after2}.json`.

**F5 (LOW) — пять экспортов, которых продакшен не вызывает; заявка на покрытие
опирается на один из них.** **CLOSED.** Удалены `sortCornerSlots`,
`isCornerSlotId`, `cornerRectsOverlap` (`cornerDockLayout.ts`), `cornerSlotLabels`
(`cornerDockLabels.ts`), `cornerDockSlots` (`CornerDock.tsx`). Три теста, проверявшие
`sortCornerSlots`, удалены вместе с заявкой «порядок не зависит от порядка
монтирования»; в тесте прямо написано, что порядок узлов DOM здесь НЕ проверяется, а
гарантию даёт цикл по `CORNER_BAR_SLOTS` в `CornerDock.tsx`.
`rg` по `apps/web/src` и `scratch` не находит ни одной ссылки на удалённые имена,
кроме упоминания в объясняющем комментарии теста. Typecheck и 533 теста зелёные.

**F6 (LOW) — часть 1: коммит навсегда фиксирует механизм, который handoff
опровергает.** **CLOSED как исправление записи** (переписать pushed-коммит нельзя).
Ложное место: тело `0112f293e` утверждает «до „Сохранить“ нельзя дотянуться — кнопка,
на которой сидит плавающий элемент, не нажимается» про обрезку «Подпи…».
Верно то, что написал сам автор U4 в `handoff.md:211-217` и что подтвердил рецензент:
обрезка вызвана собственной разметкой панели плана лечения, а не кнопкой микрофона.
**Лиду: занести это в `.agents/archon/progress.md`**, потому что там эту историю
будут читать, а в git-истории останется неверная версия.

**F6 (LOW) — часть 2: `Omnibar.tsx:88` ссылается на непроверяемый
`backdrop-filter`.** **DISPUTED, с командой и выводом.** Утверждение ВЕРНО.
Вычисленный `backdrop-filter` у `section.workspace` равен `blur(12px) saturate(1.8)`
на 390x844, 840x900 и 1600x1100 (`scratch/v1-corner-after2.json`). Источник:
`premium.css:147` перечисляет `.workspace` первым селектором правила, объявляющего на
`premium.css:172` `backdrop-filter: var(--glass-blur) saturate(180%)`, где
`--glass-blur: blur(12px)`. Поиск рецензента по литералу `saturate(1.8)` в
`styles/*.css` ничего не нашёл именно потому, что в источнике написаны проценты, а к
`1.8` их приводит браузер; и потому, что проверялись только три правила `.workspace`
в `main.css` и `dente-redesign.css`, а не `premium.css`. Комментарий не удалён, а
дополнен точной ссылкой на источник, чтобы вопрос не поднимали заново.

**F7 (LOW) — два расхождения порогов.** **CLOSED.**
`cornerDock.css`: `@media (max-width: 52.5rem)` -> `840px` (порог
`.dnt-bottom-nav`); подсказки диктовки `min-width: 60rem` -> `768px` (`md:` из
Tailwind, как было до переезда). В обоих местах написано, почему в медиавыражении
здесь px, а не rem: rem там считается от кегля браузера и разъезжается с
px-порогами оболочки.

---

## Поправки к записям предыдущего пакета и к досье

1. **`U4/handoff.md:112` — «`npm test -w @dental/web` … 496 тестов».** Сейчас их 533
   (чужие коммиты добавили). Не ошибка U4, а устаревшее число; фиксирую, чтобы его
   не сравнивали с моим.
2. **`U4/handoff.md:86-93` и `CornerDock.tsx:260` — «страница прокручивается внутри
   `.workspace`».** НЕВЕРНО. Замерено: `scrollHeight == clientHeight` у `.workspace`
   на всех трёх окнах, скроллер — `html`. Комментарий в коде исправлен.
3. **`U4/handoff.md:78-82` — «Добавлен резерв места в потоке: `.workspace` и `main,
   .app-content` берут `padding-bottom` из `--corner-dock-reserve-block`».** Второе
   правило не работало никогда (специфичность), первое не работало на <=840px
   (порядок). Резерв на телефоне не действовал вовсе.
4. **`U4/handoff.md:126` — «SMOKE VERIFIED … `voice-assistant-ui-hOs8g6dv.css`».**
   Хеш чанка после моих правок другой; ссылаться на него как на текущее состояние
   нельзя.
5. **Рецензия U4, F2 — «~304px из 844px вьюпорта», «152+152», «применяется дважды».**
   Все три неверны, разобрано выше с числами. **Лиду: поправить F2 в
   `.agents/archon/packets/U4-fab-corner-owner/review.md` или в `progress.md`**,
   иначе следующий агент будет искать двойное применение, которого не было.
6. **Рецензия U4, F6.2 — «стоящая причина держать портал в body непроверяема при
   HEAD».** Неверно, источник найден и указан выше.
7. **Наблюдение для визуального вердикта (лиду).** На экране `#patients` в правом
   нижнем углу лежит `.patient-feature-grid` — стена плиток `<article
   class="clickable-card" role="button">` высотой 113-168px. Угол теперь стоит на
   такой плитке, закрывая 12-16% её площади. Это осознанный выбор порога
   `CORNER_OBSTACLE_BLOCK_SHARE`, а не случайность, но судить его по картинке —
   ваша работа, не моя.

## Файлы пробников (untracked scratch, НЕ в индексе)

- `scratch/probe-corner-reserve.mjs` — вычисленные `padding-bottom`, геометрия угла и
  навигации, счётчики за 120 кадров прокрутки. `node scratch/probe-corner-reserve.mjs <phase>`.
- `scratch/probe-corner-obstacles.mjs` — что именно угол считает мишенью и какую долю
  её площади закрывает. `node scratch/probe-corner-obstacles.mjs [w] [h]`.
- `scratch/v1-corner-{before,after,after2}.json`, `scratch/v1-obstacles-{390x844,840x900}.json` — сырые замеры.
- `scratch/v1-mojibake-check.cjs` — проверка кодировки моих семи файлов.
