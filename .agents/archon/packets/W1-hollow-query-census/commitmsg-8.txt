docs(перепись): сдача W1 — «45 пустотелых из 50» было неверно, измерено 24 из 42

Отчёт и состояние пакета W1. Записаны три поправки к авторитетным файлам:
ast-grep из §8 конституции на этой машине не установлен, «примерно 65 модулей»
в DATABASE.md устарело, и три числа в RECON_DOSSIER.md:268 тоже.

Отдельно записана ловушка среды: в Git Bash под Windows аргумент, начинающийся
с косой черты, MSYS переписывает в путь Windows, поэтому поиск маршрута вида
"/api/..." даёт ложное «совпадений нет». На таком ложном отрицании ломается
HEAD: агент решает, что потребителей нет, и удаляет живой модуль.
