fix(аналитика): отчёт об эффективности подтверждений показывал пустоту на трёх экранах сразу

Блок «Эффективность подтверждения приёмов» стоял в аналитике и в смене, а в
коммуникациях был единственным виджетом, который оставили жить именно потому,
что «используется ещё в аналитике и в смене». Читал он таблицу
confirmation_performance_reports: писателей ноль, строк в живой базе ноль.

Три экрана давали один и тот же пустой ответ на вопрос, на который в системе уже
есть настоящий ответ — «Обзвон и подтверждения» считает подтверждения по
настоящим приёмам. Второй, вечно пустой источник правды хуже, чем его
отсутствие: он заставляет сомневаться в первом.

Удалены модуль доступа, маршрут /api/analytics/confirmation-performance-reports
и компонент; сняты все три упоминания, включая устаревший комментарий в
CommunicationsView.tsx, который обещал, что виджет ещё где-то нужен.

Заодно удалён db/familyRecommendationSourcesQuery.ts: его не импортировал никто,
ни статически, ни через await import, а маршрута под него нет вообще.

НЕ СДЕЛАНО и передаётся ведущему. FamilyRecommendationSourcesWidget.tsx и
RebookingConversionRulesWidget.tsx остались на экране: они смонтированы в
MarketingView.tsx, который сейчас правит другой автор. Оба зовут таблицы без
писателя, первый вообще получает 404. Их удаление — отдельный заход, когда
MarketingView.tsx освободится.
