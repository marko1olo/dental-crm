fix(маршруты): шесть адресов отдавали пустой список из таблиц без единого писателя

Удалены девять модулей доступа и шесть маршрутов над таблицами, в которые
приложение не пишет нигде и никогда. Перепись
scripts/census-hollow-query-modules.mjs разобрала дерево, а живая база
подтвердила счётом: писателей 0, наполнения миграцией 0, строк 0.

Маршруты и таблицы:
  /api/schedule/clipboard-items                       schedule_clipboard_items
  /api/communications/email-dispatch-logs             crm_email_dispatch_logs
  /api/integrations/prodoctorov-sync                  prodoctorov_sync_exports
  /api/communications/uis-omni-messenger-queues       uis_omni_messenger_queues
  /api/communications/quick-appointment-confirmations quick_appointment_confirmations
  /api/documents/alternative-treatment-plans          alternative_treatment_plans

Ни один из шести не звал интерфейс: ноль вызовов в apps/web. То есть это были
адреса, которые могли ответить только пустотой и которых никто не спрашивал.

Ещё три модуля удалены без маршрутов — их не импортировал вообще никто, ни
статически, ни через `await import`: uisCallSpeechTranscriptsQuery,
visitExaminationPhotoLinksQuery, egiszMultipleDiagnosesQuery.

Проверка «каждый вызванный адрес обслуживается сервером»
(apps/api/src/tests/webCallsExistingRoutes.test.ts) остаётся зелёной: удалены
только те адреса, которых никто не зовёт, поэтому список долга не вырос.

Заодно исправлено условие в тесте самой переписи. Оно требовало «модулей не
меньше сорока» и падало ровно тогда, когда уборка удавалась. Теперь перепись
сверяется с диском: те же имена, что и файлы *Query.ts, — проверка на
согласованность вместо запомненного количества мусора.

ДОЛГ, а не потеря. Буфер обмена в расписании, отправка планов на почту,
синхронизация отзывов «ПроДокторов», очередь мультимессенджера, быстрое
подтверждение приёма, альтернативные планы лечения — настоящие задачи. Каждой
нужны писатель в таблицу и экран, с которого он вызывается; ни того, ни другого
нет. Пустая панель на месте функции — не половина функции, а обещание, которое
нельзя выполнить.

ОСТАЛОСЬ: маршрут /api/egisz/multiple-diagnoses в routes/egisz.ts читает
egisz_multiple_diagnoses напрямую и тоже вернёт только пустоту. Он не тронут
намеренно: удаление адреса из модуля ЕГИСЗ — решение ведущего, не исполнителя.
