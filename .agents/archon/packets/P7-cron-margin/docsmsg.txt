[ARCHON] docs(пакет P7): чёрный ящик и отчёт по выдуманной прибыли в срезе BI

state.md, handoff.md и оба commitmsg — по протоколу пакета.

Вместе с ними лежат три скрипта проверки, все только на чтение, чтобы
ведущий мог перепроверить выводы, а не верить на слово:

  cost-data-census.cjs      — перепись источников затрат: пусты все пять,
                              поэтому маржу и нельзя посчитать честно;
  schema-reality-check.cjs  — сверка колонок запроса с реальной схемой и
                              наличие данных об успешности (27 приёмов,
                              13 завершённых);
  readonly-query-probe.ts   — тот же SELECT, что в воркере, прогнанный через
                              настоящую buildDoctorProfitabilityRow. INSERT
                              не выполняется: база общая.
