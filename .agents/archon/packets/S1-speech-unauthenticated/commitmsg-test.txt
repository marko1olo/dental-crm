test(диктовка): закрыт регресс — запись фрагмента без токена и в чужую клинику

Семь проверок через app.inject по живой базе. Переменные окружения выставлены как в dev-среде,
где дефект был снят: DENTE_CLINICAL_ALLOW_UNGUARDED_MUTATIONS=1 и незаданный
DENTE_CLINICAL_ADMIN_SECRET. Если бы защита держалась на флаге, проверка на 401 не прошла бы.

Контрольная проверка отделяет новую защиту от среды: соседний read-эндпоинт
/api/speech/status на булевом гейте requireClinicalReadAccess при тех же переменных отдает
200 запросу без единого заголовка. Значит 401 у записи получен проверкой, а не окружением.
Она же фиксирует незакрытый дефект чтения диктовки, вынесенный в отдельный пакет.

Внешний провайдер не вызывается: передается только localTranscript без audioBase64,
gateway.ts:2018-2021 сохраняет такой фрагмент как fallback_text и в сеть не выходит.

Co-Authored-By: Claude <noreply@anthropic.com>
