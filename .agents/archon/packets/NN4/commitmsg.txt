[ARCHON] fix(стражи): два стража требовали вернуть УДАЛЁННЫЕ дефекты, третий падал на переводе строк

ОБА ТЕПЕРЬ ЗЕЛЁНЫЕ, код выхода 1 → 0 у каждого, снято без конвейера.
smoke:web-render-gating-source и smoke:workspace-shell-source.

САМОЕ ВАЖНОЕ: НАЙДЕН СИСТЕМНЫЙ ДЕФЕКТ ВСЕГО СЛОЯ СТРАЖЕЙ -source — ПЕРЕВОДЫ СТРОК.
В репозитории `core.autocrlf=true`, и рабочее дерево лежит СМЕШАННЫМ. Замерено:
  apps/web/src/styles/main.css   17375 CRLF, 0 одиночных LF
  apps/api/src/sampleData.ts     12657 CRLF, 0 одиночных LF
  apps/web/src/App.tsx           0 CRLF, 5073 LF
  apps/web/src/SettingsView.tsx  0 CRLF, 2017 LF
Любое требование с литералом `\n` внутри проверяет не содержимое файла, а то, в каком виде git развернул его
на этой машине. Требование «Sidebar view hints must collapse on mobile to protect bottom navigation» падало
ровно так: искалось `.nav-copy small {\n    display: none;`, а правило в файле ЕСТЬ —
styles/main.css:13353-13355, внутри медиа-запроса, просто с `\r\n`. Страж сообщал об отсутствии мобильного
правила, которое существует. Класс общий для всех 41 стража этого семейства, потому что они сравнивают
подстроки, а не разбирают код; здесь он закрыт чтением через нормализацию `\r\n` → `\n`.

ДВА ТРЕБОВАНИЯ ТРЕБОВАЛИ ВЕРНУТЬ ТО, ЧТО УДАЛЕНО НАМЕРЕННО, И ОБА ЗАПРЕЩЕНЫ ДРУГИМ ТЕСТОМ.
Это не «страж покраснел» — это два гейта, которые не могли быть зелёными одновременно.
- 'aria-label="Настройки импорта и экспорта"'. __tests__/workspaceTopbarActions.test.ts:215-218 утверждает
  `!shellSource.includes("Настройки импорта и экспорта")`: ссылка-значок вела на общий хеш `#settings`,
  который вкладку импорта не открывает, то есть её имя для программы чтения с экрана обещало то, чего она не
  делала. workspaceShell.tsx:721-730: удалён БЕЗЫМЯННЫЙ ДУБЛЬ пункта «Настройки» бокового меню — тот же глиф
  Database, тот же адрес. Удалён дубль, а не путь.
- класс `top-dictation-button`. workspaceTopbarActions.test.ts:207-210 проверяет его отсутствие: для
  администратора, управляющего и ассистента кнопка была МЁРТВОЙ, getFilteredAppViews не содержит `visit`.
  workspaceShell.tsx:655-666: в строке стояли две кнопки в один раздел, подписанная «Прием» и безымянный
  микрофон, причём микрофон был надмножеством первой и подписи не имел именно он. Способность (курсор в поле
  диктовки) досталась подписанной кнопке — workspaceShell.tsx:676, `onClick={onGoToDictation}`.
Оба требования сняты С ПРИЧИНОЙ и ссылкой на тест-владелец правила; ярлык диктовки теперь проверяется по
поведению (подключение к onGoToDictation), а не по классу удалённой кнопки. Дублировать запреты здесь не
нужно: тест разбирает список кнопок и потому строже подстроки.

ТРЕБОВАНИЕ, ЗАСТАВЛЯВШЕЕ НАРУШАТЬ КОНСТИТУЦИЮ. Требовалось дословно
`aria-label={`Рабочий режим: ${staffRoleLabels[role]}`}` — то есть ЖЁСТКО ВПИСАННЫЙ русский текст в разметке.
В workspaceShell.tsx:567 подпись собирается из `workspaceTopbarLabels.role.region`, а сама строка «Рабочий
режим» лежит в workspaceUiLabels.ts:393. На экране результат идентичен, но текст извлечён из разметки — ровно
то, что предписывает .agents/AGENTS.md, DESIGN ADAPTABILITY MANDATE: «Do not hardcode UI text. Extract
strings to locale files». Проверка разделена на две обязательные половины: СВЯЗЬ в разметке и ТЕКСТ в модуле
подписей. Пропадёт любая — страж упадёт.

smoke:web-render-gating-source: 12 ТРЕБОВАНИЙ ИЗ 15 КРАСНЕЛИ, ДЕФЕКТОМ НЕ БЫЛО НИ ОДНО.
Продуктовое правило стража верное и остаётся: раздел ВЫЧИСЛЯЕТСЯ ТОЛЬКО КОГДА ОТКРЫТ. `{settingsTab === "x" ?
(…) : null}` ветку не строит, `hidden={settingsTab !== "x"}` строит всё всегда и лишь прячет стилем. Все 33
запрета «всегда отрендерено и спрятано» проходят.
Сломаны были сами требования: они склеивали одной строкой ДВЕ независимые вещи — условие показа И разметку
раздела, вплоть до отступа в десять пробелов:
  '{settingsTab === "protocols" ? (\n          <section className="protocol-settings"'
После разбора монолита настроек на вкладки-компоненты эта склейка недостижима в принципе: условие осталось в
SettingsView.tsx, разметка уехала в components/settings/*.tsx. Требование разделено надвое, обе половины
обязательны, ни одна не снята: 10 условий показа + 12 проверок разметки в файлах-владельцах.
Три класса переименованы при редизайне, и это не регресс — раздел на экране есть под другим именем:
  rule-studio      → rules-section-card         (SettingsRulesTab.tsx)
  recognition-lab  → ai-section-card            (SettingsAiTab.tsx)
  pricelist-studio → pricelist-studio-container  (SettingsPricesTab.tsx, теперь <div>, а не <section>)
Старые имена названы в комментарии, чтобы связь находилась поиском.
Список разделов рабочей сетки перестал закрепляться дословно: требовался
`["schedule","patients","visit","documents","finance","communications"]`, а в App.tsx:3890 появился ещё
"analytics". Добавление раздела — новая функция, а не регресс. Закреплено то, что важно: сетка строится по
условию от currentView.
Набор состояния инструментов КТ: требовался раздел `<section className="dicom-toolstate-result">`, которого в
разметке нет (класс остался только в main.css). Состояние живо и доезжает до экрана —
SourcesDicomCapability.tsx:435-438 передаёт `toolStateBundle={… ?? typedDicomViewerToolStateBundle}` в
CtPlanningToolsPanel. Закреплена эта передача.

ТРЕБОВАНИЕ, СНЯТОЕ И ОБРАЩЁННОЕ В ЗАПРЕТ — ХРАПОВИК К УДАЛЕНИЮ fe0b5081f.
Снято: '{["documents","finance","communications","settings"].includes(currentView) ? (' и
'<details className="compliance-bar" aria-label="Контроль">'. Полоса «Служебные ограничения» удалена
28.07.2026 СОЗНАТЕЛЬНО: она показывала администратору клиники наши внутренние заметки. Живой ответ
/api/dashboard кладёт в complianceWarnings три строки, и третья — «Для продажи клиникам нужен отдельный
EGISZ-адаптер и юридическая проверка шаблонов», то есть заметка о продаже продукта на рабочем экране
пользователя. Замер (высота раздела финансов 1773 → 1693) в том коммите, объяснение — в App.tsx:4406-4412.
Требование, которое велит вернуть удалённое, — не защита, а ловушка: следующий агент «починил» бы стража,
восстановив утечку. Поэтому требование не просто удалено, а ОБРАЩЕНО: `className="compliance-bar"` добавлен в
forbiddenSnippets. Теперь страж бьёт в обратную сторону.

Идиом «требование может быть образцом, а не дословной строкой» взят у ведущего из
smoke-finance-ledger-source.mjs (bc0c1e7db): закрепляем СВЯЗЬ, а не написание вокруг неё.

Кодировка обоих файлов проверена round-trip из .agents/AGENTS.md, пункт 5: валидный UTF-8, без BOM, без
U+FFFD, без cp1252-моджибаки.
