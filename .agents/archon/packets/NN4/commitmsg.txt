[ARCHON] fix(стражи): связь и финансы краснели за улучшение кода — дубль правила, общий пустой блок, починка веб-сокетов

ОБА ТЕПЕРЬ ЗЕЛЁНЫЕ, код выхода 1 → 0 у каждого, снято без конвейера.
smoke:communications-view-source и smoke:finance-view-source. Шесть непрошедших требований на двоих,
настоящим дефектом продукта — НИ ОДНО. Это класс BRITTLE из записки ведущего: код улучшили, дословная строка
за ним не пошла.

smoke:finance-view-source — ДВА ТРЕБОВАНИЯ, ОБА НАКАЗЫВАЛИ ЗА ЗАЩИТУ.
1. Требовалось `evaluations={clinicalRuleEvaluations}`, в FinanceView.tsx:278 стоит
   `evaluations={clinicalRuleEvaluations ?? []}` — запас на отсутствие списка правил. Это дословно тот пример,
   что описан в записке ведущего. Закреплена СВЯЗЬ: /evaluations=\{clinicalRuleEvaluations\b/ — источник
   пропса по-прежнему точен, обёртка вокруг него свободна.
2. Требовалось `"/api": apiProxyTarget`, а в vite.config.ts:158 стоит
   `"/api": { target: apiProxyTarget, changeOrigin: true, ws: true }`. Причина расширения записана рядом
   (:155-157): при сокращённой форме веб-сокет шёл обычным HTTP, рукопожатие с /api/ws/schedule висело до
   таймаута, и компоненты, собирающие адрес от window.location.host (например FamilyWalletPanel), не получали
   обновлений вовсе. То есть дословное требование велело ОТКАТИТЬ починку веб-сокетов. Закреплено то, что оно
   защищало на самом деле: адрес прокси /api берётся из настраиваемого apiProxyTarget, а не вписан числом.
   Обе формы записи допускаются, литеральный адрес — нет.

smoke:communications-view-source — ЧЕТЫРЕ ТРЕБОВАНИЯ, ДВА ИЗ НИХ ТРЕБОВАЛИ ВЕРНУТЬ ДУБЛИРОВАНИЕ.
1. `async function completeCommunicationTask(taskId: string, outcome: CommunicationTaskOutcome)` — подпись на
   месте, useAppLogic.tsx:12988, просто разнесена Biome на четыре строки.
2. `<div className="panel communications-panel" id="communications">` — у панели появился
   `data-testid="communications-view"` (CommunicationsView.tsx:302), и закрывающая скобка в требовании
   запрещала любые новые атрибуты. Добавленный тестовый крючок — улучшение; из требования убрана скобка.
3. `function ruCount` — ТРЕБОВАНИЕ ОБРАЩЕНО. Своя функция согласования числа убрана НАМЕРЕННО, и причина
   записана прямо на её месте (CommunicationsView.tsx:40-46): правило склонения в проекте одно, владелец у
   него один — `countLabel` из lib/russianPlural.ts, — а вторая копия того же правила через полгода даёт два
   разных ответа на один вопрос. Требовать `function ruCount` значит требовать вернуть дубль правила, что
   прямо противоречит .agents/AGENTS.md (MONOLITH PREVENTION). Теперь проверяется обратное: экран обязан
   согласовывать числа общим владельцем — `countLabel(` плюс импорт из lib/russianPlural.
4. `className="communication-empty-state"` — своя разметка пустого состояния заменена общим компонентом
   `<EmptyState>` (CommunicationsView.tsx:5 и :470). Требования к СОДЕРЖАНИЮ пустого состояния не менялись и
   проходят рядом: «Очередь связи пуста», «Открыть расписание», действие на onGoToSchedule. Проверяется
   использование общего компонента.

Здесь тоже нормализованы переводы строк (`\r\n` → `\n`) — разбор системного дефекта в предыдущем коммите
пакета: `core.autocrlf=true`, main.css целиком CRLF, App.tsx целиком LF, и любое требование с литералом `\n`
проверяло не содержимое файла, а способ его выгрузки на этой машине.

ДОКАЗАНО, ЧТО ОСЛАБЛЕННЫЕ ТРЕБОВАНИЯ ЕЩЁ КУСАЮТ — 13 ИЗ 13, по методу из записки ведущего: исходник читается
в память, `split(from).join(to)` имитирует регресс, тот же сопоставитель прогоняется по сломанной копии.
Каждое из тринадцати проходит на живом исходнике и ПАДАЕТ на сломанном. Проверенные регрессы: тенантная
проверка чтения состояний зубов подменена на no-op; замена позиций плана лечения уведена с транзакции на db;
из send-due выброшены параметры области; область клиники подменена жёстко вписанным «default-org»; рабочая
сетка возвращена к «отрендерено всегда и спрятано»; состояние инструментов КТ перестало доезжать до
просмотрщика; мобильное сворачивание подсказок меню отключено; у кнопок роли отобрано имя для программы
чтения с экрана; ярлык диктовки отвязан от подписанной кнопки; из подписи закрытия задачи связи убран
типизированный исход; общий владелец склонения заменён локальной копией; правила клиники подменены
глобальными; настраиваемый адрес прокси заменён литеральным.

Кодировка обоих файлов проверена round-trip из .agents/AGENTS.md, пункт 5: валидный UTF-8, без BOM, без
U+FFFD, без cp1252-моджибаки.

ИТОГ ПО ПАКЕТУ NN4, шесть стражей: пять зелёных, один красный ЗАКОННО. Красный —
smoke:dental-persistence-routes-source: 22 проверки исполняются впервые, 21 проходит, падает одна, и это
настоящий дефект продукта (одонтограмма пациента рассылается всем сокетам клиники через
broadcastToOrganization вместо существующего и протестированного broadcastToPatient). Продукт в этом пакете не
правился — это чужой файл и отдельная задача.
