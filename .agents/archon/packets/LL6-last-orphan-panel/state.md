НАЧАТО: LL6-last-orphan-panel — читаю ComparativePlannerDashboard целиком, иду по нитке producer→consumer (OdontogramModule:631 → pendingPlanSuggestions), решаю A/B/C по замеру.
ГОТОВО: выбран исход B (удаление). Замеры на HEAD: серверная схема не знает поля status (odontogram.ts:103-108, name по умолчанию, :430) — кнопки статуса переименовывали план; статус в базе строчный (schema.ts:1409) против «Draft» на экране; доля ДМС по contractsArray[0], тогда как смонтированный TreatmentEstimator берёт polис самого пациента (TreatmentEstimator.tsx:160-162); мост «диагноз→смета» уже работает через currentTeeth+reconcileAutoSuggestions, очередь в сторе была записью без читателя. Удалены экран, его CSS, писатель в OdontogramModule и поля стора; страж дополнен тестом на возврат. node --import tsx --test apps/web/src/tests/panelsAreMounted.test.ts → 10/10, EXIT=0.
