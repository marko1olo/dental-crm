[ARCHON] docs(прайс): отчёт пакета BB1 — где нейро-разбор терял копейки и название

Первый экземпляр агента закоммитил код 2a914a78d и умер, не оставив handoff: разбор,
границы достижимости и честный предел доказательства существовали только в его контексте.
Второй экземпляр перепроверил всё своими командами, а не по записям предшественника, и
записал результат на диск.

Две записи предшественника исправлены здесь же:

- Правка в packages/shared уже НЕ инертна. Утверждение «не действует до npm run build
  -w @dental/shared» было верно в момент коммита и неверно сейчас: dist/migration.js
  пересобран и содержит migrationMoneyTotalRubSchema с z.lazy. Денежная схема сверки
  переноса проверена в исполнении на собранном контракте: 1500,505 и 0,001 отвергаются,
  1500,50 принимается.
- Достижимость нейро-ветки уточнена по номерам строк: в брифе стояли :858 и :863,
  фактически :857 и :862.

Отдельно зафиксировано то, что решено НЕ править, с причиной:
analyzer.ts:393 — второй, избыточный округлитель денег из цикла 11. Доказуемо no-op,
потому что строка по построению имеет ровно два знака после точки; проба по всей рабочей
области значений не изменила ни одного из 25 300 значений. Поведенческой выгоды от правки
нет, риск для зелёного набора детерминированной ветки есть.

Честный предел записан командами, а не словами: сквозной HTTP-путь нейро-режима не
проверен (ключа провайдера нет, платный вызов запрещён), а детерминированный HTTP-путь не
проверен потому, что подписанный по документированному рецепту токен проверяется локально,
но живой сервер отвечает 401 — у процесса в окружении другой секрет.

Co-Authored-By: Claude <noreply@anthropic.com>
