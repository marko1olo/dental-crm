START HH2-middle-risk-tier: brief may be stale — ShiftView.tsx already compares `riskLevel === "watch"` and PatientCockpitProps is typed (commit 20608de77), guard test exists (04c507de1, 9bf3bff87). Verifying by running the test, then closing the guard's known blind spots (`==`, `!==`, `case "medium":`) if the baseline is green.

DONE HH2-middle-risk-tier: brief WAS stale — all three items already landed, baseline 6/6 green before I touched anything. Real gap was the guard recognising only `===` (5 of 6 spellings of the same defect slipped through, probed); widened it plus a self-check, committed aabad8225, 7/7 green, injection of `riskLevel !== "medium"` proven to turn it red then reverted clean. NOT FIXED: `ShiftView`'s own props are still `any` (ShiftView.tsx:78) — same defect class, but typing them is unverifiable without the typecheck gate I am forbidden to run.
