[ARCHON] fix(стражи): страж очереди речи умирал на строке 30 и показывал одно требование из 39

smoke:speech-queue-source был красным, но не потому, что нашёл дефект: он падал на РАЗБОРЕ ПЕРВОГО
БЛОКА, до единой содержательной проверки, и печатал одно сообщение — «Missing marker: function
applyUiPreferences». Сообщение врало. `applyUiPreferences` никуда не делась, она жива в
useAppLogic.tsx. Сломан был порядок склейки: логика приёма переехала в
hooks/domains/useVisitLogic.ts, который приклеивается ПОСЛЕ useAppLogic.tsx, поэтому конец блока
оказался РАНЬШЕ его начала:

  function applyUiPreferences               смещение 331901  (useAppLogic.tsx)
  async function flushPendingSpeechChunks   смещение 753137  (useVisitLogic.ts)

`indexOf(endMarker, start)` вернул -1, и страж выдал «маркера нет» вместо «границы разъехались».
Диагностика, которая называет живой код мёртвым, стоила этой волне стража целиком.

ЧТО ЗАМЕРЕНО ПО КАЖДОМУ ИЗ 39 ТРЕБОВАНИЙ. Ни одно не принято на веру: инструментированная копия
стража вне репозитория собрала полный список падений, затем каждый маркер проверен по живому
источнику, по нормализации (пробелы + висячие запятые) и обходом ВСЕХ 469 файлов .ts/.tsx/.css в
apps/web/src.

  BROKEN   1  граница блока сброса очереди (описано выше)
  BRITTLE 11  требование отличается от кода только форматированием, `?.` или переименованием
  REAL    25  поведения в продукте нет ни в одном из 469 файлов
  плюс     1  ВЫРОЖДЕННОЕ утверждение, которое не могло сработать ни на чём

BRITTLE — страж наказывал за то, что код СТАЛ БЕЗОПАСНЕЕ и что по нему прошёлся форматтер:

  страж требовал                                  в коде стало
  dashboard.activeVisit.id                        dashboard?.activeVisit?.id
  params.set("visitId", dashboard.activeVisit.id) то же с `?.`
  fetch(`/api/speech/...`                         перенос строки после fetch(
  ...MatchesActiveVisit(result: X): boolean       перенос строки + висячая запятая
  === dashboard.activeVisit.id                    === activeVisitId (локальная из стора)
  if (…) flushedRecordingIds.add(…)               то же, перенос строки
  queue.some((item) => Boolean(…))                то же, перенос + запятая
  providerId === "…" || providerId === "…"        то же, перенос строки
  pendingSpeechFlushActionLabel = … ? … : …       то же, перенос строки

Отдельно: предикат принадлежности приёму СТАЛ СТРОЖЕ — при неизвестном visitId было `return true`
(применить фрагмент), стало `return false` (не применять). Страж требовал вернуть fail-open.

ПОЧИНКА В КОРНЕ, А НЕ ПОДГОНКА ПОД СЕГОДНЯШНЕЕ НАПИСАНИЕ. Подставить `activeVisitId` и текущие
переносы значило бы снова прибить стража к именам переменных и к работе форматтера — тот же красный
при следующем переименовании. Требование теперь может быть RegExp: закрепляется СВЯЗЬ (какой
идентификатор из какого источника, что с чем сравнивается, от чего зависит подпись действия), и НЕ
закрепляется защитная обёртка вокруг связи. Полярность ужесточённого возврата намеренно не
прибивается — закрепляется, что ветка на неизвестный идентификатор вообще существует. Дословная
строка по-прежнему допустима там, где важен именно текст.

ВЫРОЖДЕННОЕ УТВЕРЖДЕНИЕ НАЙДЕНО И ЗАМЕНЕНО РАБОТАЮЩИМ. Запрет безусловной сборки очереди искал
подстроку с ВОСЕМЬЮ ПРОБЕЛАМИ отступа, а файл отбит табами: такой строки не бывает ни в исправленном
коде, ни в сломанном. Утверждение проходило всегда — оно было не «безопасно зелёным», а мёртвым.
Замерено на возврате самого дефекта (лишний безусловный `flushedRecordingIds.add`): старый запрет
даёт false и НЕ срабатывает, новый — считает добавления и краснеет («добавлений 2, под проверкой
принадлежности 1»).

REAL ОСТАЁТСЯ КРАСНЫМ ЧЕСТНО, НО НЕ СТЁРТЫМ. 25 требований описывают снесённый слой: автосброс
локальной очереди аудио, свежесть статуса шлюза на момент отправки, читаемая причина отказа
фрагмента. Их нет ни в одном файле apps/web/src. Продукт этой правкой НЕ ТРОГАЕТСЯ: требования
остаются в файле дословно и перечислены в реестре объявленного долга с ХРАПОВИКОМ В ОБЕ СТОРОНЫ —
реестр падает и когда долг закрыт (требование снова выполняется, а запись осталась), и когда запись
осталась без утверждения. Молча снятое требование — стёртая функция; здесь оно находится поиском.

Куда исчезло, тоже замерено: `git log -S 'serverVoiceRecordingShouldContinueRef' --all -- apps/web`
даёт 5 коммитов; на 7b465576d (2026-06-29) маркер жив в useAppLogic.tsx (6 вхождений), на af3e2a01c
и 624d7ae65 (2026-07-07, «chore: save today's newly merged PR features and assets») его нет нигде.
Слой снесло слиянием, а не рефакторингом.

ОСЛАБЛЕНИЕ НЕ УБИЛО НИ ОДНУ ПРОВЕРКУ, И ЭТО ИЗМЕРЕНО, А НЕ ОБЕЩАНО. Каждое ослабленное требование
прогнано по ИСКУССТВЕННО СЛОМАННОЙ КОПИИ склеенного источника (продукт не тронут, git status по
apps/web чист от моих правок). Мутации бьют в суть, а не в синтаксис — возвращают ровно тот дефект,
который требование запирает: вердикт «всегда совпадает», сравнение с patientId вместо visitId,
безусловная сборка очереди, `some` → `every`, потеря vosk_local, подпись действия без зависимости от
готовности, снятая ветка неизвестного visitId, молчаливый отказ без сообщения врачу.

  кусают 14 из 14, база на живом источнике зелёная, ни одна мутация не «не применилась»
  храповик долга при закрытом долге: EXIT=1 («Долг закрыт по 13 требованиям»)

Каждая мутация краснеет с ПРАВИЛЬНЫМ сообщением, а не любым: проверено поштучно.

Страж зелёный, истинный код выхода 0 без конвейера, и утверждения наконец видны: 21 живой лок,
25 записей объявленного долга. Из них главный — тот, ради которого страж и писался: расшифровка
диктовки одного приёма не попадает в карту другого пациента. До этой правки он не защищал ничего.

Зелёный проверен и на рабочей копии, и на HEAD (в дереве лежат незакоммиченные правки других
агентов в App.tsx/useAppLogic.tsx/AppHelpers.tsx — не мои): одинаково EXIT=0, 21 лок, 25 долга.
Правка не опирается на чужую незакоммиченную работу.
