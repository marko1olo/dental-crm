[ARCHON] Источники: 13 мёртвых имён в SourcesDicomCapability, включая тот же фантомный `integrationPresets`

Третий файл каталога `settings/sources`. Тот же механизм, что в двух предыдущих
коммитах пакета, тот же фантомный пропс.

ЧЕМ ИЗМЕРЕНО:
  npx tsc -p apps/web/tsconfig.json --noEmit --noUnusedLocals

БЫЛО в SourcesDicomCapability.tsx — 13 мёртвых имён: 11 TS6133 + 2 TS6196.
СТАЛО — 0 ошибок любого рода.

Сняты из цепочки целиком: `imagingConnectorCards`, `imagingSourceLabels`,
`downloadDicomViewerToolStateBundle`, `applyIntegrationPreset`,
`dicomIntegrationProfileLabels`, `integrationCapabilityLabels`,
`integrationStatusLabels`, `integrationCategoryLabels`, `integrationPresets`,
локали `typedImagingConnectorCards` и `typedIntegrationPresets`, импорт `ImageIcon`,
импорт `humanizeIntegrationInput`, псевдонимы типов `MprClinicalPreset` и
`StringTokenGroup`. `imagingConnectorCards` и `integrationPresets` компилятор мёртвыми
НЕ считал: их читали ровно те две локали `typed*`, которые сами были мёртвыми — то есть
имя выглядело живым за счёт мёртвого потребителя. Сняты вместе с ним.

`integrationPresets` здесь — то же несуществующее поле, что и в двух предыдущих
коммитах: его нет ни в возврате `useAppLogic` (return на строке 13771), ни в возврате
`useSettingsDerivations` (return на строке 2254). Настоящий путь —
`dashboard.clinicSettings.integrationPresets`. Итого фантом читался в ТРЁХ файлах
каталога; в двух он был мёртв, в третьем (панель пресетов) он рендерил пустую сетку.

УКУС, ПОДТВЕРЖДАЮЩИЙ, ЧТО `as any` НЕ ГЕЙТ. Копия дореформенного файла с чтением
заведомо отсутствующего поля `integrationPresetsMissingField` вместо `integrationPresets`:
компилятор НЕ дал на это ни одной ошибки. В ту же копию положена канарейка
`const biteCanary: number = "строка"` — TS2322 на строке 46 получен, значит копия была
в программе, а не пропущена. Код выхода без канарейки этого различить не даёт. Копия
удалена, `git status` по каталогу чист.

Механизм `as any` в этом файле НЕ убран: компонент читает из мешка 130+ имён, и
разделение его на панели — отдельная работа, не этот пакет. В файле оставлен
комментарий с измеренной причиной: `ReturnType<typeof useAppLogicContext>` и
`ReturnType<typeof useSettingsDerivations>` равны `any` (проверено предикатом
`0 extends 1 & T`), а `noImplicitAny: false` в tsconfig.base это скрывает.

ЗАМЕРЫ:
  - npx tsc -p apps/web/tsconfig.json --noEmit ......................... код выхода 0
  - TS6133 по всему apps/web .......................................... 3679
      было на входе пакета 3752, после первых двух коммитов 3690, −73 суммарно
  - ошибок любого рода во всех трёх файлах settings/sources ............ 0

Долг apps/web в 3679 TS6133 — не мой и НЕ закрыт. Назван, чтобы следующий агент не
прочитал каталог `settings/sources` как признак чистого apps/web.
