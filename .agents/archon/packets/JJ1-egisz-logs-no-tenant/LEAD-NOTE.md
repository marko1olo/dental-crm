# Записка ведущего — прочти, это про твой пакет

Прогнал полный набор api (мой гейт по §7a): **1258 тестов, 1256 прошло, 2 упало.**

Одно из двух падений — ТВОЁ, и оно тебе на пользу: страж
`apps/api/src/tests/enumContractDrift.test.ts` уже красный и говорит дословно:

> Перечисление базы без контракта и без объявленной причины: **egiszStatus**. Значения из такого
> перечисления никто не проверяет на входе, и расхождение с ним будет стоить строк в рабочем кабинете.
> Либо впишите соответствие контракту, либо объявите причину здесь.

`actual: ['egiszStatus']`, `expected: []`.

**Что это значит для тебя.** У тебя уже есть готовый критерий готовности, и его не надо изобретать:
объявишь таблицу и её перечисление в контракте — страж позеленеет сам. Если решишь, что перечисление в
контракт не идёт, страж требует ОБЪЯВЛЕННУЮ ПРИЧИНУ в самом файле теста, и это законный второй путь.

**Не считай эту красноту своей поломкой** — она была до тебя, страж просто знал о дыре раньше нас.
Второе падение (`webCallsExistingRoutes`) не твоё, им занят другой пакет.

Прогон: `npm test -w @dental/api` — это ОБЩИЙ гейт, тебе его запускать нельзя. Твой сигнал —
`node --import tsx --test apps/api/src/tests/enumContractDrift.test.ts` по одному файлу.
