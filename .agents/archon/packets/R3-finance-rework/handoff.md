# R3-finance-rework — сдача

HEAD: b4292f74d956e902a7d3802c6b4fc941817ef6d3 (на момент написания; поверх моих коммитов
уже лёг чужой `b4292f74d` про панораму)
Мои коммиты: `e90e1b2762d58a347b4fbfe9b13fc6558f6c3e6d` — починка,
`6d97e0e7d` — замок, третий коммит — документы пакета и поправки к записям C6.
HEAD на старте пакета: `d9c90d6852a5c17e7ce8c8f7af300940787e8673` (все заявленные файлы чистые,
индекс пуст — столкновения нет).

Переделка пакета `C6-finance-phantom-amount` по разбору
`.agents/archon/packets/C6-finance-phantom-amount/review.md`. Прежние коммиты
(`8f9243bdd`, `a4907fe62`) не откатывались и не переписывались.

---

## Что было сломано (file:line)

### Дефект переделки — сброс формы срабатывал на монтировании, а не только на смене пациента

`apps/web/src/hooks/domains/usePatientLogic.ts:220-222` (на `d9c90d685`):

```ts
useEffect(() => {
    resetPaymentComposer(useDocumentStore.getState());
}, [documentPatient?.id]);
```

Защиты от первого прогона нет, а `useEffect` на монтировании выполняется всегда,
независимо от значений в массиве зависимостей. Форма приёма оплаты лежит в общем
хранилище `documentStore`, поэтому любой новый экземпляр эффекта гасил её целиком —
включая сумму и фискальный блок, которые предыдущая починка как раз и добавила в сброс.

**Контекст создаётся НЕ один раз за сеанс.** Проверено
`rg -n "useAppLogic\(\)" apps/web/src`:

```
apps/web/src/App.tsx:956                          const appLogicValue = useAppLogic();
apps/web/src/components/useVisitDiaryLogic.ts:27  const { activeDoctor } = useAppLogic();
apps/web/src/useAppLogic.tsx:933                  export function useAppLogic(): any {
```

Живой путь второго экземпляра, каждая ссылка прочитана на `d9c90d685`:
`App.tsx:3745 <VisitView>` → `VisitView.tsx:353-355 <VisitOdontogramTab>` (только при
`visitSubViewTab === "odontogram"`, кнопка вкладки `:313-322`) →
`VisitOdontogramTab.tsx:68-73 <VisitDiaryEditor>` (при `activeAppointment?.id`) →
`VisitDiaryEditor.tsx:103 useVisitDiaryLogic(...)` → `useVisitDiaryLogic.ts:27 useAppLogic()`
→ `useAppLogic.tsx:2383 usePatientLogic({...})` → эффект сброса.

Кассир набирает сумму и переписывает ФН/ФД/ФПД с чека, уходит на «Приём», открывает
вкладку «Зубная формула и Дневник», возвращается на «Оплаты» — поля пустые. Пациента при
этом никто не переключал.

Номер строки в разборе (`219-221`) сдвинут на единицу: на `219` стоит закрытие
комментария, эффект был на `220-222`.

---

## Что изменено

1. `apps/web/src/components/finance/paymentComposerReset.ts` (+66) —
   `PAYMENT_COMPOSER_PATIENT_UNTRACKED` (отдельный `unique symbol`),
   `TrackedComposerPatientId`, `TrackedComposerPatientRef` и
   `resetPaymentComposerOnPatientChange(ref, patientId, setters): boolean`.
   Первый прогон экземпляра запоминает пациента и возвращает `false` — форма не тронута.
   Настоящая смена (в том числе переход в `undefined`, то есть снятие выбора) гасит форму
   и возвращает `true`.
   Признак первого прогона — символ, а не `undefined`: «пациент не выбран» это настоящее
   состояние формы, и если завести ссылку `undefined`, монтирование при выбранном пациенте
   снова окажется сменой пациента. Проверено исполнением, см. ПРОВЕРЕНО.
2. `apps/web/src/hooks/domains/usePatientLogic.ts` (+25 −2) — ссылка
   `paymentComposerPatientIdRef` (`:233-235`) и вызов защиты в эффекте (`:237-243`).
   Массив зависимостей не менялся. Блок возврата контекста не тронут.
3. `apps/web/src/tests/paymentComposerReset.test.ts` (+148 −19) — помощник
   `applyPatientSwitches` больше не пересказывает логику эффекта, а ИСПОЛНЯЕТ боевую
   `resetPaymentComposerOnPatientChange` и считает сбросы по её ответу. Новые случаи:
   монтирование при том же пациенте, монтирование без выбранного пациента, второй экземпляр
   контекста при заполненной форме, настоящая смена после монтирования (обход всех полей
   хранилища). Замок проводки стал терпимым к переносам строк, замок сброса после платежа
   требует переданное значение, добавлен замок на то, чем заведена ссылка.
4. `.agents/archon/packets/C6-finance-phantom-amount/handoff.md` и `state.md` — поправка
   неверного утверждения о досягаемости (пункт 2 разбора), уточнение формулировки про
   `persist` (пункт 5) и поправки к номерам строк.

---

## ПРОВЕРЕНО

**UNIT VERIFIED** — `node --import tsx --test apps/web/src/tests/paymentComposerReset.test.ts`,
EXIT=0:

```
  ✔ перезагрузка сводки при том же пациенте сбросов не даёт вовсе (0.2181ms)
  ✔ сумма без выбранного пациента переживает монтирование (0.1298ms)
  ✔ вкладка «Зубная формула» заводит второй экземпляр контекста и сумму не стирает (0.1598ms)
  ✔ после монтирования второй экземпляр гасит форму на настоящей смене (0.1063ms)
  ✔ на смене пациента не остаётся ни одного поля предыдущего (0.7619ms)
ℹ tests 17
ℹ suites 4
ℹ pass 17
ℹ fail 0
```

Случай «монтирование при том же пациенте» даёт **0 сбросов** (до переделки ожидался 1), и
набранные `12000,50` вместе с ФН `9960440301234567` остаются на месте. Случай настоящей
смены даёт ровно **1 сброс**, после которого обход всех полей подставного хранилища не
находит ни одного значения предыдущего пациента (родство — значение по умолчанию).

**UNIT VERIFIED (весь веб)** — `npm test -w @dental/web`, EXIT=0:
`tests 411 / suites 73 / pass 411 / fail 0`. До переделки разбор получал 406/406; пять
новых проверок — мои, чужих падений нет.

**TYPECHECK VERIFIED** — `npm run typecheck -w @dental/web` → `tsc -b --noEmit`, EXIT=0.
Доказывает только то, что сборка не сломана: пропсы `usePatientLogic` типизированы `any`,
поэтому компилятор в этом месте слеп.

**Защита исполнена, а не пересказана** —
`node --import tsx -e "import('./apps/web/src/components/finance/paymentComposerReset.ts')..."`
(read-only, боевой модуль, 15 подставных сеттеров считают вызовы):

```
SENTINEL seed, mount id=pat-a   -> reset: false | setter calls so far: 0
SENTINEL seed, switch to pat-b  -> reset: true | setter calls so far: 15
SENTINEL seed, same id pat-b    -> reset: false | setter calls so far: 0
UNDEFINED seed, mount id=pat-a  -> reset: true | setter calls so far: 15
```

Последняя строка — доказательство того, что символ-признак несёт нагрузку: заведи ссылку
`undefined`, и монтирование снова вызывает все 15 сеттеров, то есть стирает форму.

**Замки не пустые** — новые выражения проверены на исходнике до переделки
(`git show d9c90d685:apps/web/src/hooks/domains/usePatientLogic.ts | node -e ...`):

```
PRE-REWORK source bytes: 17266
PRE-REWORK wiring guard matches: false
PRE-REWORK seed guard matches: false
```

**Терпимость к форматированию (пункт 3 разбора) — проверена, а не заявлена**
(`node -e`, read-only):

```
HEAD source, new wiring guard matches: true
REFORMATTED (dep array on its own line), new wiring guard matches: true
C6 literal regex on the SAME code reformatted: false
```

**DB VERIFIED** — тип денег живьём, `information_schema.columns` на `127.0.0.1:5432`
(строка подключения взята из `.env`, в вывод не попадала):

```
rows: 2
cash_ledger.amount_rub = numeric(12,2)
payments.amount_rub = numeric(12,2)
postgres server_version: 18.4
```

То есть копейки представимы точно, и сброс суммы пустой строкой (а не нулём и не «0.00»)
остаётся правильным. Утверждение дознания «amountRub is an integer» неверно и на уровне
базы, а не только на уровне поля ввода.

**Кодировка** — 0 строк мождибаке (`[РС][-ÿ]`) и 0 BOM во всех
задетых файлах, включая два файла записей C6 и оба файла сообщений коммитов.

---

## НЕ ПРОВЕРЕНО

- **Поведение в живом браузере.** Ни один сценарий не исполнялся через React: проверки
  исполняют боевую функцию решения, но не сам `useEffect` и не монтирование компонентов.
  Закрывается вручную на `127.0.0.1:5173`: экран «Оплаты», выбрать пациента, набрать
  `12000,50` и ФН/ФД/ФПД, перейти «Приём» → вкладка «Зубная формула и Дневник» → вернуться
  на «Оплаты» — поля обязаны остаться заполненными; затем переключить пациента — обязаны
  очиститься. Съёмка у лида: `npm run shots:redesign`.
- **UI VERIFIED** мне не принадлежит.
- **API/БД по платежу.** Тело запроса `POST /api/billing/payments` я не отправлял:
  изменение целиком во фронтовом состоянии, контракт не менялся. Закрывается:
  `curl -s -X POST http://127.0.0.1:4100/api/billing/payments -H "x-dente-clinic-token: <токен>"
  -H "content-type: application/json" -d '{...}'`.
- **Второй экземпляр God Context как таковой не устранён** — см. Долг 1. Закрывается
  проверкой после переноса `VisitView` под провайдер: `rg -n "useAppLogic\(\)" apps/web/src`
  должен давать только `App.tsx` и объявление.

---

## Пункты разбора

| № | Пункт | Итог |
|---|---|---|
| I.1 | Прекратить стирание суммы на монтировании; переказать проверкой «монтирование с тем же id → 0 сбросов» | **CLOSED.** `e90e1b276` + `6d97e0e7d`. Взят вариант «а» (ссылка на предыдущего пациента). |
| I.2 | Поправить раздел досягаемости в `handoff.md` / `state.md` C6: у `useAppLogic()` ДВА места вызова | **CLOSED.** Поправка внесена в оба файла C6 с командой-доказательством и живым путём монтирования. |
| J.3 | Сделать выражение `tests/paymentComposerReset.test.ts:218` терпимым к пробелам | **CLOSED.** Терпимость проверена исполнением на переформатированном исходнике (см. ПРОВЕРЕНО). |
| J.4 | Замок сброса после платежа должен требовать ЗНАЧЕНИЕ, а не имя сеттера | **CLOSED.** Ожидаемое значение берётся из `emptyPaymentComposerFields()`; `setPaymentAmount(paymentAmount)` теперь не проходит. |
| J.5 | Смягчить формулировку «в documentStore нет persist» | **CLOSED.** Формулировка в `handoff.md` C6 переписана: `documentStore.ts:44` вызывает `loadUiPreferences()` (она читает `localStorage`, `AppHelpers.tsx:4022-4031`) и подставляет `paymentMethod` на `:2127`; денег и фискальных полей там нет. |
| Вариант «б» пункта I.1 | Перевести `useVisitDiaryLogic.ts:27` на `useAppLogicContext()` | **DISPUTED, с доказательством.** Единственный `AppLogicProvider` в `App.tsx` открыт на `:4166` и закрыт на `:4703`, внутри ветки `currentView === "settings"` (`:4164`). `<VisitView>` отрисован на `:3745`, вне провайдера. `useAppLogicContext()` вне провайдера возвращает `{}` (`contexts/AppLogicContext.tsx:18-23`), а не бросает исключение, поэтому `activeDoctor` стал бы `undefined` и `useVisitDiaryLogic.ts:128-131` отказывал бы в сохранении дневника («Выберите врача для приема»), а автосохранение молчало бы (`silent = true`). Тот же провайдерный разрыв уже описан в коде дважды: `App.tsx:3682-3683` и `VisitOdontogramTab.tsx:9-10` («fall back to props»). Этот вариант — обмен дефекта на более тяжёлый. |
| F | Сдвиг номеров строк в C6 | **CLOSED.** Исправлено в поправке к `handoff.md` C6: `2383`, `112`, `12662-12675`. Заодно: разбор сам указал `219-221` вместо `220-222`. |
| C (нит) | Сброс после платежа всё ещё перечисляет 14 полей у себя | **DECLARED DEBT**, см. Долг 2. Замок теперь сильнее (значения, а не имена), но объединения не делал: это God Context под тремя параллельными авторами, и порядок с `setPaymentFeedback` там значим. |

---

## Поправки к прошлой сдаче: какие именно фразы были неверны

1. «the God Context is instantiated once at the app root and is mounted for the entire
   session» (поле «reachability» доклада C6) — **неверно**. Два места вызова, см. выше.
   Второе предложение — «Because it lives in an always-mounted hook rather than in a view,
   it fires on every patient switch regardless of which screen the operator is on» — верно
   в части «видит любую смену пациента» (корневой экземпляр действительно живёт весь сеанс)
   и неверно в части «always-mounted»: у второго экземпляра монтирование происходит на
   каждом открытии вкладки дневника.
2. `state.md` C6: «FinanceView.tsx was NOT edited: a reset placed there would fire on every
   lazy remount … That would be a new defect, not a fix.» — механизм назван правильно, а
   вывод неверен: ровно этот дефект и был отправлен, только точка монтирования оказалась
   другой (`useVisitDiaryLogic`). Абзац дополнен поправкой, а не удалён.
3. `handoff.md` C6: «Хранилище без `persist`… ноль совпадений на
   persist/localStorage/createJSONStorage» — про `persist` и `createJSONStorage` верно
   (0 совпадений, проверено `rg -c`), про `localStorage` неточно: прямых совпадений в
   `documentStore.ts` нет, но `:44` читает его через `loadUiPreferences()`. Вывод
   (перенос живёт в пределах сессии) остаётся верным.

---

## Коммит

- `e90e1b2762d58a347b4fbfe9b13fc6558f6c3e6d`
  `[ARCHON] fix(касса): открытие вкладки дневника стирало набранную сумму и фискальный блок`
  2 файла, +89 −2.
- `6d97e0e7d`
  `[ARCHON] test(касса): замок на стирание суммы при монтировании второго контекста`
  1 файл, +148 −19.
- третий коммит — `.agents/archon/packets/R3-finance-rework/**` и два файла записей C6.

Каждый коммит сделан с явным списком путей. Индекс перед каждым проверялся
(`git diff --cached --name-only`): чужого в нём не было ни разу. `apps/api/dist/**`,
`apps/api/.data/*.json` и `progress.md` в рабочем дереве грязные с самого начала — ничего
из этого я не трогал и не добавлял. Автор коммитов — `marko1olo`: это настроенная в
репозитории личность git.

---

## Долг

1. **Второй полный экземпляр God Context остаётся.** `useVisitDiaryLogic.ts:27` вызывает
   `useAppLogic()` вместо контекста, потому что `<VisitView>` отрисован вне единственного
   `AppLogicProvider`. Это удваивает все эффекты и запросы контекста при открытии вкладки
   дневника. Правильная починка — накрыть провайдером всё приложение (или хотя бы
   `<VisitView>`), а не менять хук: тогда `useAppLogicContext()` станет рабочим и вторую
   копию можно убрать. Отдельный пакет: правка в `App.tsx`, который сейчас пишет другой
   автор.
2. **Сброс после записанного платежа по-прежнему перечисляет 14 полей руками**
   (`useAppLogic.tsx:12662-12675`). Держится замком на значения. Правка на будущее:
   `resetPaymentComposer(useDocumentStore.getState())`, строку состояния выставлять после.
3. **Форма приёма оплаты живёт в глобальном `documentStore`, а не при экране кассы.**
   Именно поэтому она вообще способна переживать и смену пациента, и монтирование чужих
   экранов. Правильное место — состояние, привязанное к пациенту. Большая перестройка.
4. **Пропсы `usePatientLogic` типизированы `any`** (`usePatientLogic.ts:42`). Пока так,
   забытое поле формы typecheck не заметит.
5. **i18n.** Новых видимых пользователю строк не добавлено: изменения — логика сброса,
   комментарии и проверки. Долг i18n не изменился.

---

## Найдено, не починено (тот же класс дефекта, другая полоса)

`usePatientLogic.ts:245-249` и `:251-257` — те же эффекты без защиты от первого прогона,
только пишут они не в форму оплаты, а в черновик карточки пациента общего хранилища
(`patientStore.ts:62`, `create<PatientStore>`):

```ts
useEffect(() => {
    setPatientCoreDraft(patientCoreDraftFromPatient(selectedPatient));
    setPatientCoreSaveState("idle");
    setPatientCoreDirty(false);
}, [selectedPatient?.id, selectedPatient?.updatedAt]);
```

Монтирование второго экземпляра контекста (та же вкладка «Зубная формула и Дневник»)
перезаписывает черновик значениями из сохранённого пациента и сбрасывает признак
изменённости — то есть несохранённая правка ФИО, телефона, ИНН или паспорта пропадает.
Не чинил: это карточка пациента, а не касса, и в мою заявку она не входит. Проверять надо
живьём, отдельным пакетом. Заметно, что тот же приём защиты (`useRef` с признаком первого
прогона) годится и здесь.
