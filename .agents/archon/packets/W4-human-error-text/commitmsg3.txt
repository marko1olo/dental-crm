[ARCHON] fix(стерилизация): журнал объяснял отказ кодом ответа, а раздел уже открыт пользователю

ScannerView.tsx:70 отдавал «Журнал стерилизации не показан: сервер ответил
кодом 500. Список ниже неполный.» на всё, что не 401/403 и не StaffAuthRequired.
Номер ответа сотруднику стерилизационной ничего не говорит.

Раздел живой, а не мёртвый код: workspaceShell.tsx:52 держит "scanner" в
appViews, App.tsx:4788 его отрисовывает. RECON_DOSSIER §3 числит ScannerView
среди пяти недостижимых представлений AppRouter.tsx — это устарело, и то же
касается leads и inventory. Правится дossier, а не код.

Ветка про вход сотрудника по PIN оставлена как была: она различает вход в кабинет
и вход сотрудника, чего общий словарь причин знать не может. Заменён только
провал в конец, и «Список ниже неполный» убрано — та же функция подписывает
отказ записи в журнал, где фразы про список нет смысла.
