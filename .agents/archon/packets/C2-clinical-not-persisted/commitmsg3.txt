[ARCHON] docs(пакет C2): сдача по передаче между этапами, которая не доходила до базы

Разбор дефекта, доказательства и долг. Отдельно зафиксировано, что модуль до этого пакета
был мёртвым: ноль вызывающих в проде, единственная ссылка — его собственный тест.

Скрипт проверки лежит рядом с отчётом, чтобы заявленные 201/200 и чтение строки из
clinical_tasks можно было перепроверить, а не принимать на слово. Запускать только из
apps/api: AUTH_TOKEN_SECRET лежит в apps/api/.env, а loadServerEnv читает cwd/.env первым,
и из корня репозитория токен подпишется другим секретом.
