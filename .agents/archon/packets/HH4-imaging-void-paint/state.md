started: investigating imaging void (paint/compositing, not mount)
done: PRODUCT cause = fade-up `from{opacity:0}` inherited via `.workspace > *`; committed 653977355 (section.imaging-panel animates transform only). Evidence PNGs are 79 min OLDER than the c495c2b43 `both`->`forwards` fix, so they cannot show whether it worked — lead must re-capture.
