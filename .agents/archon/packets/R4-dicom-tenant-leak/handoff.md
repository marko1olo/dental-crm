# HANDOFF — пакет R4-dicom-tenant-leak

HEAD на момент сдачи: `370d2f10f405a3b5839c9994c083a9284481297a`
(`[ARCHON] fix(телеграм): пациент получал фото второй раз...` — чужой коммит, встал поверх моего).
Мой коммит: `1635a606fd47cf4e330b8eae66fa5f2b4394e636`.
HEAD на момент начала работы: `d4029c0325184375242737931451bd1d97e9873e`.

Дефект подтверждён, исправлен, исполнен на живом сервере. Не «должно работать» — проверено ниже.

---

## Что было сломано (file:line до правки, коммит d4029c032)

1. **`apps/api/src/routes/dicomweb.ts:204-207`** — ветка демонстрационного файла внутри
   `resolveInstanceFilePath`. Параметр `organizationId` в ней не упоминался ни разу:

   ```ts
   const samplePath = sampleDicomPath();
   if (await fileCarriesRequestedUids(samplePath, studyUid, seriesUid, instanceUid)) {
     return samplePath;
   }
   ```

   Единственная ветка маршрута без арендного гейта. Пакет C1 закрыл подмену объекта по UID, но не эту.
2. **`apps/api/src/routes/dicomweb.ts:137-143`** — комментарий, честно описывавший границу («у этого
   файла нет владельца в базе, поэтому он отдаётся любой авторизованной клинике»). Комментарий
   проверкой не является: код границу не проверял.
3. **`apps/api/src/security/identity.ts:132-142`** — `requireOrganizationId` возвращает
   `organizationId` прямо из проверенной подписи и в базу не смотрит. **Проверки существования
   организации нет нигде в `apps/api`**: `rg "OrganizationNotFound|organizationExists|
   OrganizationUnknown|UnknownOrganization" apps/api/src` — 0 совпадений до этого пакета. Значит
   слабость «организации нет ни в одной строке, а токен действителен» **не локальна для dicomweb.ts,
   она в общем гейте и касается всех маршрутов**. См. раздел «Долг», пункт 1.
4. **`apps/api/src/routes/dicomweb.test.ts:41-42`** — `DENTE_CLINICAL_ALLOW_UNGUARDED_READS = "1"` и
   `delete process.env.DENTE_CLINICAL_ADMIN_SECRET`, поэтому `apps/api/src/accessGuard.ts:63`
   (`if (clinicalReadsUnguardedAllowed()) return true;`) возвращал `true` безусловно во всех девяти
   тестах. `apps/api/.env` содержит те же флаги. Гейт `requireClinicalReadAccess`, который маршрут
   заявляет на `dicomweb.ts:216`, **не исполнялся ни одним тестом и ни одним запросом**.

## Что изменено

`apps/api/src/routes/dicomweb.ts` (378 строк после правки, было 275):

- `SAMPLE_DICOM_ORGANIZATION_ID_ENV` = `DENTE_DICOM_SAMPLE_ORGANIZATION_ID` + функция
  `sampleDicomOwnerOrganizationId()`. Ветка образца работает только когда организация запроса **равна**
  владельцу. Переменная не задана или не UUID — ветки нет ни для кого. **Отказ по умолчанию.**
- `organizationExists(organizationId)` — запрос к `organizations` по первичному ключу. Вызывается в
  обработчике **до разбора адреса и до любого обращения к диску**. Неизвестный арендатор получает
  `403 OrganizationUnknown`.
- `UUID_SHAPE` — форма UUID проверяется до запроса, потому что `organizations.id` это колонка `uuid`
  (`db/schema.ts:209-210`) и строка другого формата вызвала бы ошибку PostgreSQL 22P02, то есть 500
  вместо честного отказа. Нулевой UUID форму проходит намеренно: существование решает база.
- Недоступная база отделена от отсутствующей организации: `503 OrganizationCheckUnavailable`.
  Подставить `false` («такой клиники нет») при упавшем запросе значило бы выдать выдумку за проверку.
- Ответ чужой клинике — `404 DicomInstanceNotFound`, тот же, что на выдуманный UID: по коду ответа
  нельзя перебором выяснить, что есть у соседней организации.
- Комментарий из пункта 2 удалён и заменён описанием того, что теперь действительно проверяется.

`apps/api/src/routes/dicomweb.test.ts` (491 строка, было 242): 9 тестов -> **17**.

- Заглушка `db.select` переведена с позиционной очереди (`queue[call]`) на выбор строк по таблице из
  `.from()`. Причина не косметическая: маршрут теперь делает до трёх запросов, и тест, привязанный к
  номеру вызова, молча проверял бы не ту ветку.
- Секрет администратора для тестов гейта генерируется на каждый прогон (`randomBytes(24)`). В
  репозитории не остаётся ни одной секретной строки, и сравнение проверяется на настоящем значении.

## ПРОВЕРЕНО

**UNIT VERIFIED** — `cd apps/api && node --import tsx --test src/routes/dicomweb.test.ts`

```
ℹ tests 17
ℹ pass 17
ℹ fail 0
ℹ duration_ms 1058.1876
TEST_EXIT=0
```

Новые тесты, каждый исполнен: вторая клиника не получает образец (404), организации, которой нет в
базе, снимок не выдаётся (403 `OrganizationUnknown`), идентификатор не в формате UUID отклоняется
**без обращения к базе** (`dbCalls.calls === 0`), без назначенного владельца образец не отдаётся
никому (404), недоступная база даёт 503 `OrganizationCheckUnavailable`, гейт клинического чтения без
заголовка — 403 `ClinicalReadSecretRequired` + `protectedArea === "dicom instance read"`, гейт с
неверным заголовком — 403, гейт с верным заголовком — 200 и 121356 байт с `DICM` на смещении 128.
Все девять тестов C1 сохранены и проходят.

**UNIT VERIFIED (отрицательный контроль — тесты не декоративны).** Пре-фиксный маршрут временно
положен в рабочее дерево (`git show <pre-fix>:apps/api/src/routes/dicomweb.ts > ...`), прогон, затем
`git checkout --` для восстановления. Дерево после восстановления чистое, в индекс ничего не попадало.

```
ℹ tests 17
ℹ pass 12
ℹ fail 5
TEST_EXIT=1
✖ вторая клиника установки не получает демонстрационный снимок владельца
✖ организации, которой нет в базе, снимок не выдаётся
✖ идентификатор организации не в формате UUID отклоняется без обращения к базе
✖ без назначенного владельца образец не отдаётся никому
✖ недоступная база не превращается в вывод «такой организации нет»
```

Деталь по главному тесту: `AssertionError: 200 !== 404`, `actual: 200, expected: 404`,
`dicomweb.test.ts:216`. Это ровно проба G ревьюера, воспроизведённая в тесте.

**ЧЕСТНАЯ ОГОВОРКА:** три теста гейта клинического чтения проходят и на пре-фиксном коде — гейт был
на месте с C1, его просто никто не исполнял. Эти три теста закрывают дыру в покрытии, а не в коде.

**API VERIFIED** — живой сервер `127.0.0.1:4100`, токены подписаны мной
(`AUTH_TOKEN_SECRET` из `apps/api/.env`, HMAC-SHA256, формат из `utils/cryptoHelper.ts:43`), запрос —
подлинная тройка UID образца `CompressedSamples^CT2`:

```
health                                         200
A no token                                     401 application/json  bytes=128 error=AuthRequired
B org A (4a3420d1)                             404 application/json  bytes=389 error=DicomInstanceNotFound
G org B (d0000000)                             404 application/json  bytes=389 error=DicomInstanceNotFound
H nonexistent org (nil uuid)                   403 application/json  bytes=141 error=OrganizationUnknown
I malformed org id (not a uuid)                403 application/json  bytes=141 error=OrganizationUnknown
C org A, invented UIDs (1/1/1)                 404 application/json  bytes=260 error=DicomInstanceNotFound
```

- **Проба G ревьюера была `200 application/dicom, 121356` — стало `404`.** Утечка между арендаторами
  закрыта на живом процессе.
- **Проба H ревьюера была `200 application/dicom, 121356` — стало `403 OrganizationUnknown`.**
- Ни один отказ не несёт `application/dicom`. Ни один ответ не равен 121356 байтам.
- Проба C (выдуманные UID) осталась 404 — **проверка идентичности UID из C1 не сломана**.

**Цепочка владения этим доказательством (без неё API VERIFIED ничего не стоит):** процесс на 4100 —
PID 30836, `CreationDate 28.07.2026 03:54:58`, командная строка
`node --require .../tsx/preflight.cjs --import .../tsx/loader.mjs src/server.ts`, **без `--watch`**.
Я его не перезапускал: он был перезапущен не мной уже после моего коммита (03:53:22). Что именно он
исполняет, установлено не по времени, а по поведению: код ошибки `OrganizationUnknown` отсутствует в
пре-фиксном коммите целиком (`git grep -c "OrganizationUnknown" d4029c032 -- apps/` -> 0 совпадений,
exit 1) и существует только в моём (`dicomweb.ts:317`). Живой сервер его возвращает — значит
исполняет мой код.

**DB VERIFIED** — `pg.Client`, `DATABASE_URL` из корневого `.env`, только чтение:

```
organizations:
  4a3420d1-6ffb-4459-bd8f-7f7087f5e191  Стоматология, 1 кабинет
  d0000000-0000-4000-8000-00000000d001  Демо-клиника для снимков
  строк с id = 00000000-0000-0000-0000-000000000000: 0
imaging_studies:
  org=4a3420d1-6ffb-4459-bd8f-7f7087f5e191 uid=null path=null
```

Организаций две, обе настоящие, и та, чей токен ревьюер подписал в пробе H, действительно
отсутствует. Единственная строка `imaging_studies` имеет `dicom_study_uid = null` и
`storage_path = null` — ветки 1 и 2 маршрута живым запросом недостижимы (это же говорил ревьюер, F6).

**TYPECHECK VERIFIED** — `npm run typecheck -w @dental/api`, `TYPECHECK_EXIT=0`, ноль диагностик.

**SUITE** — `npm test -w @dental/api`: `tests 911 / pass 910 / fail 1`. Единственный отказ —
`по умолчанию берётся завтрашний день в поясе клиники` в
`apps/api/src/tests/routes/dayConfirmations.test.ts` (`actual '2026-07-29' !== expected '2026-07-28'`).
Вне моего клейма, к dicomweb отношения не имеет (`rg -c dicomweb` по этому файлу — 0), и
воспроизводится при запуске одного этого файла отдельно (`tests 11 / pass 10 / fail 1`), то есть моим
изменением не вызван. Это ошибка границы суток, не моя правка.

**ENCODING** — `broken=0 bom=false U+FFFD=0` на `dicomweb.ts` (378 строк, 120 с кириллицей),
`dicomweb.test.ts` (491 строка, 82), `commitmsg.txt`, `state.md`. Тема коммита в `git log -1 --format=%s`
читается целиком, мождибаке нет.

**ДОСТИЖИМОСТЬ** — маршрут живой: `apps/api/src/server.ts:43` импортирует `registerDicomwebRoutes`,
`:418` вызывает (номера строк перепроверены сейчас, не взяты из досье). Живые ответы 401/403/404 выше
получены через HTTP, а не выведены из кода. **Клиент в приложении по-прежнему мёртв:**
`apps/web/src/ImagingView.tsx:510` указывает на `http://localhost:3000`, а слушают только 4100 и 5173
(`netstat` перепроверен). Это долг F10 ревьюера, вне моего клейма.

## НЕ ПРОВЕРЕНО

1. **Живая выдача 200 организации-владельцу.** На сервере не задан
   `DENTE_DICOM_SAMPLE_ORGANIZATION_ID`, поэтому ветка образца выключена для всех — именно это и
   означает проба B = 404 выше. Значение читается из окружения в момент запроса, но в уже запущенный
   процесс переменную не подставить. Путь проверен через `app.inject()` (200, 121356 байт, `DICM`),
   живьём — нет. Закрывающая команда (перезапуск шарового сервера — **право ведущего, не моё**):
   `printf '\nDENTE_DICOM_SAMPLE_ORGANIZATION_ID=4a3420d1-6ffb-4459-bd8f-7f7087f5e191\n' >> apps/api/.env && npm run dev -w @dental/api`
   затем `node "C:/Users/Admin/.claude/projects/c--hades/work-memory/r4-dicom-tenant-probe.mjs"` —
   строка `B org A` должна стать `200 application/dicom bytes=121356 magic@128="DICM"`.
2. **Гейт клинического чтения на живом сервере.** `apps/api/.env` держит
   `DENTE_CLINICAL_ALLOW_UNGUARDED_READS=1` при пустом `DENTE_CLINICAL_ADMIN_SECRET`, поэтому гейт
   на 4100 по-прежнему пропускает всех. Три новых теста исполняют оба его исхода через `app.inject()`,
   но живого 403 `ClinicalReadSecretRequired` я не видел. Закрывающая команда (перезапуск — ведущий):
   задать в `apps/api/.env` непустой `DENTE_CLINICAL_ADMIN_SECRET`, снять
   `DENTE_CLINICAL_ALLOW_UNGUARDED_READS`, перезапустить `@dental/api`, затем
   `curl -si -H "x-dente-clinic-token: <подписанный>" "http://127.0.0.1:4100/api/dicomweb/studies/1.3.6.1.4.1.5962.1.2.2.20040826185059.5457/series/1.3.6.1.4.1.5962.1.3.2.1.20040826185059.5457/instances/1.3.6.1.4.1.5962.1.1.2.1.2.20040826185059.5457"`
   — ожидается `403 ClinicalReadSecretRequired`.
3. **Ветки 1 и 2 (`imaging_instances`, `imaging_studies`) против настоящих строк.** Живой базе не
   хватает данных: единственная строка `imaging_studies` имеет `dicom_study_uid = null`. Проверены
   заглушкой. Закрывающая команда: `INSERT` в `imaging_studies/imaging_series/imaging_instances` с
   настоящим файлом, затем проба тем же скриптом — писать в общую базу я не имею права, её читают
   другие агенты и конвейер снимков ведущего.
4. **UI.** `UI VERIFIED` закреплено за ведущим, и цепочка всё равно мертва выше (`ImagingView.tsx:510`).
5. **`madge --circular`.** Не на PATH (известно-неверное утверждение §11), не блокер.

## Коммит

`1635a606fd47cf4e330b8eae66fa5f2b4394e636`
`[ARCHON] fix(снимки): образец DICOM уходил чужой и несуществующей организации`

`git log -1 --stat` на момент коммита — только мои файлы, ни одного чужого:

```
 .agents/archon/packets/R4-dicom-tenant-leak/commitmsg.txt |  51 ++++
 .agents/archon/packets/R4-dicom-tenant-leak/state.md      |  49 ++++
 apps/api/src/routes/dicomweb.test.ts                      | 297 +++++++++-
 apps/api/src/routes/dicomweb.ts                           | 130 ++++-
 4 files changed, 489 insertions(+), 38 deletions(-)
```

Индекс перед коммитом был пуст (`git diff --cached --name-only` — ничего), коммит сделан с явным
списком путей после `--`. Ни `dist/**`, ни `.data/*.json`, ни `tsbuildinfo`, ни чужой незакоммиченной
работы. Соседний непроиндексированный файл `apps/api/src/tests/routes/patientRecall.test.ts` (чужой)
я не трогал и не индексировал.

## Долг

1. **СЛЕДУЮЩИЙ ПАКЕТ, самый важный. Общий гейт не проверяет существование организации.**
   `security/identity.ts:132-142` `requireOrganizationId` отдаёт `organizationId` из подписи, не глядя
   в базу. Я закрыл это **только на маршруте dicomweb**, как предписано брифом, потому что правка
   общего гейта меняет поведение всех маршрутов сразу. Пока она не сделана, **любой другой маршрут
   продолжает принимать подписанный токен с несуществующей организацией как действительный**. Для
   маршрутов с фильтром по `organization_id` это выдаст пустой результат, но маршруты с ветками «нет
   строки — отдай что-нибудь по умолчанию» будут вести себя как эта ветка образца. Правильное место —
   `requireOrganizationId` плюс кэш существования организаций с ограниченным сроком жизни, иначе
   каждый маршрут получит лишний запрос на каждое обращение.
2. **F4 ревьюера (вне моего клейма, не закрыт).** `dicomweb.ts` ветка 1 (сейчас строка 248)
   стримит `instanceRow.storagePath` без проверки байтов и **без ограничения корнем хранилища**.
   `imaging_studies.storagePath` заполняется клиентом (`imaging.ts:6604`). Нужен
   `DENTE_IMAGING_STORAGE_ROOT` и отказ на всё вне него; то же для `imaging.ts:6637-6660`.
3. **F3 ревьюера (вне моего клейма, не закрыт).** `.limit(1)` без `ORDER BY` на неуникальной колонке
   `dicom_study_uid`: два исследования одной клиники с одинаковым UID дадут 404 на объект, который
   физически есть. Нужен уникальный индекс `(organization_id, dicom_study_uid)` или перебор строк.
4. **Стоимость.** Каждый запрос к маршруту теперь стоит один дополнительный запрос к базе (поиск по
   первичному ключу `organizations`). Кэш я сознательно не делал: он вводит окно, в котором удалённая
   организация ещё обслуживается, а выигрыш на фоне чтения 1 МиБ файла и разбора DICOM не измерен.
   Если появится многокадровый просмотрщик — измерять и решать тогда, вместе с пунктом 1.
5. **i18n.** Два новых сообщения об ошибке — русские литералы прямо в файле
   (`OrganizationUnknown`, `OrganizationCheckUnavailable`), как и весь остальной API. Словаря для
   серверных сообщений в проекте нет. **Я добавил к долгу i18n две строки** — это заявлено прямо, а не
   спрятано.
6. **`DENTE_DICOM_SAMPLE_ORGANIZATION_ID` нигде не задан.** Следствие сознательное: отказ по
   умолчанию. Если ведущему нужен работающий локальный просмотрщик образца — переменную надо задать в
   `apps/api/.env` (файл вне моего клейма, я его не менял) значением
   `4a3420d1-6ffb-4459-bd8f-7f7087f5e191`.

## Поправка к клейму пакета (для ведущего)

Клейм называет `apps/api/src/tests/**/dicomweb.test.ts`. **Такого файла нет.** Тесты dicomweb лежат
рядом с маршрутом: `apps/api/src/routes/dicomweb.test.ts` (так их положил C1; `npm test -w @dental/api`
использует глоб `src/**/*.test.ts`, поэтому оба каталога исполняются). Тело пакета говорит «plus the
existing dicomweb.test.ts» — я расширил существующий файл, а не создал второй с тем же именем в
другом каталоге. Больше ничего за пределами клейма не тронуто.
