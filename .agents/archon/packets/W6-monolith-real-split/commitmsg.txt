fix(документы): вынесенная форма заявления не подключалась, а экран рисовал её копию

Файл components/documents/forms/TaxDeductionApplicationForm.tsx не импортировал
никто: `rg` по apps/web/src находил только одноимённый тип из @dental/shared.
Экран рисовал ту же форму копией внутри DocumentsView.tsx, строки 2263-2374, то
есть правка в вынесенном файле не доходила до пациента вовсе, а правка в копии
расходилась с ним молча. Копия удалена, вынесенный файл подключён, его пропсы
получили настоящие типы вместо `any`.

Заодно из того же монолита вынесены шесть форм согласий, которые лежали
сплошным полотном в одном компоненте на 5094 строки: информированное согласие,
процедурное согласие, журнал анестезии, фото и видео, согласие на ПДн и отказ от
вмешательства. Значения и сеттеры формы берут из хранилища документов сами,
сверху приходят только подсказки активного визита — врач, жалоба, зона лечения.
DocumentsView.tsx: 5094 -> 4363 строки.

Общая оболочка карточки (DocumentPayloadCard) собрана без inline-стилей,
которыми были обвешаны все 28 копий. Эти стили ничего не рисовали: в
styles/dente-redesign.css:1262-1287 те же свойства объявлены с `!important`, а
важное авторское объявление сильнее атрибута style. Копии обещали
borderRadius 8px, marginTop 16px, gap 16px и color var(--brand-700), а экран все
это время показывал 10px, 12px, 10px и var(--teal-dark) из CSS. Проверка
равенства свойств зафиксирована тестом: уберут `!important` — тест упадёт.

Оболочка формы отказа оставлена своей: у неё есть наведение на сводке и
фокус-обводка на кнопках-подсказках, которых нет у остальных 27 карточек.
Замена на общую карточку убрала бы их, то есть изменила бы экран.

Поведение не менялось: ни одной новой или убранной возможности, ни одной
переписанной подписи. 25 проверок в src/tests/documentPayloadForms.test.ts
рисуют все семь форм по-настоящему и сверяют текст, справочники и оболочку.

Co-Authored-By: Claude <noreply@anthropic.com>
