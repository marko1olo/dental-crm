docs(archon): [ARCHON] QQ5 — досье по 61 красному smoke-гейту, разделены среда и дефект

ЗАЧЕМ: число «64 красных» передавалось дальше как «64 дефекта». Это неверно втрое, и на
неразделённом числе следующая волна выбрала бы не те пакеты.

ЗАМЕР (тихая машина, PostgreSQL 18 на 5432 поднята, api-сервера нет, по одному гейту,
таймаут 110 с): прогнано 126, зелёных 65, красных 61. Не прогнан 1 —
smoke:schedule-configuration начинается с db:reset-seed, сиды пакету запрещены;
smoke:all — агрегатор, не гейт. Ключей smoke:* в package.json 128, а не 127:
smoke:mobile — дубль smoke:mobile-overflow (одна и та же командная строка).

РАЗДЕЛЕНИЕ 61 КРАСНОГО ПО КЛАССАМ:
  NEEDS_ENV 26 (db-seed 12, browser 5, build 4, api-server 3, db-seed+session 2)
  BROKEN    15 (13 из них — один общий дефект авторизации гейта, см. ниже)
  REAL       8 (из них 3 требуют вернуть слой, снесённый 2026-07-07)
  STALE      6
  BRITTLE    6
  RESURRECTION 0 отдельно, COMMENT 0

ОТВЕТ НА ГЛАВНЫЙ ВОПРОС: только из-за среды краснеют 26 из 61. Остальные 35 никакая
среда не перекрасит. Четыре из 26 снимаются одной командой сборки.

ПРИЧИНА 13 КРАСНЫХ, НАЙДЕННАЯ И ИЗМЕРЕННАЯ: арендатор берётся только из подписанного
токена (accessGuard.ts:141-152), а токен сотрудника принимается лишь с userId-строкой
(security/identity.ts:160). Гейты подписывают {organizationId, role} БЕЗ userId —
smoke-payment-idempotency.mjs:93-95 признаётся в этом комментарием, который заодно
ссылается на PGlite, которой в проекте нет. Все боевые входы (routes/auth.ts:233,522,
579,691) подписывают userId. Средой не лечится: чинить сам гейт.

COMMENT-класса в красных НЕТ, механизм обратный: 521 строка «// Compliance:» в
useSettingsDerivations.tsx — готовый способ покрасить 36 гейтов зелёным без починки,
поэтому стражи её сознательно не читают (smoke-settings-view-source.mjs:34).

ПОБОЧНО, не ловится ни одним гейтом: в apps/api/drizzle/meta/_journal.json 28 тегов и
28 из 28 не имеют .sql на диске (журнал 0000_gifted_masked_marvel против диска
0000_freezing_randall_flagg), при 104 .sql в папке. Связка «.sql + journal + snapshot»
из .agents/AGENTS.md 8b разорвана целиком.

Продукт не изменён: пакет только читает scripts/** и apps/**.
