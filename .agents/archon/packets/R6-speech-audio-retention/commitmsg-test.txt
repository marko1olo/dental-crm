test(диктовка): закрыть границу ожидания задания AssemblyAI и удаления аудио у провайдера

Шесть проверок на подменённом провайдере: ни одного сетевого запроса и ни одного
реального ключа.

Прежний предел: длинное задание доводится до готового текста на 41-м опросе —
при старых 15 попытках фрагмент терялся молча. Истёкший бюджет ожидания
проверяется по строке, которую видит врач: она обязана называть секунды и число
опросов и не имеет права выглядеть как «источник не вернул готовый текст».

Удаление: DELETE /v2/transcript/{id} уходит с ключом источника и на готовом
тексте, и на исчерпанном ожидании. Провал удаления проверяется трижды — обе
попытки выполнены, предупреждение с кодом ответа и числом попыток попало в
предупреждения фрагмента, запись ушла в лог, а расшифровка при этом НЕ
уничтожена. Отдельно закрыт узкий случай без документированного удаления (аудио
загружено, задание не создано) и запрет молча подменять неверный
ASSEMBLYAI_API_BASE_URL публичным хостом.

Co-Authored-By: Claude <noreply@anthropic.com>
