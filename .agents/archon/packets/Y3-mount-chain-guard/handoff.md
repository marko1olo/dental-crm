# Y3-mount-chain-guard — handoff

HEAD: ab33125957d739350fd7166c2f67fd9a3a00d18e
(на входе в пакет было 2cf36a1e7a2decc3323b92ed721a969382eaabdf; мои два коммита —
75db7eb5d508343b57831df41ef95d9c53da94f4 и ab33125957d739350fd7166c2f67fd9a3a00d18e)

## Что было сломано (file:line)

**1. Мёртвая цепочка онбординга — подтверждено.**
`apps/web/src/components/workspace/WorkspaceOnboardingNoticeBars.tsx:5` — единственная
ссылка на весь репозиторий было его собственное объявление. Проверено
`git grep -n "WorkspaceOnboardingNoticeBars" HEAD -- .` → одна строка. Значит
`WorkspaceOnboardingInline.tsx:13` и все восемь `InlineStep*` не монтировались никогда:
10 файлов, 1888 строк. Предыдущий пакет починил выбор роли в `InlineStepRole` и заверил
это сильнейшей меткой достижимости — правка не доходила до людей вообще.

**2. Досье ошибается дважды. Правьте досье, не код.**

*(а) Мёртвая ветка — не 2551, а 2450.*
`App.tsx:2069` = ранний return при `!onboardingDismissed && !isLocalOnboardingDismissed`.
Значит до строки 2450 доходим только при `onboardingDismissed || isLocalOnboardingDismissed`.
Гейт `App.tsx:2450` = `!onboardingDismissed && !showFullOnboardingGuide && !isLocalOnboardingDismissed`
— прямое отрицание того, что необходимо для входа. **Блок 2450-2468 мёртв при любом
состоянии, арифметически.**
А вот 2551 живёт внутри блока `App.tsx:2470`, чей гейт — только `showFullOnboardingGuide`.
`useAppLogic.tsx:13669-13673` определяет его как
`!onboardingDismissed && currentView === "settings" && settingsTab === "clinic" && onboardingGuideExpanded`.
Совместив с условием входа: блок 2470 (и роль-пикер 2543/2551) рендерится **ровно при
`!onboardingDismissed && isLocalOnboardingDismissed`** — то есть в прогоне скриншот-стенда
или у человека со устаревшим ключом. Не «недостижим», а «достижим только не пользователем».

*(б) «Настоящий ключ» назван неверно.*
Досье пишет, что настоящий ключ — `dental-crm:web-ui-preferences:v1`
(`AppHelpers.tsx:687`). Это ключ блоба настроек интерфейса вообще. Канонический ключ
именно признания онбординга — `onboardingStorageKey = "dental-crm:onboarding:v1"`
(`AppHelpers.tsx:692`), и пишется он **с областью организации**:
`onboardingLocalKey()` (`AppHelpers.tsx:844`) → `organizationScopedLocalStorageKey()`
(`AppHelpers.tsx:815-818`) даёт `dental-crm:onboarding:v1:<organizationId>`, когда id есть.
Писатель — `saveOnboardingDismissed` (`AppHelpers.tsx:4260-4277`), читатель —
`mergeLocalOnboardingDismissal` (`AppHelpers.tsx:4240-4257`), и он кладёт результат в
состояние `onboardingDismissed`.

*(в) «Ничто в репозитории не пишет `dente_ui_preferences_v1`» — неточно.*
Ни одна строка **приложения** его не пишет, это верно (единственное упоминание в `apps/` —
чтение на `App.tsx:2010`). Но пишут его **больше шестидесяти** файлов `scratch/` и
`scripts/` — все скриншотные и аудиторские прогоны сеют его, чтобы проскочить онбординг
(`scripts/dente-redesign-shots.mjs:405`, `scripts/playwright-audit.cjs:267`, десятки
`scratch/probe-*.mjs` и `verify-*.mjs`). Это не мёртвый ключ, это чёрный ход стенда. Из
чего и следует, что блок 2470 достижим только в стенде.

**3. Два источника правды на одну настройку — оба члена `isLocalOnboardingDismissed` читают
не тот ключ.** `App.tsx:2008-2011`: первый член читает **неспециализированный**
`dental-crm:onboarding:v1`, тогда как приложение пишет ключ с областью организации;
второй читает легаси-имя `dente_ui_preferences_v1`, которого приложение не пишет вовсе.
Оба чтения дублируют то, что `mergeLocalOnboardingDismissal` уже делает правильно.

## Что изменено

**Коммит 75db7eb5d — страж (реальный результат пакета).**
- `scripts/check-component-mount-reachability.mjs` (758 строк) — обходит граф от
  единственной настоящей точки монтирования `apps/web/src/main.tsx` и различает состояния:
  `rendered`, `rendered-via-lazy-route`, `rendered-by-value-only`,
  `declared-but-never-rendered`, `imported-but-never-rendered`, `orphaned`,
  `rendered-only-inside-an-unreachable-tree` (та самая ловушка «два звена из трёх»),
  `reachable-only-from-tests`. Печатает опись и выходит с кодом 1.
- `scripts/lib/component-mount-rules.yml` (276 строк) — правила ast-grep отдельным
  читаемым файлом: что считается компонентом (экспортированное объявление в PascalCase, в
  теле которого есть JSX — проверено на отрицательном контроле: объект, тип и
  функция-со-строчной буквы не подходят) и что считается рендером.
- `scripts/tests/check-component-mount-reachability.test.mjs` (285 строк) — 6 тестов,
  каждый копирует настоящий скрипт в каталог **вне репозитория** и разворачивает там
  приложение из трёх звеньев.

Ключевые решения стража, каждое с причиной в коде:
- `App.tsx` и `workspaceShell.tsx` **не** объявлены корнями намеренно — они обязаны
  оказаться достижимыми сами, и отчёт это печатает (сейчас `да`/`да`). Объявив их
  корнями, страж спрятал бы ровно ту поломку, которую ищет.
- Флага `--root` нет намеренно: иначе им можно было бы навести проверку на пустой каталог
  и получить зелёный.
- Предзагрузка чанка (`workspacePreload.ts` тянет 14 экранов голым `import()`) рендером
  **не** считается. Иначе один этот файл отбелил бы все экраны сразу.
- Ленивая цель маршрута (`lazy(() => import(X).then(m => ({default: m.X})))`) считается
  рендером по отдельному правилу — иначе главный компонент приложения был бы объявлен
  мёртвым (см. «ложные срабатывания» ниже).
- Если ast-grep недоступен — падает с кодом 2 и инструкцией, **не** скатывается на
  текстовый поиск.

**Коммит ab3312595 — удаление мёртвой цепочки.** 10 файлов, 1888 строк.
Удалено, а не смонтировано, по четырём причинам:
1. Все четыре секции `NoticeBars` уже есть в `App.tsx`: компактная полоса 2451, тот же
   мастер 2470, полоса черновика 3471, полоса реквизитов 3483.
2. Полосы `App.tsx:3471` и `:3483` **живые** — их гейт требует `onboardingDismissed`, а это
   состояние достижимо. Монтирование `NoticeBars` выдало бы одно и то же предупреждение
   дважды (§3, §4).
3. Гейты вынесенной копии **слабее**: в `NoticeBars:26` нет члена
   `!isLocalOnboardingDismissed`, который стоит в `App.tsx:2450`. Монтирование вернуло бы
   на экран полосу, которую `App.tsx` душит намеренно.
4. Ничего не потеряно: все восемь заголовков панелей есть в `App.tsx` дословно (сверено),
   и правка выбора ролей тоже — `App.tsx:2063-2067` считает `onboardingRoleChoices` тем же
   `staffRoleChoices(roleFocusOrder, resolveClinicMode(...), selectedWorkspaceRole)`, что и
   удалённый `InlineStepRole:37-41`, и раскрывает список на 2551.

## Две ложных тревоги самого стража, найденные и убитые ДО коммита

Обе показывают, зачем брифинг требовал ast-grep, и обе доказывают, что цифру нельзя
принимать с первого прогона — в том числе от меня.

1. **Одинарные кавычки.** Шаблоны вида `import { $$$SPECS } from "$SRC"` не видят
   `import { AuthHub } from './components/auth/AuthHub'` (`App.tsx:11`). В `apps/web/src`
   таких строк **145 в 47 файлах**. Первый прогон объявил сиротами `AuthHub`,
   `ClinicLogin`, `Register`, `ScheduleView`, `SettingsView` и ещё десятки живых
   компонентов: 70 нарушений вместо 40. Исправлено: путь берётся метапеременной без
   кавычек (`from $SRC` + `kind: import_statement`), кавычки снимает скрипт.
2. **Ленивые маршруты.** Все 16 экранов и сам `App` грузятся через
   `lazy(() => import(X).then(m => ({ default: m.X })))`, причём в `AppShell.tsx:6`
   локальное имя `DentalWorkspace` не совпадает с именем экспорта `App`. Проверка по тегу
   объявила `App` нерендерящимся. Исправлено отдельным правилом `lazy-target-named`.

## ПРОВЕРЕНО

- **SMOKE VERIFIED** — `node scripts/check-component-mount-reachability.mjs`, TRUE_EXIT=1
  (красный по замыслу). Опись на HEAD ab3312595:
  `просмотрено файлов 368 · файлов с компонентами 153 · компонентов объявлено 159 ·
  файлов достижимо 257 · App.tsx достижим да · workspaceShell достижим да`
  По состояниям: `rendered 128 · rendered-via-lazy-route 1 · rendered-by-value-only 0 ·
  declared-but-never-rendered 0 · imported-but-never-rendered 0 · orphaned 19 ·
  rendered-only-inside-an-unreachable-tree 11 · reachable-only-from-tests 0`.
  ИТОГ: нарушений 30, в исключениях 0. Неоднозначных импортов по умолчанию 0.
- **SMOKE VERIFIED, доказательство падения** — до удаления цепочки тот же прогон давал
  `orphaned 20 · rendered-only-inside-an-unreachable-tree 20`, ИТОГ 40, и называл
  `WorkspaceOnboardingNoticeBars.tsx:5` сиротой, а `WorkspaceOnboardingInline.tsx:13` и все
  восемь `InlineStep*` — «рендерится внутри недостижимой ветки». После удаления 40 → 30
  (19 + 11), то есть страж отслеживает изменение, а не печатает константу.
- **UNIT VERIFIED** — `node --import tsx --test scripts/tests/check-component-mount-reachability.test.mjs`
  → `tests 6 · pass 6 · fail 0`, TRUE_EXIT=0. Тесты доказывают, что страж
  краснеет на сироте по имени, на мёртвом импорте, на третьем звене недостижимой ветки и на
  предзагруженном компоненте; **зеленеет** на здоровом дереве из трёх звеньев; и что
  исключение снимает нарушение только вместе с непустой причиной. Копия скрипта
  разворачивается в `os.tmpdir()`, вне репозитория.
- **Достоверность стража сверена вручную**, а не принята на слово. По каждому из 19
  «сирот» прогнан `git grep -l <Base> HEAD -- apps/web/src`. Пять самых опасных
  разобраны отдельно, потому что по числу файлов выглядели живыми:
  `Badge` (20 файлов — все подстрока `BadgeCheck`/css, ни одного `from ".../Badge"`),
  `components/Odontogram.tsx` (11 файлов — живёт `components/odontogram/*`, плоский файл
  не импортирует никто), `LegacyMigrationStudio` (3 файла — два упоминания в
  **комментариях** `MigrationWizard.tsx:8` и `SettingsClinicTab.tsx:14`),
  `VisitDictation` (8 файлов — всё это `parseVisitDictationLocal`,
  `startVisitDictation`, `goToVisitDictation`), `DocumentUkepSignButton` (тест
  `tests/documentsViewDecomposition.test.ts:170` держит **строку с именем файла**, а не
  импорт). Ложных срабатываний в выборке нет.
- **Удаление подтверждено на HEAD** — `git grep -n "<Base>" HEAD -- apps/` пусто для всех
  десяти имён. Во всём репозитории остались только две прозаические ссылки:
  `.agents/archon/cycle9.workflow.js` (брифинг пакета) и пояснительная шапка
  `scripts/check-component-mount-reachability.mjs`, где инцидент описан как история —
  это и есть причина существования гейта.
- **Кодировка** — `npm run smoke:web-text-encoding` → `ok: true, checkedFiles: 421,
  mojibakeHits: 0, garbledQuestionHits: 0`, TRUE_EXIT=0. Плюс round-trip по каждому моему
  новому файлу: `mojibake=false bom=false validUtf8=true U+FFFD=false` на всех пяти.
  ЧЕСТНО: значение `checkedFiles` **до** удаления я не измерял, поэтому объяснять разницу с
  числом 429 из брифинга не берусь — перемерьте сами.
- **Индекс чужой работы не задет** — оба коммита сделаны формой
  `git commit -F <msg> -- <явный список>`. `git diff --cached --name-only` до и после
  показывает чужие `git rm` (`apps/api/src/db/rebookingConversionRulesQuery.ts`,
  `apps/web/src/components/analytics/RebookingConversionRulesWidget.tsx`) — они остались в
  индексе нетронутыми, в мои коммиты не попали (`git log -1 --stat`: 3 файла и 10 файлов).

## НЕ ПРОВЕРЕНО

- **Типизация после удаления.** TYPECHECK не запускался (§7a — гейт лида).
  Закрывается: `npm run typecheck -w @dental/web`.
  Логическое основание, почему риск нулевой: все десять удалённых файлов не импортировал
  никто (`git grep` по каждому имени пуст), поэтому сломанного импорта возникнуть не может.
  Это аргумент, а не прогон.
- **Полный набор тестов веба.** Не запускался (§7a). Закрывается: `npm test -w @dental/web`.
- **Что мёртвый мастер `App.tsx:2470+` действительно недостижим для человека в браузере.**
  Доказано арифметикой по гейтам, не наблюдением. Закрывается прогоном в браузере с чистым
  localStorage: `node scripts/dente-redesign-shots.mjs` **не** годится — он сам сеет
  `dente_ui_preferences_v1` и потому создаёт ровно то состояние, в котором блок оживает.
- **Что 19 сирот и 11 недостижимых — действительно мёртвый код, а не незаконченная работа
  соседей.** Страж доказывает недостижимость, а не ненужность. Решение по каждому — за лидом.
- Полосы `App.tsx:3471` и `:3483` названы живыми по арифметике гейта
  (`onboardingDismissed && onboardingDraftMode && !onboardingReadyToFinish`), в браузере не
  наблюдались. UI VERIFIED заявить не могу и не заявляю.

## Коммит

- `75db7eb5d508343b57831df41ef95d9c53da94f4` —
  `feat(гейты): компонент, который никто не монтирует, проходил все проверки незамеченным`
  3 файла, +1319 строк.
- `ab33125957d739350fd7166c2f67fd9a3a00d18e` —
  `fix(онбординг): вынесенный мастер первого запуска дублировал App.tsx и не монтировался никем`
  10 файлов, −1888 строк.

## Долг

**1. Ключ признания онбординга — второй источник правды. Патч готов, не применён.**
Причина, по которой не применён (§7a + чужая территория): `App.tsx` — файл второго автора,
правка меняет поведение и требует `npm run typecheck -w @dental/web`, который принадлежит
лиду; и от `dente_ui_preferences_v1` зависят 60+ скриптов стенда — убрав член, вы в той же
волне ослепите скриншотный парк. Это решение о порядке работ, не о коде.
Точный патч: удалить `App.tsx:2008-2011` целиком и снять член `!isLocalOnboardingDismissed`
из `App.tsx:2069` и `App.tsx:2450`. `onboardingDismissed` уже приходит из
`mergeLocalOnboardingDismissal` (`AppHelpers.tsx:4240`), который читает **правильный**
ключ с областью организации. Следствия, обязательные к учёту в том же коммите:
  - `App.tsx:2450-2468` станет достижимым, а он дублирует `App.tsx:2470` по смыслу — надо
    решить, какая полоса остаётся;
  - `App.tsx:2470-…` (второй мастер, ~500 строк) станет достижимым для людей — его надо
    либо удалить, либо признать вкладочным видом мастера;
  - каждый скрипт стенда, сеющий `dente_ui_preferences_v1`, надо перевести на
    `dental-crm:onboarding:v1:<organizationId>`, иначе прогоны начнут упираться в мастер.
Закрывается: `npm run typecheck -w @dental/web` + `npm test -w @dental/web` +
`node scripts/smoke-ui-preferences.mjs`.

**2. Страж нашёл ещё две мёртвые цепочки онбординга и дашбордов — вне моей претензии.**
  - `apps/web/src/OnboardingPreview.tsx:37` (сирота) → `OnboardingSetupWizard.tsx:20` →
    `Step1Specializations`…`Step7Migration` + `SharedOnboardingUI` (`GlassCard:51`,
    `SliderControl:85`) + хук `onboarding/useOnboardingLogic.ts`. **Одиннадцать
    компонентов в десяти файлах, третий по счёту мастер первого запуска в одном
    репозитории.** Это ровно та же ловушка: `Step*` честно рендерятся из
    `OnboardingSetupWizard`, два звена живы, а корня нет.
  - `apps/web/src/pages/FinancialDashboard.tsx:18` (сирота) →
    `pages/DoctorPayoutDashboard.tsx:16`.
  Не тронул: другая цепочка, не мой claim, и §8 — не растекаться.

**3. `.agents/AGENTS.md` §8 предписывает `npx @ast-grep/cli`. На этой машине эта форма
падает:** `npm error could not determine executable to run` — бинарь в пакете называется
`ast-grep`, npx не выводит его имени из скоупнутого имени пакета. Работает
`npx --no-install ast-grep` (версия 0.44.1, установлен глобально в
`C:\Users\Admin\AppData\Roaming\npm`). Страж перебирает четыре формы и берёт первую живую,
но документ стоит поправить, иначе следующий агент решит, что ast-grep недоступен, и уйдёт
в регулярку.

**4. Границы точности стража, честно (в коде описаны там же).**
  - Привязки в camelCase (`viewLabels`, `appViews`) правило `pascal-ref` не собирает,
    поэтому мёртвый импорт хука страж не заметит и ребро считает проходимым. Осознанно:
    ложно объявить компонент живым нельзя, а мёртвый импорт хука — другой класс дефекта.
  - Форма правила ре-экспорта не отдаёт список имён (шаблон с `$$$SPECS` ast-grep 0.44.1
    отвергает). Замерено: в `apps/web/src` ровно **три** ре-экспорта
    (`ctPlanningTools.tsx:20`, `AppHelpers.tsx:322`, `:323`), и ни один не проносит
    компонент — только словари и типы. Бочек компонентов сейчас нет, потеря ничего не
    искажает. Появится бочка — вылезет ложное «объявлен и не рендерится», и лечить это надо
    возвратом списка имён, а не записью в исключения.
  - Компонент, который рендерится только внутри своего же файла, считается живым, если
    файл достижим. Помощник, которого рендерит лишь другой мёртвый помощник в том же файле,
    проскочит.
