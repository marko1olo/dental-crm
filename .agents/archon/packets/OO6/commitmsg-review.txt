[ARCHON] review(OO6): пять стражей кусаются, но три требования из пяти файлов не проверяют ничего

EXIT_BITE перепроверен мной, а не принят на слово: 19 мутаций на теневой копии
исходников, база на неизменённой копии 0 у каждой, разбор мутантов через
ts.createSourceFile чист — сломан смысл, не синтаксис. Все заявленные укусы
воспроизводятся, обе заявленные терпимости к формату тоже. Пять «ДО» = 1 (версии
стражей из <commit>^ по текущему дереву), пять «ПОСЛЕ» = 0. Коммиты чистые:
только пути scripts/, трейлеры пусты, автор marko1olo, чужая незакоммиченная
работа не подметена. Оставить, не откатывать.

ПОЧЕМУ НЕ SOUND. В поставленном артефакте осталось три требования, которые не
кусаются, и два из них того класса, которым пакет озаглавлен.

  F1 smoke-telegram-outbox-sla-source:70-72 — единственный forbiddenSnippets не
     может совпасть никогда: игла на \n при CRLF-файле, на двух пробелах при
     табуляциях, в одну строку при разнесённом коде. Доказано мутацией, а не
     чтением: убираю postVisitInstructionTaskKeepsOutboxClaim(task) из предиката
     покрытия — страж ЗЕЛЁНЫЙ (exit 0). Теряется поведение из его же reason:
     проваленная задача после визита снова душит запасную отправку, и пациент не
     получает рекомендаций. requiredSnippets не спасает — там голое имя, оно
     совпадает с объявлением: обёртка есть, связь порвана.

  F2 smoke-document-payload-ui-source:878-881 — вторая вырезка осталась на
     indexOf по отступу с маркером `\n {2}\}` при табуляциях. Окно 182576
     символов / 6018 строк вместо функции на 24 (12963-12986), бежит за конец
     useAppLogic.tsx и внутрь склеенного AppHelpers.tsx. Доказано мутацией:
     вызов createDocument( добавлен в ДРУГУЮ, невиновную функцию
     completeCommunicationTask — страж краснеет и винит нетронутую функцию. Тот
     же дефект, что он заменил на ts.createSourceFile 250 строками выше, в том же
     файле и том же коммите, поэтому подзаголовок f31ff26b2 «границы функции
     теперь берёт ts.createSourceFile» верен только для одной из двух вырезок.

  F3 smoke-document-html-preview-source:141-145 — требование о запасном
     скачивании при заблокированном окне выполняет ЧУЖОЙ вызов: тот же текст
     стоит в функции дважды, и includes устраивает любой. Доказано мутацией:
     удаляю только вызов ветки заблокированного окна — страж ЗЕЛЁНЫЙ, а оператор
     читает «CRM запускает скачивание» и не получает файла.

НАЙДЕНО СВЕРХ ПРЕДМЕТА, продукт не правил. Восемь строк русского интерфейса
несут лишнюю заглавную И, вставленную прямо перед «в»: скачиИвание ×2,
РаспознаИвание ×4, отозИвана ×2 (useAppLogic.tsx 7703/7713/12668/12681/13317/13324,
useVisitLogic.ts 685/1315). Не mojibake — коды чистые, U+0418 между U+0438 и
U+0432, подпись машинной правки. Внесено 471fb19ef, не этим пакетом. При этом
smoke:web-text-encoding зелёный на 470 файлах: createMojibakePattern знает
четыре семейства, и порча внутри одной кириллицы не подходит ни к одному —
третий промах этого стража по его же комментарию.

Три FOUND исполнителя проверил, все три верны, включая ловушку замера: Git Bash
переписывает аргумент с ведущим слэшем, поэтому rg -F '/api/settings/telegram'
даёт ноль совпадений там, где без слэша находит два файла.

Харнесс ревью — node_modules/.rev-oo6/, закрыт .gitignore, продуктовое дерево и
scripts/ мной не изменены.
