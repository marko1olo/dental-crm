## VERDICT: NEEDS_REWORK

Задача 1 закрыта по существу и проверена настоящей проверкой. Задача 2 сделана в коде верно, но её
проверка ЗЕЛЕНА НА СЛОМАННОМ КОДЕ — я это измерил, а не предположил. Плюс задача 1 внесла измеренный
регресс: законная цена превращается не в `price_not_found` при видимой строке, а в УДАЛЁННУЮ СТРОКУ.

## ЧТО ПРОВЕРЕНО КОМАНДОЙ, ДОСЛОВНО, С ИСТИННЫМ КОДОМ ВЫХОДА

`git show --stat` — оба коммита существуют, автор `marko1olo`, оба на `main`
(`git merge-base --is-ancestor` → 0 для обоих), чужой `13a086cff` между ними им не мешает.
`git log -1 --format='%(trailers)'` для `3908529e8` и для `b1cb29df3` — ПУСТ.
Пути ровно заявленные, лишних нет: `3908529e8` = `analyzer.ts` (+104), `pricelistYearPrice.test.ts` (+264),
`packets/NN1/commitmsg.txt`; `b1cb29df3` = `analyzer.ts` (+111/−14), тот же тест (+149/−1),
`packets/NN1/commitmsg2.txt`.

Кодировка обоих исходников: BOM нет, U+FFFD 0, mojibake нет.

Набор исполнителя, без конвейера:
`cd apps/api && node --import tsx --test src/pricelist/{pricelistYearPrice,pricelistRowGate,pricelistKopecks,analyzer,pricelistLastNumber,pricelistBrandDigits,groqPricelistKopecks}.test.ts`
→ **EXIT=0, tests 102, pass 102, fail 0**. Числа исполнителя подтверждены.

Зонд ведущего `cd apps/api && npx tsx ../../scratch/probe-year-as-price.ts` → **EXIT=0**. «Отбеливание 2025»
даёт `items:1, title:"Отбеливание 2025", price:null, warnings:["price_not_found"]`; «Прайс-лист 2025»,
«Прайс 2025 в рублях», «Редакция 2024» — 0 позиций, `pricelist_rows_skipped:1`. Заявленное совпало.

Соседние потребители разбора: `node --import tsx --test src/tests/noFabricatedDataFallback.test.ts
src/tests/ingestion/documentExtractor.test.ts` → **EXIT=0, 16/16**.

`npm run smoke:pricelist-analyzer` → **EXIT=1**, «Сборка старше исходника: dist … 05:34:29Z, а
src/pricelist/analyzer.ts изменён 06:16:25Z». `apps/api/dist` НЕ в индексе (`git ls-files` пуст), собран в
09:34 локального — то есть после `46298c9fb` (08:57) и до этих двух коммитов. Значит smoke по предмету
пакета был зелёным и стал красным именно от этих правок; правка через dist не прогонялась ни разу.
Исполнитель это назвал сам и причину назвал верно. `npm run build` — гейт ведущего, не запускал.

## ПРОВЕРКА НА ЗЕЛЁНЫЙ ТЕСТ, НЕ ПРОВЕРЯЮЩИЙ НИЧЕГО (пункт 4)

Ломал правку по-настоящему: снимал копию `analyzer.ts` под другим именем, мутировал её, направлял на неё
копию набора. Исходники в дереве не менялись, все временные файлы удалены, `git status` по
`apps/api/src/pricelist/` чист.

**Задача 1 — проверка настоящая.** Копия `3908529e8~1` (дерево до правки) + тот же набор → **EXIT=1,
pass 6 / fail 4**. Краснеют ровно четыре проверки дефектной стороны, включая главную «заголовок раздела
прайса с годом не даёт цену». Шесть проверок законной стороны зелены и до, и после — это правильная
форма: они стерегут ПЕРЕотказ.

**Законная сторона тоже кусает.** Копия текущего кода с заменой `hasExplicitPriceMark(line)` на
`lowHasCurrency` → **EXIT=1**, падает «знак рубля оставляет год ценой во всех написаниях»:
`AssertionError: потеряна цена, подписанная знаком рубля («Консультация 2025 рублей»)`. Раздел
CONTRADICTED исполнителя — измеренный факт, а не отговорка: бриф в этом месте действительно неверен,
и отклонение обосновано.

**Задача 2 — проверка НЕ ПРОВЕРЯЕТ ДЕФЕКТ.** Копия текущего кода, в которой я вернул РОВНО тот дефект,
который чинит `b1cb29df3`: нейро-ветка `analyzePricelist` снова зовёт `responseFromItems` с
`parsedRows.items` без `selectPricelistServiceRows` и без `skippedRowsWarnings`. Прогон полного нового
набора → **EXIT=0, tests 15, pass 15, fail 0**. Все пять новых проверок задачи 2 ЗЕЛЕНЫ на коде с
полностью восстановленным дефектом.

Причина в самом наборе: помощник `analyzeGroqRows` (`pricelistYearPrice.test.ts:324-327`) не вызывает
разбор, а ПОВТОРЯЕТ последовательность боевой ветки —
`pricelistItemsFromGroqRows(...)` → `selectPricelistServiceRows(parsed.items, parsed.droppedRows)`.
Проверяются две вынутые функции, а не то, что ветка их зовёт. Это тот самый образец «замер сравнивал две
синхронные ветки», названный в брифе.

**Отговорка, которой это оправдано, неверна.** Исполнитель пишет, что поднять ветку подделкой fetch
нельзя, потому что `recordProviderKeySuccess` писала бы на диск состояние здоровья ключей. Ключ здоровья
имеет выключатель: `apps/api/src/speech/keyPool.ts:198-201`,
`process.env.DENTAL_SPEECH_KEY_HEALTH_FILE` со значением `off` возвращает `null` и запись отменяет; им уже
пользуются три скрипта репозитория (`scripts/smoke-speech-key-rotation.mjs:24`,
`smoke-speech-provider-errors.mjs:109`, `smoke-speech-groq-chunk-floor.mjs:12`). Сквозная проверка
нейро-ветки стоила одной переменной среды и одной заглушки fetch и не сделана.

Поэтому строка CLAIM «Один и тот же прайс даёт в обоих режимах одинаковый состав позиций и одинаковое
число потерь — это проверено, а не заявлено» ЗАВЫШЕНА. Проверено свойство композиции двух функций,
которую собирает сам тест. Что её собирает `analyzePricelist`, проверено только чтением диффа — читал,
там верно, но это статика, а не проверка.

## ПУНКТ 5: НОЛЬ ВМЕСТО «НЕИЗВЕСТНО»

`git show 3908529e8 b1cb29df3 | rg '^\+.*(\?\? 0|\|\| 0)'` — совпадений НЕТ. Отказ от цены даёт
`priceRub: null` и `price_not_found`, числом не подменяется (проверено зондом). `droppedBeforeGate = 0` —
законное значение по умолчанию для ветки, где шага «до гейта» не существует, а не ноль вместо неизвестного;
`skippedRowsWarnings(0)` предупреждения не печатает, то есть «потерь не было» и «потерь 0» не путаются.
Претензий по этому пункту нет.

## ЧТО ОСТАЛОСЬ

Не проверял (гейты ведущего): `npm run typecheck`, `npm run build`, весь `npm test`, миграции. Типы
`analyzer.ts` по-прежнему видел только транспилятор `tsx`, `tsc` — нет. Сквозной путь
`POST /api/pricelist/analyze` с `useServerAi: true` не исполнялся ни исполнителем, ни мной. Смоук по
предмету пакета красный до пересборки.

## FOUND

**F1 (P1, регресс этой правки, измерено). Отказ от года удаляет СТРОКУ ЦЕЛИКОМ, а не только цену, если
категория услуги не опознана.** Бриф разрешил исход «price_not_found + строка видна»; здесь строки нет.
`apps/api/src/pricelist/analyzer.ts:812` (отказ) + `isPricelistServiceRow` (~:1229): после отказа
`priceRub === null`, категория `other`, знака рубля нет — гейт возвращает `false` и позиция исчезает.
Замер (мой зонд, тот же вызов `analyzePricelist`, EXIT=0), до правки → после:

| строка | до `3908529e8` | после |
|---|---|---|
| «Полировка одного зуба 2025» | позиция, цена 2025 | **0 позиций**, `pricelist_rows_skipped:1` |
| «Полировка 2025» | позиция, цена 2025 | **0 позиций** |
| «Реабилитация 2025» | позиция, цена 2025 | **0 позиций** |
| «Отбеливание 2025» | позиция, цена 2025 | позиция, `price_not_found` (категория hygiene) |

Клиника видит только счётчик, но не видит, КАКАЯ строка выпала и что в ней было написано. Это ровно то,
что запрещает стоячий закон, который сам исполнитель цитирует в комментарии к гейту:
«отказ от цены — законный исход, а вот удаление всей позиции из прайса законным не является». Слова
«полировка» и «реабилитация» — те же два, из-за которых этот гейт и правили в `46298c9fb`. Слепое пятно
набора ровно здесь: единственная проверка видимости строки стоит на «Отбеливание», у которого категория
опознана.

**F2 (P1, чистая потеря законной цены, измерено). Список слов документа отвергает год ВНЕ окна на законном
названии услуги.** `documentEditionPatterns` (`analyzer.ts:~657-666`) содержит `/изменени/` и `/тариф/` и
проверяется по ВСЕЙ строке. «Изменение формы зуба 2000» до правки давало цену 2000 ₽ — ПРАВИЛЬНУЮ, — после
правки даёт **0 позиций**. То же «Тариф Комфорт 2000» → 0 позиций. Год 2000 в окно `−6…+1` не входит, его
уносит именно слово. Здесь нет обмена дефекта на дефект: было верно, стало потеряно вместе со строкой —
именно то, о чём бриф предупреждал («унесёшь законные цены и превратишь выдуманную цену в потерянную»).

**F3 (P1, исходный дефект уцелел, измерено). Признак рубля берётся по СТРОКЕ, поэтому любой знак рубля в
строке снимает проверку года целиком.** `analyzer.ts:812`: `if (!hasExplicitPriceMark(line) && looksLike…)`.
Если подписанное деньгами число отсекается полом цены 300 ₽, ценой становится год. Замер: «Прайс-лист 2025,
скидка 100 руб» → позиция «Прайс-лист , скидка 100 руб» ценой **2025 ₽**, одинаково ДО и ПОСЛЕ правки.
Комментарий над этой строкой прямо утверждает обратное («если знак рубля подписывает ДРУГОЕ число, то у
того числа стоит explicit, и extractPrice выбирает из явных кандидатов, а не из года») — записанный в
исходник инвариант ЛОЖЕН, и на него будет опираться следующий правящий.

**F4 (P1, проверка задачи 2 пуста по отношению к дефекту).** Подробности выше: возврат дефекта в боевую
точку вызова оставляет 15/15 зелёными, EXIT=0. Нужна проверка, которая зовёт `analyzePricelist` с
`useServerAi: true`, `DENTAL_SPEECH_KEY_HEALTH_FILE=off` и заглушкой fetch, и читает
`pricelist_rows_skipped:N` из `warnings` ОТВЕТА. Это же закроет и «НЕ ПРОВЕРЕНО» самого исполнителя.

**F5 (P2, окно года прибито к системным часам).** `looksLikeEditionYear` считает окно от
`new Date().getFullYear()`, то есть поведение разбора прайса зависит от даты машины: восемь значений
(сегодня 2020–2027) без знака рубля цены не дают, а 2019 и 2028 дают (проверено зондом: «Отбеливание 2019»
→ 2019 ₽, «Отбеливание 2028» → 2028 ₽). Полоса отказа ползёт каждый 1 января, и один и тот же файл прайса
разбирается по-разному в разные годы. Замысел вменяемый, но у клиники нет способа это увидеть и нет
предупреждения «цена отвергнута как год».

**F6 (P3, подтверждённые находки исполнителя — проверил по диску, обе верны).**
`apps/web/src/SettingsView.tsx:1238` ссылается на «analyzer.ts:713» как на правило отказа от догадки —
правило сейчас не на 713. `apps/api/src/pricelist/pricelistRowGate.test.ts:242-248` содержит комментарий
«Тот дефект живёт в extractPrice … и этой правкой не закрыт» с утверждением, что «Прайс-лист 2025» даёт
услугу за 2025 ₽ — после `3908529e8` это ложь в чужом файле, который исполнитель верно не стал трогать.

**F7 (P2, названо исполнителем, подтверждаю как отдельную работу).** Нейро-ветка отдаёт год ценой своим
путём: `readMoneyRubOrNull` принимает `priceRub: 2025` от модели без всякой проверки года, и два режима
снова расходятся на одной строке. Правка `3908529e8` закрывает только детерминированное чтение строки.
Также `itemFromGroq` при отказе контракта молча подменяет разбор модели детерминированным откатом —
различить исходы можно лишь по id `price-N` против `price-ai-N`, на экране признака нет.

## ПОЧЕМУ НЕ REVERT

Вреда, который бриф считает основанием для отката, нет: чужие правки не потеряны, секретов не утекло,
кириллица целая (BOM/U+FFFD/mojibake проверены), в исходники скриптом никто не писал, трейлеры пусты.
Головной P0 — «Отбеливание 2025» ценой 2025 ₽ без единого предупреждения — закрыт по-настоящему и
проверкой, которая краснеет на прежнем коде. Возврат вернул бы выдуманную цену в подписываемый документ.
Доработка: F1, F2, F3 (правка задачи 1 переотказывает и одновременно недоотказывает) и F4 (проверка
задачи 2 не касается дефекта).
