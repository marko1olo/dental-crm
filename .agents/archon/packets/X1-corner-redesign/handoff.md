# X1-corner-redesign — handoff

HEAD: 5fd41faad7b54d822d8660792405f402a37f7563
Мои коммиты: `f0121f0c2` (перепроектирование), `5fd41faad` (правка ширины пункта навигации)

---

## Что было сломано (file:line на родителе)

1. `floatingCorner/cornerDockLayout.ts:126` — `CORNER_OBSTACLE_BLOCK_SHARE = 0.5`.
   `:261-270` `cornerBlocksTarget` = `overlapArea / targetArea >= 0.5`. При равной высоте это
   `ширина панели / ширина мишени`, то есть порог недостижим для всего, что шире панели: выше
   336px при окне 390x844 (панель 168) и выше 556px при 1600x1100 (панель 278).
   Главная кнопка продукта `button.primary-button` «Запись» — **364x44 на четырёх маршрутах из
   пяти**, максимум доли `168·44/(364·44)` = **0.4615**. Панель не могла уступить ей никогда.
2. `IncomingCallToast.tsx:67` — `fixed bottom-6 right-6 z-[999999] w-96`, доля 0.089-0.155.
   Чтобы порог сработал, тост должен быть не выше 69px. Ревьюер V1 внедрил его точную геометрию
   в живую страницу и дёрнул слушатель самой панели: подъём остался 0.
3. Обратная сторона того же порога: при 1600x1100 на `#patients` кнопка поиска дока накрывала
   `<label>Email</label>` на 44.3% — ниже порога, подъёма нет, — и `document.elementFromPoint` в
   центре подписи возвращал кнопку дока. Нажатие по середине подписи открывало поиск.
4. `CornerDock.tsx:160-190` — `collectObstacles`: 5 вызовов `document.elementsFromPoint` за проход.
   `:349-352` — слушатель `scroll` в фазе перехвата на `window`, то есть проход за кадр прокрутки.
5. `dente-redesign.css:843-845` — единственный потребитель `--corner-dock-reserve-block`.
   Хвостовой пустой низ при 390x844 складывался из `.patients-panel` 20 + `.work-grid` 96 +
   `.workspace` 144 = **299px из 844**, то есть 35% экрана (замер ревьюера V1, F-E).

Никакое значение порога это не лечит: геометрия запрещает. Поэтому механизм не настроен, а удалён.

## Что изменено

**Удалено целиком** — `apps/web/src/components/floatingCorner/` (6 файлов, 2360 строк вычтено):
выборка помех, подъём, компактный режим, чтение геометрии за проход, потоковые проходы на
прокрутке, порог перекрытия, резерв `--corner-dock-reserve-block` и его потребитель в CSS.
Вместе с геометрией удалены 54 её теста: тест, переживший свой механизм, охраняет пустоту.

**Создано** — `apps/web/src/components/workspaceActions/`:
- `workspaceActionsPlacement.ts` — куда садится группа и в каком порядке стоят кнопки. Оба
  порядка ВЫВЕДЕНЫ из одного списка `WORKSPACE_ACTION_SLOTS`, а не переписаны рядом. Порог
  ширины (840px) НЕ дублируется в TypeScript: вопрос задаётся самой навигации.
- `WorkspaceActions.tsx` — владелец. Ни `elementsFromPoint`, ни `getBoundingClientRect`, ни
  слушателя `scroll`, ни `requestAnimationFrame`, ни `ResizeObserver` (проверено тестом).
- `workspaceActions.css` — ни одного `position: fixed` (проверено тестом), только токены.
- `workspaceActionsLabels.ts` — у каждого действия появилась ВИДИМАЯ подпись и объяснение.
- `voiceMeter.ts` — переехал; из JSX вынут радиус свечения (`voiceGlowRadiusPx`).
- `workspaceActionsPlacement.test.ts` — 26 тестов.

**Куда переехали действия**: широкий экран — строка действий топбара, ОДНОЙ сегментированной
группой `role="group"` перед кнопками клиники, «Запись» остаётся последней. Узкий — нижняя
навигация: РОВНО ОДИН подписанный пункт «Голос» плюс панель над навигацией с тремя действиями.

**Одна точка монтажа на проект** — `workspaceShell.tsx` в `.top-actions`. В навигацию группа
въезжает сама: вставляет туда свой контейнер (`display: contents`) и рисует в него портал.
**`App.tsx` НЕ ПРАВИЛСЯ** — см. «Коллизия».

**Исправлено при переносе, две настоящие ошибки:**
- Первое чтение раскладки шло во время рендера, когда нижней навигации в документе ещё нет
  (`querySelector` → null → «header»). На телефоне пункт «Голос» не появился бы до первого
  изменения размера окна, то есть никогда. Теперь раскладка перечитывается в `subscribe`, то
  есть после коммита.
- Пункт «Голос» брал равную долю ширины и уводил подпись «Пациенты» на вторую строку
  (коммит `5fd41faad`, замеры ниже).

## ПРОВЕРЕНО

**UNIT VERIFIED** — `node --import tsx --test apps/web/src/components/workspaceActions/workspaceActionsPlacement.test.ts`
```
tests 26 | suites 6 | pass 26 | fail 0 | duration_ms 175.7   TRUE_EXIT=0
```
Среди них: раскладка и оба порядка кнопок; ни один слот не теряется ни в одной раскладке;
у каждой кнопки есть видимая подпись, объяснение и подсказка; в CSS группы нет `position: fixed`;
переменной резерва не осталось ни в одном стиле (с вырезанными комментариями); владелец не
опрашивает DOM геометрией; каталога `floatingCorner` нет; **точка монтажа реально стоит в
`.top-actions`, а жильцы реально переехали на слоты** (именно этого не было, когда пакет
оборвался в первый раз — четыре готовых файла и ноль импортов).

**MEASURED (живой браузер, 5173, read-only; сервер не перезапускался)**
`node scratch/probe-corner-reserve.mjs x1-after` → `scratch/v1-corner-x1-after.json`, TRUE_EXIT=0
`node scratch/probe-x1-actions-placement.mjs final|click` → `scratch/x1-actions-{final,click}.json`, TRUE_EXIT=0

| замер | 390x844 | 720x1100 | 1600x1100 |
|---|---|---|---|
| `#dente-corner-dock` (старый док) | **0** | — | **0** |
| computed `--corner-dock-reserve-block` | **""** (нет) | — | **""** |
| `position:fixed` в правом нижнем углу | **1** — и это сама `.dnt-bottom-nav` | **1** — сама навигация | **0** |
| хост группы | 1, `position: static`, внутри `.dnt-bottom-nav` | 1, внутри навигации | 1, `position: static`, внутри `.topbar` |
| раскладка (`data-placement`) | `nav` | `nav` | `header` |

**Хвостовой пустой низ, 390x844** — то, о чём просил пакет:
`.patients-panel` 20 + `.work-grid` 96 + `.workspace` **72** = **188px**; фактическое расстояние
от низа последнего элемента до конца прокрутки — **190px**.
Было (замер ревьюера V1): 20 + 96 + **144** = **299px**. **Стало на 111px меньше, 35.4% → 22.5%
высоты экрана.** При 1600x1100: 48 + 20 + 0 = **68px**, фактически **70px**.

**Цена прохода на прокрутке, 120 кадров, в каждом из трёх окон:**
`elementsFromPoint` — **0 вызовов**; `getBoundingClientRect` — **0 вызовов**; время внутри
чтения геометрии — **0.00 мс**. База из данных V1: 295 попаданий и 19.34 мс при 390x844 до U4,
90 попаданий и 0.36 мс после V1.
**ЧЕСТНО, И ЭТО ПРЯМО ОТВЕТ НА П.3 ПАКЕТА: выигрыш V1 я не «сохранил» — он стал неприменим.**
Проход раскладки удалён целиком, поэтому мерить в нём нечего. Заявлять улучшение «19.34 → 0.27 мс»
как своё было бы измерением по кривой, которой больше не существует.

**Нижняя навигация не пострадала (390x844):** 6 пунктов, высота навигации **64px** — та же, что
ревьюер V1 замерил до пакета. Ширины: пять соседей по 64px, «Голос» 38px. Высота span подписи у
всех шести — **12px, то есть одна строка**; ни одна подпись не обрезана.
До правки `5fd41faad` было: навигация **76px**, span «Пациенты» **24px** (две строки).

**«Запись» — та самая кнопка, которой док не мог уступить:** при 390x844 замерена **364x44**
(в точности как у V1), центр принадлежит самой кнопке, **над центром 0 элементов `position: fixed`**.
То же при 720x1100 (89x36) и 1600x1100 (101x36).

**Панель на телефоне действительно открывается** (390x844, реальный клик по пункту):
`hidden` true → **false**, `display: flex`, `aria-expanded="true"`, панель 390x299 в
y 481..780, **зазор от низа панели до верха навигации = 0px** (садится на её кромку, а не
поверх). Внутри три действия, у каждого видимая подпись (18px) И видимое объяснение (16-32px),
высоты 73 / 57 / 57px, центр каждого принадлежит ему самому. Порядок: «Голос», «Поиск», «Справка» —
главное действие первым, как задано `workspaceActionBarOrder("nav")`.

**Удаление не оставило висячих ссылок** — `git grep -l "<BaseName>" HEAD -- .` по ВСЕМУ репозиторию
для `CornerDock`, `cornerDockLayout`, `cornerDockLabels`, `floatingCorner`,
`CORNER_OBSTACLE_BLOCK_SHARE`, `corner-dock-reserve-block`, `cornerBlocksTarget`,
`resolveCornerPlacement`. Все попадания вне `.agents/` — это комментарии «что здесь было раньше»
(14 строк) и строки утверждений теста, доказывающие отсутствие. **Живых ссылок в коде нет.**

**SMOKE VERIFIED** — `node scripts/check-css-tokens.mjs` TRUE_EXIT=0:
`НЕ РАЗРЕШАЕТСЯ НИ В ОДНОЙ ТЕМЕ: 0 имён, 0 вхождений` (51 css-файл, 146 объявлено, 170 использовано).
`npm run smoke:web-text-encoding` TRUE_EXIT=0: `checkedFiles 429, mojibakeHits 0`.

**Индекс и кодировка** — `git log -1 --stat`: только мои файлы, в обоих коммитах. Тема первого
коммита в `od -c`: `321 203 320 263 320 276 320 273` = «угол», валидный UTF-8, мождибаке нет.
gitleaks на обоих коммитах: `no leaks found`.

## НЕ ПРОВЕРЕНО

1. **Типизация.** Гейт не мой (§7a). Точечная сборка вне проектного конфига
   (`npx tsc --noEmit --jsx react-jsx … <4 файла>`) дала **0 ошибок в моих четырёх .tsx**, но 142
   ошибки в чужих файлах из-за моих самодельных флагов — это НЕ гейт и я его так не называю.
   Закрывающая команда: `npm run typecheck -w @dental/web`
2. **Весь веб-набор тестов.** Удалено 54 теста геометрии, добавлено 26. Счётчик набора изменится.
   Закрывающая команда: `npm test -w @dental/web`
3. **Хит-тест центра `<label>Email</label>`.** Не сделан. Подпись лежит далеко ниже сгиба
   (y=2444 при 390x844, y=1110 при 1600x1100), мой `scrollIntoView` не подействовал синхронно,
   центр так и не попал во вьюпорт, и `elementFromPoint` вернул null — это не доказывает ничего
   ни в одну сторону. Вместо него у меня есть **более сильный, исчерпывающий** замер: в правом
   нижнем квадранте при 1600x1100 **ноль** элементов `position: fixed`, а при узких — только сама
   навигация. Но это перечисление, а не хит-тест именно этой точки.
   Закрывающая команда: в `scratch/probe-x1-actions-placement.mjs` заменить `el.scrollIntoView(…)`
   на `await page.locator('label:has-text("Email")').scrollIntoViewIfNeeded()` +
   `await page.waitForTimeout(300)` перед чтением, затем
   `node scratch/probe-x1-actions-placement.mjs email`
4. **Открытие панели при 720x1100.** В прогоне `click` приложение на этой ширине не успело
   смонтироваться за 4.5 с: `.dnt-bottom-nav` отсутствовала в DOM целиком, хост 0. Это флейк
   пробы, не дефект — в прогоне `final` та же ширина дала `placement: nav`, навигацию 64px и
   пункт «Голос» 38px. Взаимодействие там всё равно не замерено.
   Закрывающая команда: `node scratch/probe-x1-actions-placement.mjs click720` с
   `waitForTimeout(9000)`
5. **Накладка в топбаре на широком экране.** `revealWorkspaceActions()` не проверен ни разу:
   ни разу не нажималась «Справка» при 1600x1100 на прокрученной странице.
   Закрывающая команда: при 1600x1100 прокрутить вниз, кликнуть
   `.dnt-actions__control[aria-expanded]`, утверждать, что бокс `.dnt-actions__panel` попадает
   во вьюпорт
6. **Пиксели.** UI VERIFIED мне не принадлежит. Ни одной темы я не смотрел: light / dark / night
   при 390x844, 720x1100, 1600x1100 — за ведущим.
7. **StrictMode.** Двойной вызов подписки раскладки не наблюдался.

## Коммит

- `f0121f0c2` — 14 файлов, +1857 / −2360. Внимание аудитору: git записал **4 строки
  `delete mode` и 2 переименования** (`cornerDockLabels.ts` → `workspaceActionsLabels.ts` 59%,
  `voiceMeter.ts` → `workspaceActions/voiceMeter.ts` 63%), а не 6 удалений. Файлов из каталога
  ушло шесть, каталога больше нет.
- `5fd41faad` — 1 файл, +10 / −1. Ширина пункта навигации.

## Коллизия — `App.tsx`, доложено, не тронуто

В 14:17 `apps/web/src/App.tsx` стал ` M`, и это не я: моя правка была ОТКЛОНЕНА инструментом
(«File has been modified since read»). Это второй, не-флотский автор. Я не редактировал, не
откатывал и не индексировал `App.tsx`.
**Последствие оказалось к лучшему:** вторая точка монтажа не нужна вообще — группа сама въезжает
в живую навигацию, и монолит на 4850 строк остался не тронут.

Второе: после моего коммита `apps/web/src/styles/dente-redesign.css` снова стал ` M`, и
единственное изменение — **снятый BOM в строке 1**. В моём коммите ханка на строку 1 не было
(только 683-922), значит это тоже не я. По правилу не откатывал и не коммитил; лежит грязным.

## Долг

1. **Два микрофона в топбаре на широком экране.** Существовавший `.top-dictation-button`
   (иконка без подписи, `onGoToDictation` → вкладка диктовки ЭМК) плюс «Голос» группы. Действия
   разные, глиф один. Старую кнопку не удалял: она подключена через `onGoToDictation` из
   `App.tsx:2360`, а App.tsx мне не принадлежит и был грязным. Следующий шаг — сложить её в
   группу в пакете, который владеет `onGoToDictation`.
2. **`.omnibar-trigger-btn` / `.omnibar-trigger-text` остались в `main.css:16578-16610` без
   потребителя.** Класс больше никто не ставит (`rg -l "omnibar-trigger" apps/web/src` → только
   `main.css`), правила инертны, но это мёртвый CSS. `main.css` (16 895 строк) вне моего клейма.
3. **Накладка в топбаре едет вместе со страницей.** `.topbar` не `sticky`, прокручивается
   документ. `revealWorkspaceActions()` подтягивает группу в вид при открытии, но дальнейшая
   прокрутка уводит накладку. Осознанный выбор: альтернатива — слой `position: fixed`, то есть
   ровно та болезнь, которую пакет удалял.
4. **`.work-grid` 96px из `main.css:13013` — теперь САМЫЙ КРУПНЫЙ остаток** хвостового низа
   (96 из 188 при <=860px). Вне моего клейма.
5. **Панель на телефоне не закрывается нажатием мимо и не держит фокус.** Отсутствие закрытия по
   нажатию мимо — намеренно: внутри живёт диктовка, случайный тап оставил бы включённый микрофон
   без признака на экране. Escape и своя кнопка работают. Ловушки фокуса нет.
6. **Просвет под навигацию задан числом** `calc(72px + env(safe-area-inset-bottom))` при
   замеренных 64px высоты навигации. Причина, почему px, а не rem, записана в CSS: высота
   навигации задана в этом же файле в пикселях и от базового кегля браузера не зависит, поэтому
   rem-порог при мелком кегле ушёл бы ниже 64px. Если состав навигации изменят, число нужно
   перепроверить — тестом это не охраняется.

## Поправки к брифу пакета

1. «reuse `probe-corner-reserve.mjs` и `probe-corner-obstacles.mjs` rather than writing new ones»:
   первый переиспользован без изменений и работает. **Второй не может запуститься на этом HEAD по
   построению** — он читает `--corner-dock-lift` с хоста, которого больше нет (ревьюер V1 уже
   зафиксировал его `TRUE_EXIT=1`). Поэтому написан один новый —
   `scratch/probe-x1-actions-placement.mjs` — ровно под два замера, которых не даёт ни один
   существующий.
2. Бриф описывает кадр, в котором `<label>Email</label>` лежит в полосе дока при 1600x1100
   (y≈1030). На этом HEAD подпись лежит на y=1110 при высоте окна 1100, то есть ниже сгиба, и
   накрывающего элемента больше не существует — тот кадр невоспроизводим (см. НЕ ПРОВЕРЕНО п.3).
3. `button.primary-button` в брифе — это «Запись». В DOM первым `button.primary-button` идёт
   «Запустить мастер» из демо-баннера. Мерить нужно `.top-actions button.primary-button`, иначе
   получаются 314x36 вместо 364x44.
