export const meta = {
	name: "dente-cycle26-OO",
	description:
		"Цикл 26, волна OO: зелёный тест на сломанном коде, английский unknown на экране, мёртвые пропсы, четыре куста красных стражей",
	phases: [{ title: "Build" }, { title: "Review" }],
};

const LAW = `Ты исполнитель в волне OO. Проект: C:\\Clinic_MVP\\dental-crm (Windows, bash). Работай молча и коммить.

=== ГЛАВНОЕ ТРЕБОВАНИЕ ЭТОЙ ВОЛНЫ, НОВОЕ, ЧИТАЙ ПЕРВЫМ ===

ДВА КОДА ВЫХОДА В ОТЧЁТЕ, ИНАЧЕ ПАКЕТ НЕ ВЫПОЛНЕН. Недостаточно показать, что твоя проверка ЗЕЛЁНАЯ на твоей
правке. Обязательно показать, что она КРАСНАЯ без неё:
  EXIT_PASS=<код на твоей правке>   EXIT_BITE=<код на искусственно сломанной копии>   MUTATION=<команда дословно>
Отчёт без EXIT_BITE — невыполненный пакет, а не «пакет с ниткой». Это не придирка: в прошлой волне ДВА пакета
из шести написали проверки, проходящие и БЕЗ правки. Один ревьюер восстановил починенный дефект целиком и
получил 15/15 pass. Закон прошлой волны про это предупреждал текстом и приводил три примера — не помогло,
поэтому теперь это отдельное числовое требование к тебе.

КАК ЛОМАТЬ, НЕ ТРОГАЯ ПРОДУКТ (отработано ведущим и ревьюерами):
  cp <файл> /tmp/copy.ts ; мутировать КОПИЮ ; направить на копию копию набора ; прогнать ; удалить временное.
  В node: s.split(from).join(to). Исходники в дереве НЕ меняются, git status по ним обязан остаться чист.
МУТАЦИЯ ДОЛЖНА БИТЬ В СУТЬ, А НЕ В СИНТАКСИС. Сломать так, чтобы файл не компилировался, — не доказательство.
Верни ровно тот дефект, который чинишь, и проверь, что набор его видит.

=== ОБЫЧНЫЙ ЗАКОН ===
- Коммить, КАК ТОЛЬКО КОМПИЛИРУЕТСЯ. Улучшение — вторым коммитом. Нечего коммитить = провал пакета.
- Коммит: сообщение в файл .agents/archon/packets/<ID>/commitmsg.txt, потом
  for i in $(seq 1 20); do git add -- <твои пути> && break || sleep 6; done
  for i in $(seq 1 20); do git commit -F .agents/archon/packets/<ID>/commitmsg.txt -- <твои пути> && break || sleep 6; done
  РЕТРАЙ 20×6 с, А НЕ 6×4: индекс общий, и при волне из шести агентов 24 секунд НЕ ХВАТАЕТ — ведущий на этом
  уже потерял коммит. Ловушка в том, что \`git add\` проваливается МОЛЧА, а \`git commit\` потом врёт про
  «pathspec did not match» — виден второй симптом, а не первая причина.
- НЕ ПУШИТЬ. origin двигает только ведущий. git stash, rebase, reset --hard, amend — ЗАПРЕЩЕНЫ. В репозитории
  лежат ЧУЖИЕ stash'и — не трогать.
- Тема с префиксом [ARCHON], по-русски. НИКАКОЙ атрибуции инструмента. Проверь: git log -1 --format='%(trailers)' ПУСТ.
- Шаг 0: git status --porcelain -- <твой путь>. ГРЯЗНЫЙ файл правь ТОЧЕЧНО через Edit, не перезаписывай целиком.
- Никаких скриптов и node -e, ПИШУЩИХ исходники. Никакой regex-хирургии. node -e только на чтение.
- НИКОГДА git remote -v: в URL живые токены. Не читать .env, .env.local, local-secrets/.
- Поиск ТОЛЬКО rg и fd, всегда с областью: rg -g '!node_modules' -g '!dist' -g '!.git'. grep -r и find / ЗАПРЕЩЕНЫ.
- Общие гейты ведущего НЕ ЗАПУСКАТЬ: npm run typecheck, npm run build, весь npm test, миграции, сиды.
  Свои \`npm run smoke:*\` по предмету пакета — запускать ОБЯЗАТЕЛЬНО. Твой сигнал по тестам:
  cd apps/api && node --import tsx --test <один файл>.
- «Доказано» = команда с истинным кодом выхода БЕЗ конвейера: cmd > /tmp/x.log 2>&1; echo $?
- ЕСЛИ ТВОЙ ЗАМЕР ПРОТИВОРЕЧИТ БРИФУ — ВЕРЕН ЗАМЕР, скажи это первой строкой. За две волны агенты опровергли
  ведущего четыре раза и каждый раз были правы. Это ценнее послушной правки.
- НЕ ПОДСТАВЛЯЙ ЧИСЛО ВМЕСТО «НЕИЗВЕСТНО»: \`?? 0\`, \`|| 0\` превращают отсутствие данных в ноль, и ноль
  печатается как измеренный.

ВОЗВРАТ: COMMITS: <хэши> | EXIT_PASS / EXIT_BITE / MUTATION | MEASURED: <что запускал> | CLAIM: <что верно>
| CONTRADICTED: <где бриф неверен или none> | FOUND: <новые дефекты, по одному на строку>`;

const PACKETS = [
	{
		id: "OO1",
		label: "пять проверок нейро-ветки зелены на полностью сломанном коде",
		files:
			"apps/api/src/pricelist/pricelistYearPrice.test.ts (правишь ТОЛЬКО набор). apps/api/src/pricelist/analyzer.ts — ЧИТАТЬ, менять только если без этого ветку не поднять, и тогда сказать",
		brief: `P0. Проверка, которая проходит на сломанном коде, хуже отсутствующей: она создаёт уверенность.

ИЗМЕРЕНО РЕВЬЮЕРОМ ВОЛНЫ NN, не выведено: он восстановил в копии РОВНО тот дефект, который чинил коммит
b1cb29df3 — нейро-ветка \`analyzePricelist\` снова зовёт \`responseFromItems\` с \`parsedRows.items\` без
\`selectPricelistServiceRows\` и без \`skippedRowsWarnings\`, — прогнал полный новый набор и получил
**EXIT=0, tests 15, pass 15, fail 0**. Все пять новых проверок задачи 2 зелены на полностью сломанном коде.

ПРИЧИНА НАЗВАНА ТОЧНО: помощник \`analyzeGroqRows\` (около pricelistYearPrice.test.ts:324-327) не ЗОВЁТ
разбор, а ПОВТОРЯЕТ последовательность боевой ветки:
  pricelistItemsFromGroqRows(...) → selectPricelistServiceRows(parsed.items, parsed.droppedRows)
То есть набор проверяет композицию двух функций, которую собирает САМ ТЕСТ. Что эту композицию собирает
\`analyzePricelist\`, не проверяется ничем. Это ровно образец «замер сравнивал две синхронные ветки».

ОТГОВОРКА ПРЕДЫДУЩЕГО ИСПОЛНИТЕЛЯ ОПРОВЕРГНУТА, НЕ ПОВТОРЯЙ ЕЁ. Он написал, что поднять ветку подделкой
fetch нельзя, потому что \`recordProviderKeySuccess\` писала бы на диск состояние здоровья ключей. У этой
записи ЕСТЬ ВЫКЛЮЧАТЕЛЬ: \`apps/api/src/speech/keyPool.ts:198-201\`, переменная среды
\`DENTAL_SPEECH_KEY_HEALTH_FILE\` со значением \`off\` возвращает \`null\` и запись отменяет. Им уже пользуются
ТРИ скрипта репозитория — посмотри, как именно: \`scripts/smoke-speech-key-rotation.mjs:24\`,
\`scripts/smoke-speech-provider-errors.mjs:109\`, \`scripts/smoke-speech-groq-chunk-floor.mjs:12\`.

ЧТО СДЕЛАТЬ: поднять НАСТОЯЩУЮ нейро-ветку — заглушка \`fetch\`, отдающая правдоподобный ответ Groq
(\`choices[0].message.content\` с JSON-объектом \`{items:[...],warnings:[...]}\`), ключ в пуле, выключатель
здоровья ключей в \`off\` — и проверять состав позиций и счёт потерь через \`analyzePricelist\` с
\`useServerAi: true\`. Ключевое утверждение пакета: ОДИН И ТОТ ЖЕ прайс даёт в детерминированном и в
нейро-режиме одинаковый состав позиций и одинаковое число потерь, и это проверено ВЫЗОВОМ, а не сборкой
последовательности в тесте.

Как выглядит успешный ответ и где ветка его разбирает — читай \`callGroqPricelist\` и \`itemFromGroq\` в
\`analyzer.ts\`. Пул ключей и выбор ключа — \`apps/api/src/speech/keyPool.ts\`.

EXIT_BITE ОБЯЗАТЕЛЕН И ЗДЕСЬ ОН — ВЕСЬ СМЫСЛ ПАКЕТА: мутация должна быть той же, что у ревьюера (нейро-ветка
зовёт \`responseFromItems\` напрямую, без гейта и без счётчика), и твой набор ОБЯЗАН на ней покраснеть.
Регресс: батарея из семи наборов прайса должна остаться зелёной, 102 теста были до тебя.`,
	},
	{
		id: "OO2",
		label: "клинике выводится английский unknown с нейро-пути",
		files:
			"apps/web/src/pricelistUiMeta.ts + при необходимости apps/api/src/pricelist/analyzer.ts (промпт) — но analyzer.ts держит OO1, поэтому согласуй: правь ТОЛЬКО промпт и скажи об этом",
		brief: `P0. Пакет прошлой волны включил мёртвую функцию — и вместе с ней включил спавший в ней дефект.

ИЗМЕРЕНО РЕВЬЮЕРОМ ВОЛНЫ NN НА ЖИВОЙ ФУНКЦИИ, вывод дословно:
  Материал по разбору: unknown
  Материал по разбору: zirconia crown
  Материал по разбору: full ceramic · Керамика · Коронка
Русской клинике показывается сырой английский токен.

МЕХАНИЗМ: \`apps/web/src/pricelistUiMeta.ts:78-82\`, \`pricelistCrownTypeLabel\` кончается
\`pricelistCrownTypeLabels[value] ?? value\` — то есть неизвестное значение печатается КАК ЕСТЬ. А \`crownType\`
в контракте объявлен \`z.string().nullable()\` (\`packages/shared/src/index.ts:1746\`), НЕ перечислением.
Системный промпт Groq (\`analyzer.ts\`, около :1325-1338) перечисляет допустимые значения для \`category\`,
\`specialty\`, \`materialKind\`, \`restorationType\` — и НЕ перечисляет для \`crownType\`, при этом прямо
разрешает: «If a material/brand/crown type is uncertain, use unknown or null». \`asString\` пропускает строку
как есть, \`|| null\` обнуляет только пустую.

ДЕТЕРМИНИРОВАННЫЙ ПУТЬ ЧИСТ, И ЭТО ВАЖНО ДЛЯ ВЫБОРА ПОЧИНКИ: \`detectCrownType\` (\`analyzer.ts:320-329\`)
отдаёт ровно 7 значений, и все 7 в карте есть. Утечка только на НЕЙРО-пути, но он продуктовый.

УРОК, КОТОРЫЙ ШИРЕ ЭТОГО ДЕФЕКТА, и его надо держать в голове всю правку: до прошлой волны функция не
звалась НИ РАЗУ, и \`?? value\` был безвреден. Включение мёртвого кода — это не «подключить готовое», это
ПЕРВОЕ ИСПОЛНЕНИЕ кода, который никто не проверял. В этой цепочке ещё восемь таких имён.

ЧТО СДЕЛАТЬ — РЕШЕНИЕ ТВОЁ, обоснуй письменно. Варианты, каждый со своей ценой:
  а) резервная ветка печатает русскую фразу вместо сырого значения. Дешево, но теряет информацию;
  б) промпт перечисляет допустимые значения \`crownType\`, как он уже делает для четырёх других полей, и
     контракт получает перечисление. Дороже, лечит причину, но затрагивает \`analyzer.ts\` (его держит OO1 —
     тогда правь ТОЛЬКО текст промпта, точечно, и скажи это в отчёте);
  в) неизвестное значение считать отсутствующим и не печатать строку материала вовсе.
Побочное наблюдение ревьюера, проверь его: при всех значениях «unknown» резервная фраза тоже вырождается.

EXIT_BITE: проверка обязана краснеть, если в карту подать значение, которого в ней нет. Проверяй ИСПОЛНЕНИЕМ
функции на таком значении, а не чтением — ревьюер прошлой волны так и сделал, повтори.`,
	},
	{
		id: "OO3",
		label: "три вкладки настроек всё ещё держат мёртвые пропсы",
		files:
			"apps/web/src/components/settings/SettingsPricesTab.tsx, SettingsAuditTab.tsx, SettingsImportsTab.tsx + при необходимости App.tsx, AppHelpers.tsx, useAppLogic.tsx (последний ГРЯЗНЫЙ — точечно!)",
		brief: `P1, остаток требования, которое прошлая волна не закрыла: «оставить протянутым нельзя».

ПЕРЕПИСЬ ВЕДУЩЕГО ПО HEAD, признак мёртвого пропса — \`rg -c <имя>\` по файлу-потребителю даёт РОВНО 1,
единственную строку деструктуризации (0 = не протянут, >1 = используется):

  имя                             PricesTab  AuditTab  ImportsTab
  pricelistWarningsText               1          1          1
  pricelistImageNote                  1          1          1
  pricelistItemMaterialText           1          1          1
  pricelistMaterialSummaryText        1          1          1
  pricelistImageName                  3          1          1
  pricelistParserModeLabels           2          1          1

Прошлая волна нарисовала часть этих поверхностей в РОДИТЕЛЕ (\`SettingsView.tsx\`), а пропсы в трёх вкладках
остались мёртвыми. Живые, не трогай: \`pricelistRecognitionBrandGroups\`,
\`pricelistRecognitionServiceGroups\`, \`pricelistSourceKind\`.

ПО КАЖДОМУ МЁРТВОМУ ИМЕНИ РЕШЕНИЕ ОДНО ИЗ ДВУХ, и оба законны:
  • вкладка рисует его САМА — если поверхность для него в этой вкладке есть по смыслу;
  • имя снимается из ВСЕЙ цепочки: \`App.tsx\` → \`AppHelpers.tsx\` → \`useAppLogic.tsx\` → вкладка.
Оставить протянутым — единственный незаконный исход, потому что это и есть дефект.

ОСТОРОЖНО, ГЛАВНАЯ ЛОВУШКА ЭТОГО ПАКЕТА: снимая имя из цепочки, снимай ЦЕЛИКОМ и проверяй \`rg -c\` по КАЖДОМУ
файлу цепочки — иначе останется пропс, который передают и не принимают, а это не лучше исходного дефекта.
И НАОБОРОТ: включая имя, помни, что оно могло не исполняться НИ РАЗУ (см. пакет OO2: включение мёртвой
функции обнажило в ней утечку английского токена на экран). Включил — прогони и посмотри, что печатается.

\`useAppLogic.tsx\` СЕЙЧАС ГРЯЗНЫЙ (другой автор). Правь его Edit по конкретным строкам, коммить только свои
пути. Если правка требует широкой перестройки этого файла — не делай, опиши в FOUND.

ДОКАЗАТЬ: \`npm run smoke:pricelist-analyzer > /tmp/oo3.log 2>&1; echo $?\` обязан быть 0 — он читает и
\`SettingsView.tsx\`, и три вкладки, и требует локализованных подписей. Плюс \`rg -c\` по каждому тронутому
имени во всех файлах цепочки, вывод дословно: ни одной единицы остаться не должно.
EXIT_BITE: покажи, что смоук КРАСНЕЕТ, если убрать одну из требуемых им локализованных подписей в копии.`,
	},
	{
		id: "OO4",
		label: "куст красных стражей: речь и диктовка",
		files:
			"ТОЛЬКО: scripts/smoke-speech-queue-source.mjs, scripts/smoke-speech-recorder-resilience-source.mjs, scripts/smoke-speech-final-ready-status-source.mjs, scripts/smoke-visit-dictation-simplified-actions-source.mjs",
		brief: `P1. Четыре стража речи и диктовки красные. Замер ведущего: из 41 стража \`-source\` было красных 31,
после двух пакетов прошлой волны стало 21 — твои четыре из них.

ТАКСОНОМИЯ УЖЕ ПОСТРОЕНА ПРЕДЫДУЩЕЙ ВОЛНОЙ, пять классов. Ставь по каждому стражу ОДИН:
  BROKEN       — скрипт физически не работает: путь собран сложением строк вместо path.join (было
                 «dental-crmpps\\web» вместо «dental-crm\\apps\\web»), вызов без обязательного аргумента,
                 needles описывают МЁРТВУЮ КОПИЮ файла. ЧИНИ СКРИПТ.
  STALE        — текст ПЕРЕЕХАЛ при разборе монолита на компоненты, поведение есть. ЧИНИ НАБОР ФАЙЛОВ.
  BRITTLE      — страж краснеет за то, что код СТАЛ БЕЗОПАСНЕЕ: искомая строка отличается от кода только
                 \`?.\`, \`?? []\`, \`!\`, переименованием переменной или пробелами. ЧИНИ ТРЕБОВАНИЕ образцом
                 (RegExp), закрепляя СВЯЗЬ, а не обёртку. Подставлять сегодняшнее написание НЕЛЬЗЯ — это
                 снова прибивает стража к имени переменной.
  RESURRECTION — страж требует ВЕРНУТЬ УДАЛЁННЫЙ ДЕФЕКТ (найден в прошлой волне дважды). Требование снять,
                 записав причину; молча не стирать.
  REAL         — поведения в продукте НЕТ. НЕ ЧИНИ ПРОДУКТ, запиши в FOUND точную строку требования и
                 команду \`rg\`, которой установил отсутствие.
Образцы правок смотри в истории: \`bc0c1e7db\` (BRITTLE, с доказательством кусаемости 4/4), \`691059455\`
(BROKEN + объявленный долг с храповиком), \`a3a0d45d5\` и \`7067b05bf\` (RESURRECTION), \`46c34f548\` (мёртвая копия).

ЗАПРЕЩЕНО ДЕЛАТЬ СТРАЖ ЗЕЛЁНЫМ, УДАЛИВ ТРЕБОВАНИЕ БЕЗ ПРИЧИНЫ. Снятое молча требование — стёртая функция.
Идиом объявленного долга с храповиком в обратную сторону (падает, когда долг закрыт) — в \`691059455\`.

ОСОБОЕ ПРО РЕЧЬ: у пула ключей есть выключатель записи здоровья \`DENTAL_SPEECH_KEY_HEALTH_FILE=off\`
(\`apps/api/src/speech/keyPool.ts:198-201\`), им пользуются три смоука речи. Если страж падает из-за записи
состояния на диск — причина скорее всего здесь.

ДОКАЗАТЬ: таблица страж | класс | код выхода ДО | ПОСЛЕ, истинный код без конвейера по каждому из четырёх.
EXIT_BITE обязателен для КАЖДОГО ослабленного требования: прогон образца по искусственно сломанной копии
исходника в памяти, продукт не трогая. Класс REAL остаётся красным законно — так и напиши, это не провал.`,
	},
	{
		id: "OO5",
		label: "куст красных стражей: dicom и снимки",
		files:
			"ТОЛЬКО: scripts/smoke-imaging-viewer-usability-source.mjs, scripts/smoke-browser-imaging-scan-progress-source.mjs, scripts/smoke-api-dicom-scan-abort-yield-source.mjs, scripts/smoke-dicom-workbench-offline-source.mjs, scripts/smoke-dicom-workbench-ui-cancel-source.mjs",
		brief: `P1. Пять стражей dicom и снимков красные — самый крупный куст из оставшихся 21.

ТАКСОНОМИЯ, ставь по каждому ОДИН класс:
  BROKEN       — скрипт не работает: путь собран сложением строк, вызов без обязательного аргумента, needles
                 описывают МЁРТВУЮ КОПИЮ файла. ЧИНИ СКРИПТ.
  STALE        — текст ПЕРЕЕХАЛ при разборе монолита, поведение есть. ЧИНИ НАБОР ФАЙЛОВ, которые он читает.
  BRITTLE      — краснеет за \`?.\`, \`?? []\`, переименование, пробелы, то есть за улучшение кода. ЧИНИ
                 ТРЕБОВАНИЕ образцом (RegExp), закрепляя СВЯЗЬ, а не обёртку вокруг неё.
  RESURRECTION — требует вернуть УДАЛЁННЫЙ дефект. Снять с причиной.
  REAL         — поведения нет. В FOUND, с точной строкой требования и командой \`rg\`.
Образцы: \`bc0c1e7db\`, \`691059455\`, \`a3a0d45d5\`, \`7067b05bf\`, \`46c34f548\`.

ИЗВЕСТНЫЕ СООБЩЕНИЯ ДВУХ ИЗ НИХ, снятые ведущим — читай их как ПРОДУКТОВЫЕ утверждения, а не как строки:
  smoke:imaging-viewer-usability-source → «Imaging viewer primary study image must decode asynchronously
                                          without lazy-loading the act…»
  smoke:browser-imaging-scan-progress-source → «Settings browser imaging scan UI missing marker:
                                          browser-cancel-local-imaging-folder-scan»
Второе выглядит как маркер \`data-testid\`, а такие маркеры в этом репозитории уже находили ЗАЯВЛЕННЫМИ В
КОММЕНТАРИИ вместо разметки: в \`apps/web/src/useSettingsDerivations.tsx\` лежит стена из 517 строк
\`// Compliance: …\`, и 36 из них не существуют больше НИГДЕ, кроме комментария. Проверь, не тот ли случай:
если маркер есть только в \`// Compliance:\`, это REAL, а не STALE, и в FOUND надо сказать именно так.
Стену комментариев саму НЕ ТРОГАЙ — ни один страж её не читает (\`rg -l useSettingsDerivations scripts/\` даёт 0),
это отдельный пункт очереди ведущего.

ДОКАЗАТЬ: таблица страж | класс | код выхода ДО | ПОСЛЕ по каждому из пяти, истинный код без конвейера.
EXIT_BITE обязателен для каждого ослабленного требования — прогон по искусственно сломанной копии в памяти.`,
	},
	{
		id: "OO6",
		label: "куст красных стражей: telegram и документы",
		files:
			"ТОЛЬКО: scripts/smoke-telegram-outbox-sla-source.mjs, scripts/smoke-telegram-url-ui-source.mjs, scripts/smoke-telegram-handoff-source.mjs, scripts/smoke-document-html-preview-source.mjs, scripts/smoke-document-payload-ui-source.mjs",
		brief: `P1. Пять стражей telegram и документов красные.

ТАКСОНОМИЯ, ставь по каждому ОДИН класс: BROKEN (скрипт не работает — путь сложением строк, вызов без
обязательного аргумента, needles про МЁРТВУЮ КОПИЮ) · STALE (текст переехал при разборе монолита, поведение
есть — чини набор файлов) · BRITTLE (краснеет за \`?.\`, \`?? []\`, переименование — чини требование RegExp,
закрепляя СВЯЗЬ, а не обёртку) · RESURRECTION (требует вернуть УДАЛЁННЫЙ дефект — снять с причиной) ·
REAL (поведения нет — в FOUND с точной строкой и командой \`rg\`, продукт НЕ чинить).
Образцы: \`bc0c1e7db\`, \`691059455\`, \`a3a0d45d5\`, \`7067b05bf\`, \`46c34f548\`.

ИЗВЕСТНОЕ ПО ДВУМ, снято ведущим:
  smoke:telegram-url-ui-source        → «saveTelegramSettings body not found»
  smoke:document-html-preview-source  → «Missing source end after async function
                                        openIssuedDocumentHtml(documentId: string): async …»
Второе сообщение само по себе подозрительно: страж ищет ТЕКСТ ОКОНЧАНИЯ функции по её сигнатуре, то есть
разбирает TypeScript текстовым поиском. В этом же цикле пакет MM2 заменил рукописное вырезание комментариев
на настоящий компилятор (\`import ts from "typescript"\`, \`ts.isStringLiteralLike\`), потому что рукописный
разбор терял шаблонные строки и не отличал закомментированный \`app.get(...)\` от живого. Если твой страж
падает именно на разборе синтаксиса — тот же ход законен и предпочтителен: где нужен разбор TypeScript,
надо звать TypeScript. \`typescript\` уже в зависимостях.

ОСОБО ПРО TELEGRAM: \`apps/api/src/routes/telegram.ts\` — файл, в котором скрипт с regex-хирургией однажды
уничтожил 10 554 кириллических символа. Читать можно, ПРАВИТЬ его в этом пакете НЕ НУЖНО и не надо: твой
предмет — стражи в \`scripts/\`. Если найдёшь дефект в самом \`telegram.ts\` — в FOUND, не в правку.

ДОКАЗАТЬ: таблица страж | класс | код выхода ДО | ПОСЛЕ по каждому из пяти, истинный код без конвейера.
EXIT_BITE обязателен для каждого ослабленного требования.`,
	},
];

phase("Build");

const done = await pipeline(
	PACKETS,
	(packet) =>
		agent(
			`${LAW}\n\n=== ТВОЙ ПАКЕТ ${packet.id}: ${packet.label} ===\nФАЙЛЫ, КОТОРЫЕ ТЫ ДЕРЖИШЬ: ${packet.files}\nЧужих файлов не трогай — их держат другие агенты этой же волны.\n\n${packet.brief}`,
			{ label: `build:${packet.id}`, phase: "Build", effort: "high" },
		),
	(buildResult, packet) => {
		if (!buildResult) return null;
		return agent(
			`Ты ревьюер пакета ${packet.id} в волне OO. Проект C:\\Clinic_MVP\\dental-crm.
Ты НЕ ПРАВИШЬ КОД. Пишешь ОДИН файл: .agents/archon/packets/${packet.id}/review.md, коммитишь только его
(ретрай 20×6 с вокруг git add И git commit, сообщение в файл, [ARCHON] в теме, без атрибуции инструмента,
git log -1 --format='%(trailers)' пуст). НЕ ПУШИТЬ.

ПЕРВОЙ СТРОКОЙ review.md — вердикт в форме РОВНО:
## VERDICT: <ОДНО СЛОВО>
одно слово из: SOUND, SOUND_WITH_NITS, NEEDS_REWORK, REVERT. НЕ КОПИРУЙ СПИСОК ВАРИАНТОВ — строка с четырьмя
словами вердиктом не считается, ведущий уже доложил по такой строке два ложных SOUND и сам себя опроверг.
Пиши сразу, правь по ходу. Пустое тело вердиктом тоже не считается.

=== БРИФ ИСПОЛНИТЕЛЯ (суди по нему) ===
${packet.brief}

=== ЧТО ОН ВЕРНУЛ ===
${buildResult}

ПРОВЕРЯЙ ПО ДИСКУ, НЕ ПО ЕГО СЛОВАМ:
1. ГЛАВНОЕ В ЭТОЙ ВОЛНЕ: он обязан был вернуть EXIT_PASS, EXIT_BITE и команду MUTATION. Если EXIT_BITE нет —
   это НЕВЫПОЛНЕННЫЙ пакет (NEEDS_REWORK), даже когда правка выглядит верной. Если EXIT_BITE есть — ПРОВЕРЬ
   ЕГО САМ: повтори мутацию и убедись, что набор действительно краснеет. Мутация, ломающая компиляцию вместо
   поведения, доказательством НЕ является.
2. Коммиты существуют? git show --stat. Только заявленные пути? Трейлеры пусты? Чужая незакоммиченная работа
   не подметена?
3. Решена ЗАЯВЛЕННАЯ задача или соседняя подешевле?
4. Прогони его проверку сам, истинный код без конвейера: cmd > /tmp/rev-${packet.id}.log 2>&1; echo $?
   Общие гейты (полный typecheck, build, весь npm test) НЕ ЗАПУСКАЙ. Отдельные smoke:* по предмету — НУЖНО.
5. Есть ли зелёный тест, не проверяющий ничего. Мысленно сломай правку и спроси, покраснеет ли. В этой
   кампании класс встречался ПЯТЬ раз, включая прошлую волну дважды: тест читал поля из JSON.parse(body) без
   проверки статуса; «замер» сравнивал две синхронные ветки; страж прайса падал до первого из одиннадцати
   утверждений; помощник теста повторял последовательность боевой ветки вместо её вызова.
6. Не подставлен ли ноль вместо «неизвестно» (\`?? 0\`, \`|| 0\`).
7. Если пакет ВКЛЮЧАЛ мёртвый код — проверь, что включённое не льёт на экран сырых значений. В прошлой волне
   включение мёртвой функции обнажило английский токен \`unknown\` в интерфейсе русской клиники.
8. REVERT только при реальном вреде: потеря чужих правок, утечка секрета, разрушение кириллицы, запись в
   исходники скриптом. Несделанная работа — NEEDS_REWORK.

Дальше: что проверено командой дословно с кодом выхода, что осталось, и раздел «## FOUND» с новыми дефектами.
FOUND — самая ценная часть твоей работы: в прошлых двух волнах ревьюеры нашли то, чего не видел никто —
год как цену услуги, зелёный тест на сломанном коде, английский токен на экране и регресс, вернувший уже
закрытый P0. Не оставляй раздел пустым из вежливости, но и выдуманный дефект хуже пустого.`,
			{ label: `review:${packet.id}`, phase: "Review", effort: "high" },
		);
	},
);

log(
	`волна OO завершена: ${done.filter(Boolean).length}/${PACKETS.length} пакетов вернули ревью`,
);

return {
  packets: PACKETS.map((packet, index) => ({ id: packet.id, label: packet.label, review: done[index] ?? null }))
};
