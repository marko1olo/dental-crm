[ARCHON] docs(кампания): ведущий заразил коммит в третий раз — собственным же правилом

Коммит a457fb49f должен был нести два пути: удаление виджета логов внешних ботов и снятие его
монтирования из ScheduleView.tsx. Он унёс с собой ещё и удаление
apps/web/src/components/documents/NdflTaxCalculatorsWidget.tsx, которое лежало подготовленным в
общем индексе от другого автора.

ПРИЧИНА: я выполнил git commit -F <сообщение> БЕЗ -- <пути>. Правило, запрещающее ровно это,
написал я сам, оно стоит в каждом скрипте цикла словами «всегда коммить с явным списком путей», и
я его опустил. Это третий мой личный случай после 8c87dcd93 и попытки снять dist с учёта.

УЩЕРБ ОГРАНИЧЕН, HEAD НЕ СЛОМАН. Проверено: git grep по HEAD не находит ни одного живого импорта
NdflTaxCalculatorsWidget, остались только пояснения в apps/api/src/tests/
webCallsExistingRoutes.test.ts:57 и :257, где второй автор сам пишет, что удалил виджет «вместе с
обещанием». То есть удаление было им задумано — я закоммитил его раньше времени и под сообщением
про другой виджет. Его правка теста при этом осталась несохранённой, поэтому его работа теперь
легла наполовину. Веб-гейт зелёный и до, и после: 0 ошибок, причём в рабочем дереве файла уже не
было в момент прогона.

Историю не переписываю: коммит отправлен, и в ветку параллельно коммитит второй автор.

ПРОЦЕДУРНОЕ ИСПРАВЛЕНИЕ, ЭТО НА МНЕ. Печатать git diff --cached --name-only в ОДНОЙ команде с
коммитом бесполезно: заражение я увидел в выводе уже после того, как оно случилось. Дальше
последовательность из трёх отдельных шагов — подготовить, посмотреть список глазами, коммитить со
списком путей. Один список путей предотвратил бы это независимо от того, что ещё лежит в индексе.
