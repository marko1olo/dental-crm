[ARCHON] типы(api): дотянут уже существовавший тип снимка и назван unknown у сырой строки SQL — неявных any 8 → 5

Продолжение находки о том, что "strict": true в базе объявлен и тут же снят через "noImplicitAny": false.
Цена включения по областям измерена: packages/shared 0 (включено, 030897b0b), apps/api 8, apps/web 149.

ЧИСЛО «7» ИЗ ПРОШЛОГО ОТЧЁТА БЫЛО НЕВЕРНЫМ: их восемь, и они РАЗНОЙ ПРИРОДЫ. Поштучный разбор оказался
важнее числа:
  • 3 сводятся к ОДНОЙ отсутствующей зависимости @types/ws (TS7016 в websocket.ts:30 и websocketBroker.ts:1,
    плюс TS7006 у `raw` в websocket.ts:107 как следствие). Это не правка кода вовсе;
  • 2 лежат в apps/api/src/scripts/seedOpsScreenshotDemo.ts — скрипт-СИРОТА, ноль упоминаний в package.json
    (перепись 13e8e8e06);
  • 1 был в routes/telegram.ts:2300 и УШЁЛ САМ, пока я разбирался: его закрыл пакет QQ6, который держит этот
    файл в текущей волне;
  • 2 живых и безрисковых — их и правлю здесь.

ЧТО ИСПРАВЛЕНО:

1. routes/imaging.ts — `function previewSvg(study: any)` стало `study: ImagingStudy`. ТИП УЖЕ БЫЛ, его просто
   не дотянули: getImagingStudyById (db/imagingQuery.ts:95) объявлен как Promise<ImagingStudy | null>,
   единственный вызывающий (:6725) зовёт функцию ПОСЛЕ проверки на null, а читает она ровно четыре поля —
   kind, title, toothCode, region, — и все четыре есть в imagingStudySchema. Второго владельца типа не создано.
   Цена any здесь не абстрактная: kindLabels[study.kind] с посторонним значением молча даёт undefined, и в
   SVG-предпросмотр снимка уехала бы пустая подпись вместо названия исследования.

2. db/clinicalTasksQuery.ts:229 — `(row)` стало `(row: unknown)`. У сырого SQL через db.execute тип строки
   неизвестен, а приведение к Record<string, unknown> уже стояло внутри вызова; аннотация делает это явным и
   поведения не меняет.

ГЕЙТЫ, истинные коды выхода без конвейера, с ЧИСЛОМ тестов:
  npm run typecheck -w @dental/api                          EXIT=0
  npx tsc -p apps/api/tsconfig.json --noEmit --noImplicitAny  осталось 5 (было 8)
  тесты снимков (3 файла)                                    EXIT=0, 5 тестов, 5 прошло, 0 пропущено
  тесты клинических задач                                    EXIT=0, 20 тестов, 20 прошло, 0 пропущено

ПРАВИЛО ЭТОГО ЖЕ ЦИКЛА СРАБОТАЛО НА МНЕ ЧЕРЕЗ ДЕСЯТЬ МИНУТ ПОСЛЕ ТОГО, КАК Я ЕГО ВПИСАЛ. Первый прогон
тестов снимков я дал маской "src/tests/routes/imaging*.test.ts" и получил EXIT=0 при «tests 0 pass 0»: маска
не совпала ни с чем, код выхода был ложным зелёным. Поймал только потому, что печатаю COUNT. Настоящие тесты
снимков лежат в трёх других местах — src/routes/imaging.test.ts, src/routes/tests/imaging.test.ts,
src/tests/imaging/parseImagingManifest.test.ts. Это ровно тот класс, из-за которого COUNT и затребован от
исполнителей: код выхода необходим и НЕ достаточен.

ЧТО ОСТАЛОСЬ ДЛЯ ВКЛЮЧЕНИЯ noImplicitAny В api: поставить @types/ws (закроет 3 из 5) и решить судьбу
скрипта-сироты (2 из 5). Установку зависимости НЕ делаю сейчас намеренно: npm install при работающей волне
из шести пакетов перепишет lock-файл и потревожит node_modules под нагрузкой. Это дисциплина машины, а не
осторожность ради осторожности.
