/**
 * УДАЛЕНО 2026-08-06. Локальный наблюдатель за папкой рентгена был удалён: снимки
 * загружаются через интерфейс VisiographAnalyzer напрямую. Функция оставлена пустой,
 * чтобы не ломать вызов из server.ts, но она больше ничего не сторожит.
 *
 * РЕШЕНИЕ: удалить и саму функцию, и её вызов, если владелец подтвердит, что
 * никакого другого сторожения (пул соединений, зависшие транзакции, переполнение
 * очередей) не планируется. Оставлять пустую функцию с живым вызовом — это
 * обещание без реализации.
 */
export function startWatchdog() {
  // Намеренно пусто — см. докстринг выше.
}
