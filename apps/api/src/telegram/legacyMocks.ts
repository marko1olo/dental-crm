/**
 * telegram/legacyMocks.ts — совместимый реэкспорт единственного хранилища.
 *
 * ЧТО БЫЛО НЕ ТАК
 *
 * Этот файл был ПОЛНОЙ КОПИЕЙ apps/api/src/sampleData.ts: 12 531 строка,
 * 439 КБ, отличий — около 260 строк. Рядом лежала третья копия,
 * sampleData_opt.ts (429 КБ), на которую не ссылался никто. Все три файла
 * были записаны в пределах девяти миллисекунд — это следы одной операции
 * копирования, после которой правки расходились по копиям независимо.
 *
 * Третья копия УДАЛЕНА вместе со своим генератором `test_loop_perf.cjs` и
 * замером `measure_perf.cjs`, который сравнивал её с оригиналом по сборке
 * `dist/sampleData_opt.js` — а такой сборки в дереве не было вовсе. Пока копия
 * лежала, поиск по `buildBillingSummary` давал ДВЕ реализации, и у чинящего был
 * ровно половинный шанс починить мёртвую.
 *
 * Из-за копии в приложении существовало ДВА независимых экземпляра одного и
 * того же изменяемого состояния (appointments, patients, payments,
 * communicationTasks, documents, treatmentPlanItems …):
 *
 *   • sampleData.ts        — его импортируют db/appointmentsQuery.ts,
 *                            db/clinicalQuery.ts, db/dashboardQuery.ts,
 *                            db/documentQuery.ts, db/patientsQuery.ts;
 *   • telegram/legacyMocks — его импортировал routes/telegram.ts.
 *
 * Оба модуля на старте вызывали applyPersistentState() и оба имели по 32
 * вызова persistMutableState() — то есть писали ВЕСЬ свой снимок состояния в
 * ОДИН И ТОТ ЖЕ файл .data/dental-crm-state.json.
 *
 * ПОСЛЕДСТВИЕ. Любое действие в Telegram (привязка чата, выдача кода, отметка
 * о доставке) сохраняло снимок СВОЕЙ копии и затирало в файле всё, что за это
 * время записала копия из sampleData: заведённых пациентов, созданные записи,
 * платежи. После перезапуска они не восстанавливались. И наоборот: сохранение
 * из sampleData откатывало привязки Telegram-чатов.
 *
 * ЧТО СТАЛО. Копия удалена, остался один модуль состояния — sampleData.ts.
 * Путь импорта сохранён, чтобы не ломать существующие тесты и импорты.
 *
 * Полезные отличия, которые жили только в копии, перенесены в sampleData.ts:
 * группировка по пациенту вместо шести полных проходов на каждого пациента в
 * buildPatientInsights, индекс активных пациентов в построителе налоговых
 * запросов, единый проход по фрагментам речи, getServiceCatalogItem с
 * запасным поиском по прайсу.
 *
 * НЕ перенесены сознательно: ветки process.env.DENTAL_MOCK_* в
 * upsertVisitDraftAutosave и acceptVisitDraft. Они позволяли переменной
 * окружения уронить сохранение медицинского черновика, при этом ни один тест
 * их не использовал.
 *
 * @deprecated Импортируйте напрямую из "../sampleData.js".
 */

export * from "../sampleData.js";
