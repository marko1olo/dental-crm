/**
 * Messaging Template Engine (Facade-Delegate per Mandates 8s, 8j)
 *
 * Consolidates messaging template interpolation over canonical SSOT in @dental/shared.
 * Preserves all function interfaces, types, and catalogs for messaging routes and notification gateways.
 */

import {
	BUILT_IN_TEMPLATES,
	formatKopecksRu,
	formatMoney,
	interpolateMessageTemplate,
	interpolateVariables,
	kopecksToNumericString,
	parseKopecks,
	renderMessageTemplate,
	rublesToKopecks,
	TemplateEngine,
	templateEngine,
	type InteractiveButton,
	type RenderedTemplate,
	type SupportedLocale,
	type TemplateDefinition,
} from "@dental/shared";

export {
	BUILT_IN_TEMPLATES,
	formatKopecksRu,
	formatMoney,
	interpolateMessageTemplate,
	interpolateVariables,
	kopecksToNumericString,
	parseKopecks,
	renderMessageTemplate,
	rublesToKopecks,
	TemplateEngine,
	templateEngine,
	type InteractiveButton,
	type RenderedTemplate,
	type SupportedLocale,
	type TemplateDefinition,
};


