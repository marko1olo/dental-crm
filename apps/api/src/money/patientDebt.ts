import {
	formatKopecksRu,
	type Kopecks,
	kopecksToNumericString,
	multiplyKopecks,
	parseKopecks,
	sumKopecks,
} from "@dental/shared";

/**
 * patientDebt.ts — ОДИН дом для вопроса «сколько пациент должен клинике».
 *
 * ═══════════════════════════════════════════════════════════════════════════
 * ЗАЧЕМ ЭТОТ ФАЙЛ СУЩЕСТВУЕТ
 * ═══════════════════════════════════════════════════════════════════════════
 *
 * До него долг считался в ДЕВЯТИ местах, и они давали разные ответы на одних и
 * тех же данных. Измерено 2026-07-29 прямым SQL к живой базе `dental_crm`,
 * клиника `d0000000-…-00000000d001`, 10 позиций лечения и 8 оплат:
 *
 *   отчёт дебиторки  (managerReports.ts, receivables)     →  53 000,00 ₽
 *   главный экран    (sampleData.ts, buildBillingSummary)  →  51 400,00 ₽
 *   картотека        (patientsQuery.ts, rowToPatient)      →       0,00 ₽
 *
 * Разница между первыми двумя — ровно 1 600,00 ₽, это две переплаты по 800,00 ₽.
 * Главный экран вычитает оплаченное из назначенного ПО ВСЕЙ КЛИНИКЕ одним
 * действием, поэтому переплата одного пациента молча гасит долг другого. Третий
 * ответ — константа `0`, не связанная с данными вообще.
 *
 * Что это значит для клиники, а не для кода: администратор открывает отчёт
 * дебиторки, звонит пациенту и называет сумму, которой нет на главном экране;
 * оператор видит два разных долга на двух экранах и не знает, какому верить; два
 * пациента, переплатившие по 800 ₽, не видны ни в одном отчёте, и клиника не
 * знает, что должна им деньги.
 *
 * Полный разбор всех девяти мест с числами, вердиктами и порядком переезда:
 * `.agents/lead/recon-debt-formula-sprawl.md`.
 *
 * ═══════════════════════════════════════════════════════════════════════════
 * ЧТО ЭТОТ ФАЙЛ НЕ ДЕЛАЕТ: ВТОРОЙ РЕАЛИЗАЦИИ ДЕНЕГ ЗДЕСЬ НЕТ
 * ═══════════════════════════════════════════════════════════════════════════
 *
 * Копейки, разбор строки колонки `numeric`, сложение, умножение на количество и
 * печать суммы живут в ОДНОМ месте на весь репозиторий —
 * `packages/shared/src/utils/money.ts` — и берутся отсюда импортом. Правило это
 * не моё: оно уже записано в `apps/api/src/documents/guards.ts` словами
 * «Считать деньги здесь теперь нечем, кроме `packages/shared/src/utils/money.ts`;
 * третьей копии не создаётся», после того как вторую копию в этом дереве нашли
 * дважды и одна из них отказывала законной квитанции.
 *
 * ЧЕСТНО О СВОЕЙ ОШИБКЕ: первую версию этого файла я написал с собственным
 * разбором копеек, не найдя существующий модуль — то есть ровно та болезнь, от
 * которой лечит этот файл, и первым её носителем оказался я. Переписано до
 * коммита. Здесь остался ТОЛЬКО слой долга: чем этот слой отличается от общего,
 * сказано ниже поимённо, и каждое отличие сужает поведение, а не меняет его.
 *
 * ОТЛИЧИЕ 1. `parseKopecks` на НЕЦЕЛОМ ЧИСЛЕ округляет через `toFixed(2)` — по
 * замыслу, как «то, что записалось бы в numeric(12, 2)». Для общего разбора это
 * верно, для расчёта долга — нет: так `1500.505` молча становится `1500.51`, а
 * `4500.299999999999` (это `1500.10 * 3` в плавающей точке) молча становится
 * `4500.30`. Расчёт долга обязан УЗНАТЬ, что кто-то уже посчитал деньги в
 * плавающей точке, а не подтвердить это своей подписью. Поэтому здесь вход из
 * числа проходит через `kopecksFromRubles`, который такие значения ОТВЕРГАЕТ. На
 * чистых суммах поведение побитово совпадает с `parseKopecks`; расходятся они
 * только на грязи, и там громкий отказ честнее тихого округления.
 *
 * ОТЛИЧИЕ 2. `parseKopecks(null)` и `parseKopecks("")` законно дают 0. Здесь
 * отсутствующая цена — отказ: «сумма не пришла» и «лечение бесплатное» это разные
 * факты, а колонки `unit_price_rub`, `discount_rub` и `amount_rub` объявлены
 * `not null`, поэтому пустое значение означает поломку чтения, а не ноль.
 *
 * ОТЛИЧИЕ 3. `multiplyKopecks` разрешает количество 0. Здесь ноль — отказ:
 * позицию без объёма надо помечать статусом `cancelled`, а не выставлять счёт.
 *
 * ═══════════════════════════════════════════════════════════════════════════
 * КАКАЯ ФОРМУЛА БЫЛА КАНОНИЧЕСКОЙ И ПОЧЕМУ ОСТАЛЬНЫЕ ЕЙ НЕ РАВНЫ
 *
 * Это ЕДИНСТВЕННОЕ место в проекте, где причина записана. Не удаляй её вместе с
 * рефакторингом: без неё следующий агент выберет формулу заново, и их снова
 * станет девять.
 * ═══════════════════════════════════════════════════════════════════════════
 *
 * КАНОН — выражение из отчёта дебиторки, `apps/api/src/services/reports/
 * managerReports.ts`, `receivables()`:
 *
 *     строка позиции = greatest(unit_price_rub * quantity - discount_rub, 0)
 *     сальдо пациента = Σ строк(status <> 'cancelled') - Σ оплат(status = 'paid')
 *     дебиторка клиники = Σ сальдо по пациентам, у которых сальдо >= порога
 *
 * Канон выбран не по вкусу, а по пяти проверяемым признакам:
 *
 *  1. Считает ПО ПАЦИЕНТУ. Долг — величина человека, а не клиники: клиника не
 *     «должна себе». Именно поэтому агрегат по всей клинике (главный экран) не
 *     может быть каноном — он теряет то, ради чего долг вообще считают, то есть
 *     кому звонить.
 *  2. Не смешивает переплату одного с долгом другого. Деньги пациента А не
 *     гасят обязательство пациента Б ни юридически, ни бухгалтерски.
 *  3. Арифметика точная: суммирование идёт в `numeric` на стороне PostgreSQL,
 *     приведение `::numeric(12,2)` стоит на уровне суммы, копейки не теряются.
 *  4. `greatest(…, 0)` на СТРОКЕ не даёт отрицательной позиции (скидка больше
 *     цены) тайно уменьшить долг по другим позициям того же пациента.
 *  5. Отчёт САМ печатает свою несходимость с главным экраном
 *     («53 000,00 − 1 600,00 = 51 400,00 ₽»), то есть его автор уже знал, что
 *     второй ответ существует, и назвал разницу. Остальные восемь мест молчат.
 *
 * ПОЧЕМУ ОСТАЛЬНЫЕ ЕМУ НЕ РАВНЫ — по одной причине на каждое:
 *
 *  • `sampleData.ts:1349` `buildBillingSummary().totalDueRub` = 51 400,00.
 *    НЕ равно, потому что считает НЕТТО ПО КЛИНИКЕ: `Σназначено − Σоплачено`
 *    одним вычитанием. Это законная бухгалтерская величина («сколько ещё не
 *    собрано»), но она отвечает на другой вопрос, а называется долгом. Здесь
 *    она называется `netUncollectedKopecks`.
 *
 *  • `sampleData_opt.ts:1087` — тот же расчёт БЕЗ единого округления. Мёртвый:
 *    ноль импортов, исключён из `apps/api/tsconfig.json` и `tsconfig.tests.json`.
 *    Опасен не тем, что работает, а тем, что `rg buildBillingSummary` находит две
 *    реализации, и половина шансов, что правку внесут в мёртвую. Подлежит
 *    удалению отдельным коммитом.
 *
 *  • `domainStateHydration.ts:546` `patient.balanceRub` = `Math.round(оплачено −
 *    назначено)`. НЕ равно по двум причинам: обратный знак и ОКРУГЛЕНИЕ ДО
 *    ЦЕЛОГО РУБЛЯ. Округление несимметрично: `Math.round(-49899.5) = -49899`, а
 *    `Math.round(49899.5) = 49900` — должнику 50 копеек прощают, переплатившему
 *    приписывают. Контракт копейки уже разрешил (`moneyRubSchema`), а этот
 *    `Math.round` остался на месте.
 *
 *  • `patientsQuery.ts:67` `balanceRub: 0`. НЕ равно, потому что это не формула,
 *    а константа: на семи живых пациентах с ненулевым сальдо отвечает `0` семь
 *    раз из семи.
 *
 *  • `sampleData.ts:1720` `patientInsight.balanceDueRub`. Формула та же, но
 *    позиции берутся БЕЗ фильтра `status !== "cancelled"` — отменённое лечение
 *    продолжает висеть на пациенте долгом ровно там, где администратор читает
 *    сумму перед звонком.
 *
 *  • `sampleData.ts:8575` `patientPaymentBalanceRub`. Формула та же, но без
 *    округления, и её результат уходит в ключ идемпотентности напоминания
 *    округлённым до рубля (`:8548`). Доплата 40 копеек ключ не меняет, значит
 *    второе напоминание не уйдёт никогда.
 *
 *  • `(цена − скидка) × количество` вместо `цена × количество − скидка`. НЕ равно
 *    на величину `скидка × (кол-во − 1)`: на живых строках со скидкой 800 ₽ при
 *    количестве 2 это 1 600,00 ₽. Скидка в этом проекте задана строкой позиции
 *    (`treatment_items.discount_rub`, `numeric(12,2)` — `db/schema.ts`), а не
 *    ценой за единицу, поэтому вычитать её надо ОДИН раз из итога строки.
 *    ФАЙЛОВ С ЭТОЙ ФОРМУЛОЙ В ДЕРЕВЕ БОЛЬШЕ НЕТ: их было два,
 *    `apps/api/src/treatment/TreatmentPlanBuilder.ts` и его дословная копия в
 *    корне репозитория `full_treatment_plan.ts`, оба с нулём импортёров, оба
 *    удалены 2026-08-05/06. Ссылаться на них здесь больше нельзя, а довод выше
 *    от их удаления не изменился: он держится на колонке, а не на файле.
 *    Сама разность закреплена прогоняемым тестом (`patientDebt.test.ts`,
 *    «расхождение равно скидка × (количество − 1) на любом количестве»), поэтому
 *    она проверяется, а не только описывается.
 *
 *  • `useAppLogic.tsx:5171` (веб) вычитает ещё и страховое покрытие под тем же
 *    именем поля `totalDueRub`. Это ШЕСТАЯ величина («к оплате пациентом после
 *    страховки»), а не шестая формула долга, и в этот модуль она не входит:
 *    сервер данных договора ДМС в сводку не отдаёт вовсе.
 *
 * ГДЕ МОЙ МОДУЛЬ СОЗНАТЕЛЬНО ОТСТУПАЛ ОТ КАНОНА — И ПОЧЕМУ ОТСТУПАТЬ БОЛЬШЕ НЕ
 * ОТ ЧЕГО. Отступление было одно: канон в SQL писал `greatest(quantity, 1)`, а
 * `domainStateHydration` ещё и `Math.round(quantity)`. Оба — тихие правки данных,
 * и здесь вместо них стоит отказ. К 2026-08-06 обе тихие правки из дерева ушли:
 * `domainStateHydration` удалён вместе со своим `Math.round`, а
 * `greatest(quantity, 1)` убран из `services/reports/managerReports.ts` (три
 * места) и из `services/communications/audience.ts` — разбор стоит в первом из
 * них. Больше того, с миграции 0162 количество не может нарушить контракт даже
 * прямым SQL: у колонки есть ограничения `quantity > 0` и
 * `quantity = trunc(quantity)`.
 *
 * Требование этого модуля не изменилось и не смягчается: количество обязано
 * прийти целым положительным, ровно как объявлено в общем контракте
 * (`packages/shared/src/index.ts:1819`, `quantity: z.number().int().positive()`),
 * иначе выбрасывается исключение. Проверка остаётся, потому что ограничение базы
 * ловит не всё: колонка `numeric(10, 2)` приводит вход к двум знакам ДО проверки,
 * поэтому `1.004` дойдёт до расчёта как `1.00`, а контракт такое число отвергает.
 * На контрактно верных данных все поведения совпадают побитово (проверено: в
 * живой базе `min(quantity) = max(quantity) = 1.00`, дробных нет).
 *
 * ═══════════════════════════════════════════════════════════════════════════
 * КОПЕЙКИ: ПОЧЕМУ ВНУТРИ НЕТ НИ ОДНОГО РУБЛЯ В ПЛАВАЮЩЕЙ ТОЧКЕ
 * ═══════════════════════════════════════════════════════════════════════════
 *
 * Деньги приходят из колонок `numeric(12,2)` и обязаны уйти обратно теми же.
 * Плавающая точка этого не гарантирует, и это измерено, а не предположено:
 *
 *     1500.10 * 3                  → 4500.299999999999
 *     1000.00 + 1001.82 + 1489.67  → 3491.4900000000002
 *     0.1 + 0.2                    → 0.30000000000000004
 *
 * `3491.4900000000002` — это ПРОВАЛ, а не «то же самое, что 3491,49». Такая
 * сумма не проходит `moneyRubSchema` (третий знак после запятой отвергается,
 * `packages/shared/src/tests/money-contract-kopecks.test.ts`), то есть маршрут
 * ответит ошибкой на верных данных, а если и пройдёт — колонка `numeric(12,2)`
 * молча обрежет её, и на руках у пациента и в базе окажутся разные суммы.
 *
 * Поэтому здесь нет ни одного сложения рублей. Всё внутри — целые копейки
 * (`Kopecks` из общего модуля), точные до 2^53 копеек ≈ 9·10^13 ₽, что на семь
 * порядков выше предела колонки `numeric(12,2)`.
 */

export type { Kopecks };

/** Текст денежной колонки `numeric(12, 2)`: `"1500.50"`, `"-800.00"`, `"0"`. */
export type NumericText = string;

/** Сумма на входе: текст колонки `numeric` либо число рублей с ≤ 2 знаками. */
export type MoneyInput = NumericText | number;

/** Статус позиции лечения, который канон исключает из назначенного. */
export const CANCELLED_ITEM_STATUS = "cancelled";

/** Статус платежа, который канон считает полученными деньгами. */
export const PAID_PAYMENT_STATUS = "paid";

/**
 * Порог значимости по умолчанию — 1 ₽ = 100 копеек.
 *
 * Взят у канона (`receivables`, `minDebtRub = Math.max(1, … ?? 1)`) и оставлен
 * параметром, а не константой в теле: это шумовой фильтр отчёта, а не свойство
 * денег. Из-за него `Σ` строк отчёта — НЕ полная дебиторка.
 *
 * СВЕРЯТЬ С БУХГАЛТЕРИЕЙ ПОРОГ БОЛЬШЕ НЕ МЕШАЕТ, и ставить его в ноль для этого
 * не нужно: `clinicDebtTotals` отдаёт рядом со списочными суммами полные
 * (`fullReceivableKopecks`, `fullRefundLiabilityKopecks`) и то, что порог из
 * списков унёс (`subThreshold*`). Пока этих полей не было, «поставьте порог в
 * ноль» оставалось советом в комментарии — а итог клиники тем временем считался с
 * порогом и молча расходился с кассовой арифметикой на отброшенные копейки.
 */
export const DEFAULT_SIGNIFICANCE_KOPECKS = 100;

/**
 * Деньги потеряли точность. Не округляем — отказываемся.
 *
 * Отдельный класс, а не `Error`, чтобы вызывающий мог отличить «данные пришли
 * грязными» от любой другой поломки и ответить пользователю по делу, а не
 * пятисоткой.
 */
export class MoneyPrecisionError extends Error {
	readonly statusCode = 422;

	constructor(
		readonly field: string,
		readonly received: unknown,
		reason: string,
	) {
		super(
			`Сумма «${field}» не может быть представлена в копейках без потери точности: ${reason}. Получено: ${String(received)}.`,
		);
		this.name = "MoneyPrecisionError";
	}
}

/** Количество в позиции лечения нарушает общий контракт. */
export class QuantityContractError extends Error {
	readonly statusCode = 422;

	constructor(
		readonly received: unknown,
		reason: string,
	) {
		super(
			`Количество в позиции лечения обязано быть целым положительным числом (packages/shared/src/index.ts, quantity: z.number().int().positive()): ${reason}. Получено: ${String(received)}.`,
		);
		this.name = "QuantityContractError";
	}
}

/** Текст суммы: знак, целая часть, не более двух знаков дробной. */
const NUMERIC_MONEY_TEXT = /^-?\d+(?:\.\d{1,2})?$/;

/**
 * Копейки из текста колонки `numeric` — с отказом вместо тихого нуля.
 *
 * Само преобразование делает `parseKopecks` из `@dental/shared`: второй разбор
 * денег в этом репозитории запрещён. Здесь добавлены только два отказа, которых
 * общий разбор по замыслу не делает:
 *   • пустая строка и `null` — это не ноль, а непрочитанная сумма;
 *   • всё, что не похоже на `numeric(12, 2)`, отбивается ДО общего разбора, чтобы
 *     вызывающий получил `MoneyPrecisionError` с полем и причиной, а не
 *     безымянный `Error`.
 */
export function kopecksFromNumericText(text: string, field = "сумма"): Kopecks {
	if (typeof text !== "string") {
		throw new MoneyPrecisionError(field, text, "ожидалась строка");
	}

	const trimmed = text.trim();
	if (trimmed === "") {
		throw new MoneyPrecisionError(field, text, "пустая строка — это не ноль");
	}
	if (!NUMERIC_MONEY_TEXT.test(trimmed)) {
		// Сюда попадают три знака после запятой, экспонента, NaN, Infinity и мусор.
		throw new MoneyPrecisionError(
			field,
			text,
			"допустимы только цифры со знаком и не более двух знаков после запятой",
		);
	}

	try {
		return parseKopecks(trimmed);
	} catch (error) {
		throw new MoneyPrecisionError(
			field,
			text,
			error instanceof Error ? error.message : "общий разбор денег отказал",
		);
	}
}

/**
 * Копейки из числа рублей — только если число САМО себя знает точно.
 *
 * Проверка идёт по десятичному представлению (`String(value)`), а не по остатку
 * от умножения. `String` в JavaScript печатает кратчайшую запись, которая
 * читается обратно в то же число, поэтому она — точный детектор грязи:
 *
 *     String(1500.5)              → "1500.5"              → принято
 *     String(1500.505)            → "1500.505"            → ОТКАЗ, третий знак
 *     String(4500.299999999999)   → "4500.299999999999"   → ОТКАЗ, грязь умножения
 *     String(3491.4900000000002)  → "3491.4900000000002"  → ОТКАЗ, грязь сложения
 *
 * Последние две — не «почти 4500,30» и не «почти 3491,49». Это следы того, что
 * кто-то уже посчитал деньги в плавающей точке; `parseKopecks` привёл бы их к
 * двум знакам через `toFixed(2)` и тем самым подтвердил бы чужую потерю точности.
 * Расчёт долга обязан об этом узнать, а не сгладить.
 */
export function kopecksFromRubles(value: number, field = "сумма"): Kopecks {
	if (typeof value !== "number" || !Number.isFinite(value)) {
		throw new MoneyPrecisionError(field, value, "не конечное число");
	}

	const text = String(value);
	if (text.includes("e") || text.includes("E")) {
		throw new MoneyPrecisionError(
			field,
			value,
			"экспоненциальная запись — сумма вне денежного диапазона",
		);
	}

	// Дальше — общий разбор: текст уже доказал, что копейки в нём точные.
	return kopecksFromNumericText(text, field);
}

/** Копейки из текста колонки или из числа рублей. */
export function toKopecks(value: MoneyInput, field = "сумма"): Kopecks {
	return typeof value === "number"
		? kopecksFromRubles(value, field)
		: kopecksFromNumericText(value, field);
}

/**
 * Рубли числом — ТОЛЬКО на границе с контрактом, где схема требует `number`.
 *
 * В общем модуле такой функции нет намеренно: там есть `kopecksToWholeRubles`,
 * который БРОСАЕТ на суммах с копейками, потому что целые рубли — отдельный
 * законный тип поля. Здесь нужно другое: `moneyRubSchema` принимает копейки и
 * ждёт `number`, значит перевод обязан их сохранить.
 *
 * Деление целого числа копеек на 100 даёт ближайшее представимое число, и схема
 * его принимает. Опасно КОПИТЬ в этом виде, поэтому функция названа так, чтобы
 * её вызов был виден в диффе: любое сложение её результатов — возврат к дефекту.
 */
export function rublesFromKopecks(kopecks: Kopecks): number {
	assertKopecks(kopecks, "копейки");
	return kopecks / 100;
}

function assertKopecks(value: Kopecks, field: string): void {
	if (!Number.isSafeInteger(value)) {
		throw new MoneyPrecisionError(field, value, "ожидалось целое число копеек");
	}
}

/**
 * Ровно те три поля, из которых считается итог строки.
 *
 * Вынесено из `TreatmentChargeRow` не ради красоты: тот же итог считают ворота
 * и печатная форма документа, а у них на входе строка ПЛАТЁЖНОГО ДОКУМЕНТА, у
 * которой нет ни пациента, ни статуса позиции. Без этого типа им пришлось бы
 * подставлять выдуманные `patientId` и `status`, чтобы позвать общий расчёт, —
 * то есть врать типу ради переиспользования. `TreatmentChargeRow` его
 * расширяет, поэтому ни один существующий вызывающий не меняется.
 */
export interface ChargeLineInput {
	readonly unitPriceRub: MoneyInput;
	/** Целое положительное. Дробное или ноль — отказ, а не тихая правка. */
	readonly quantity: number | string;
	readonly discountRub: MoneyInput;
	/*
	 * Поля-соседи из `TreatmentChargeRow`. Расчёту итога строки они не нужны и
	 * им не читаются — объявлены здесь необязательными только затем, чтобы
	 * существующие вызывающие могли передать сюда объектный литерал позиции
	 * целиком (`routes/odontogram.ts`, три места). Без них TypeScript отбивает
	 * такой литерал проверкой лишних свойств, и переиспользование общего расчёта
	 * пришлось бы покупать переписыванием чужого файла.
	 */
	readonly patientId?: string;
	readonly visitId?: string | null;
	readonly status?: string;
}

/** Позиция лечения в том виде, в каком её отдаёт `treatment_items`. */
export interface TreatmentChargeRow extends ChargeLineInput {
	readonly patientId: string;
	/**
	 * Приём, к которому привязана позиция (`treatment_items.visit_id`, nullable).
	 *
	 * Для вопросов про пациента и клинику поле не нужно и потому необязательно:
	 * долг пациента складывается из ВСЕХ его позиций, привязаны они к приёму или
	 * нет. Нужно оно ровно одному ответу — сальдо ОДНОГО приёма, см.
	 * `buildVisitLedger`.
	 */
	readonly visitId?: string | null;
	/** `"cancelled"` исключается из назначенного — это часть канона. */
	readonly status: string;
}

/** Платёж в том виде, в каком его отдаёт `payments`. */
export interface PaymentRow {
	readonly patientId: string;
	/** Приём, к которому привязан платёж (`payments.visit_id`, nullable). */
	readonly visitId?: string | null;
	/** Только `"paid"` считается полученными деньгами. */
	readonly status: string;
	readonly amountRub: MoneyInput;
}

/**
 * Итог одной строки позиции: `цена × количество − скидка`, не ниже нуля.
 *
 * Порядок действий здесь — не стилистика. `цена × количество − скидка` и
 * `(цена − скидка) × количество` расходятся на `скидка × (количество − 1)`. Прав
 * первый, и причина — не вкус, а объявление колонки: скидка в этом проекте
 * задана строкой позиции целиком (`treatment_items.discount_rub`,
 * `numeric(12,2)`, `db/schema.ts`), а не ценой за единицу. Вычитание её из
 * КАЖДОЙ единицы умножило бы скидку на количество.
 *
 * Второй способ в дереве уже был — двумя копиями одного модуля
 * (`treatment/TreatmentPlanBuilder.ts` и `full_treatment_plan.ts` в корне), обе
 * с нулём импортёров, обе удалены 2026-08-05/06. Имён их файлов здесь больше
 * нет намеренно: ссылка на несуществующий файл — это приглашение искать его
 * снова. Довод держится на колонке, которая никуда не делась, а расхождение
 * закреплено прогоняемым тестом в `patientDebt.test.ts`.
 *
 * `Math.max(0, …)` стоит на СТРОКЕ, а не на итоге: иначе позиция со скидкой
 * больше цены тайно уменьшила бы долг по остальным позициям того же пациента.
 * Клинике это выглядело бы как «пациент вдруг стал должен меньше», без причины
 * в его собственном лечении.
 *
 * Умножение и вычитание — целые копейки через `multiplyKopecks` из общего
 * модуля, поэтому потерять копейку здесь физически нечем.
 */
export function chargeLineKopecks(row: ChargeLineInput): Kopecks {
	const quantity = assertContractQuantity(row.quantity);
	const unitPrice = toKopecks(row.unitPriceRub, "цена за единицу");
	const discount = toKopecks(row.discountRub, "скидка");
	return Math.max(0, multiplyKopecks(unitPrice, quantity) - discount);
}

/**
 * Тот же итог строки, но БЕЗ исключения — для мест, где бросать нельзя.
 *
 * ЗАЧЕМ ЭТО ПОЯВИЛОСЬ, ЧИСЛОМ. Итог строки лечения считался в проекте ЧЕТЫРЬМЯ
 * способами, и два из них — ворота и печать ОДНОГО И ТОГО ЖЕ юридического
 * документа. Замер 2026-08-05 на цене 1 000,00 ₽, количестве 1,5 и нулевой
 * скидке, через публичные входы обеих сторон:
 *
 *   documents/guards.ts   `validateDocumentCreation` → «строка должна иметь
 *                          сумму 2000.00 руб.»  (там стоял `Math.round(кол-во)`)
 *   documents/renderDocument.ts `renderDocumentHtml` → ячейка «Сумма»: 1 500 руб.
 *                          (там стоял `Math.round(цена × кол-во)`)
 *
 * То есть валидатор сметы и печатная форма той же сметы расходились на 500,00 ₽
 * на одних и тех же данных. Оба «округления» — тихая догадка о том, чего в
 * контракте нет: количество объявлено целым положительным
 * (`packages/shared/src/index.ts`, `treatmentPlanItemSchema.quantity`,
 * `serviceLines[].quantity`), а колонка `treatment_items.quantity` объявлена
 * `numeric(10, 2)` и дробное значение физически пропускает.
 *
 * ПОЧЕМУ ОТДЕЛЬНЫЙ ВХОД, А НЕ try/catch ПО МЕСТАМ. `chargeLineKopecks` обязан
 * бросать: расчёт долга и отчёты должны останавливаться на грязных деньгах.
 * Но валидатор документа возвращает ПРИЧИНУ ОТКАЗА строкой, а печатная форма
 * обязана отрисоваться всегда — исключение там превратило бы объяснимый 409 в
 * пустую пятисотку и в невыдаваемый документ без причины. Свой try/catch в
 * каждом из этих мест — это две копии политики, а копий политики в этом файле
 * уже хоронили девять. Здесь ОДНА арифметика (вызов ниже — тот же
 * `chargeLineKopecks`) и ОДНА политика отказа.
 *
 * `ok: false` — это «ответа нет», а НЕ ноль: ноль неотличим от бесплатной
 * услуги, и именно на такой неразличимости в этом дереве уже стоял долг
 * `balanceRub: 0` на семи пациентах подряд.
 */
export type ChargeLineOutcome =
	| { readonly ok: true; readonly kopecks: Kopecks }
	| { readonly ok: false; readonly reason: string };

export function chargeLineOutcome(row: ChargeLineInput): ChargeLineOutcome {
	try {
		return { ok: true, kopecks: chargeLineKopecks(row) };
	} catch (error) {
		if (
			error instanceof QuantityContractError ||
			error instanceof MoneyPrecisionError
		) {
			return { ok: false, reason: error.message };
		}
		throw error;
	}
}

function assertContractQuantity(value: number | string): number {
	const parsed =
		typeof value === "number" ? value : Number(String(value).trim());
	if (!Number.isFinite(parsed)) {
		throw new QuantityContractError(value, "не число");
	}
	if (!Number.isInteger(parsed)) {
		/*
		 * Здесь расходились три существовавших места, и молчаливого выбора быть не
		 * должно: канон в SQL брал `greatest(quantity, 1)` и посчитал бы 1,5
		 * единицы как 1,5, `domainStateHydration` брал `Math.round(quantity)` и
		 * посчитал бы как 2, `sampleData` берёт количество сырым. Первые два из
		 * дерева ушли 2026-08-05/06, но отказ остаётся нужным: колонка
		 * `numeric(10, 2)` приводит вход к двум знакам ДО того, как его увидит
		 * ограничение `quantity = trunc(quantity)` из миграции 0162, поэтому
		 * `1.004` дойдёт сюда как `1.00` и базой отвергнут не будет. На дробном
		 * значении правильный ответ — отказ с объяснением, а не догадка.
		 */
		throw new QuantityContractError(
			value,
			"дробное количество: три существовавших расчёта округляли его по-разному, поэтому здесь оно отвергается, а не угадывается",
		);
	}
	if (parsed <= 0) {
		// Канон подставлял 1 через `greatest(quantity, 1)`, то есть выставлял счёт
		// за единицу, которой в позиции нет. 2026-08-06 это выражение убрано из
		// отчётов и рассылок, а миграция 0162 закрыла такую строку ограничением
		// `quantity > 0`. Ноль означает, что позицию надо пометить `cancelled`, а
		// не выставить.
		throw new QuantityContractError(
			value,
			"количество меньше единицы: позицию без объёма надо отменять статусом cancelled, а не считать как одну единицу",
		);
	}
	return parsed;
}

/**
 * Сальдо одного пациента. Первичная величина: пять ответов ниже — её чтения.
 *
 * Область здесь — пациент целиком, поэтому позиции и оплаты берутся независимо
 * от того, привязаны они к приёму или нет. Для вопроса про ОДИН приём есть своя
 * первичная величина — `VisitLedger`, та же арифметика с другим ключом отбора.
 */
export interface PatientLedger {
	readonly patientId: string;
	/** Назначено: сумма строк позиций, кроме отменённых. */
	readonly chargedKopecks: Kopecks;
	/** Оплачено: сумма платежей в статусе `paid`. */
	readonly paidKopecks: Kopecks;
	/**
	 * Назначено − оплачено. Больше нуля — должен пациент, меньше — должна
	 * клиника.
	 *
	 * Знак взят у канона (`receivables`), а НЕ у `domainStateHydration`, где он
	 * обратный. Причина: канон отвечает на вопрос, который задаёт оператор —
	 * «сколько ОН должен», а не «какое у него сальдо». Обе конвенции сегодня
	 * живут в дереве одновременно и нигде не описаны; здесь она названа.
	 */
	readonly balanceKopecks: Kopecks;
}

/**
 * Сальдо по каждому пациенту, встречающемуся ХОТЯ БЫ в одной из двух таблиц.
 *
 * Объединение, а не соединение по позициям лечения: пациент, заплативший вперёд
 * до любых назначений, в позициях не встречается вовсе, и по одной таблице его
 * переплату не увидеть. Ровно на этом отчёт дебиторки терял переплативших, пока
 * в нём не появилось объединение множеств ключей.
 */
export function buildPatientLedgers(
	charges: readonly TreatmentChargeRow[],
	payments: readonly PaymentRow[],
): Map<string, PatientLedger> {
	const chargeLines = new Map<string, Kopecks[]>();
	for (const row of charges) {
		if (row.status === CANCELLED_ITEM_STATUS) continue;
		const lines = chargeLines.get(row.patientId) ?? [];
		lines.push(chargeLineKopecks(row));
		chargeLines.set(row.patientId, lines);
	}

	const paymentLines = new Map<string, Kopecks[]>();
	for (const row of payments) {
		if (row.status !== PAID_PAYMENT_STATUS) continue;
		const lines = paymentLines.get(row.patientId) ?? [];
		lines.push(toKopecks(row.amountRub, "сумма платежа"));
		paymentLines.set(row.patientId, lines);
	}

	const ledgers = new Map<string, PatientLedger>();
	for (const patientId of new Set<string>([
		...chargeLines.keys(),
		...paymentLines.keys(),
	])) {
		// sumKopecks из общего модуля: он проверяет целость каждого слагаемого,
		// поэтому испорченная сумма падает на сложении, а не расползается в итог.
		const chargedKopecks = sumKopecks(chargeLines.get(patientId) ?? []);
		const paidKopecks = sumKopecks(paymentLines.get(patientId) ?? []);
		ledgers.set(patientId, {
			patientId,
			chargedKopecks,
			paidKopecks,
			balanceKopecks: chargedKopecks - paidKopecks,
		});
	}
	return ledgers;
}

/** Сальдо одного пациента, когда строки ещё не отфильтрованы по нему. */
export function buildPatientLedger(
	patientId: string,
	charges: readonly TreatmentChargeRow[],
	payments: readonly PaymentRow[],
): PatientLedger {
	return (
		buildPatientLedgers(
			charges.filter((row) => row.patientId === patientId),
			payments.filter((row) => row.patientId === patientId),
		).get(patientId) ?? {
			patientId,
			chargedKopecks: 0,
			paidKopecks: 0,
			balanceKopecks: 0,
		}
	);
}

/* ═════════════════════════════════════════════════════════════════════════
 * ШЕСТЬ ОТВЕТОВ НА ШЕСТЬ ВОПРОСОВ ОПЕРАТОРА — ПРО ПАЦИЕНТА И ПРО КЛИНИКУ
 *
 * Функций ровно столько, сколько РАЗНЫХ вопросов задаёт оператор, и каждое имя
 * отвечает на свой вопрос. Девять расчётов до этого модуля отвечали на эти же
 * вопросы, но их имена этого не сообщали: три разных величины назывались
 * словом «долг».
 *
 * ШЕСТОЙ ОТВЕТ (`patientAccountBalanceKopecks`) добавлен 2026-07-29 при переезде
 * картотеки (`db/patientsQuery.ts`, где стояла константа `balanceRub: 0`). Он не
 * новая формула, а то же сальдо со знаком, который требует общий контракт поля
 * `Patient.balanceRub` — обратным к канону; причина и доказательство «это не
 * третий расчёт» стоят у самой функции.
 *
 * Шестой и седьмой вопрос — про ОДИН ПРИЁМ (`VisitLedger`,
 * `visitOutstandingKopecks`, `visitOverpaidKopecks`) — стоят ниже отдельной
 * секцией: у них другая область отбора, а не другая формула. Тот вопрос, который
 * `.agents/lead/recon-debt-formula-sprawl.md` называет шестой величиной («к
 * оплате пациентом после страховки», веб), здесь по-прежнему НЕ отвечен: сервер
 * данных договора ДМС в сводку не отдаёт вовсе.
 * ═════════════════════════════════════════════════════════════════════════ */

/**
 * «Сколько ЭТОТ пациент должен клинике?»
 *
 * Ноль, если рассчитался ровно ИЛИ переплатил: долг не бывает отрицательным, и
 * контракт это требует (`patientInsight.balanceDueRub` —
 * `nonNegativeMoneyRubSchema`). Цена такого зажима — переплата в этом ответе
 * безымянна, поэтому рядом обязан стоять `clinicOwesPatientKopecks`, иначе
 * «ноль» означает сразу две разные ситуации.
 */
export function patientOwesClinicKopecks(ledger: PatientLedger): Kopecks {
	return Math.max(0, ledger.balanceKopecks);
}

/**
 * «Сколько клиника должна ЭТОМУ пациенту?»
 *
 * Величина, которой до отчёта дебиторки не существовало ни на одном экране:
 * переплативший пациент отбрасывался фильтром долга, а его деньги молча гасили
 * чей-то чужой долг на главном экране. На живых данных таких пациентов двое, по
 * 800,00 ₽ каждый.
 */
export function clinicOwesPatientKopecks(ledger: PatientLedger): Kopecks {
	return Math.max(0, -ledger.balanceKopecks);
}

/**
 * «Плюс или минус у ЭТОГО пациента на счету, и на сколько?»
 *
 * СЕДЬМАЯ величина этого дома, и заведена она не для удобства, а потому что
 * оператор действительно задаёт этот вопрос отдельно: в карточке пациента стоит
 * ОДНО число со знаком, и по нему администратор решает, звонить о долге или
 * возвращать переплату. Два ответа выше («сколько должен» и «сколько ему должны»)
 * в это поле не помещаются: их два, а поле одно.
 *
 * ЗНАК ЗДЕСЬ ОБРАТНЫЙ КАНОНУ, И ЭТО НЕ ОПЕЧАТКА. Поле `Patient.balanceRub`
 * объявлено в общем контракте со знаком, и его собственный комментарий называет
 * конвенцию дословно: «Баланс пациента: оплачено минус запланировано.
 * Отрицательное значение — долг» (`packages/shared/src/index.ts`,
 * `patientSchema`). ЗДЕСЬ СТОЯЛО УТВЕРЖДЕНИЕ, КОТОРОЕ БЫЛО ЛОЖНЫМ И ТОГДА:
 * «ту же конвенцию уже используют ДВА живых производителя этого поля —
 * `db/domainStateHydration.ts` и `treatment/TreatmentPlanBuilder.ts`». Второй
 * живым не был никогда: у него был НОЛЬ импортёров, то есть в поле он не писал
 * ни разу; удалён 2026-08-05 вместе с дословной копией `full_treatment_plan.ts`.
 * Первого в дереве тоже больше нет — ни в рабочем каталоге, ни в индексе git
 * (проверено `git ls-files`), от него остался только осиротевший
 * `dist/db/domainStateHydration.js`. Живой производитель `Patient.balanceRub`
 * сегодня ОДИН: `db/patientsQuery.ts`, и он берёт значение отсюда же
 * (`patientAccountBalanceKopecks`).
 *
 * Знак от этого не меняется: его задаёт КОНТРАКТ поля, а не тот, кто в поле
 * пишет. Отдать в него канонический знак значило бы перевернуть карточку каждому
 * пациенту: должник выглядел бы переплатившим. Замер 2026-07-29 на клинике
 * `d0000000-…-d001` остаётся историческим свидетельством той же конвенции:
 * гидратация отдавала `0103 = −26 500`, `0100 = +800`, то есть минус — это долг.
 *
 * НОВОЙ АРИФМЕТИКИ ЗДЕСЬ НЕТ, и это проверяемо, а не заявлено: для ЛЮБОГО сальдо
 * значение равно `clinicOwesPatientKopecks − patientOwesClinicKopecks`, то есть
 * это те же два ответа выше, а не третий расчёт. Тест на это стоит в
 * `patientDebt.test.ts` и гоняется по всем живым сальдо, а не по одному примеру.
 *
 * ЧЕМ ОТЛИЧАЕТСЯ ОТ ПРЕЖНЕГО ПРОИЗВОДИТЕЛЯ: у `domainStateHydration` тот же знак,
 * но снаружи стоит `Math.round` до ЦЕЛОГО РУБЛЯ, а он несимметричен по знаку
 * (`Math.round(-49899.5) = -49899` при `Math.round(49899.5) = 49900`): должнику
 * 50 копеек прощают, переплатившему приписывают. Здесь копейки целые и точные,
 * округлять нечего.
 *
 * ПОЧЕМУ НЕ `-ledger.balanceKopecks`, ХОТЯ ЭТО КОРОЧЕ. Унарный минус на нуле даёт
 * ОТРИЦАТЕЛЬНЫЙ НОЛЬ, и он не безобиден: `Object.is(-0, 0) === false`, то есть
 * сравнение сальдо с нулём внезапно ложно, а `(-0).toLocaleString("ru-RU")`
 * печатает «-0» — рассчитавшийся до копейки пациент получил бы в карточке «−0 ₽».
 * Так и печатают деньги и в этом дереве (`sampleData.ts`, чипы карточки и
 * подсказка администратору). Поймано тестом при первом же прогоне: `assert.
 * strictEqual(-0, 0)` падает. Разность двух неотрицательных сумм такого нуля не
 * даёт никогда, и записана она ровно теми словами, которыми конвенцию объявил
 * контракт: «оплачено минус запланировано».
 */
export function patientAccountBalanceKopecks(ledger: PatientLedger): Kopecks {
	return ledger.paidKopecks - ledger.chargedKopecks;
}

/** Порог значимости: суммы меньше него в итоги клиники не идут. */
export interface ClinicTotalsOptions {
	/**
	 * По умолчанию 100 копеек = 1 ₽, как у канона. Ноль означает «без фильтра»:
	 * так итог сходится с бухгалтерией до копейки.
	 */
	readonly significanceKopecks?: Kopecks;
}

/**
 * Три итога клиники, посчитанные ОДНИМ проходом.
 *
 * Возвращаются вместе намеренно: как только их считают в разных местах, они
 * расходятся — это и есть история этого файла. Экран, показывающий «долг»,
 * обязан иметь под рукой и переплаты, иначе его число необъяснимо.
 */
export interface ClinicDebtTotals {
	/**
	 * «Сколько должны пациенты, попавшие в СПИСОК для звонков?» — ≥ 0, С ПОРОГОМ.
	 *
	 * Это число отчёта дебиторки, и порог из него не убран намеренно: список
	 * должников — инструмент обзвона, и строка на 50 копеек в нём — шум. Но именно
	 * поэтому это НЕ вся дебиторка; полная стоит рядом (`fullReceivableKopecks`), и
	 * подменять одну другой нельзя ни в какую сторону.
	 */
	readonly receivableKopecks: Kopecks;
	/** «Сколько клиника должна вернуть тем, кто в списке переплативших?» — ≥ 0, с порогом. */
	readonly refundLiabilityKopecks: Kopecks;
	/**
	 * Долги НИЖЕ порога: в список для звонков не попали, деньгами быть не перестали.
	 *
	 * Величина заведена не для полноты картины, а потому что без неё порог утекал в
	 * итоги клиники молча. Замер живой демо-клиники `d0000000-…-d001` 2026-07-29,
	 * уже после приведения цен к прайсу: пациент `…0106` назначено 7 200,50,
	 * оплачено 7 200,00 — долг РОВНО 0,50 ₽, то есть ниже порога 1 ₽. Его карточка
	 * этот долг показывает (карточка порога не имеет), отчёт дебиторки — нет, и
	 * сквозная сверка объявила расхождение двух экранов на 50 копеек, потому что
	 * сравнивала отфильтрованную величину с неотфильтрованной.
	 */
	readonly subThresholdReceivableKopecks: Kopecks;
	/** Переплаты ниже порога — тот же случай со стороны возврата. */
	readonly subThresholdRefundKopecks: Kopecks;
	/**
	 * «Сколько всего должны пациенты?» — ПОЛНАЯ дебиторка, порог не применялся.
	 *
	 * Это сумма долгов ВСЕХ пациентов, то есть ровно то, что показывают карточки
	 * пациентов на главном экране (`sampleData.ts`, `patientInsight.balanceDueRub`
	 * через `patientOwesClinicKopecks`): у карточки порога нет и быть не может —
	 * пациент, задолжавший 50 копеек, должен видеть 50 копеек.
	 */
	readonly fullReceivableKopecks: Kopecks;
	/** «Сколько всего клиника должна вернуть?» — без порога. */
	readonly fullRefundLiabilityKopecks: Kopecks;
	/**
	 * «Сколько клиника ещё не собрала, нетто?» = ПОЛНАЯ дебиторка − ПОЛНЫЕ возвраты.
	 *
	 * Именно это число показывает главный экран под словом «долг», и оно обязано
	 * сходиться с `Σ назначено − Σ оплачено` по всей клинике до копейки.
	 *
	 * ЗДЕСЬ БЫЛА ПОТЕРЯ, И ОНА НАЗВАНА ЧИСЛОМ. Стояло
	 * `receivableKopecks − refundLiabilityKopecks`, то есть итог собирался из
	 * ОТФИЛЬТРОВАННЫХ ПОРОГОМ сторон, а порог — шумовой фильтр СПИСКА, а не
	 * свойство денег. На живой демо-клинике `d0000000-…-d001` (замер 2026-07-29
	 * после приведения цен к прайсу) это давало 51 402,98 ₽ против 51 403,48 ₽ у
	 * главного экрана: ровно те 50 копеек долга пациента `…0106`, которые порог
	 * выкинул из списка и вместе со списком — из итога клиники. Для клиники это
	 * значит дырка в 50 копеек между «не собрано» и кассовой арифметикой
	 * «назначено − оплачено», которую бухгалтеру нечем объяснить: она не видна ни в
	 * одной строке отчёта, потому что строку и убрали.
	 *
	 * Итог СО ЗНАКОМ: если переплаты превысят долги, клиника должна больше, чем ей
	 * должны, и `Math.max(0, …)` вокруг итога (как в `buildBillingSummary`)
	 * превратил бы этот факт в безобидный ноль.
	 */
	readonly netUncollectedKopecks: Kopecks;
	/** Сколько пациентов в списке для звонков. */
	readonly debtorCount: number;
	/** Сколько пациентов в списке на возврат. */
	readonly overpaidCount: number;
	/** Сколько должников порог из списка выкинул. Ноль — фильтр ничего не унёс. */
	readonly subThresholdDebtorCount: number;
	/** Сколько переплативших порог из списка выкинул. */
	readonly subThresholdOverpaidCount: number;
	/** Порог, с которым посчитаны итоги. В отчёте его надо показывать. */
	readonly significanceKopecks: Kopecks;
}

/**
 * Итоги по клинике из сальдо всех пациентов.
 *
 * Порог применяется К СТОРОНАМ ОТДЕЛЬНО, ровно как у канона: пациент с долгом в
 * 50 копеек не попадает ни в СПИСОК должников, ни в список на возврат. Это
 * шумовой фильтр, и его наличие обязано быть видно в ответе — поэтому
 * `significanceKopecks` возвращается вместе с числами, а не остаётся внутри.
 *
 * ЧЕГО ПОРОГ НЕ ИМЕЕТ ПРАВА ДЕЛАТЬ — уносить деньги из ИТОГА. Отброшенные им
 * суммы возвращаются отдельными числами (`subThreshold*`), полная дебиторка и
 * полный возврат считаются без него, а `netUncollectedKopecks` собирается из
 * полных сторон. Иначе фильтр списка молча меняет итог клиники: измеренная
 * потеря — 50 копеек на живой демо-клинике, разбор у поля `netUncollectedKopecks`.
 * Проверяется это не словом, а свойством: при любом пороге (0, 1 ₽, 100 ₽) итог
 * на одних и тех же сальдо обязан быть ОДИН И ТОТ ЖЕ (`patientDebt.test.ts`).
 *
 * Принимает и `Map`, и любой перебор сальдо. Это НЕ удобство, а закрытая
 * ловушка, на которую я сам наступил при первом прогоне тестов:
 * `buildPatientLedgers` возвращает `Map`, а `for (… of map)` перебирает ПАРЫ
 * `[ключ, значение]`, не значения. С сигнатурой `Iterable<PatientLedger>` такой
 * вызов не падал — он тихо возвращал НУЛИ по всем трём итогам, то есть «долгов в
 * клинике нет». Ровно тот класс дефекта, ради которого этот файл написан, и
 * обнаружил его тест, а не типизация: `tsx` типы не проверяет. Поэтому здесь два
 * рубежа — `Map` разворачивается явно, а форма каждого сальдо проверяется.
 */
export function clinicDebtTotals(
	ledgers: Iterable<PatientLedger> | ReadonlyMap<string, PatientLedger>,
	options: ClinicTotalsOptions = {},
): ClinicDebtTotals {
	const significanceKopecks = Math.max(
		0,
		options.significanceKopecks ?? DEFAULT_SIGNIFICANCE_KOPECKS,
	);
	assertKopecks(significanceKopecks, "порог значимости");

	const debts: Kopecks[] = [];
	const refunds: Kopecks[] = [];
	/*
	 * Отброшенное порогом НЕ выбрасывается, а складывается отдельно. Раньше эти две
	 * корзины отсутствовали, и деньги из них исчезали вместе со строкой списка.
	 */
	const subThresholdDebts: Kopecks[] = [];
	const subThresholdRefunds: Kopecks[] = [];

	const rows =
		ledgers instanceof Map
			? ledgers.values()
			: (ledgers as Iterable<PatientLedger>);
	for (const ledger of rows) {
		assertLedgerShape(ledger);
		const owed = patientOwesClinicKopecks(ledger);
		if (owed > 0) {
			(owed >= significanceKopecks ? debts : subThresholdDebts).push(owed);
			continue;
		}
		const refund = clinicOwesPatientKopecks(ledger);
		if (refund > 0) {
			(refund >= significanceKopecks ? refunds : subThresholdRefunds).push(
				refund,
			);
		}
	}

	const receivableKopecks = sumKopecks(debts);
	const refundLiabilityKopecks = sumKopecks(refunds);
	const subThresholdReceivableKopecks = sumKopecks(subThresholdDebts);
	const subThresholdRefundKopecks = sumKopecks(subThresholdRefunds);
	const fullReceivableKopecks =
		receivableKopecks + subThresholdReceivableKopecks;
	const fullRefundLiabilityKopecks =
		refundLiabilityKopecks + subThresholdRefundKopecks;

	return {
		receivableKopecks,
		refundLiabilityKopecks,
		subThresholdReceivableKopecks,
		subThresholdRefundKopecks,
		fullReceivableKopecks,
		fullRefundLiabilityKopecks,
		// Порог сюда не входит: итог клиники обязан равняться Σ сальдо всех
		// пациентов, то есть «назначено − оплачено» по клинике до копейки.
		netUncollectedKopecks: fullReceivableKopecks - fullRefundLiabilityKopecks,
		debtorCount: debts.length,
		overpaidCount: refunds.length,
		subThresholdDebtorCount: subThresholdDebts.length,
		subThresholdOverpaidCount: subThresholdRefunds.length,
		significanceKopecks,
	};
}

/**
 * Сальдо обязано быть сальдо, а не парой `[ключ, значение]` и не `undefined`.
 *
 * Без этой проверки неверная форма даёт `Math.max(0, NaN) = NaN`, `NaN >= порог`
 * = `false`, и итог получается НОЛЬ — то есть «клиника ничего никому не должна».
 * Молчаливый ноль на деньгах хуже исключения.
 */
function assertLedgerShape(ledger: PatientLedger): void {
	if (
		ledger === null ||
		typeof ledger !== "object" ||
		!Number.isSafeInteger((ledger as PatientLedger).balanceKopecks)
	) {
		throw new MoneyPrecisionError(
			"сальдо пациента",
			ledger,
			"ожидался объект PatientLedger с целым balanceKopecks; перебор Map даёт пары [ключ, значение], передавайте map.values() или сам map",
		);
	}
}

/**
 * Готовое объяснение расхождения двух экранов — словами и с копейками.
 *
 * Существует затем, чтобы оператор, видящий на одном экране одно число, а на
 * другом другое, читал ПРИЧИНУ, а не догадывался. Печать суммы — общая
 * `formatKopecksRu` (неразрывные пробелы, типографский минус, знак рубля), своего
 * форматирования денег здесь нет.
 */
export function explainDebtTotals(totals: ClinicDebtTotals): string {
	const belowThreshold = explainSubThreshold(totals);

	if (totals.fullRefundLiabilityKopecks === 0 && belowThreshold === "") {
		return (
			`Пациенты должны клинике ${formatKopecksRu(totals.receivableKopecks)} ` +
			`(${totals.debtorCount} чел.). Переплат нет, поэтому «не собрано, нетто» — та же сумма.`
		);
	}
	/*
	 * АРИФМЕТИКА В ЭТОЙ СТРОКЕ ОБЯЗАНА СХОДИТЬСЯ ГЛАЗАМИ. Поэтому вычитание
	 * печатается ПОЛНЫМИ величинами: пока итог собирался из отфильтрованных сторон,
	 * оператор читал «53 001,49 − 1 598,51 = 51 402,98», а главный экран показывал
	 * 51 403,48 — и объяснение расхождения само становилось его источником.
	 */
	return (
		`Пациенты должны клинике ${formatKopecksRu(totals.fullReceivableKopecks)} ` +
		`(${totals.debtorCount + totals.subThresholdDebtorCount} чел.). Клиника должна вернуть ` +
		`${formatKopecksRu(totals.fullRefundLiabilityKopecks)} ` +
		`(${totals.overpaidCount + totals.subThresholdOverpaidCount} чел.). ` +
		`Не собрано, нетто: ${formatKopecksRu(totals.fullReceivableKopecks)} − ` +
		`${formatKopecksRu(totals.fullRefundLiabilityKopecks)} = ` +
		`${formatKopecksRu(totals.netUncollectedKopecks)}. Это две РАЗНЫЕ величины, и ` +
		`переплату одного пациента нельзя зачитывать в долг другого.${belowThreshold}`
	);
}

/**
 * Что порог унёс из СПИСКОВ — словами и копейками. Пустая строка, если ничего.
 *
 * Отдельным предложением, а не сноской: пока этой фразы не было, отчёт печатал
 * одну сумму, карточки пациентов — другую, и разница в 50 копеек не имела причины
 * ни на одном экране. Причина обязана называться причиной, иначе следующий
 * читатель объявит расхождение дефектом расчёта и пойдёт «чинить» верные числа.
 */
function explainSubThreshold(totals: ClinicDebtTotals): string {
	const parts: string[] = [];
	if (totals.subThresholdReceivableKopecks > 0) {
		parts.push(
			`долги на ${formatKopecksRu(totals.subThresholdReceivableKopecks)} ` +
				`(${totals.subThresholdDebtorCount} чел.)`,
		);
	}
	if (totals.subThresholdRefundKopecks > 0) {
		parts.push(
			`переплаты на ${formatKopecksRu(totals.subThresholdRefundKopecks)} ` +
				`(${totals.subThresholdOverpaidCount} чел.)`,
		);
	}
	if (parts.length === 0) return "";
	return (
		` Ниже порога значимости ${formatKopecksRu(totals.significanceKopecks)} остались ` +
		`${parts.join(" и ")}: в списки для звонков и возвратов они не попадают как шум, ` +
		`но в «не собрано, нетто» входят полностью — иначе итог клиники разошёлся бы с ` +
		`«назначено − оплачено» ровно на эту сумму.`
	);
}

/* ═════════════════════════════════════════════════════════════════════════
 * ЕЩЁ ДВА ВОПРОСА — ПРО ОДИН ПРИЁМ, А НЕ ПРО ПАЦИЕНТА И НЕ ПРО КЛИНИКУ
 *
 * «Сколько осталось получить ПО ЭТОМУ ПРИЁМУ?» и «Сколько по ЭТОМУ приёму
 * заплачено сверх назначенного?». Это ДРУГИЕ вопросы, чем пять выше: там область
 * — пациент и клиника, здесь — один приём. Тот же пациент за год приходит
 * двадцать раз, и «он должен 26 500» не отвечает администратору, закрывающему
 * приём, ни да, ни нет.
 *
 * НОВОЙ ФОРМУЛЫ ЗДЕСЬ НЕТ, И ЭТО ПРОВЕРЯЕМО. Первичная величина та же —
 * `назначено − оплачено`; строка позиции считается тем же `chargeLineKopecks`,
 * отменённые отбрасываются той же `CANCELLED_ITEM_STATUS`, оплаты отбираются тем
 * же `PAID_PAYMENT_STATUS`, сложение — тем же `sumKopecks`. Меняется РОВНО ОДНО:
 * ключ отбора строк. Что это не разошедшаяся копия, а та же величина в другой
 * области, закреплено тестом «сальдо приёма совпадает с сальдо пациента, когда
 * у пациента один приём и все его строки к нему привязаны»
 * (`patientDebt.test.ts`).
 *
 * ПОЧЕМУ НЕ ПЕРЕИСПОЛЬЗОВАН `buildPatientLedgers` С ДРУГИМ КЛЮЧОМ. Он группирует
 * по пациенту, и вынести из него общий сборщик значило бы переписать функцию, на
 * которой стоит канон и 49 проверок. Здесь повторён цикл, а не арифметика; цена
 * — восемь строк перебора, выигрыш — канон не тронут.
 *
 * ЧЕГО ЭТОТ ОТВЕТ НЕ ЗНАЕТ, И ЭТО НАДО ГОВОРИТЬ ВСЛУХ. Он видит только то, что
 * привязано к приёму. Оплата, принятая без приёма (`payments.visit_id` = null —
 * маршрут это разрешает: `db/billingQuery.ts`, `visitId: input.visitId || null`),
 * в сальдо приёма не попадёт, и приём покажет остаток, которого пациент уже не
 * должен. Поэтому наружу вместе с суммой уходят СЧЁТЧИКИ строк: «остаток 26 500,
 * позиций 1, оплат 0» отличимо от «остаток 26 500, позиций 1, оплат 1». Вопрос
 * «должен ли пациент вообще» остаётся за `patientOwesClinicKopecks`, и подменять
 * один ответ другим нельзя ни в ту, ни в другую сторону.
 * ═════════════════════════════════════════════════════════════════════════ */

/**
 * Сальдо ОДНОГО приёма. Первичная величина для обоих ответов ниже.
 *
 * Счётчики строк — часть ответа, а не отладка: без них ноль неотличим от
 * незнания, а именно на этом различии стоит галочка «оплата связана» в карточке
 * закрытия приёма.
 */
export interface VisitLedger {
	readonly visitId: string;
	/** Назначено по приёму: сумма строк позиций, кроме отменённых. */
	readonly chargedKopecks: Kopecks;
	/** Оплачено по приёму: сумма платежей в статусе `paid`. */
	readonly paidKopecks: Kopecks;
	/** Назначено − оплачено. Знак тот же, что у `PatientLedger`. */
	readonly balanceKopecks: Kopecks;
	/** Всего позиций приёма, включая отменённые. */
	readonly chargeRowCount: number;
	/** Из них попавших в назначенное. */
	readonly billedLineCount: number;
	/** Всего платежей приёма, включая неоплаченные. */
	readonly paymentRowCount: number;
	/** Из них попавших в оплаченное. */
	readonly paidPaymentCount: number;
	/**
	 * Есть ли по приёму ХОТЬ ОДНА строка денег — в любом статусе.
	 *
	 * `false` означает «ответа нет», а не «остаток ноль». Различие не
	 * стилистическое: приём, к которому ещё не завели ни позиций, ни оплат, и
	 * приём, оплаченный до копейки, для администратора — разные состояния, и
	 * закрытая по незнанию галочка хуже незакрытой.
	 *
	 * Отменённые позиции и неоплаченные платежи считаются ЗДЕСЬ намеренно: их
	 * наличие доказывает, что деньги приёма кто-то заводил. Приём с единственной
	 * отменённой позицией назначил ноль ИЗМЕРЕННО, а не по незнанию.
	 */
	readonly hasRecords: boolean;
}

/**
 * Сальдо приёма из строк, ещё не отфильтрованных по нему.
 *
 * Пустой идентификатор — отказ, а не «ничего не нашлось»: строки с
 * `visitId = null` не совпали бы с ним ни разу, и вызывающий получил бы
 * уверенный ноль по приёму, которого не назвал.
 */
export function buildVisitLedger(
	visitId: string,
	charges: readonly TreatmentChargeRow[],
	payments: readonly PaymentRow[],
): VisitLedger {
	if (typeof visitId !== "string" || visitId.trim() === "") {
		throw new MoneyPrecisionError(
			"приём",
			visitId,
			"сальдо приёма нельзя собрать по пустому идентификатору: ни одна строка с visit_id = null с ним не совпадёт, и ответом стал бы уверенный ноль",
		);
	}

	const chargeLines: Kopecks[] = [];
	let chargeRowCount = 0;
	for (const row of charges) {
		if (row.visitId !== visitId) continue;
		chargeRowCount += 1;
		if (row.status === CANCELLED_ITEM_STATUS) continue;
		chargeLines.push(chargeLineKopecks(row));
	}

	const paymentLines: Kopecks[] = [];
	let paymentRowCount = 0;
	for (const row of payments) {
		if (row.visitId !== visitId) continue;
		paymentRowCount += 1;
		if (row.status !== PAID_PAYMENT_STATUS) continue;
		paymentLines.push(toKopecks(row.amountRub, "сумма платежа"));
	}

	const chargedKopecks = sumKopecks(chargeLines);
	const paidKopecks = sumKopecks(paymentLines);
	return {
		visitId,
		chargedKopecks,
		paidKopecks,
		balanceKopecks: chargedKopecks - paidKopecks,
		chargeRowCount,
		billedLineCount: chargeLines.length,
		paymentRowCount,
		paidPaymentCount: paymentLines.length,
		hasRecords: chargeRowCount > 0 || paymentRowCount > 0,
	};
}

/**
 * «Сколько осталось получить ПО ЭТОМУ ПРИЁМУ?»
 *
 * `null` — «ответа нет»: по приёму не заведено ни позиций, ни оплат. Тип
 * возврата назван так СПЕЦИАЛЬНО: `Kopecks | null` заставляет вызывающего
 * написать ветку незнания, тогда как ноль он подставил бы молча. Именно молчаливый
 * ноль и есть тот дефект, из-за которого в карточке закрытия приёма стоял остаток
 * по всей клинике.
 */
export function visitOutstandingKopecks(ledger: VisitLedger): Kopecks | null {
	if (!ledger.hasRecords) return null;
	return Math.max(0, ledger.balanceKopecks);
}

/**
 * «Сколько по ЭТОМУ приёму заплачено сверх назначенного?»
 *
 * Отдельный ответ по той же причине, по которой рядом с
 * `patientOwesClinicKopecks` стоит `clinicOwesPatientKopecks`: остаток зажат в
 * ноль, поэтому без этой величины «ноль» означал бы сразу и «рассчитались
 * ровно», и «переплатили». На живой базе такой приём есть — назначено 6 400,00,
 * получено 7 200,00 (приём `…-000000000400`, замер 2026-07-29).
 */
export function visitOverpaidKopecks(ledger: VisitLedger): Kopecks | null {
	if (!ledger.hasRecords) return null;
	return Math.max(0, -ledger.balanceKopecks);
}

/**
 * Сумма для записи в колонку `numeric(12, 2)`.
 *
 * Тонкая пересылка в `kopecksToNumericString` из общего модуля — стоит здесь
 * только ради того, чтобы вызывающему не приходилось импортировать деньги из
 * двух пакетов и чтобы поиск по этому файлу показывал полный путь суммы от базы
 * до базы. Своей реализации печати нет.
 */
export function debtNumericText(kopecks: Kopecks): NumericText {
	return kopecksToNumericString(kopecks);
}
