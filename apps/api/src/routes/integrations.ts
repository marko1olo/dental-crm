import type { FastifyInstance } from "fastify";

export default async function registerIntegrationsRoutes(_app: FastifyInstance) {
	// Integrations routes (EGISZ, Diagnocat, Flexbe) are registered in their respective dedicated modules
}
