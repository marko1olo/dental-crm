/**
 * Backward compatibility proxy: re-exports all domain schemas and pgTable declarations from ./schema/
 * (includes pgTable(...) definitions from domain modules)
 */
export * from "./schema/index.js";

