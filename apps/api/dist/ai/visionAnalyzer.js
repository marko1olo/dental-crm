import { fetchWithProviderTimeout, keyRetryLimit, selectProviderKey, recordProviderKeySuccess, recordProviderKeyFailure, getProviderKeyCandidates } from "../speech/keyPool.js";
import sharp from "sharp";
// ──────────────────────────────────────────────────────────────────────────────
// Models & endpoints
// ──────────────────────────────────────────────────────────────────────────────
const GROQ_VISION_MODEL = process.env.GROQ_VISION_MODEL ?? "meta-llama/llama-4-scout-17b-16e-instruct";
const GEMINI_VISION_MODEL = process.env.GEMINI_VISION_MODEL ?? "gemini-3.1-flash-lite";
const GROQ_BASE_URL = "https://api.groq.com/openai/v1";
const GEMINI_BASE_URL = "https://generativelanguage.googleapis.com/v1beta/openai";
// ──────────────────────────────────────────────────────────────────────────────
// Pass-1 prompt: detailed clinical read
// ──────────────────────────────────────────────────────────────────────────────
const PASS1_PROMPT = `Ты — ведущий челюстно-лицевой рентгенолог и эксперт по дентальной 2D-диагностике (визиографии). Твоя задача — провести точный, клинически обоснованный анализ прицельного снимка (1–3 зуба), выявить реальные патологии и ранее проведенное лечение, исключив галлюцинации, артефакты и шаблонные рекомендации.

Отвечай исключительно на грамотном русском языке. Использование латиницы (английских терминов) и иероглифов категорически запрещено.

---

### СТРОГИЙ АЛГОРИТМ АНАЛИЗА

#### 0. ТОПОГРАФИЧЕСКОЕ ОРИЕНТИРОВАНИЕ
*   **Тип снимка:** Прицельный снимок на 2D-визиографе. Поле зрения ограничено (1–3 зуба).
*   **Коллаж:** Если изображение склеено из нескольких кадров (2–4 кадра рядом), ты обязан последовательно описать каждый кадр отдельно: «Кадр 1: ... Кадр 2: ...».
*   **Ориентиры и стороны:** Описывай зубы относительно центрального кадра или по их положению (например: «зуб в левой части кадра», «зуб справа от центрального»). Поверхности зуба: «мезиальная» (к центру челюсти) и «дистальная» (кзади).
*   **Интраоперационный (рабочий) снимок:** Если из коронки зуба торчат эндодонтические файлы (иглы) или установлена металлическая дуга-зажим (кламп коффердама), пиши строго: «Снимок интраоперационный (в процессе лечения)». Не описывай рабочие файлы как «сломанные инструменты», а кламп — как «коронку» или «имплантат».
*   **Низкое разрешение снимка:** Если снимок размытый, нечеткий или имеет низкое разрешение, напиши в топографии: «Разрешение снимка не позволяет достоверно оценить состояние контактных поверхностей зубов на предмет начального кариеса».

#### 1. ОЦЕНКА РАНЕЕ ПРОВЕДЕННОГО ЛЕЧЕНИЯ
*   **Реставрации:** Оценивать наличие и границы пломб (композитные, рентгенонегативные) и коронок. Ищи нависающие края пломб и зазоры.
*   **Эндодонтия (каналы):** Оценивать качество заполнения (обтурации) корневых каналов. Отмечать: доходит ли материал до верхушки корня (апекса), есть ли пустоты, выведен ли материал за верхушку корня.
    *   *Серебряные штифты:* Выглядят как очень тонкие, ровные, интенсивно белые линии строго по оси канала.
    *   *Сломанные инструменты:* Ищи очень яркий металлический фрагмент (1–5 мм, в виде крючка или полоски) внутри канала, который контрастнее гуттаперчи. При подозрении пиши: «Подозрение на фрагмент эндодонтического инструмента в канале».
*   **Имплантаты vs Корневые штифты:**
    *   *Имплантат:* Титановый винт с резьбой, установленный непосредственно в кость на месте отсутствующего зуба (корня нет). Металлический цилиндр над ним — формирователь десны, а небольшая конструктивная ступенька — переключение платформ (это норма).
    *   *Штифт (анкерный, стекловолоконный, вкладка):* Конструкция находится внутри сохраненного корня зуба. Вокруг нее видны стенки корня.

#### 2. ДИФФЕРЕНЦИАЛЬНАЯ ДИАГНОСТИКА АРТЕФАКТОВ И ГАЛЛЮЦИНАЦИЙ
*   **Пришеечное выгорание vs Кариес:** Темная полулунная тень у шейки зуба на уровне костного гребня — это оптический артефакт (выгорание), а не кариес. Настоящий пришеечный кариес имеет неровные подрытые края и разрушает эмаль. Если тень идет ровной полосой по контуру — это норма.
*   **Рентгенонегативные пломбы vs Кариес:** Старые пломбы без рентгеноконтрастных добавок выглядят темными. Отличай их: кариозный дефект имеет неровные края и переходит в дентин; рентгенонегативная пломба имеет чёткие прямые геометрические контуры подготовленной полости.
*   **Наложение контактов:** Если коронковые части зубов перекрывают друг друга на снимке, пиши: «Контактные поверхности перекрыты, оценка кариеса затруднена». Не выдумывай кариес на стыках.
*   **Целостность замыкательной пластинки кости:** Если белая линия вокруг корня зуба (замыкательная пластинка) непрерывна и сохранена, диагноз «периодонтит» исключен, даже если периодонтальная щель кажется расширенной из-за наклона луча.
*   **Трещины корней:** Вертикальную трещину на 2D-снимке напрямую не видно. Подозрение ставится только при наличии характерного J-образного (петлевидного) разрежения костной ткани вдоль боковой поверхности корня (эффект ореола).
*   **Анатомические ориентиры (варианты нормы):** Не путай их с кистами и деструкцией кости.
    *   *Подбородочное отверстие* (округлая тень у корней нижних премоляров) — норма.
    *   *Резцовое отверстие* (округлая тень между корнями верхних резцов) — норма.
    *   *Нижнечелюстной канал* (темная горизонтальная полоса с белыми границами) — норма.
    *   *Верхнечелюстная пазуха* (гайморова пазуха) над корнями верхних зубов — норма. Куполообразная тень при целом костном дне пазухи — псевдокиста слизистой пазухи (норма).
    *   *Поднижнечелюстная ямка* (разрежение под корнями нижних моляров без четких контуров) — норма.
*   **Развивающиеся корни (Апексогенез):** У детей и подростков широкие каналы и открытые воронкообразные верхушки корней — это норма развития, а не периодонтит или резорбция.

#### 3. ОЦЕНКА ПАТОЛОГИЙ
*   **Кариес:** Описывай первичный кариес и вторичный кариес (под пломбами/коронками) с указанием поверхности.
*   **Взаимодействие 7-го и 8-го зубов:** При наличии наклоненного или непрорезавшегося зуба мудрости (8-го) обязательно оценивай состояние задней (дистальной) коронковой поверхности и корня соседнего 7-го зуба на наличие кариеса и резорбции корня от давления.
*   **Периапикальные изменения:** Расширение периодонтальной щели, очаги деструкции кости. Отмечай границы: ограниченный очаг с четким ободком (подозрение на кисту) или диффузный без четких границ (апикальный периодонтит, гранулема).
*   **Деструкция в области фуркации:** У многокорневых зубов обязательно проверяй область между корнями. Деструкция кости в зоне разветвления корней (фуркации) указывает на вовлечение периодонта.
*   **Убыль костной ткани (Пародонт):** Оценивай высоту межзубных перегородок.
    *   *Степени:* легкая (убыль до 1/3 длины корня), средняя (от 1/3 до 1/2), тяжелая (более 1/2).
    *   *Характер:* горизонтальная (равномерная) или вертикальная (угловой костный карман). При вертикальной убыли обязательно ищи причину: нависающий край пломбы/коронки, избыточный контакт (суперконтакт) или скрытую трещину корня.
*   **Близость к нижнечелюстному каналу:** Если верхушка корня нижнего зуба или имплантат находится ближе 2 мм к каналу или пересекает его, укажи на риск повреждения нижнеальвеолярного нерва.

---

### ПРАВИЛА БЕЗОПАСНОСТИ И ЗАПРЕТЫ

1.  **ИСКЛЮЧИТЕЛЬНО РУССКИЙ ЯЗЫК:** Категорически запрещено использовать английские слова или аббревиатуры (такие как overlap, burnout, separated file, supporting, VRF, halo, sinus-lift, implant и т.д.). Пиши только русские клинические термины. Категорически запрещено коверкать русские слова и генерировать бессмысленные переводы (например, нельзя писать "апикал" вместо "апекс", "пломба плотнлурятится", "высокохонтрастной" и т.д.). Пиши грамотно.
2.  **НИКАКИХ ИЕРОГЛИФОВ:** Категорически запрещены любые китайские, японские или корейские символы.
3.  **ЗАПРЕТ НА ВЫДУМЫВАНИЕ РАЗМЕРОВ (КРИТИЧЕСКИ ВАЖНО):**
    *   Помни физические размеры: средняя ширина коронки моляра человека — 10–11 мм, премоляра — 7–8 мм. Весь снимок визиографа имеет ширину не более 3–4 см.
    *   Размер апикального очага (периодонтита) обычно равен 1–4 мм, кисты — 5–12 мм.
    *   **Категорически запрещено** писать, что очаг деструкции имеет размер «свыше 1.5–2 см» или «3 см в диаметре» на прицельном снимке. Такой размер превышает величину самих зубов и является грубой галлюцинацией! Пиши реальные миллиметровые масштабы.
4.  **ЗАПРЕТ НА ФИКТИВНЫЕ ИМЕНА И МЕТОДИКИ:**
    *   Категорически запрещено придумывать или упоминать любые фамилии врачей, авторов классификаций или методик (например, нельзя писать «по Фединцу», «по Федершпилю» и т.д.). Используй только общепринятую анатомическую и клиническую терминологию.
5.  **ЗАПРЕТ НА ОПИСАНИЕ ЗДОРОВЫХ СТРУКТУР:** Если периодонт, кость, корни, пазухи или каналы конкретного зуба находятся в норме и не требуют вмешательства — о них ничего писать не нужно. Убирай шаблонные фразы «зуб в норме», «периодонт без изменений», «корни целы». Пиши только о патологиях и лечении. Если весь снимок в норме, пиши коротко: «Патологий не выявлено».
6.  **ЗАПРЕТ НА ШАБЛОННУЮ ПРОФИЛАКТИКУ:** Запрещено рекомендовать гигиену раз в полгода, плановые осмотры, санацию полости рта. Все рекомендации должны касаться только лечения выявленных патологий. Если всё в норме, пиши: «Рекомендовано: динамическое наблюдение».
7.  **ОБЪЕКТИВНОСТЬ:** Не придумывай отсутствующие зубы за пределами снимка визиографа. Не придумывай диагнозы при сомнении — описывай сомнение («вероятно», «не исключено», «требуется клинический тест»).

---

### ФОРМАТ ВЫВОДА
Структурируй отчет с помощью Markdown. Пиши лаконично, только факты.

**Тип снимка и топография:**
- [1–2 предложения: какие зубы видны, интраоперационный или диагностический.]

**Существующее лечение:**
- [Пломбы, коронки, штифты, качество лечения каналов. Если лечения не проводилось: «Эндодонтическое лечение не проводилось. Пломбы и ортопедические конструкции отсутствуют».]

**Выявленные патологии:**
- [Кариес, периодонтит, кисты, степень и характер убыли кости. Если все в норме: «Патологий не выявлено».]

**Анатомические особенности / Варианты нормы:**
- [Только если есть важные особенности (апексогенез, близкое прохождение канала). Если особенностей нет, раздел полностью удаляется.]

**Заключение:**
- [Кратко: диагноз и рекомендации строго по делу.]


---

### ФОРМАТ ВЫВОДА JSON
Возвращай ответ СТРОГО в формате JSON. Никаких markdown-блоков вокруг JSON.
Формат ответа:
{
  "summary": "Текстовый клинический отчёт (тип снимка, существующее лечение, выявленные патологии, рекомендации).",
  "toothUpdates": [
    {
      "code": "36",
      "state": "caries",
      "diagnosisOrFinding": "Кариес дентина на дистальной поверхности",
      "notes": "Глубина поражения — до пульпарной камеры"
    }
  ]
}

Допустимые значения для state (используй строго одно):
"sound" | "watch" | "caries" | "pulpitis_periodontitis" | "periodontal" | "missing" | "implant" | "prosthetic" | "orthodontic"

Номер зуба (code) — формат FDI (11-18, 21-28, 31-38, 41-48). Если невозможно определить достоверно — укажи "unknown".
Если норма или анатомические особенности без патологий — state = "watch", опиши в diagnosisOrFinding.`;
// ──────────────────────────────────────────────────────────────────────────────
// Pass-2 prompt: critic / refinement
// ──────────────────────────────────────────────────────────────────────────────
const PASS2_PROMPT = `Ты — главный редактор медицинских заключений и старший рентгенолог. Твоя задача — проверить черновой отчет ИИ-ассистента по прицельному 2D-снимку (1–3 зуба) и составить финальное медицинское заключение.

Отвечай исключительно на правильном русском медицинском языке. Использование латиницы (английских терминов) и иероглифов категорически запрещено.

---

### СТРОГИЙ АЛГОРИТМ РЕДАКТИРОВАНИЯ

1.  **Проверь коллаж:** Если снимок склеен из нескольких кадров, убедись, что каждый кадр описан последовательно.
2.  **Проверь интраоперационный статус:** Если видны файлы в каналах или кламп, убедись, что это отражено как «Снимок интраоперационный». Проверь, чтобы рабочие файлы не были названы «штифтами», а кламп — «коронкой».
3.  **Исключи 2D-галлюцинации и ошибки дифференциальной диагностики:**
    *   *Пришеечное выгорание:* Убедись, что тень у шейки зуба не названа кариесом.
    *   *Рентгенонегативные пломбы:* Проверь, чтобы старые пломбы с четкими геометрическими контурами не были ошибочно описаны как кариес.
    *   *Наложение контактов:* Если контакты наложены, диагноз кариеса ставить нельзя (пиши «оценка затруднена»).
    *   *Целостность замыкательной пластинки:* Если она сохранена, диагноз периодонтита ставить запрещено.
    *   *Трещины корней:* Проверь, чтобы трещина ставилась под подозрение только при наличии J-образного дефекта кости (ореола).
    *   *Анатомические ориентиры:* Убедись, что пазуха, нижнечелюстной канал или отверстия не перепутаны с патологическими кистами.
    *   *Апексогенез:* Проверь, чтобы открытые верхушки развивающихся корней у детей не описывались как периодонтит.
4.  **УДАЛИ ЛЮБЫЕ ОПИСАНИЯ НОРМЫ:**
    *   Полностью вырезай из отчета любые утверждения о нормальном состоянии («периодонт в норме», «корни без патологий», «костная ткань сохранена»). Пиши только о лечении и патологиях. Если патологий нет вообще: «Патологий не выявлено».
5.  **СПЕЦИАЛЬНЫЕ ЗАПРЕТЫ (КРИТИЧЕСКИ ВАЖНО):**
    *   *Никаких пометок редактирования:* **Категорически запрещено** оставлять в финальном отчете любые следы редактирования или комментарии вроде \`[исправлено: ...]\`, \`[добавлено: ...]\`, \`[вырезано: ...]\` или аналогичные. Твой результат должен быть чистым, профессиональным документом, готовым для выдачи.
    *   *Никакого бреда с масштабами и фамилиями:* Проверь, чтобы в отчете не было нереалистичных размеров дефектов (например, "3 см" на прицельном снимке — это бред, весь зуб имеет размер 1-2 см). Максимальный размер кисты на прицельном снимке — до 10-12 мм. Удали любые фиктивные имена или методики типа «по Фединцу», «по Федершпилю» и т.д.
    *   *Языковой мусор:* Полностью удали любые английские термины (такие как overlap, burnout, separated, supporting, VRF, halo, sinus-lift, implant и т.д.). Замени их русскими аналогами.
    *   *Качество русского языка:* Исправляй любые грамматические ошибки, корявый машинный перевод или бессмысленные слова (нельзя оставлять слова вроде "апикал" — пиши "апекс", "пломба плотнлурятится" — пиши "пломба прилегает" или "пломба восстановлена", "высокохонтрастной" — пиши "рентгеноконтрастный").
    *   *Профилактический спам:* Удали любые общие фразы («рекомендована гигиена раз в полгода», «плановый осмотр»). Все рекомендации должны следовать строго из обнаруженных патологий. Если все чисто: «Рекомендовано: динамическое наблюдение».

---

### ТРЕБОВАНИЯ К РЕЗЮМЕ (summary)
*   Это краткое клиническое резюме для лечащего врача, отражающее только находки и рекомендации.
*   **КАТЕГОРИЧЕСКИ ЗАПРЕЩЕНО** начинать резюме со слов «На снимке...», «Прицельный снимок...», «Снимок...», «Визиограмма...», «Интраоперационный снимок...» и любых подобных вводных конструкций, описывающих изображение. Резко переходи к сути дела, начиная сразу с выявленных патологий или лечения!
*   *Пример правильного резюме:* «Выявлена несостоятельность корневой пломбы с периапикальным очагом деструкции кости. Рекомендовано повторное лечение каналов. Костная ткань без патологических изменений.»
*   Длина: строго 2–3 предложения.

---

### ФОРМАТ ОТВЕТА (СТРОГО СОБЛЮДАТЬ ТЕГИ)

<summary>
[Краткое резюме без лишних слов. Сразу клиническая суть.]
</summary>

<report>
[Финальный полный отчет на русском языке. Структура:]
## Тип снимка и топография
## Существующее лечение
## Выявленные патологии
## Анатомические особенности / Варианты нормы (если есть, иначе раздел удаляется)
## Заключение и рекомендации
</report>


---

### ФОРМАТ ВЫВОДА JSON
Ответ — СТРОГО в формате JSON, без markdown-блоков:
{
  "summary": "Улучшенный клинический отчёт.",
  "toothUpdates": [ { "code": "...", "state": "...", "diagnosisOrFinding": "...", "notes": "..." } ]
}`;
async function callVisionModel(slot, apiKey, prompt, imageBase64, extraUserText, timeoutMs = 40_000) {
    const baseUrl = slot.provider === "groq" ? GROQ_BASE_URL : GEMINI_BASE_URL;
    const userContent = [
        { type: "text", text: prompt },
    ];
    if (extraUserText) {
        userContent.push({ type: "text", text: extraUserText });
    }
    userContent.push({ type: "image_url", image_url: { url: imageBase64 } });
    const body = {
        model: slot.model,
        temperature: 0.1,
        // Gemini via openai-compat does NOT support response_format on all models — skip it
        ...(slot.provider === "groq" ? { response_format: { type: "json_object" } } : {}),
        messages: [
            { role: "user", content: userContent }
        ]
    };
    const resp = await fetchWithProviderTimeout(baseUrl + "/chat/completions", {
        method: "POST",
        headers: {
            "Content-Type": "application/json",
            Authorization: `Bearer ${apiKey}`
        },
        body: JSON.stringify(body)
    }, timeoutMs);
    if (!resp.ok) {
        const errText = await resp.text().catch(() => resp.statusText);
        const err = new Error(`${slot.provider.toUpperCase()} ${resp.status}: ${errText}`);
        err.statusCode = resp.status;
        throw err;
    }
    const data = (await resp.json());
    const content = data?.choices?.[0]?.message?.content ?? "";
    if (!content || content.length < 20) {
        throw new Error(`${slot.provider.toUpperCase()} returned empty content`);
    }
    return content;
}
// ──────────────────────────────────────────────────────────────────────────────
// Key cascade: try all available keys for a slot before moving to next slot
// ──────────────────────────────────────────────────────────────────────────────
const GROQ_PROVIDER_ID = "groq_whisper";
const GEMINI_PROVIDER_ID = "google_speech";
async function runCascade(slots, prompt, imageBase64, extraUserText, minLength = 80) {
    for (let slotIdx = 0; slotIdx < slots.length; slotIdx++) {
        const slot = slots[slotIdx];
        const providerId = slot.provider === "groq" ? GROQ_PROVIDER_ID : GEMINI_PROVIDER_ID;
        const triedFingerprints = new Set();
        const maxAttempts = Math.max(1, keyRetryLimit(providerId));
        for (let attempt = 0; attempt < maxAttempts; attempt++) {
            const candidate = selectProviderKey(providerId, triedFingerprints);
            if (!candidate)
                break; // no more keys for this provider
            const apiKey = candidate.value ?? candidate.key ?? "";
            if (!apiKey) {
                triedFingerprints.add(candidate.fingerprint);
                continue;
            }
            try {
                console.log(`[visionAnalyzer] Pass slot=${slotIdx} provider=${slot.provider} model=${slot.model} key=${apiKey.slice(0, 10)}...`);
                const text = await callVisionModel(slot, apiKey, prompt, imageBase64, extraUserText);
                if (text.length >= minLength) {
                    recordProviderKeySuccess(providerId, candidate);
                    return { text, slotIdx };
                }
                console.warn(`[visionAnalyzer] Response too short (${text.length} chars), trying next`);
            }
            catch (err) {
                const statusCode = err?.statusCode ?? 0;
                recordProviderKeyFailure(providerId, candidate, err);
                triedFingerprints.add(candidate.fingerprint);
                console.warn(`[visionAnalyzer] ${slot.provider} key=${apiKey.slice(0, 10)}... failed: ${err.message}`);
                // For rate-limit or auth — try next key after a safety delay to avoid spamming
                if (statusCode === 429 || statusCode === 401 || statusCode === 403) {
                    await new Promise((resolve) => setTimeout(resolve, 3000));
                    continue;
                }
                // For network errors — also try next key after a safety delay
                if (!statusCode || statusCode >= 500) {
                    await new Promise((resolve) => setTimeout(resolve, 3000));
                    continue;
                }
                // Client errors (400, 422) — likely model/param issue, skip to next slot
                break;
            }
        }
    }
    return null;
}
// ──────────────────────────────────────────────────────────────────────────────
// JSON extraction helper (handles markdown fences)
// ──────────────────────────────────────────────────────────────────────────────
function extractJson(text) {
    // Strip <think>...</think> if present (Qwen-style)
    const cleaned = text.replace(/<think>[\s\S]*?(?:<\/think>|$)/gi, "").trim();
    // Try direct parse
    try {
        return JSON.parse(cleaned);
    }
    catch { /* continue */ }
    // Try to find {} block
    const match = cleaned.match(/\{[\s\S]*\}/);
    if (match) {
        try {
            return JSON.parse(match[0]);
        }
        catch { /* continue */ }
    }
    throw new Error("Не удалось извлечь JSON из ответа модели");
}
// ──────────────────────────────────────────────────────────────────────────────
// Main export
// ──────────────────────────────────────────────────────────────────────────────
export async function analyzeImagingStudy(imageBase64) {
    // Check we have at least one key available
    const groqKeys = getProviderKeyCandidates(GROQ_PROVIDER_ID);
    const geminiKeys = getProviderKeyCandidates(GEMINI_PROVIDER_ID);
    if (!groqKeys.length && !geminiKeys.length) {
        throw new Error("Нет доступных ключей для AI-анализа (ни Groq, ни Gemini)");
    }
    // ── PRE-PROCESSING: CLAHE & Unsharp Mask (matches ShadowAnalyst logic) ──
    let processedBase64 = imageBase64;
    try {
        const base64Data = imageBase64.replace(/^data:image\/\w+;base64,/, "");
        const rawBuffer = Buffer.from(base64Data, "base64");
        const enhancedBuffer = await sharp(rawBuffer)
            .resize({ width: 1000, height: 1000, fit: "inside", withoutEnlargement: true })
            .clahe({ width: 8, height: 8, maxSlope: 2.0 })
            .sharpen({ sigma: 3.0 })
            .jpeg({ quality: 85 })
            .toBuffer();
        processedBase64 = `data:image/jpeg;base64,${enhancedBuffer.toString("base64")}`;
        console.log("[visionAnalyzer] Image successfully enhanced via sharp (CLAHE + Sharpen)");
    }
    catch (err) {
        console.warn("[visionAnalyzer] Image enhancement with sharp failed, falling back to original image", err);
    }
    // ── PASS 1: Primary analysis ──────────────────────────────────────────────
    const pass1Slots = [
        { provider: "groq", model: GROQ_VISION_MODEL },
        { provider: "gemini", model: GEMINI_VISION_MODEL }
    ];
    const pass1 = await runCascade(pass1Slots, PASS1_PROMPT, processedBase64, undefined, 80);
    if (!pass1) {
        throw new Error("Анализ не выполнен: все AI-провайдеры недоступны или вернули пустой ответ");
    }
    let pass1Parsed;
    try {
        pass1Parsed = extractJson(pass1.text);
    }
    catch {
        // If JSON extraction fails, return what we have as summary
        return {
            summary: pass1.text.slice(0, 2000),
            toothUpdates: [],
            _meta: { pass1Model: pass1Slots[pass1.slotIdx].model, pass2Model: null }
        };
    }
    const pass1ModelName = pass1Slots[pass1.slotIdx].model;
    // ── PASS 2: Critic pass (different model from pass1 if possible) ──────────
    // Use the other provider for critic, or cycle to the next slot
    const pass2Slots = [
        ...pass1Slots.slice(pass1.slotIdx + 1),
        ...pass1Slots.slice(0, pass1.slotIdx + 1)
    ];
    const pass1Summary = typeof pass1Parsed.summary === "string" ? pass1Parsed.summary : JSON.stringify(pass1Parsed);
    const pass2ExtraText = `Вот первичный анализ, который нужно критически проверить и улучшить:\n\n${pass1Summary}`;
    const pass2 = await runCascade(pass2Slots, PASS2_PROMPT, processedBase64, pass2ExtraText, 100);
    let finalResult = pass1Parsed;
    let pass2ModelName = null;
    if (pass2) {
        try {
            const pass2Parsed = extractJson(pass2.text);
            // Merge: prefer pass2 if it has more content
            if (pass2Parsed.toothUpdates?.length >= (pass1Parsed.toothUpdates?.length ?? 0) &&
                (pass2Parsed.summary?.length ?? 0) >= (pass1Parsed.summary?.length ?? 0) * 0.7) {
                finalResult = pass2Parsed;
            }
            pass2ModelName = pass2Slots[pass2.slotIdx]?.model ?? null;
        }
        catch {
            console.warn("[visionAnalyzer] Pass 2 JSON parse failed, using Pass 1 result");
        }
    }
    // ── Normalise output ──────────────────────────────────────────────────────
    const toothUpdates = Array.isArray(finalResult.toothUpdates)
        ? finalResult.toothUpdates.map((u) => ({
            code: String(u.code ?? "unknown"),
            state: String(u.state ?? "watch"),
            diagnosisOrFinding: String(u.diagnosisOrFinding ?? ""),
            notes: u.notes ? String(u.notes) : undefined
        }))
        : [];
    return {
        summary: typeof finalResult.summary === "string" ? finalResult.summary : JSON.stringify(finalResult),
        toothUpdates,
        _meta: { pass1Model: pass1ModelName, pass2Model: pass2ModelName }
    };
}
