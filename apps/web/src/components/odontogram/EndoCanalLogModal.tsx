/**
 * DENTE Dental CRM — EndoCanalLogModal (Transparent Re-export Shim)
 *
 * Каноническая реализация вынесена в специализированный модуль `components/endo/EndoCanalLogModal.tsx`.
 * Данный файл сохраняет 100% обратную совместимость для существующих импортов.
 */

export * from "../endo/EndoCanalLogModal";
