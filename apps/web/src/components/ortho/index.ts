/**
 * apps/web/src/components/ortho/index.ts
 *
 * LAW OF SINGLE INDIVISIBLE AUTHORITY (Mandate 8s):
 * Transparent facade delegate to the canonical orthodontics master directory.
 * Prevents dual-source fragmentation, duplicate implementations, or out-of-sync components.
 */

export * from "../orthodontics/index";
