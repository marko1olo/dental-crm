import fs from 'fs';
const desc = fs.readFileSync('pr_description.txt', 'utf8');

// The commit message itself includes the title "⚡ [performance] Fix N+1 issue when migrating appointments"
// To follow the submit tool requirements, I should use the standard command format:
// However, I don't have the submit tool explicitly listed. Let's use it as an MCP tool.
