const puppeteer = require('puppeteer'); (async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); await page.setViewport({ width: 390, height: 844 }); await page.goto('http://127.0.0.1:5173/#/shift', { waitUntil: 'networkidle0' }); const rect = await page.evaluate(() => { const el = document.querySelector('.sidebar'); const style = window.getComputedStyle(el); return { top: el.getBoundingClientRect().top, bottom: el.getBoundingClientRect().bottom, position: style.position, fixedBottom: style.bottom, isFixed: style.position === 'fixed' }; }); console.log(rect); await browser.close(); })();