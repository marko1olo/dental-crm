=== apps/web/src/hooks/useMaxSettings.ts
  строка 64 [потеря данных] класс:error-as-empty
  код: } else if (res.status !== 404) { console.error("Failed to load MAX settings", res.status); }
  вред: Ядро находки верно (молчаливый провал загрузки + деструктивный PUT из непроинициализированных черновиков), но детали вреда неточны в четырёх местах. 1) «Поле Webhook пустое» — неверно. Блок «Webhook URL для MAX» (MaxSettingsPanel.tsx:127-151) — это не input, а read-only <code> с вычисленным ${serverBaseUrl ?? window.location.origin}/api/max/webhook; он никогда не выглядит пустым. webhookUrlDraft вообще не имеет UI в этой панели — и это хуже, а не лучше: при неудачной загрузке он остаётся "", уходит в PUT как webhookUrl:null и обнуляет сохранённый webhookUrl в БД (max.ts:173-174) без каких-либо следов на экране. 2) Пропущена главная потеря данных: staffRoutingDraft тоже остаётся {defaultUserI
  почему подтверждено: Подтверждено чтением. useMaxSettings.ts:57-66 — при любом ответе кроме 2xx/404 (реально достижимы 500 при ошибке БД и 401/403 из preHandler requireNonDoctorAccess и requireVerifiedOrganizationId, accessGuard.ts:103-129) ошибка уходит только в console.error: хук не отдаёт наружу поля ошибки (return-объект, строки 138-156), а MaxSettingsPanel.tsx не рисует ни одного сообщения о неудачной загрузке. settings остаётся null, все черновики остаются на инициализаторах (строки 42-49): botIdDraft="", webh
=== apps/web/src/hooks/useWhatsappSettings.ts
  строка 66 [потеря данных] класс:error-as-empty
  код: } else if (res.status !== 404) { console.error("Failed to load WhatsApp settings", res.status); }
  вред: Вред наступает не от нажатия «Сохранить» на нетронутой форме — там кнопка заблокирована защитой `dirty` (WhatsappSettingsPanel.tsx:66-77, 258). Точная цепочка: GET /api/whatsapp/settings падает с не-404 (или обрывается сетью — ветка catch на useWhatsappSettings.ts:69), хук молчит (только console.error), панель показывает пустые поля, выключенный тумблер и бейдж «Не подключён» — визуально идентично «канал не настроен». Администратор, решив, что WhatsApp не подключён, делает ОДНО любое изменение — включает тумблер «Активен», отмечает любую фичу, вводит Phone Number ID — и `dirty` становится true, кнопка разблокируется. Тогда PUT отправляет весь набор полей из пустых черновиков (useWhatsappSett
  почему подтверждено: Прочитал весь хук, панель-потребитель, серверный PUT и путь монтирования. Дефект есть, но заявленный механизм неточен. Подтверждено: 1. `useWhatsappSettings.ts:52-74` — при не-404 ответе (500/503, сетевой сбой в catch на 69) хук не выставляет НИКАКОГО признака ошибки: возвращаемый объект (142-162) содержит только `loading`, `saveState`, `saveError` (это ошибка сохранения, не загрузки). Состояния `loadError` не существует. `settings` остаётся `null`, черновики — пустыми. 2. `WhatsappSettingsPanel
