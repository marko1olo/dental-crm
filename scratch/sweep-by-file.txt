=== apps/web/src/AppHelpers.tsx
  строка 3484 [ложь на экране] класс:prefilled-lie
  код: "36 | окклюзионная, дистальная | кариес | кариес дентина 36 зуба по осмотру и снимку | ..."
  вред: Дефект реален, но механика вреда описана неточно в двух местах. (а) «Врач жмёт создать не раскрывая форму» — так не бывает ни для одного из этих документов: у каждого валидатор упирается либо в пустое по умолчанию обязательное поле (procedureConsentProcedureName; prescriptionMedication/Dosage/Instructions/Duration; все шесть полей наряда ЗТЛ; xrayClinicalQuestion/xrayIndication; recordExtractPeriodStart/End — documentStore.ts:1832, 2439-2463, 2471-2473), либо в неотмеченный чекбокс (план лечения ×3 — documentStore.ts:2299-2303; согласование плана; выписка ×2 — 2602-2604; карта 025/у ×3; процедурное согласие ×3 — 1866-1870), а все эти контролы лежат ВНУТРИ того же свёрнутого блока «✏️ Ручная 
  почему подтверждено: Подтверждено чтением всей цепочки, защиты нет. 1) AppHelpers.tsx:3483-3484 — живая экспортируемая константа с конкретным зубом и готовым диагнозом «кариес дентина 36 зуба по осмотру и снимку». 2) documentStore.ts:2267 — это начальное значение поля clinicalToothRowsText; стор создаётся один раз (documentStore.ts:2678), функции сброса/перегидратации нет, по всему репозиторию только ДВА писателя этого ключа: инициализация стора и onChange текстареа (useAppLogic.tsx:11982) — то есть ни чарт, ни ИИ, 

=== apps/web/src/PaymentCapture.tsx
  строка 380 [косметика] класс:ghost-token
  код: background: "var(--paper)", padding: "16px", borderRadius: "8px", border: "1px solid var(--brand-200)"
  вред: Уточнения к формулировке, сам дефект стоит. Достижимость: блок не виден сразу. InstallmentCalculator рендерится как `<details ... open={isOpen}>` с `isOpen={false}` (PaymentCapture.tsx:352, :717), то есть калькулятор свёрнут по умолчанию — кассир видит только строку summary «Рассрочка от клиники, без банка» и должен по ней щёлкнуть. Обработчика onToggle нет, поэтому после клика блок остаётся раскрытым. Вред проявляется только на раскрытом блоке, а не при входе на экран оплат. Насколько «читается» плашка — точные числа. В светлой теме родитель `.smart-details-content` получает инлайновый `rgba(13,148,136,0.07)` поверх `#f7fbf9` у `.payment-capture-detail-section` (main.css:11843), итог ≈ `#e7
  почему подтверждено: Опровергнуть не удалось — все четыре канала опровержения проверены и закрыты. 1) Токен действительно отсутствует. Поиск `--brand-200\s*:` по всему репозиторию (без node_modules) — ноль совпадений. Единственный другой потребитель, apps/web/src/styles/main.css:16224 (`.smart-ai-hints-popup`), защищён запасом: `border: 1px solid var(--brand-200, #bae6fd)`. В инлайне PaymentCapture.tsx:380 запаса нет. Показательно, что apps/web/src/styles/token-aliases.css — файл, созданный именно для добора отсутст

=== apps/web/src/ScannerView.tsx
  строка 84 [ложь на экране] класс:prefilled-lie
  код: const [status, setStatus] = useState<"passed" | "failed">("passed");
  вред: Описание верно по сути, но требует трёх уточнений. Ссылка на строки: комментарий про фокус и useEffect — это строки 122-125 (плюс ref на 96 и 265), а 236-238 — это текстовое поле автоклава, не фокус. Значение не спрятано: селект «Результат» стоит прямо над полем штрих-кода и всё время показывает «Обработан», а после отправки тост прямо говорит «записан как обработанный» (193-198), и в журнале ниже рисуется бейдж статуса (321-329). То есть это не подлог за спиной пользователя, а отсутствие обязательного выбора: аффирмативное значение по умолчанию на поле, которым клиника отчитывается, и рабочий цикл (автофокус на штрих-коде + Enter от сканера) никогда не заставляет его тронуть. Более острая п
  почему подтверждено: Опровергнуть не удалось — ни один из четырёх путей опровержения не сработал. 1) Экран отрисовывается. `appViews` в apps/web/src/workspaceShell.tsx:54 содержит "scanner", ярлык «Стерилизация» (:68), роли doctor (:174) и assistant (:177) видят раздел в меню; ветка монтирования живая: apps/web/src/App.tsx:409 (lazy import) и :4814-4820 (`currentView === "scanner"` → `<ScannerView />` под Suspense и WorkspaceRouteErrorBoundary). Мёртвый AppRouter.tsx здесь ни при чём. 2) Защиты рядом нет. Автор явно

=== apps/web/src/VisitView.tsx
  строка 1113 [ложь на экране] класс:ghost-token
  код: background: 'var(--amber-50)', ... border: '1px solid var(--amber-200)'
  вред: Дефект реален, но два пункта описания надо уточнить. Первое — «единственное объяснение» неверно в общем случае. В репозитории есть вторая, корректно оформленная копия той же плашки: `apps/web/src/components/visit/VisitEmkTab.tsx:292-306`. Там классы Tailwind (`bg-amber-50 dark:bg-amber-950/40 border border-amber-200 dark:border-amber-900/60`), инлайновых стилей нет вовсе, поэтому `.visit-note-missing` из main.css применяется целиком, да ещё и тёмные варианты присутствуют. `<VisitEmkTab />` рендерится на `VisitView.tsx:350` внутри вкладки `visitSubViewTab === "emk"`, а эта вкладка — значение по умолчанию (`VisitView.tsx:155`), и стоит она ВЫШЕ сломанной копии. Обе плашки несут один и тот же `
  почему подтверждено: Подтверждено чтением окружения, опровергнуть не удалось ни по одному из критериев. 1) Токенов нет. `--amber-50/-200/-800/-900` не определены нигде в репозитории (grep по *.css/*.ts/*.tsx/*.html — ноль определений). `main.css` знает только `--amber` и `--amber-soft` (строки 29-30, 91-92, 151-152). Tailwind v4 подключён через `@tailwindcss/vite`, но он эмитит палитру под другими именами: в собранном `apps/web/dist/assets/index-p--V_Gf0.css` есть `--color-amber-50:oklch(98.7% .022 95.277)`, а `--am

=== apps/web/src/components/PatientPortal.tsx
  строка 185 [ложь на экране] класс:numeric-string
  код: (s: number, i: any) => s + (i.totalAmount || 0),
  вред: Точное описание вреда (два уточнения к заявленному). Кто и как видит. Пути «пациент открывает портал» в текущем коде нет: PatientPortal импортируется ровно в одном месте — SettingsTelegramTab.tsx:5/626, модалка «Превью Портала Пациента». URL-маршрута к нему нет (main.tsx рендерит только AppShell, в реестре appViews в workspaceShell.tsx:54 записи портала нет). Реально наблюдаемый путь один: администратор/управляющий (любая роль, кроме doctor) → Настройки → вкладка «Мессенджеры» (id "telegram") → поле «Портал пациента» → кнопка «Предпросмотр» → вход по телефону пациента и коду из OTP. После входа блок «План лечения» показывает нули. Что именно показывается. Нулями выводятся не два, а все три п
  почему подтверждено: Дефект подтверждён чтением окружения, опровергнуть не удалось. 1) Поля totalAmount не существует. apps/api/src/routes/portal.ts (GET /me, ~строки 606-630) возвращает сырые строки: `const plans = await db.select().from(treatmentPlans).where(eq(treatmentPlans.patientId, patient.id))` и отдаёт их в ответе как `plans` без какого-либо маппинга. В apps/api/src/db/schema.ts:1338-1356 таблица treatment_plans объявлена так: `totalPriceRub: numeric("total_price_rub", { precision: 12, scale: 2 })` и `total

  строка 411 [ложь на экране] класс:numeric-string
  код: {(stage.totalAmount || 0).toLocaleString()} ₽
  вред: Дефект настоящий, но вред описан неточно в двух местах. Кто это видит. Единственная точка рендера PatientPortal в репозитории — модал «Превью Портала Пациента» в CRM: Настройки → Мессенджеры → Telegram → кнопка «Предпросмотр» (SettingsTelegramTab.tsx:601-627). Отдельного пациентского входа нет: у apps/web одна точка входа (index.html/main.tsx), и других мест рендера PatientPortal grep по apps/web/src не находит. Плюс список планов вообще отрисовывается только при isAuthenticated === true (строки 345+), а стартовое состояние — false (строка 141), т.е. до списка нужно пройти вход по телефону+OTP либо иметь уже лежащий patient_token в localStorage. Поэтому «0 ₽» увидит сотрудник клиники (не doc
  почему подтверждено: Подтверждено чтением всей цепочки данных, а не одной строки. 1) Колонки totalAmount не существует. apps/api/src/db/schema.ts:1338-1356 — treatmentPlans содержит totalPriceRub: numeric("total_price_rub", {precision:12, scale:2}) и totalPrice: numeric("total_price", {precision:12, scale:2}) (обе без mode:"number", т.е. Drizzle отдаёт строку). Поля totalAmount в таблице нет. 2) Сервер не переименовывает поля. apps/api/src/routes/portal.ts:608-632 — plans = await db.select().from(treatmentPlans).whe

=== apps/web/src/components/SmartMicrophoneButton.tsx
  строка 81 [косметика] класс:ghost-token
  код: color: isRecording ? 'var(--red-600)' : isProcessing ? 'var(--blue-600)' : 'var(--brand-500)'
  вред: Дефект реальный, но список мест и оценка потери преувеличены. НЕ ЗАСЧИТЫВАТЬ два из названных мест: - PatientCoreForm.tsx:54 — мёртвый код. `PatientCoreForm` экспортируется, но не импортируется НИГДЕ в apps/ и packages/ (единственные упоминания — сам файл). Пользователь этой кнопки не видит. Заодно это поле «Заметки для команды», а не «быстрый ввод пациента». - SettingsImportsTab.tsx:4065 — передаёт `style={{ color: 'var(--slate-500)', borderColor: 'var(--slate-300)' }}`; `appearance` разворачивается ПОСЛЕ `color` (строка 96), а `--slate-500` объявлен (main.css:43, dente-redesign.css:52). Значит глиф там всегда приглушённо-серый во всех трёх состояниях, и недействительные `--red-600`/`--blue
  почему подтверждено: Подтверждено чтением. 1) Объявлений `--red-600`, `--blue-600`, `--blue-100` в репозитории НЕТ ни одного: grep по apps/ и packages/ даёт только сами две строки использования (SmartMicrophoneButton.tsx:80-81), а поиск `--red-600:` по всему дереву (кроме node_modules/.postgres) — 0 совпадений. Tailwind v4.3.3 действительно подключён (styles/tailwind.css импортирует `tailwindcss/theme.css layer(theme)`), но его палитра называется `--color-red-600`/`--color-blue-600` (node_modules/tailwindcss/theme.c

=== apps/web/src/components/auth/StaffPinPad.tsx
  строка 50 [ложь на экране] класс:error-as-empty
  код: const data = await response.json(); if (!response.ok) throw new Error(data.message || "Неверный PIN-код");
  вред: Дефект есть, но вред описан неверно в трёх пунктах, и один из них переворачивает исправление. ЧТО НА САМОМ ДЕЛЕ УВИДИТ ПОЛЬЗОВАТЕЛЬ. Тост НЕ говорит «Неверный PIN-код». `showToast(err.message ...)` (строка 60) выводит сырое сообщение парсера браузера: в Chrome `Unexpected token '<', "<html>..." is not valid JSON`, при пустом теле 500 — `Unexpected end of JSON input`. Поэтому неверно, что сбой сервера «выглядит ТОЧНО как неверный PIN»: текст визуально ОТЛИЧАЕТСЯ от штатного «Неверный PIN-код.» Совпадает только физическая хореография отказа — красный тост, тряска поля (61), стирание набранного PIN (62). Реальный вред: врач получает нечитаемую английскую техническую строку, которая не сообщает 
  почему подтверждено: Подтверждено чтением, опровергнуть не удалось ни по одному из четырёх критериев. 1) Защиты рядом нет. StaffPinPad.tsx:50 — `const data = await response.json();` вызывается безусловно: нет `.catch(() => ({}))`, нет проверки `content-type`, нет `?.`/`??`, нет проверки `response.ok` выше. Единственный `try` — общий на весь блок (39-67), поэтому SyntaxError парсера попадает в тот же `catch` (58), что и логический отказ сервера. 2) Глобальная обёртка fetch не спасает. `apps/web/src/lib/apiAuthFetch.t

=== apps/web/src/components/finance/FamilyWalletPanel.tsx
  строка 79 [потеря денег] класс:error-as-empty
  код: setFamily(res.ok ? await res.json() : null);
  вред: Механика описана верно, но последствия завышены. Точнее так: любой не-ok ответ (500, 401/403 от requireResolvedOrganizationId, обрыв сети) приводит к family=null, и панель исчезает молча — без текста ошибки и без возможности повторить загрузку; отличить это от штатного «пациент не в семье» невозможно, потому что API отдаёт 404 и на штатный случай (finance_family.ts:210-212). Чего заявленный вред НЕ доказывает: деньги при этом не теряются и проводки не искажаются — панель ничего не пишет до нажатия кнопки, долг остаётся зафиксированным, и списание с семейного счёта можно сделать позже, когда панель загрузится. Формулировка «кассир берёт всю сумму наличными» — возможное следствие, а не то, что
  почему подтверждено: Подтверждено чтением окружения, а не одной строки. (1) Защиты рядом нет: строка 79 `setFamily(res.ok ? await res.json() : null)` сводит ЛЮБОЙ не-ok ответ к null, ветка catch (80-84) делает то же; ни `res.status`, ни отдельного состояния ошибки в компоненте нет, тоста тоже нет — для HTTP-отказа даже console.error не вызывается (он только в catch). Строка 231 `if (!family) return null;` действительно убирает панель целиком. Существующие `family?.balance` (113) и `family.members ?? []` (246) защища

=== apps/web/src/components/imaging/VisiographAnalyzer.tsx
  строка 207 [ложь на экране] класс:error-as-empty
  код: if (!res.ok) return;
  вред: Две поправки к описанию вреда. (а) Строка 466 гасит НЕ «весь блок архива», а только круглый счётчик в `<summary>` (span 466-477). Сам `<details>` (456), подпись «ИИ-Анализ снимка (ShadowAnalyst)» (463-465), шапка «ShadowAnalyst · Dental AI» (496) и зона загрузки (554-607) рендерятся всегда. Исчезают именно счётчик и секция «История снимков (N)» на 744-800. Панель на экране остаётся, поэтому врач не заподозрит поломку — она выглядит полностью рабочей. (б) Пропущено более тяжёлое следствие той же строки: показ снимков ДРУГОГО пациента. Эффект на 193-199 очищает `scanHistory` только когда пациент НЕ выбран; при прямом переключении A→B (`setSelectedPatientId` в `store/patientStore.ts:69` пишет i
  почему подтверждено: Подтверждено чтением кода, не опровергнуто. 1. Код на месте, не исправлен. `apps/web/src/components/imaging/VisiographAnalyzer.tsx:201-215`: `loadHistory` ставит `setIsLoadingHistory(true)`, на 207 делает `if (!res.ok) return;`, на 210-211 `catch { // silent }`, в `finally` (212-214) гасит `isLoadingHistory`. `setError` в этой функции не вызывается НИ РАЗУ — ни защиты `?.`/`??`, ни ветки по умолчанию, ни обработки на границе нет. `scanHistory` не трогается вообще. 2. Компонент реально отрисовыва

  строка 562 [мёртвая кнопка] класс:ghost-token
  код: border: `2px dashed ${isDragOver ? 'var(--teal)' : 'var(--border)'}`
  вред: Дефект есть, но вред описан шире и резче, чем происходит на экране. Точное описание: а) Панель по умолчанию СВЁРНУТА. Строка 456 — `<details className="visiograph-analyzer-details">` без `open`, и класс `.visiograph-analyzer-details` не описан ни в одном CSS (единственное вхождение во всём apps/ — сама эта строка). Пока врач не нажмёт `<summary>`, зоны загрузки на экране нет вообще. Сам «вход в ИИ-диагностику» — это строка summary (иконка ScanLine цветом `var(--teal)`, подпись «ИИ-Анализ снимка (ShadowAnalyst)», счётчик на `var(--teal)`/`var(--on-teal)`), и она НЕ сломана: все её токены объявлены. Утверждение «точка входа во всю ИИ-диагностику выглядит как обычный абзац» неверно. б) После ра
  почему подтверждено: Опровергнуть не удалось. Проверено чтением окружения: 1) Токены не объявлены нигде. Поиск объявлений `(^|[;{\s})--border\s*:` и `--bg-inset\s*:` по всем `*.css` в `apps/` (main.css, token-aliases.css, dente-redesign.css, shadow-analyst.css, dente-operations.css, premium.css, contrast-fixes.css и т.д.), по `apps/web/index.html` и по `node_modules/tailwindcss/theme.css` даёт НОЛЬ совпадений. Существуют только другие имена: `--border-default`, `--border-subtle` (token-aliases.css:61-62), `--border-

  строка 516 [мёртвая кнопка] класс:ghost-token
  код: border: '1px solid var(--border)', borderRadius: '8px'
  вред: Дефект есть, но описание вреда неточно в трёх местах, и в одном — недооценивает масштаб. НЕВЕРНО «ни один токен не объявлен»: --teal объявлен (main.css:21, 83, 143). Поэтому кнопка «Озвучить» сломана только в состоянии покоя: при isSpeaking она получает background:var(--teal) и color:'white' и рисуется нормальной залитой бирюзовой плашкой (строки 514-515). НЕВЕРНО «фактически не находятся глазами». color:var(--text-muted) недействителен, а color НАСЛЕДУЕТСЯ, поэтому свойство берёт значение предка — обычный основной цвет текста (:root{color:#111827} в светлой, #f8fafc в тёмной, main.css:6 и 71). То есть глифы рисуются НЕ приглушённым серым, а полноконтрастным цветом текста — виднее, чем задум
  почему подтверждено: Опровергнуть не удалось. (1) Токены действительно не объявлены: сплошной поиск по всем 33 таблицам стилей apps/web/src и по inline-стилям даёт НОЛЬ объявлений для --border, --text-muted, --surface, --bg-inset и --text. Реальная палитра проекта — --line/--line-strong, --muted, --ink, --paper (main.css:13-19, 68-126, 128-184). (2) Защиты рядом нет: все 14 вхождений var(--border) в этом файле стоят без второго аргумента, в строках 516, 528, 540 запаса нет. (3) Механика верна: inline-объявление выиг

  строка 481 [косметика] класс:ghost-token
  код: border: '1px solid var(--border)', borderRadius: '14px', background: 'var(--surface)'
  вред: Находка верна, но описание вреда занижено и в одной детали неточно. Неточность: панель лежит внутри `<details>` без атрибута `open` (стр. 456). Врач видит дефект не сразу при входе на экран приёма, а после раскрытия сводки «ИИ-Анализ снимка (ShadowAnalyst)». Сама сводка (стр. 457-477) оформлена другими токенами и выглядит нормально. Также «растворяется» — сильное слово: текст, иконки и кнопки внутри блока по-прежнему видны; теряется именно карточка — граница, заливка и отделяющая полоса шапки, из-за чего блок читается как рыхлый текст на фоне экрана, а не как модуль. Занижение: те же необъявленные имена стоят без запаса ещё в 16 местах ЭТОГО же файла, поэтому внутренности блока теряют рамки 
  почему подтверждено: Опровергнуть не удалось — ни одно из четырёх оснований не сработало. 1. Токены действительно не объявлены нигде. Поиск по всему репозиторию (кроме node_modules) объявлений `--border:`, `--surface:`, `--bg-inset:` в любом виде — в .css, .html, .ts, .tsx, в `@property`, через `style.setProperty` — даёт НОЛЬ совпадений. Контрольный тот же запрос по `--paper:` и `--line:` находит по 9 объявлений в main.css / dente-redesign.css / premium.css, то есть регулярка рабочая. `apps/web/src/styles/token-alia

  строка 689 [косметика] класс:ghost-token
  код: borderBottom: idx < reportSections.length - 1 ? '1px solid var(--border)' : 'none'
  вред: Механика верна, но оценка последствий завышена в одном месте. Что реально видит врач: карточка «Полный отчёт ShadowAnalyst» теряет рамку (676) — не «серую рамку по умолчанию», а border-style: none целиком; шапка теряет и заливку (678, background становится transparent), и нижний разделитель (680); линии между разделами (689) не рисуются; открытая строка раздела получает transparent против transparent (694) и по фону НИКОГДА не отличается от закрытых. Это подтверждено. Неточность: раскрытый раздел выдаёт не «только повёрнутая стрелка» — сразу под его заголовком рендерится тело раздела (711–721) с отступом padding-left 34px и обычным начертанием, тогда как заголовки идут жирными (fontWeight 60
  почему подтверждено: Опровергнуть не удалось. (1) Токены действительно не объявлены: main.css задаёт --line/--line-strong/--paper/--ink/--muted/--teal*, а не --border; token-aliases.css — файл псевдонимов — объявляет --border-default: var(--line-strong) и --border-subtle: var(--line), но НЕ --border и НЕ --bg-inset. Машинный подсчёт по всем 15 css в apps/web/src: --border 0 объявлений, --bg-inset 0, --text-muted 0, --text 0, --surface 0. В node_modules/tailwindcss/theme.css (единственный подключённый слой темы, pref

  строка 662 [косметика] класс:ghost-token
  код: border: `1px solid ${isCritical ? 'transparent' : isDone ? 'var(--teal)' : 'var(--border)'}`
  вред: Вред занижен: нейтральный чип теряет не два, а три свойства. Помимо background: var(--bg-inset) (660) и border: 1px solid var(--border) (662) недействителен и color: var(--text-muted) (661) — это имя в CSS встречается только с запасом (main.css:14773+, shadow-analyst.css:196/262, contrast-fixes.css:104) и не объявлено ни в одной теме. Механика: недействительное на этапе вычисления значение делает свойство unset — background не наследуется, значит transparent; border-шорткат откатывается к border-style: none (ширина обнуляется); color наследуется, значит берётся цвет текста родителя. Итог для врача: чипы состояния 'planned' (и любого нераспознанного состояния из ответа модели) рисуются без за
  почему подтверждено: Подтверждено чтением окружения. (1) Ни --border, ни --bg-inset не объявлены нигде: анкорный поиск '^\s*--border\s*:' и '^\s*--bg-inset\s*:' по всем *.css — пусто; поиск по всему репозиторию по любым расширениям даёт только справочные сниппеты в .agents/skills/** (shadcn-theming.md, tailwind-integration.md), не в apps/web. Из JS они тоже не выставляются: setProperty('--border'…) отсутствует, а единственные кастомные свойства из JS-объектов стиля — --mpr-axis-deg, --mpr-slab-width, --mpr-slice-pos

  строка 771 [косметика] класс:ghost-token
  код: border: '1px solid var(--border)', background: 'var(--bg-inset)'
  вред: Механизм верен, но вред описан шире и уже одновременно, чем есть. Точно: --border, --bg-inset, --surface не объявлены нигде и стоят без запаса. Строка 771 — вся сокращённая запись border и весь background строки истории недействительны на этапе вычисления значения, поэтому border-style берёт начальное none, background-color — transparent. То же на 777/778 для квадрата 36x36. Строка истории — это <button> без className, и unset гасит в том числе браузерное оформление кнопки: плоский прозрачный прямоугольник без рамки. Рядом только gap: 6px (строка 761) и padding 10px 12px, то есть разделены записи лишь 6-пиксельным просветом, без линии и без подложки. Неточно в трёх местах: 1. «без плашки-пре
  почему подтверждено: Опровергнуть не удалось. (1) Перечислил все объявления кастомных свойств во всех 51 css-файле apps/web/src — 146 имён; --border, --bg-inset, --surface среди них НЕТ (есть только --border-default/--border-subtle/--border-300/400 и --surface-0…--surface-900). Расширил поиск на все .css/.scss/.html в apps/ и packages/ (64 файла) и на node_modules/tailwindcss/theme.css — объявлений нет. Ни @property, ни setProperty, ни инлайновое объявление их не задают (из JS ставятся только --mpr-*, --sa-viewer-*,

  строка 459 [косметика] класс:ghost-token
  код: padding: '8px 0', userSelect: 'none', listStyle: 'none', color: 'var(--text-muted)'
  вред: Дефект есть, но счёт «20 мест» завышен, а список последствий надо разделить. Точно: видимо меняются только 16 мест с --text-muted — они рисуются цветом --ink (main.css:14262-14265 body { color: var(--ink) }), то есть ровно основным цветом текста, в светлой, тёмной и ночной теме. Четыре места с var(--text) (583, 591, 717, 783) визуального дефекта НЕ дают: наследуемое значение и есть --ink, то есть именно то, чем --text задумывался, — это латентный долг имени, а не видимая поломка. Значит «текст отчёта» (717) и имя файла в истории (783) из описания вреда надо убрать: они уже отрисованы верным цветом. Уточнения по механике: для пяти <button> (515, 527, 539, 733, 751) inline-объявление всё равно
  почему подтверждено: Подтверждено чтением окружения. (1) Имя не объявлено: поиск по всему apps/ не находит ни одного объявления --text-muted или --text; в css-файлах имя стоит только с запасом (main.css:14773+ `var(--text-muted, #718096)`, styles/shadow-analyst.css:196,262 `#94a3b8`, styles/contrast-fixes.css:104 `#5a6878`), а в собранном apps/web/dist/assets/index-p--V_Gf0.css встречаются исключительно формы с запасом — объявления нет. Реальные токены темы — --ink/--ink-2 (main.css:16,78,138); в premium.css есть --

=== apps/web/src/components/odontogram/TreatmentEstimator.tsx
  строка 153 [потеря данных] класс:error-as-empty
  код: .then((response) => (response.ok ? response.json() : null))
  вред: Дефект настоящий, но врач в типичном случае увидит НЕ пустой план. Утверждение «items остаются [], состояние ровно одно — «План лечения пуст»» верно только для пациента без отмеченных патологий. Эффект автоподбора (строки 175-413) на каждое изменение currentTeeth заполняет items из зубной формулы, а подписанный план почти всегда означает отмеченные зубы. То есть после отказа GET калькулятор показывает полный список автоматически подобранных позиций и ненулевое «Итого» — внешне нормальную смету, без единого признака, что сохранённый план не загрузился. Врачу даже не надо ничего набирать заново: достаточно нажать «Сохранить». planId === null, `id: null` проходит zod-валидацию, сервер уходит в 
  почему подтверждено: Не опровергается ни по одному из оснований. (1) Монтирование живое: TreatmentEstimator рендерится безусловно в OdontogramModule.tsx:740, тот подключён из PatientsView.tsx:489 (при выбранном пациенте) и VisitOdontogramTab.tsx:45; "patients" и "visit" есть в appViews (workspaceShell.tsx:54), App.tsx:3758-3770 рендерит PatientsView для currentView === "patients". (2) Защиты рядом нет: `data?.plans?.[0]` спасает только от падения; при не-ok ответе data === null, latestPlan === undefined, ранний выхо

=== apps/web/src/components/settings/SettingsPricesTab.tsx
  строка 336 [косметика] класс:ghost-token
  код: <FolderTree size={48} color="var(--border)" />
  вред: Дефект настоящий, но вред шире и выглядит иначе, чем описано. Триггер не только «пустой поиск»: условие Object.keys(groupedCatalog).length === 0 (строка 334) срабатывает и когда dashboard.serviceCatalog пуст, то есть у свежей клиники без услуг это ПЕРВОЕ, что администратор видит на Настройки -> Прайс, а не редкий случай безрезультатного поиска. Верстка при этом не съезжает: у svg заданы width/height=48 (Icon.js подставляет size в width/height), поэтому элемент занимает своё место в потоке, и администратор видит пунктирную рамку с колонкой по центру, где сверху ~48px пустоты, а под ней (margin-top: 16px у .empty-catalog-state p) надпись «Ничего не найдено» — то есть не «подпись без картинки»,
  почему подтверждено: Подтверждено чтением окружения, защиты нет. (1) Ветка отрисовывается: App.tsx:4238 монтирует SettingsView, "settings" есть в реестре appViews (workspaceShell.tsx:54), таб {id:"prices"} зарегистрирован в AppHelpers.tsx:6082, SettingsView.tsx:1496 рендерит <SettingsPricesTab />, а внутри компонента activeTab по умолчанию "catalog" — то есть блок со строкой 336 это дефолтный экран. (2) Механика lucide подтверждена по установленному коду apps/web/node_modules/lucide-react/dist/esm/Icon.js:22-34: def

  строка 238 [косметика] класс:ghost-token
  код: background: "rgba(59, 130, 246, 0.1)", color: "var(--blue)"
  вред: Механизм описан почти верно, но упущено, что у класса есть собственный цвет: .pricelist-section-icon задаёт `color: var(--teal)` (SettingsPricesTab.css:267), и --teal объявлен (#0d9488 в светлой, #2dd4bf в тёмной, #e0a458 в третьей теме — styles/dente-redesign.css:23/78/126, styles/main.css:21). Инлайн-стиль перебивает класс по специфичности, а так как --blue не объявлен, объявление становится invalid at computed-value time и по спецификации CSS Variables эквивалентно `unset`, что для наследуемого свойства color означает `inherit`. Откат на тиловый цвет класса НЕ происходит — глиф берёт цвет текста родителя (.pricelist-section-header и .pricelist-section-card своего color не задают, поэтому 
  почему подтверждено: Не удалось опровергнуть ни по одному из четырёх оснований. (1) Ветка отрисовывается: appViews содержит "settings" (workspaceShell.tsx:54) -> App.tsx:4226 монтирует <SettingsView> при currentView === "settings" -> SettingsView.tsx:1496 рендерит <SettingsPricesTab /> при settingsTab === "prices" -> settingsTabs содержит { id: "prices", title: "Прайс", group: "clinical" } (AppHelpers.tsx:6082). Внутри компонента activeTab по умолчанию "catalog" (строка 84), а спорная шапка стоит под `activeTab === 

=== apps/web/src/components/settings/SettingsProtocolsTab.tsx
  строка 336 [мёртвая кнопка] класс:ghost-token
  код: backgroundColor: "var(--dente-red-10)", color: "var(--dente-red-60)"
  вред: Дефект реален, но формулировку вреда надо уточнить в четырёх местах. (а) Вкладка «Протоколы» не ограничена ролью: фильтры в SettingsView.tsx:1200-1214 срезают только marketing/reporting/bpmn/rules/insurance/telegram, поэтому кнопку удаления видит любой сотрудник, включая врача, а не только администратор/владелец. (б) Наследуемый цвет — не «неизвестно какой»: ближайший источник — инлайн `color: var(--ink)` на `.settings-zone` (SettingsView.tsx:1296), поэтому глиф Trash2 (lucide рисует currentColor) выходит обычным цветом текста — читаемым, но ровно таким же, как нейтральная иконка, без единого красного пикселя. (в) Разница с «Редактировать» — в рамке и размере, а не в контрасте фона: `.protoc
  почему подтверждено: Подтверждено чтением. (1) `danger-button` действительно не описан ни одним правилом: repo-wide grep по всем файлам (без node_modules/dist) даёт единственное вхождение — сам className в SettingsProtocolsTab.tsx:332. (2) Токены `--dente-red-10`/`--dente-red-60` не объявлены нигде: поиск по шаблону объявления `--dente-red-N:` не даёт совпадений, и файл apps/web/src/styles/token-aliases.css, существующий ровно для подстановки необъявленных имён (--tomato, --danger, --red-100 и т.д.), их не покрывает

=== apps/web/src/components/useVisitDiaryLogic.ts
  строка 60 [потеря данных] класс:error-as-empty
  код: .then((r) => r.json())
  вред: Точный вред: при неудачном GET `/api/diaries/visit/{visitId}` (500 от БД, обрыв сети, либо 403/503 от `requireClinicalReadAccess` в конфигурации с NODE_ENV=production, поскольку клиент на строке 59 не посылает `x-dente-admin-secret`) хук молча оставляет `EMPTY_DIARY`. Врач видит уже сохранённый дневник как ПУСТОЙ и полностью редактируемый, без тоста, спиннера и любого признака ошибки — визуально неотличимо от нового приёма. Дополнительно: `isLocked`/`lockedAt`/`diaryHash`/`revisionCount` тоже остаются в исходном состоянии (строки 52-57), поэтому ПОДПИСАННЫЙ и запечатанный дневник выглядит неподписанным и открытым к правке, а кнопка «Подписать» доступна (ошибка вскроется только на 409 от `/lo
  почему подтверждено: Дефект на строке 59-60 подтверждён, но заявленный вред (затирание клинического текста) невозможен — проверено по серверу. ЧТО ПОДТВЕРЖДЕНО (первая половина находки): - `apps/web/src/components/useVisitDiaryLogic.ts:59-90`: `fetch(...).then(r => r.json())` без проверки `r.ok`. При ошибочном ответе с JSON-телом (`{error, message}` — например 500, либо 403 `ClinicalReadSecretRequired` / 503 `ClinicalReadSecretMissing` из `apps/api/src/accessGuard.ts:56-84`) поле `diaryData.diary` отсутствует, `if (

=== apps/web/src/hooks/useMaxSettings.ts
  строка 64 [потеря данных] класс:error-as-empty
  код: } else if (res.status !== 404) { console.error("Failed to load MAX settings", res.status); }
  вред: Ядро находки верно (молчаливый провал загрузки + деструктивный PUT из непроинициализированных черновиков), но детали вреда неточны в четырёх местах. 1) «Поле Webhook пустое» — неверно. Блок «Webhook URL для MAX» (MaxSettingsPanel.tsx:127-151) — это не input, а read-only <code> с вычисленным ${serverBaseUrl ?? window.location.origin}/api/max/webhook; он никогда не выглядит пустым. webhookUrlDraft вообще не имеет UI в этой панели — и это хуже, а не лучше: при неудачной загрузке он остаётся "", уходит в PUT как webhookUrl:null и обнуляет сохранённый webhookUrl в БД (max.ts:173-174) без каких-либо следов на экране. 2) Пропущена главная потеря данных: staffRoutingDraft тоже остаётся {defaultUserI
  почему подтверждено: Подтверждено чтением. useMaxSettings.ts:57-66 — при любом ответе кроме 2xx/404 (реально достижимы 500 при ошибке БД и 401/403 из preHandler requireNonDoctorAccess и requireVerifiedOrganizationId, accessGuard.ts:103-129) ошибка уходит только в console.error: хук не отдаёт наружу поля ошибки (return-объект, строки 138-156), а MaxSettingsPanel.tsx не рисует ни одного сообщения о неудачной загрузке. settings остаётся null, все черновики остаются на инициализаторах (строки 42-49): botIdDraft="", webh

=== apps/web/src/hooks/useVoiceAssistant.ts
  строка 50 [падение] класс:error-as-empty
  код: .then(res => res.json()).then(data => { setSpeechGatewayStatus(data); })
  вред: Дефект реален, но три уточнения. (а) Условие: ломается только когда /api/speech/status отвечает не-2xx с JSON-телом — 403 ClinicalReadSecretRequired (в localStorage нет dente_clinical_admin_secret_session, а на сервере секрет задан) или 503 ClinicalReadSecretMissing (секрет на сервере не задан и DENTE_CLINICAL_ALLOW_UNGUARDED_READS не выставлен). В теле {error, message, protectedArea}, а не просто {"message":"..."}. При здоровом шлюзе ничего не падает. (б) Радиус больше заявленного: ближайший обработчик — не панель ИИ, а route-boundary всего раздела Настройки (App.tsx:4227), поэтому в заглушку уходит весь экран Настроек вместе с полосой вкладок, и кнопка «Повторить открытие» падает снова (se
  почему подтверждено: Подтверждено чтением всей цепочки, опровергнуть не удалось. (1) Монтирование живое: main.tsx -> AppShell -> lazy import ./App (AppShell.tsx:6), в App.tsx:4830 <VoiceAssistantUI/> стоит без условий и без фильтра по роли, VoiceAssistantUI.tsx:23 безусловно вызывает useVoiceAssistant; "settings" есть в appViews (workspaceShell.tsx:54). (2) Защиты на записи нет: useVoiceAssistant.ts:45-56 при falsy статусе делает fetch и пишет res.json() в стор без проверки res.ok и без разбора схемой; стор нетипизи

=== apps/web/src/hooks/useWhatsappSettings.ts
  строка 66 [потеря данных] класс:error-as-empty
  код: } else if (res.status !== 404) { console.error("Failed to load WhatsApp settings", res.status); }
  вред: Вред наступает не от нажатия «Сохранить» на нетронутой форме — там кнопка заблокирована защитой `dirty` (WhatsappSettingsPanel.tsx:66-77, 258). Точная цепочка: GET /api/whatsapp/settings падает с не-404 (или обрывается сетью — ветка catch на useWhatsappSettings.ts:69), хук молчит (только console.error), панель показывает пустые поля, выключенный тумблер и бейдж «Не подключён» — визуально идентично «канал не настроен». Администратор, решив, что WhatsApp не подключён, делает ОДНО любое изменение — включает тумблер «Активен», отмечает любую фичу, вводит Phone Number ID — и `dirty` становится true, кнопка разблокируется. Тогда PUT отправляет весь набор полей из пустых черновиков (useWhatsappSett
  почему подтверждено: Прочитал весь хук, панель-потребитель, серверный PUT и путь монтирования. Дефект есть, но заявленный механизм неточен. Подтверждено: 1. `useWhatsappSettings.ts:52-74` — при не-404 ответе (500/503, сетевой сбой в catch на 69) хук не выставляет НИКАКОГО признака ошибки: возвращаемый объект (142-162) содержит только `loading`, `saveState`, `saveError` (это ошибка сохранения, не загрузки). Состояния `loadError` не существует. `settings` остаётся `null`, черновики — пустыми. 2. `WhatsappSettingsPanel

=== apps/web/src/hooks/useWorkspaceProfile.ts
  строка 267 [потеря данных] класс:error-as-empty
  код: await fetch("/api/workspace/profile", { method: "POST", ... });
  вред: Дефект реален, но вред сформулирован и слабее, и не в ту сторону. а) Вред НЕ зависит от отказа сервера. POST-обработчик (apps/api/src/routes/workspaceProfile.ts:475-510) деструктурирует семнадцать флагов и не пишет ни одного — только `updatedAt` у организации, после чего отвечает 200 `{ok:true}`. Писать некуда: у таблицы `organizations` (apps/api/src/db/schema.ts:211-232) нет ни одной колонки под эти флаги. То есть переключатель не сохраняется на сервере НИКОГДА, а не только при 401/500; успешный ответ теряет данные ровно так же. Отсутствие проверки `res.ok` — это то, что скрывает потерю от владельца, но не её причина. б) Пример выбран неверно. `hasOrthodontics` и `hasPayrollModule` в DEFAUL
  почему подтверждено: Подтверждено чтением окружения, опровергнуть не удалось ни по одному из оснований. 1) Защиты рядом нет. `saveWorkspaceFlags` (useWorkspaceProfile.ts:263-281) выбрасывает результат `fetch` целиком: ни `res.ok`, ни `res.status`, ни повторного throw. `try/catch` на строке 272 ловит только сетевой reject — HTTP 401/500 резолвится нормально, поэтому даже `console.warn` не печатается. Строки 277-280 безусловно пишут все ключи `partial` в стор. 2) Отрисовывается пользователю. `WorkspaceFeaturesSelector

=== apps/web/src/store/appStore.ts
  строка 303 [ложь на экране] класс:prefilled-lie
  код: communicationNote: "Пациенту передана информация, задача закрыта.",
  вред: Находка верна; уточнения к формулировке вреда, а не опровержение. - «Подсказка никогда не показывается» — точнее: не показывается в исходном состоянии, а оно наступает при каждой загрузке страницы, потому что стор не персистится (обычный zustand `create`, `appStore.ts:1`, без `persist`). Placeholder всплывёт, только если администратор вручную вычистит поле целиком — и тогда как раз сработает нейтральный fallback `|| "Задача связи закрыта."` (`useAppLogic.tsx:12838`). То есть защита в коде есть, но покрывает лишь очищенное поле, а не предзаполненный дефолт, который её затеняет. Даже если администратор один раз исправит текст, перезагрузка возвращает ложное утверждение. - Блок заметки отрисовы
  почему подтверждено: Не удалось опровергнуть ни по одному из критериев — защиты нет, код отрисовывается, вред описан по существу верно. 1) Значение реально попадает в UI. `appStore.ts:303` — литерал `communicationNote: "Пациенту передана информация, задача закрыта."`. Стор — обычный zustand `create` (`appStore.ts:1`), без `persist`/гидрации, так что этот литерал — состояние при каждой загрузке страницы. Он течёт `App.tsx:1075` → `App.tsx:4156 communicationNote={communicationNote}` → `CommunicationsView.tsx:318 value

  строка 279 [мёртвая кнопка] класс:prefilled-lie
  код: newRuleTitle: "Кариес требует снимок и изоляцию",  newRuleTriggerServiceId: "svc-therapy-caries",
  вред: Дефект реален, но два элемента вреда описаны неверно. ГЛАВНОЕ — последняя фраза ложна и противоречит самой находке. Текст «Это правило снижает риск повторного лечения» пациенту в план лечения НЕ уходит. Сопоставление триггера идёт строгим равенством id (apps/api/src/sampleData.ts:1568-1571: `rule.triggerServiceIds.filter((serviceId) => serviceIds.has(serviceId))`, и при пустом результате правило немедленно отбрасывается). Раз `svc-therapy-caries` не может совпасть с UUID из каталога, правило не срабатывает никогда — а значит и `patientText` никогда не доходит до плана. Находка одновременно утверждает «правило никогда не сработает» и «текст уходит пациенту»; верно только первое. patientText л
  почему подтверждено: Подтверждено чтением, попытка опровержения не удалась ни по одному из каналов. 1) Значения на месте в текущем состоянии файлов: appStore.ts:279 `newRuleTitle: "Кариес требует снимок и изоляцию"`, :291 `svc-therapy-caries`, :293 `svc-therapy-cofferdam`, :295 `svc-therapy-caries`, :297 `svc-prosthetics-crown`, :299 текст предупреждения; patientStore.ts:101 `newRulePatientText: "Это правило снижает риск повторного лечения..."`. Ирония в том, что рядом, в том же файле (appStore.ts:305-317), стоит ко

=== apps/web/src/store/documentStore.ts
  строка 2278 [потеря денег] класс:prefilled-lie
  код: "Диагностика и подготовка | ... | 0\nОсновной этап | ... | 0\nКонтроль | ... | 0"
  вред: Ядро находки верное, но нужны три уточнения. (а) Строка печати для самого плана лечения — renderDocument.ts:1726, а :1804 относится к согласованию плана; форматтер rub указан верно (:58). (б) Ноль не просто печатается — он ещё и обходит шлюз выдачи. documentHasUnresolvedPlaceholders (renderDocument.ts:81-95, применяется в documentIssueBlockReason на :3889) блокирует выдачу по литералу "не указана" — ровно на то, во что превратился бы null. Поэтому реально пустая сумма этапа была бы поймана и выдачу бы остановила, а подсунутый дефолтом 0 рендерится как "0 руб." и проходит молча. Это делает дефект хуже, чем описано: единственный существующий предохранитель обезврежен именно нулём. (в) «Врач ци
  почему подтверждено: Подтверждено чтением всей цепочки. (1) documentStore.ts:2278-2279 и :2320-2321 — дефолты действительно оканчивают каждую строку этапа на "| 0". (2) useAppLogic.tsx:11131-11147: amount == "0" — непустая строка, значит тернарник идёт в Number("0".replace(/[^\d]/g,"")) = 0, Number.isFinite(0) === true, и на выходе estimatedAmountRub: 0, а не null. Защиты рядом нет: ?? стоит только на stageName/plannedServices/plannedTiming/clinicalNotes. Тот же код для согласования на :11089-11104. (3) renderDocume

  строка 2080 [потеря денег] класс:prefilled-lie
  код: `Первый платеж | ${dateInputValuePlusDays(7)} | 0 | запланировано\nФинальный платеж | ${...(21)}...`
  вред: Дефект реальный, но вред у двух полей разный, и для графика рассрочки он условный. Срок оплаты счёта (строка 2020) — вред безусловный: `paymentInvoiceDueDate` попадает в выданный счёт как есть (`dueDate: paymentInvoiceDueDate.trim()`, documentLogic.ts:615), фолбэка нет, валидатор проверяет только непустоту (documentValidators.ts:277). Поле рядом («Дата счета», documentStore.ts:2009) пустое и заставляет администратора его заполнить, а срок оплаты предзаполнен и потому не привлекает внимания — в счёте окажется «7 дней от дня открытия вкладки». Дополнительно значение подставлено в ISO-виде (`2026-08-04`), тогда как поле ждёт русский формат (placeholder «например: до 25.05.2026»). График рассроч
  почему подтверждено: Подтверждено чтением окружения. (1) Момент вычисления: стор создаётся plain `create<DocumentState>(...)` на уровне модуля (documentStore.ts:2678) и разворачивает `createFinancialSlice(set)`, а zustand выполняет создатель состояния сразу при конструировании стора — значит `dateInputValuePlusDays(7)`/`(21)` считаются один раз. Обёртка `(() => X)()` ничего не отложила, она эквивалентна прямому вызову; это единственные два таких IIFE в файле. (2) Жадность импорта: App.tsx:5 статически импортирует us

  строка 2191 [потеря денег] класс:prefilled-lie
  код: paymentPayerRelationship: "пациент",
  вред: Дефект реальный, но ломается не имя плательщика в справке, а флаг тождества плательщика и пациента. Что не так в описании. payerNameForTax (apps/api/src/documents/renderDocument.ts:1038) возвращает `payment.payerFullName?.trim() || patient.fullName`, поэтому в строке «ФИО налогоплательщика» справки КНД 1151156 печатается ФИО МАТЕРИ, а не пациента. Утверждение «справка называет плательщиком пациента» неверно. Что происходит на самом деле. normalizedTaxpayerRelationship("пациент") -> "self" (renderDocument.ts:1085), и от этого: 1. renderDocument.ts:1428 печатает «Налогоплательщик и пациент являются одним лицом: 1 - да», а renderDocument.ts:1435/1479 — «Родство с пациентом: пациент» — рядом с Ф
  почему подтверждено: Проверено чтением окружения, защиты нет. documentStore.ts:2191 действительно инициализирует paymentPayerRelationship = "пациент"; paymentComposerReset.ts:74 (DEFAULT_PAYER_RELATIONSHIP) и useAppLogic.tsx:12757 возвращают то же значение после каждого платежа — комментарий в paymentComposerReset.ts:69-73 прямо заявляет это как намерение («Пустым это поле не оставляем»). Из-за этого обе проверки на пустоту недостижимы: PaymentCapture.tsx:539 (`taxDeductionRequested && !payerRelationship.trim()`) и 

  строка 1872 [ложь на экране] класс:prefilled-lie
  код: photoVideoLabTransferAllowed: true,
  вред: Механизм верен, но две детали в описании вреда неточны. (а) Ссылка на documentLogic.ts:991-992 устарела — фактически documentLogic.ts:1085-1086. (б) Утверждение «материалов, которых ему даже не перечислили» неверно: validatePhotoVideoConsent (documentValidators.ts:1201-1203) не даёт создать документ, пока не отмечен хотя бы один тип материалов, поэтому пустой перечень материалов в выданном согласии невозможен. Точное описание: из семи чекбоксов формы согласия на фото, видео и снимки два открываются уже отмеченными — «Можно передавать в зуботехническую лабораторию» и «Можно показывать коллегам для консультации» (PhotoVideoConsentForm.tsx:75,83 от documentStore.ts:1872,1874), остальные пять ст
  почему подтверждено: Подтверждено чтением живого кода, опровергнуть не удалось. (1) Дефолты живые: documentStore.ts:1872 photoVideoLabTransferAllowed: true и 1874 photoVideoColleagueConsultationAllowed: true лежат внутри createIntakeAndConsentSlice (1740-1956), который спредится в реальный стор (create<DocumentState> на 2678-2688). Соседние пять полей (1876-1885) — false. Комментарий про этот же дефект стоит рядом, 1886-1894, над photoVideoMaterials: [] (1895). (2) Защиты нет: в documentStore.ts нет persist, нет res

  строка 2353 [ложь на экране] класс:prefilled-lie
  код: postVisitProcedureName: postVisitCarePresets.filling_restoration.procedureName,
  вред: Суть верна, но три детали в описании вреда неточны, и один отягчающий фактор пропущен. Неточность 1 — про «видит в селекте правильную тему». Селект темы лежит ВНУТРИ того же свёрнутого `<details>` (DocumentsView.tsx:2356, атрибута `open` нет), что и все текстовые поля: сам select на строке 2362 внутри div на 2358. Врач не видит правильную тему рядом со скрытым неправильным текстом — он не видит НИЧЕГО: ни темы, ни текста. Расхождение невидимо потому, что свёрнут весь блок целиком. Если врач всё-таки развернёт `<details>`, он увидит и правильную тему, и неправильный текст filling_restoration рядом, включая поле «Процедура» со значением «Пломба / композитная реставрация» (строка 2403). Неточно
  почему подтверждено: Подтверждено чтением окружения, опровергнуть не удалось ни по одному критерию. 1) Расхождение реально. documentStore.ts:44 `loadUiPreferences()` (AppHelpers.tsx:4056 — всегда возвращает объект, null не бывает, падения нет), строка 2351 берёт тему из сохранённых настроек, строки 2353-2391 жёстко берут девять полей из `postVisitCarePresets.filling_restoration`. 2) Защиты рядом нет. Единственный писатель этих девяти полей — `applyPostVisitCarePreset` (useAppLogic.tsx:11704-11736). Полный список его

  строка 1769 [ложь на экране] класс:prefilled-lie
  код: intakeCardioEndocrineNotes: "Сердечно-сосудистые, эндокринные и иные системные риски требуют уточнения врачом..."
  вред: Точное описание вреда: Неверно в исходной формулировке: 1. Сценарий «врач создаёт анкету, не раскрыв свёрнутую форму» невозможен. В том же свёрнутом <details> (DocumentsView.tsx:1990) лежат шесть других обязательных полей, пустых по умолчанию (intakeChiefComplaint, intakeAllergyStatus, intakeCurrentMedications, intakeChronicConditions, intakeAnticoagulants, intakeInfectiousRiskNotes) и чекбокс intakeAccuracyConfirmed=false. validatePatientIntakeQuestionnaire (documentValidators.ts:606-631) не пропустит документ, пока форму не раскрыли и не заполнили. Значит форму раскрывают всегда. 2. Это не ложное отрицание. Текст не утверждает, что у пациента нет болезней сердца или диабета, — он говорит, 
  почему подтверждено: Подтверждено чтением, но с поправками к описанию вреда. Что подтвердилось: 1. documentStore.ts:1769-1770 — жёстко вписанная непустая строка, причём она стоит ВНУТРИ того самого комментария (строки 1743-1756), который объясняет, почему соседние поля анамнеза были очищены. Все соседи в этом блоке пусты: intakeAllergyStatus, intakeCurrentMedications, intakeChronicConditions, intakeAnticoagulants, intakeInfectiousRiskNotes = "". Поле cardio — единственное исключение, то есть пропуск в этой самой чис

