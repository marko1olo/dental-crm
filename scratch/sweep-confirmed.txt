ITOG: {"подтверждено": 34, "опровергнуто": 23, "поВреду": [{"вред": "потеря денег", "сколько": 4}, {"вред": "потеря данных", "сколько": 5}, {"вред": "падение", "сколько": 1}, {"вред": "ложь на экране", "сколько": 11}, {"вред": "мёртвая кнопка", "сколько": 4}, {"вред": "косметика", "сколько": 9}]}

### потеря денег (4)
  apps/web/src/components/finance/FamilyWalletPanel.tsx:79  [error-as-empty]
    код: setFamily(res.ok ? await res.json() : null);
    вред: Механика описана верно, но последствия завышены. Точнее так: любой не-ok ответ (500, 401/403 от requireResolvedOrganizationId, обрыв сети) приводит к family=null, и панель исчезает молча — без текста ошибки и без возможности повторить загрузку; отличить это от

  apps/web/src/store/documentStore.ts:2278  [prefilled-lie]
    код: "Диагностика и подготовка | ... | 0\nОсновной этап | ... | 0\nКонтроль | ... | 0"
    вред: Ядро находки верное, но нужны три уточнения. (а) Строка печати для самого плана лечения — renderDocument.ts:1726, а :1804 относится к согласованию плана; форматтер rub указан верно (:58). (б) Ноль не просто печатается — он ещё и обходит шлюз выдачи. documentHa

  apps/web/src/store/documentStore.ts:2080  [prefilled-lie]
    код: `Первый платеж | ${dateInputValuePlusDays(7)} | 0 | запланировано\nФинальный платеж | ${...(21)}...`
    вред: Дефект реальный, но вред у двух полей разный, и для графика рассрочки он условный.

Срок оплаты счёта (строка 2020) — вред безусловный: `paymentInvoiceDueDate` попадает в выданный счёт как есть (`dueDate: paymentInvoiceDueDate.trim()`, documentLogic.ts:615), ф

  apps/web/src/store/documentStore.ts:2191  [prefilled-lie]
    код: paymentPayerRelationship: "пациент",
    вред: Дефект реальный, но ломается не имя плательщика в справке, а флаг тождества плательщика и пациента.

Что не так в описании. payerNameForTax (apps/api/src/documents/renderDocument.ts:1038) возвращает `payment.payerFullName?.trim() || patient.fullName`, поэтому 

### потеря данных (5)
  apps/web/src/components/useVisitDiaryLogic.ts:60  [error-as-empty]
    код: .then((r) => r.json())
    вред: Точный вред: при неудачном GET `/api/diaries/visit/{visitId}` (500 от БД, обрыв сети, либо 403/503 от `requireClinicalReadAccess` в конфигурации с NODE_ENV=production, поскольку клиент на строке 59 не посылает `x-dente-admin-secret`) хук молча оставляет `EMPTY

  apps/web/src/components/odontogram/TreatmentEstimator.tsx:153  [error-as-empty]
    код: .then((response) => (response.ok ? response.json() : null))
    вред: Дефект настоящий, но врач в типичном случае увидит НЕ пустой план. Утверждение «items остаются [], состояние ровно одно — «План лечения пуст»» верно только для пациента без отмеченных патологий. Эффект автоподбора (строки 175-413) на каждое изменение currentTe

  apps/web/src/hooks/useMaxSettings.ts:64  [error-as-empty]
    код: } else if (res.status !== 404) { console.error("Failed to load MAX settings", res.status); }
    вред: Ядро находки верно (молчаливый провал загрузки + деструктивный PUT из непроинициализированных черновиков), но детали вреда неточны в четырёх местах.

1) «Поле Webhook пустое» — неверно. Блок «Webhook URL для MAX» (MaxSettingsPanel.tsx:127-151) — это не input, 

  apps/web/src/hooks/useWhatsappSettings.ts:66  [error-as-empty]
    код: } else if (res.status !== 404) { console.error("Failed to load WhatsApp settings", res.status); }
    вред: Вред наступает не от нажатия «Сохранить» на нетронутой форме — там кнопка заблокирована защитой `dirty` (WhatsappSettingsPanel.tsx:66-77, 258). Точная цепочка: GET /api/whatsapp/settings падает с не-404 (или обрывается сетью — ветка catch на useWhatsappSetting

  apps/web/src/hooks/useWorkspaceProfile.ts:267  [error-as-empty]
    код: await fetch("/api/workspace/profile", { method: "POST", ... });
    вред: Дефект реален, но вред сформулирован и слабее, и не в ту сторону.

а) Вред НЕ зависит от отказа сервера. POST-обработчик (apps/api/src/routes/workspaceProfile.ts:475-510) деструктурирует семнадцать флагов и не пишет ни одного — только `updatedAt` у организации

### падение (1)
  apps/web/src/hooks/useVoiceAssistant.ts:50  [error-as-empty]
    код: .then(res => res.json()).then(data => { setSpeechGatewayStatus(data); })
    вред: Дефект реален, но три уточнения. (а) Условие: ломается только когда /api/speech/status отвечает не-2xx с JSON-телом — 403 ClinicalReadSecretRequired (в localStorage нет dente_clinical_admin_secret_session, а на сервере секрет задан) или 503 ClinicalReadSecretM

### ложь на экране (11)
  apps/web/src/components/PatientPortal.tsx:185  [numeric-string]
    код: (s: number, i: any) => s + (i.totalAmount || 0),
    вред: Точное описание вреда (два уточнения к заявленному).

Кто и как видит. Пути «пациент открывает портал» в текущем коде нет: PatientPortal импортируется ровно в одном месте — SettingsTelegramTab.tsx:5/626, модалка «Превью Портала Пациента». URL-маршрута к нему н

  apps/web/src/components/PatientPortal.tsx:411  [numeric-string]
    код: {(stage.totalAmount || 0).toLocaleString()} ₽
    вред: Дефект настоящий, но вред описан неточно в двух местах.

Кто это видит. Единственная точка рендера PatientPortal в репозитории — модал «Превью Портала Пациента» в CRM: Настройки → Мессенджеры → Telegram → кнопка «Предпросмотр» (SettingsTelegramTab.tsx:601-627)

  apps/web/src/VisitView.tsx:1113  [ghost-token]
    код: background: 'var(--amber-50)', ... border: '1px solid var(--amber-200)'
    вред: Дефект реален, но два пункта описания надо уточнить.

Первое — «единственное объяснение» неверно в общем случае. В репозитории есть вторая, корректно оформленная копия той же плашки: `apps/web/src/components/visit/VisitEmkTab.tsx:292-306`. Там классы Tailwind 

  apps/web/src/components/auth/StaffPinPad.tsx:50  [error-as-empty]
    код: const data = await response.json(); if (!response.ok) throw new Error(data.message || "Неверный PIN-код");
    вред: Дефект есть, но вред описан неверно в трёх пунктах, и один из них переворачивает исправление.

ЧТО НА САМОМ ДЕЛЕ УВИДИТ ПОЛЬЗОВАТЕЛЬ. Тост НЕ говорит «Неверный PIN-код». `showToast(err.message ...)` (строка 60) выводит сырое сообщение парсера браузера: в Chrom

  apps/web/src/components/imaging/VisiographAnalyzer.tsx:207  [error-as-empty]
    код: if (!res.ok) return;
    вред: Две поправки к описанию вреда.

(а) Строка 466 гасит НЕ «весь блок архива», а только круглый счётчик в `<summary>` (span 466-477). Сам `<details>` (456), подпись «ИИ-Анализ снимка (ShadowAnalyst)» (463-465), шапка «ShadowAnalyst · Dental AI» (496) и зона загру

  apps/web/src/AppHelpers.tsx:3484  [prefilled-lie]
    код: "36 | окклюзионная, дистальная | кариес | кариес дентина 36 зуба по осмотру и снимку | ..."
    вред: Дефект реален, но механика вреда описана неточно в двух местах. (а) «Врач жмёт создать не раскрывая форму» — так не бывает ни для одного из этих документов: у каждого валидатор упирается либо в пустое по умолчанию обязательное поле (procedureConsentProcedureNa

  apps/web/src/store/appStore.ts:303  [prefilled-lie]
    код: communicationNote: "Пациенту передана информация, задача закрыта.",
    вред: Находка верна; уточнения к формулировке вреда, а не опровержение.

- «Подсказка никогда не показывается» — точнее: не показывается в исходном состоянии, а оно наступает при каждой загрузке страницы, потому что стор не персистится (обычный zustand `create`, `ap

  apps/web/src/store/documentStore.ts:1872  [prefilled-lie]
    код: photoVideoLabTransferAllowed: true,
    вред: Механизм верен, но две детали в описании вреда неточны. (а) Ссылка на documentLogic.ts:991-992 устарела — фактически documentLogic.ts:1085-1086. (б) Утверждение «материалов, которых ему даже не перечислили» неверно: validatePhotoVideoConsent (documentValidator

  apps/web/src/store/documentStore.ts:2353  [prefilled-lie]
    код: postVisitProcedureName: postVisitCarePresets.filling_restoration.procedureName,
    вред: Суть верна, но три детали в описании вреда неточны, и один отягчающий фактор пропущен.

Неточность 1 — про «видит в селекте правильную тему». Селект темы лежит ВНУТРИ того же свёрнутого `<details>` (DocumentsView.tsx:2356, атрибута `open` нет), что и все текст

  apps/web/src/store/documentStore.ts:1769  [prefilled-lie]
    код: intakeCardioEndocrineNotes: "Сердечно-сосудистые, эндокринные и иные системные риски требуют уточнения врачом..."
    вред: Точное описание вреда:

Неверно в исходной формулировке:
1. Сценарий «врач создаёт анкету, не раскрыв свёрнутую форму» невозможен. В том же свёрнутом <details> (DocumentsView.tsx:1990) лежат шесть других обязательных полей, пустых по умолчанию (intakeChiefComp

  apps/web/src/ScannerView.tsx:84  [prefilled-lie]
    код: const [status, setStatus] = useState<"passed" | "failed">("passed");
    вред: Описание верно по сути, но требует трёх уточнений.

Ссылка на строки: комментарий про фокус и useEffect — это строки 122-125 (плюс ref на 96 и 265), а 236-238 — это текстовое поле автоклава, не фокус.

Значение не спрятано: селект «Результат» стоит прямо над п

### мёртвая кнопка (4)
  apps/web/src/components/imaging/VisiographAnalyzer.tsx:562  [ghost-token]
    код: border: `2px dashed ${isDragOver ? 'var(--teal)' : 'var(--border)'}`
    вред: Дефект есть, но вред описан шире и резче, чем происходит на экране. Точное описание:

а) Панель по умолчанию СВЁРНУТА. Строка 456 — `<details className="visiograph-analyzer-details">` без `open`, и класс `.visiograph-analyzer-details` не описан ни в одном CSS 

  apps/web/src/components/imaging/VisiographAnalyzer.tsx:516  [ghost-token]
    код: border: '1px solid var(--border)', borderRadius: '8px'
    вред: Дефект есть, но описание вреда неточно в трёх местах, и в одном — недооценивает масштаб.

НЕВЕРНО «ни один токен не объявлен»: --teal объявлен (main.css:21, 83, 143). Поэтому кнопка «Озвучить» сломана только в состоянии покоя: при isSpeaking она получает backg

  apps/web/src/components/settings/SettingsProtocolsTab.tsx:336  [ghost-token]
    код: backgroundColor: "var(--dente-red-10)", color: "var(--dente-red-60)"
    вред: Дефект реален, но формулировку вреда надо уточнить в четырёх местах. (а) Вкладка «Протоколы» не ограничена ролью: фильтры в SettingsView.tsx:1200-1214 срезают только marketing/reporting/bpmn/rules/insurance/telegram, поэтому кнопку удаления видит любой сотрудн

  apps/web/src/store/appStore.ts:279  [prefilled-lie]
    код: newRuleTitle: "Кариес требует снимок и изоляцию",  newRuleTriggerServiceId: "svc-therapy-caries",
    вред: Дефект реален, но два элемента вреда описаны неверно.

ГЛАВНОЕ — последняя фраза ложна и противоречит самой находке. Текст «Это правило снижает риск повторного лечения» пациенту в план лечения НЕ уходит. Сопоставление триггера идёт строгим равенством id (apps/

### косметика (9)
  apps/web/src/components/imaging/VisiographAnalyzer.tsx:481  [ghost-token]
    код: border: '1px solid var(--border)', borderRadius: '14px', background: 'var(--surface)'
    вред: Находка верна, но описание вреда занижено и в одной детали неточно.

Неточность: панель лежит внутри `<details>` без атрибута `open` (стр. 456). Врач видит дефект не сразу при входе на экран приёма, а после раскрытия сводки «ИИ-Анализ снимка (ShadowAnalyst)». 

  apps/web/src/components/imaging/VisiographAnalyzer.tsx:689  [ghost-token]
    код: borderBottom: idx < reportSections.length - 1 ? '1px solid var(--border)' : 'none'
    вред: Механика верна, но оценка последствий завышена в одном месте. Что реально видит врач: карточка «Полный отчёт ShadowAnalyst» теряет рамку (676) — не «серую рамку по умолчанию», а border-style: none целиком; шапка теряет и заливку (678, background становится tra

  apps/web/src/components/imaging/VisiographAnalyzer.tsx:662  [ghost-token]
    код: border: `1px solid ${isCritical ? 'transparent' : isDone ? 'var(--teal)' : 'var(--border)'}`
    вред: Вред занижен: нейтральный чип теряет не два, а три свойства. Помимо background: var(--bg-inset) (660) и border: 1px solid var(--border) (662) недействителен и color: var(--text-muted) (661) — это имя в CSS встречается только с запасом (main.css:14773+, shadow-

  apps/web/src/components/imaging/VisiographAnalyzer.tsx:771  [ghost-token]
    код: border: '1px solid var(--border)', background: 'var(--bg-inset)'
    вред: Механизм верен, но вред описан шире и уже одновременно, чем есть.

Точно: --border, --bg-inset, --surface не объявлены нигде и стоят без запаса. Строка 771 — вся сокращённая запись border и весь background строки истории недействительны на этапе вычисления зна

  apps/web/src/components/imaging/VisiographAnalyzer.tsx:459  [ghost-token]
    код: padding: '8px 0', userSelect: 'none', listStyle: 'none', color: 'var(--text-muted)'
    вред: Дефект есть, но счёт «20 мест» завышен, а список последствий надо разделить. Точно: видимо меняются только 16 мест с --text-muted — они рисуются цветом --ink (main.css:14262-14265 body { color: var(--ink) }), то есть ровно основным цветом текста, в светлой, тё

  apps/web/src/components/settings/SettingsPricesTab.tsx:336  [ghost-token]
    код: <FolderTree size={48} color="var(--border)" />
    вред: Дефект настоящий, но вред шире и выглядит иначе, чем описано. Триггер не только «пустой поиск»: условие Object.keys(groupedCatalog).length === 0 (строка 334) срабатывает и когда dashboard.serviceCatalog пуст, то есть у свежей клиники без услуг это ПЕРВОЕ, что 

  apps/web/src/components/settings/SettingsPricesTab.tsx:238  [ghost-token]
    код: background: "rgba(59, 130, 246, 0.1)", color: "var(--blue)"
    вред: Механизм описан почти верно, но упущено, что у класса есть собственный цвет: .pricelist-section-icon задаёт `color: var(--teal)` (SettingsPricesTab.css:267), и --teal объявлен (#0d9488 в светлой, #2dd4bf в тёмной, #e0a458 в третьей теме — styles/dente-redesign

  apps/web/src/components/SmartMicrophoneButton.tsx:81  [ghost-token]
    код: color: isRecording ? 'var(--red-600)' : isProcessing ? 'var(--blue-600)' : 'var(--brand-500)'
    вред: Дефект реальный, но список мест и оценка потери преувеличены.

НЕ ЗАСЧИТЫВАТЬ два из названных мест:
- PatientCoreForm.tsx:54 — мёртвый код. `PatientCoreForm` экспортируется, но не импортируется НИГДЕ в apps/ и packages/ (единственные упоминания — сам файл). П

  apps/web/src/PaymentCapture.tsx:380  [ghost-token]
    код: background: "var(--paper)", padding: "16px", borderRadius: "8px", border: "1px solid var(--brand-200)"
    вред: Уточнения к формулировке, сам дефект стоит.

Достижимость: блок не виден сразу. InstallmentCalculator рендерится как `<details ... open={isOpen}>` с `isOpen={false}` (PaymentCapture.tsx:352, :717), то есть калькулятор свёрнут по умолчанию — кассир видит только

OPROVERGNUTO (23):
  apps/web/src/components/patients/PatientFamilyCard.tsx:149
  C:\Clinic_MVP\dental-crm\apps\web\src\ScheduleView.tsx:249
  C:\Clinic_MVP\dental-crm\apps\web\src\VisitView.tsx:235
  C:\Clinic_MVP\dental-crm\apps\web\src\VisitView.tsx:677
  apps/web/src/components/settings/SettingsPricesTab.tsx:391
  apps/web/src/components/useVisitDiaryLogic.ts:135
  apps/web/src/components/patients/PatientFamilyCard.tsx:88
  apps/web/src/components/settings/InsuranceContractsPanel.tsx:122
  apps/api/src/db/settingsQuery.ts:108
  apps/web/src/components/patients/PatientLoyaltyHeader.tsx:46
  apps/web/src/components/patients/OrthodonticProgressWidget.tsx:107
  apps/api/src/db/settingsQuery.ts:268
  apps/api/src/db/settingsQuery.ts:192
  apps/api/src/db/settingsQuery.ts:111
  apps/web/src/components/odontogram/TreatmentEstimator.tsx:100
  apps/web/src/components/patients/PatientOverviewTab.tsx:66
  apps/web/src/components/EgiszMonitor.tsx:38
  apps/web/src/components/settings/SettingsBpmnTab.tsx:42
  apps/web/src/components/patients/RecallListPanel.tsx:129
  apps/web/src/store/appStore.ts:301
  apps/web/src/components/schedule/LabOrdersPanel.tsx:67
  apps/web/src/store/documentStore.ts:1795
  apps/web/src/store/documentStore.ts:2467
