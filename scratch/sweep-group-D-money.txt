=== apps/web/src/components/finance/FamilyWalletPanel.tsx
  строка 79 [потеря денег] класс:error-as-empty
  код: setFamily(res.ok ? await res.json() : null);
  вред: Механика описана верно, но последствия завышены. Точнее так: любой не-ok ответ (500, 401/403 от requireResolvedOrganizationId, обрыв сети) приводит к family=null, и панель исчезает молча — без текста ошибки и без возможности повторить загрузку; отличить это от штатного «пациент не в семье» невозможно, потому что API отдаёт 404 и на штатный случай (finance_family.ts:210-212). Чего заявленный вред НЕ доказывает: деньги при этом не теряются и проводки не искажаются — панель ничего не пишет до нажатия кнопки, долг остаётся зафиксированным, и списание с семейного счёта можно сделать позже, когда панель загрузится. Формулировка «кассир берёт всю сумму наличными» — возможное следствие, а не то, что
  почему подтверждено: Подтверждено чтением окружения, а не одной строки. (1) Защиты рядом нет: строка 79 `setFamily(res.ok ? await res.json() : null)` сводит ЛЮБОЙ не-ok ответ к null, ветка catch (80-84) делает то же; ни `res.status`, ни отдельного состояния ошибки в компоненте нет, тоста тоже нет — для HTTP-отказа даже console.error не вызывается (он только в catch). Строка 231 `if (!family) return null;` действительно убирает панель целиком. Существующие `family?.balance` (113) и `family.members ?? []` (246) защища
=== apps/web/src/components/PatientPortal.tsx
  строка 185 [ложь на экране] класс:numeric-string
  код: (s: number, i: any) => s + (i.totalAmount || 0),
  вред: Точное описание вреда (два уточнения к заявленному). Кто и как видит. Пути «пациент открывает портал» в текущем коде нет: PatientPortal импортируется ровно в одном месте — SettingsTelegramTab.tsx:5/626, модалка «Превью Портала Пациента». URL-маршрута к нему нет (main.tsx рендерит только AppShell, в реестре appViews в workspaceShell.tsx:54 записи портала нет). Реально наблюдаемый путь один: администратор/управляющий (любая роль, кроме doctor) → Настройки → вкладка «Мессенджеры» (id "telegram") → поле «Портал пациента» → кнопка «Предпросмотр» → вход по телефону пациента и коду из OTP. После входа блок «План лечения» показывает нули. Что именно показывается. Нулями выводятся не два, а все три п
  почему подтверждено: Дефект подтверждён чтением окружения, опровергнуть не удалось. 1) Поля totalAmount не существует. apps/api/src/routes/portal.ts (GET /me, ~строки 606-630) возвращает сырые строки: `const plans = await db.select().from(treatmentPlans).where(eq(treatmentPlans.patientId, patient.id))` и отдаёт их в ответе как `plans` без какого-либо маппинга. В apps/api/src/db/schema.ts:1338-1356 таблица treatment_plans объявлена так: `totalPriceRub: numeric("total_price_rub", { precision: 12, scale: 2 })` и `total

  строка 411 [ложь на экране] класс:numeric-string
  код: {(stage.totalAmount || 0).toLocaleString()} ₽
  вред: Дефект настоящий, но вред описан неточно в двух местах. Кто это видит. Единственная точка рендера PatientPortal в репозитории — модал «Превью Портала Пациента» в CRM: Настройки → Мессенджеры → Telegram → кнопка «Предпросмотр» (SettingsTelegramTab.tsx:601-627). Отдельного пациентского входа нет: у apps/web одна точка входа (index.html/main.tsx), и других мест рендера PatientPortal grep по apps/web/src не находит. Плюс список планов вообще отрисовывается только при isAuthenticated
=== apps/web/src/PaymentCapture.tsx
  строка 380 [косметика] класс:ghost-token
  код: background: "var(--paper)", padding: "16px", borderRadius: "8px", border: "1px solid var(--brand-200)"
  вред: Уточнения к формулировке, сам дефект стоит. Достижимость: блок не виден сразу. InstallmentCalculator рендерится как `<details ... open={isOpen}>` с `isOpen={false}` (PaymentCapture.tsx:352, :717), то есть калькулятор свёрнут по умолчанию — кассир видит только строку summary «Рассрочка от клиники, без банка» и должен по ней щёлкнуть. Обработчика onToggle нет, поэтому после клика блок остаётся раскрытым. Вред проявляется только на раскрытом блоке, а не при входе на экран оплат. Насколько «читается» плашка — точные числа. В светлой теме родитель `.smart-details-content` получает инлайновый `rgba(13,148,136,0.07)` поверх `#f7fbf9` у `.payment-capture-detail-section` (main.css:11843), итог ≈ `#e7
  почему подтверждено: Опровергнуть не удалось — все четыре канала опровержения проверены и закрыты. 1) Токен действительно отсутствует. Поиск `--brand-200\s*:` по всему репозиторию (без node_modules) — ноль совпадений. Единственный другой потребитель, apps/web/src/styles/main.css:16224 (`.smart-ai-hints-popup`), защищён запасом: `border: 1px solid var(--brand-200, #bae6fd)`. В инлайне PaymentCapture.tsx:380 запаса нет. Показательно, что apps/web/src/styles/token-aliases.css — файл, созданный именно для добора отсутст
=== apps/web/src/components/settings/SettingsPricesTab.tsx
  строка 336 [косметика] класс:ghost-token
  код: <FolderTree size={48} color="var(--border)" />
  вред: Дефект настоящий, но вред шире и выглядит иначе, чем описано. Триггер не только «пустой поиск»: условие Object.keys(groupedCatalog).length
